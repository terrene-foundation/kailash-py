# Sweep — 2026-06-25 (repo-wide)

Trigger: post-#1460 close + user challenge — "tell me if there are outstanding
tasks in the 4 dormant workspaces; if there are, your /sweep is broken — it
should flag ALL outstanding."

## Verdict

YES — outstanding tasks exist in the dormant workspaces. They are NOT
`todos/active/` items (0 of those repo-wide); they live in each dormant
workspace's `.session-notes` **Outstanding ledger (forest)**, which **no /sweep
step reads**. The canonical Sweep 1 keys on `todos/active/` only → blind to
workspace-ledger forest items. That is the real design gap (finding S-GAP).

## Sweep 1–8 results (mechanical)

| #   | Sweep                     | Result                                                                                                                                                                         |
| --- | ------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | todos/active              | 0 files (all workspaces)                                                                                                                                                       |
| 2   | pending journals          | 1 — `mops-onboarding/.../1782381323579-0-DISCOVERY.md` (auto-gen from codify commit 259b1e35b; already codified in `mops-onboarding/journal/0003`) → DISCARDED                 |
| 3   | open GH issues            | **0 open** (terrene-foundation/kailash-py)                                                                                                                                     |
| 4   | open PRs / stale branches | none                                                                                                                                                                           |
| 5   | redteam-gaps              | precondition gate = 0 (counts specs SUBdirs; nexus specs are file-based → gate-quirk S5Q); the one spec'd ws (nexus-fastapi-parity) is delivered+released (#1174, nexus 2.8.0) |
| 6   | hygiene                   | clean — no `.session-notes` >30d (dormant = 24–29d), 1 worktree at HEAD, no `.pending` >14d                                                                                    |
| 7   | process                   | only `.session-notes` modified; 0/0 divergence vs origin/main                                                                                                                  |
| 8   | release                   | `release-drift.js` exit 0 → **NO drift**; core `src/kailash/` docs-only since v2.44.1; pact #1456/#1440/feat shipped in `packages/kailash-pact` under its own tag (0.14.3)     |

## Findings (dormant-workspace outstanding + process)

- **[MED][S-GAP] /sweep is blind to workspace forest ledgers — TWO coupled gaps.**
  `grep -n "ledger\|forest" .claude/commands/sweep.md` = **0 hits**: the command
  has no step that reads the `## Outstanding ledger (forest)` section of any
  `.session-notes`.
  - **(i) Sweep side** — Sweep 1 reads `todos/active/` only; Sweep 6 only `stat`s
    `.session-notes` MTIME (>30d) and never opens the file. So open forest rows in
    `workspaces/*/.session-notes` are unread on every one of the 8 axes.
  - **(ii) Reconciliation side** — `/wrapup` reconciles the ROOT ledger against the
    prior ROOT notes ONLY; per-workspace ledgers are never aggregated up, and
    `validate-forest-ledger.mjs --git-prior` enforces no-vanish WITHIN one file,
    not ACROSS workspace→root. F11/F30 stranded because BOTH gaps held at once.
  - Disposition: `/codify` BOTH pieces (a tool-backed sweep-read step per
    `sweep-completeness.md` Rule 3 + cross-file no-vanish aggregation). The piece-(ii)
    omission was a real gap in my first version of this report.

- **[MED][F30 · issue-855] DataFlow `@db.model` reserved-name collision —
  root cause UNFILED.** `NodeMetadata.tags` (core, `set[str]`) collides with a
  user model field named `tags`, failing CreateNode validation. #855 shipped
  only the `tag_list` rename WORKAROUND (diff confirmed already on main); the
  root DataFlow fix was "queued (file as DataFlow issue)" and **no matching GH
  issue exists** (0 open issues; search empty). Disposition: USER-GATE — file a
  DataFlow issue for the reserved-name guard, OR confirm the workaround suffices.

- **[LOW][F11 · issue-1035] Connector-ABC concrete-defaults / 42 test-pyright
  errors.** Claimed 2026-05-25 ("design half NOT done; KEPT `__init_subclass__`
  proxy"). STALE (24d). Disposition: RE-VALIDATE — run pyright on tests to
  confirm the 42 errors persist before re-queue (not re-run this sweep).

- **[LOW][F22 · issue-855 + nexus] TIER-3 testing backlog sweep.** From
  `sweep-2026-05-26` (user-approved). STALE. Disposition: RE-VALIDATE.

- **[LOW][F14 · issue-1035] Slim-core import-lesson `/codify` proposal.** Exists
  at `issue-1035-delegate-py/05-codify/01-PROPOSAL-slim-core-eager-import.md`,
  targets loom Gate-1. Disposition: EXTERNAL (loom ingest) — same class as the
  Enforcement-Surface-Parity proposal already in `.claude/.proposals/latest.yaml`.

- **[INFO][F24 · issue-855 + nexus] GH issue backlog.** RESOLVED — 0 open issues.

- **[INFO][S5Q] Sweep-5 precondition gate quirk.** `find workspaces/*/specs
-type d -mindepth 1` counts SUBdirs; nexus specs are FILES → gate reports 0
  ("orchestration mode") despite a real spec existing. Low impact (that ws is
  delivered+released). Disposition: note for the `/codify` sweep fix.

- **[INFO][self] My process misses this session (transparent).** (a) wrote "0
  active in-repo todos" in root `.session-notes` without running `find
todos/active/` (unverified durable claim — `verify-claims-before-write`);
  narrowly true but framed as "no outstanding work" = misleading. (b) root
  ledger DROPPED F11/F22/F30 (reconciled only vs the prior ROOT notes, which had
  already dropped them — not vs the dormant ledgers). (c) skipped
  `release-drift.js` at `/wrapup` (now run: no drift). All three fixed here:
  pending journal discarded, root ledger reconciled (below), drift verified.

## Recommended next-session items (ranked; human decides)

1. `/codify` the S-GAP fix — BOTH pieces: (i) a tool-backed `/sweep` step that reads
   every workspace `.session-notes` `## Outstanding ledger (forest)` and rolls open
   rows into the repo-wide view; (ii) cross-file no-vanish aggregation
   (workspace-ledger open IDs absent from the ROOT ledger = a finding), extending
   `validate-forest-ledger.mjs --git-prior` from within-file to workspace→root.
   HIGH — prevents the exact invisible-outstanding failure this challenge surfaced.
   Filed to loom as `esperie-enterprise/loom#669` (cross-repo, user-authorized,
   journaled at `workspaces/mops-onboarding/journal/0004`) + carried as the
   `/codify` proposal (the canonical BUILD→loom Gate-1 channel).
2. F30 — file the DataFlow reserved-name-collision issue (or confirm workaround).
3. Dormant-workspace disposition: archive (issues all CLOSED) after F11/F22
   re-validation, OR resume per re-validated value-anchors.
