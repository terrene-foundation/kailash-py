# Sweep — 2026-05-06

Repo-wide outstanding-work audit run after kailash-dataflow 2.7.9 release for issue #835. Working tree clean post-release except for the just-written `.session-notes`. No open PRs, no stale remote branches, no orphan worktrees. The findings below cluster around stale workspace todos (released-but-not-archived), a small set of GH issues that warrant triage, and one ungroomed workspace.

## Findings

### [HIGH] [Sweep 1] `dataflow-engine-pyright-cleanup` is ungroomed — 10 active todos, 0 completed, no .session-notes

- **Location**: `workspaces/dataflow-engine-pyright-cleanup/todos/active/T1-T8 + variants`
- **Disposition**: FILE-ISSUE or move-to-completed (next session decides which)
- **Evidence**: T1-T8 plus 00-defaults / T6 / T7a / T7b. The deploy record at `deploy/deployment-config.md` cites "Prior 2026-05-04 — kailash-dataflow 2.7.7 — engine.py pyright cleanup (dataflow-engine-pyright-cleanup workspace) (T1–T8)" — the workspace's todos were resolved by 2.7.7 but never moved out of `active/`.
- **Why this matters**: Stale `active/` todos make `/sweep` Sweep 1 noisy on every future cycle; the 10 entries scroll past every session without changing.
- **Action taken**: None inline. Recommend a 5-minute housekeeping pass that either moves the 10 .md files to `completed/` (if 2.7.7 actually closed each one) or files a follow-up issue tracking the ones still open.

### [MED] [Sweep 1] `issue-835-dataflow-transaction-eventloop` — 2 of 3 active todos already done

- **Location**: `workspaces/issue-835-dataflow-transaction-eventloop/todos/active/`
- **Disposition**: FIX-NOW candidate (move to completed/) — minor housekeeping
- **Evidence**: Todo 05 (PR assembly) — landed via PR #842; Todo 08 (post-merge release cycle) — kailash-dataflow 2.7.9 published today. Only Todo 06 (kailash-rs cross-SDK companion, user-gated) remains genuinely open.
- **Why this matters**: The next session reading `todos/active/` should see the user-gated cross-SDK question front and center, not buried under two completed phases.
- **Action taken**: Not moved inline because the user explicitly chose the cleanup→/wrapup→/sweep ordering and `/wrapup` already wrote `.session-notes` referencing the 06 disposition. Recommend the next session moves 05 + 08 to `completed/` as a one-line `git mv`.

### [MED] [Sweep 1] `issue-814-kaizen-pyright` — Phase 2 todos may be partially stale

- **Location**: `workspaces/issue-814-kaizen-pyright/todos/active/` (6 active, 5 completed)
- **Disposition**: DEFER-WITH-REASON (next /implement cycle on this workspace decides)
- **Evidence**: kailash-kaizen 2.18.2 (closes #814) shipped 2026-05-04, kaizen 2.19.0 shipped 2026-05-05, kaizen 2.20.0 shipped 2026-05-06. Phase 2 todos (2.1-2.6) reference orphan-deletion, optional-extras, kaizen-tools spec, follow-up issue, version bump. Some may have landed via 2.19.0/2.20.0 cycles and never been moved.
- **Why this matters**: The 6 active todos may be cargo-culted forward when the underlying work shipped weeks ago.
- **Action taken**: None inline. Recommend the next session reads each Phase 2 todo against `git log -- packages/kailash-kaizen/` and reconciles.

### [MED] [Sweep 2] 5 pending journal entries awaiting promotion

- **Location**: `workspaces/issue-814-kaizen-pyright/journal/.pending/` (1) + `workspaces/issue-835-dataflow-transaction-eventloop/journal/.pending/` (4)
- **Disposition**: FIX-NOW candidate (review + promote/discard per `rules/journal.md`)
- **Evidence**: 1 DISCOVERY entry in #814 (likely from 2.18.2 or older), 4 RISK entries auto-generated by the issue-835 commits.
- **Why this matters**: `.pending/` exists because auto-journaling captures commit bodies that may or may not be high-value. Letting them age silently defeats the institutional-memory function.
- **Action taken**: None inline. Recommend the next session reads each `.pending/*.md` and either promotes (renames out of `.pending/`) or discards.

### [LOW] [Sweep 3] 11 open GH issues — all under 30d old, none deferred-without-tracking

- **Location**: GitHub
- **Disposition**: DEFER-WITH-REASON for now; cross-check at next sweep cycle
- **Evidence**: oldest is #596 (Apr 30, 6d). #760 has explicit follow-up date "2026-05-08". #819, #816, #815, #803 are recent bug/chore reports; #693 is a spec-promotion task (post-M2); #643 is a major ml migration; #630 is a Trust Vault follow-up; #772 is a refactor; #839 is a feature request.
- **Why this matters**: The issue tracker is small and current — the only triage need is #760's 2026-05-08 follow-up timer (2 days from now).
- **Action taken**: None.

### [LOW] [Sweep 4] No open PRs, no stale remote branches

- Clean state. PR #844 (workspace cleanup) was the last to merge; deletion was set automatically.

### [LOW] [Sweep 5] Spec-vs-code gap audit — only one workspace has a local `specs/`

- **Location**: `workspaces/dataflow-engine-pyright-cleanup/specs/` (the only workspace-local specs dir found)
- **Disposition**: DEFER (covered by the stale-workspace finding above)
- **Evidence**: Repo-root `specs/` is the canonical spec authority (per `rules/specs-authority.md`); workspace-local specs/ is rare and serves the engine-pyright cleanup. The actively-released code (kailash-dataflow 2.7.9) was Tier-2 verified by 9 regression tests against real Postgres, with `specs/dataflow-cache.md` §12.1 + §13.4 updated and merged in PR #842.
- **Why this matters**: A full repo-wide spec-vs-code AST/grep sweep is a `/redteam` task, not a `/sweep` task. None of the recently-released packages flagged spec drift in their /redteam rounds.
- **Action taken**: None.

### [LOW] [Sweep 6] One uncommitted file: `workspaces/issue-835-dataflow-transaction-eventloop/.session-notes`

- **Location**: working tree
- **Disposition**: EXPECTED — `/wrapup` writes the file as the last act of the session
- **Evidence**: The file is the deliverable of `/wrapup` and was written immediately before this sweep.
- **Why this matters**: Per the `.session-notes` convention, the file lives in the working tree until the next session either commits it or overwrites it.
- **Action taken**: None — leaving as-is per session-notes convention.

### [LOW] [Sweep 7] Branch divergence 0/0; no zero-tolerance markers introduced this session

- Working tree clean except `.session-notes`. No new TODO/FIXME/HACK/XXX/NotImplementedError introduced.

## Cross-cutting observations

- **Workspace hygiene drift**: 3 of 5 workspaces have active/ todos that don't match their delivered-PR state. The pattern is "ship the PR, never move the todos." Recommend a standing 1-minute housekeeping rule at the end of every release (move active/ → completed/ before declaring done).
- **No release drift**: every framework package is at parity with PyPI as of release-time enumeration this session. The `/release` cycle for kailash-dataflow 2.7.9 also surfaced no sibling drift.
- **Cross-SDK debt is bounded**: only one user-gated thread is open (kailash-rs Tokio sqlx parity for #835). Issue #760's 2026-05-08 cross-SDK follow-up is on the calendar.

## Recommended next-session items (ranked)

1. **Workspace-todo housekeeping pass** — move completed todos out of `active/` for `dataflow-engine-pyright-cleanup` (10 entries) + `issue-835-dataflow-transaction-eventloop` (2 entries) + reconcile `issue-814-kaizen-pyright` Phase 2 against shipped 2.19.0/2.20.0 (6 entries). 15-30 min total.
2. **Pending journal triage** — promote/discard 5 `.pending/` entries across #814 + #835 workspaces. 10 min.
3. **Cross-SDK kailash-rs companion** — answer the user-gated question on `issue-835` Todo 06 (file the parity issue against `esperie/kailash-rs`?). User decision; agent drafts body if approved.
4. **Cross-SDK follow-up** — issue #760's 2026-05-08 verification (kailash-rs#723 fingerprint pin + #759 DPI-A repro). Calendar reminder.
5. **`dataflow-engine-pyright-cleanup` workspace lifecycle decision** — archive to `workspaces/_archive/` (the work shipped as 2.7.7) or keep as a living spec home. User call.
