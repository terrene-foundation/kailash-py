apiVersion: v1
kind: Service
metadata:
  name: kailash-server
  labels:
    app: kailash
    component: server
spec:
  type: ClusterIP
  selector:
    app: kailash
    component: server
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
