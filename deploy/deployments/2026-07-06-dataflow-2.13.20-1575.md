# Deployment — kailash-dataflow 2.13.20 (#1575)

**Date:** 2026-07-06
**Type:** dataflow-only patch release (no core change)
**Issue:** #1575 (sibling of #1572 — adapter pools created on transient / owned event loops **outside** the sync→async bridge leaked at GC: `RuntimeError: Event loop is closed` + `ResourceWarning: Unclosed connection`)

## What shipped

- **kailash-dataflow 2.13.19 → 2.13.20** — every remaining non-bridge site that creates an adapter pool now drains it on teardown:
  - **Schema-discovery inspect paths** — `_inspect_database_schema_real` (PostgreSQL + SQLite branches) drains the adapter in a guarded `try/finally` on every exit path (idempotent; logs exception TYPE only, never a DSN). Pre-fix the close sat outside `try/finally`, so a mid-discovery exception stranded the pool.
  - **Transient loop-creating sites** — `discover_schema`, `validate_schema`, the `current_schema` fallback, and the model-registry workflow bridge route through `async_safe_run` (stamps `BRIDGE_LOOP_ATTR` → the #1572 `loop_pool_registry` drains the loop before close). The running-loop `DATAFLOW-SESSION-LOOP-DEADLOCK-001` guard is preserved unchanged.
  - **`SyncExpress` (`db.express_sync`) owned persistent loop** — previously had no teardown, leaking {daemon thread + loop + loop-bound connections} per DataFlow instance. `DataFlow.close()`/`close_async()` now drive `SyncExpress.close()`, which drains loop-bound adapters ON the owning loop (while alive) then stops/joins(5s)/closes it; `__del__` emits `ResourceWarning` if never closed (mirrors `SyncTransactionManager`). `SyncTransactionManager` was already correct (per-transaction connections close before the loop does).
  - **Dispatch hardening** — `_run_sync` raises a typed "SyncExpress is closed" error after `db.close()` (not an opaque `AttributeError`); a call in flight when `close()` runs settles with `CancelledError` in bounded time instead of hanging on `future.result()`; the submit-vs-close window is serialized under the existing pending-futures lock (real work never serialized — `future.result()` stays outside the lock).

**dataflow-only rationale:** the drain mechanism lives in core `kailash.utils.loop_pool_registry` (shipped in kailash 2.45.4, #1572). #1575 uses those existing symbols — **no new core symbols**, so no core release; floor stays `kailash>=2.45.4`.

## Fix + release chain

- Fix PR **#1578** (merge `1946b8ec9`, fix commits `f141afcf0`…`589c1180e` + release-prep `857f3415f`) — full matrix GREEN (20 SUCCESS / 4 SKIPPED / 0 fail on head `857f3415f`). #1575 auto-closed via `Fixes #1575`.
- Tag `dataflow-v2.13.20` → publish-pypi OIDC run **28760862262** SUCCESS (GitHub Release created).
- TestPyPI skipped per the patch human-approval precedent (user: "go" / "publish").

## Verification (clean-venv, live PyPI)

`uv pip install --refresh kailash-dataflow==2.13.20` (attempt 2 after the documented PyPI/uv index-propagation lag):

- `dataflow.__version__ == "2.13.20"`.
- Wheel smoke: `SyncExpress.close`, `SyncExpress._drain_loop_resources`, `SyncExpress.__del__`, and the `_run_sync` closed-guard ("SyncExpress is closed") all present — the fix shipped, not just the version bump.

## Test + red-team evidence

- Tier-2 regression on real PostgreSQL:5434 + MySQL:3307 + file-backed SQLite (`test_issue_1575_transient_loop_pool_drain.py`): **11/11 green**, suite clean under `-W error::ResourceWarning`. Deterministic exception-path drain assertions for the PG + SQLite inspect branches (fail pre-fix / pass post-fix), a `SyncExpress` owned-loop lifecycle GC-capture on PG **and** MySQL, a model-registry bridge invariant, the after-close typed-error + concurrent-close no-strand hardening tests, and an idempotent-close test.
- Red team — 5 rounds to convergence: R1 core transient-drain CLEAN; R2 caught the `SyncExpress` owned-loop teardown gap (HIGH) → fixed; R3 caught fix-introduced concurrent-close hang + typed-guard gap + a strict secret-logging deviation → hardened; R4 CLEAN (TOCTOU unreproducible in 1500 stressed settlements; collateral warnings proven pre-existing); R5 CLEAN (model-registry no-pool claim verified 33 sync / 0 async nodes; F2 concurrent-close stress 960 settlements, 0 hangs, 0 deadlocks).

## Out-of-scope (tracked separately, SHA-verified pre-existing)

- 14 `tests/integration/transactions/` failures (`'ProductionPostgreSQLAdapter' object has no attribute 'transaction'`) — byte-identical base↔HEAD, different subsystem → follow-up issue.
- `#1552`-class driver-error `str(e)` logs at `engine.py` 4644/4805/7045/7068 (schema-discovery error path) → #1552 driver-error-redaction workstream.
- Cross-SDK #1575 inspection of the Rust SDK for the same transient/owned-loop drain gap → requires explicit cross-repo authorization (repo-scope-discipline), not self-initiated.
