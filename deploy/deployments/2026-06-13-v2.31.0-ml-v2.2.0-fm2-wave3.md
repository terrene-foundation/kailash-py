# Deployment Record — 2026-06-13 — kailash 2.31.0 + kailash-ml 2.2.0 (FM2 Wave 3)

Two-package coordinated minor release completing the M2 feature store (FM2 Wave 3 of 3, closes #693).

## Scope

- **kailash 2.30.0 → 2.31.0** (minor) — new public exception class `OnlineStoreUnavailableError` in `kailash.ml.errors` (a `FeatureStoreError` subclass, `src/kailash/ml/errors.py:758`), supporting the kailash-ml online-store adapter. Purely additive to the canonical ML error taxonomy.
- **kailash-ml 2.1.1 → 2.2.0** (minor) — Redis-backed online feature-store adapter (`OnlineFeatureStore`, graduates spec §11.4a) + self-healing model re-registration on fresh-instance read + spec graduation (`dataflow-ml-integration.md` 1.0.0-draft → 2.0.0; `ml-feature-store.md` §6/§11 citations re-derived). Core pin floored `kailash>=2.31.0`.

## PR / merge

- PR #1310 (`feat/fm2-wave3`) — admin-merged to `main` at merge commit `86f190b1f` (#693 auto-closed).
- 10 commits: 3 shards (C online-store + S6 spec graduation) + §11.7 conflict-resolution merge + version bumps + redteam fix + CI redis-`[dev]` fix.

## Holistic terminal /redteam (Waves 1/2/3)

Three parallel reviewers against the union of merged FM2 shards:

- **Closure-parity / spec-accuracy: CONVERGED** — all 7 §11.7 `errors.py:NNN` citations exact (682/694/711/722/737/477 + OnlineStoreUnavailableError:758); contract fully wired (subclass + re-export + masked raise-site at `online_store.py:432`); graduation framing clean; zero conflict markers.
- **Security: APPROVE** — tenant isolation structural (tenant embedded in canonical cache key; colon-injection rejected); credentials env-sourced + masked at every surface; error messages leak no schema/credentials; self-heal model byte-identical to write path, driven by authored schema; no raw SQL/DDL.
- **Code review: CHANGES-REQUIRED → resolved** — `OnlineFeatureStore` async-resource-class missing `__del__`/`ResourceWarning` (patterns.md § Async Resource Cleanup); fixed in `77cc0d7` (warn-and-return only, no close()/logging from `__del__`) + regression tests (unclosed → ResourceWarning at GC; closed → silent). Defense-in-depth schema-name re-validation (security LOW) in the same commit.

Two bounded non-blocking follow-ups deferred: (1) consolidate `_mask_redis_url` onto the shared `kailash.utils.url_credentials` masker (behaviorally compliant today; cross-package refactor); (2) correct two stale citations in a pre-existing `specs/ml-registry.md` historical audit block (`engines/engine.py:75-80` no longer exists; `model_registry.py:473` write is at `:537`) — registry domain, not in this PR's diff.

## CI

PR #1310 PR-gate matrix green (25 pass / 0 fail; CUDA-GPU smoke non-required + queued, irrelevant to the Redis/spec blast radius). First CI run surfaced a real red — `Base (Python 3.12)` Coverage step, 7 `OnlineFeatureStore` tests failing at construction with the loud `redis`-missing ImportError — diagnosed as a CI-config gap (Base installs `[dev]`; `redis` lived only in `[online-store]`) and fixed by declaring `redis>=6.2.0` in `[dev]` (`Declared = Imported`, matching the pandas/onnxruntime test-time entries) so the wiring tests RUN in the Base matrix. Coverage itself 76% > 60%.

## Release mechanism

Lightweight tags pushed individually → `publish-pypi.yml` (OIDC trusted publisher):

- `v2.31.0` → run `27459453161` SUCCESS
- `ml-v2.2.0` → run `27459460095` SUCCESS

TestPyPI skipped per the minor-release human-approval exception (in-session "approve release").

## Clean-venv install verification (done-gate)

Fresh `/tmp` venv (Python 3.12), `pip install --no-cache-dir` (the `build-repo-release-discipline.md` Rule 2 fallback — `uv pip install --refresh` hit the documented deeper-index-cache lag while `…/2.31.0/json` + `…/2.2.0/json` returned HTTP 200):

- `kailash.__version__ == "2.31.0"` ✓
- `kailash_ml.__version__ == "2.2.0"` ✓
- `kailash.ml.errors.OnlineStoreUnavailableError is kailash_ml.errors.OnlineStoreUnavailableError` (re-export identity) ✓; `issubclass(..., FeatureStoreError)` ✓
- `kailash_ml.features.OnlineFeatureStore` importable ✓

## Sibling drift

No sibling drift — only `kailash` + `kailash-ml` had `main` ahead of PyPI at release-time enumeration (kailash 2.31.0 vs PyPI 2.30.0; kailash-ml 2.2.0 vs PyPI 2.1.1). All other framework packages AT-PARITY.

## Outstanding (user action)

- **F-YANK**: `kailash-ml` **2.1.0** remains live on PyPI with a broken `kailash>=2.16.0` pin (ships ImportError to fresh installs). 2.2.0 (and the 2.1.1 hotfix) floor `>=2.31.0`/`>=2.30.0` correctly; 2.1.0 should be yanked from PyPI (web-only operator action).
