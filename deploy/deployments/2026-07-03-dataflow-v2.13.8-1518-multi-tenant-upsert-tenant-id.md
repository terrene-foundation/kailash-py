# Deployment Record — kailash-dataflow 2.13.8 (#1518)

**Date:** 2026-07-03
**Package:** kailash-dataflow `2.13.7 → 2.13.8` (patch, dataflow-only)
**Issue:** #1518 — multi_tenant upsert mis-maps `tenant_id` (cross-tenant leak class)
**Fix PR:** #1525 · **Release-prep PR:** #1527 · **Tag:** `dataflow-v2.13.8` · **Publish run:** `28657016873` (SUCCESS)

## What shipped

On a `multi_tenant=True` DataFlow, a single-record upsert persisted the **wrong** value in
`tenant_id` — it received the row's `id` value instead of the active tenant, so write-path tenant
scoping was silently built against a bogus `tenant_id` (`rules/tenant-isolation.md` cross-tenant
leak class).

Root cause (traced end-to-end): the SQLite precheck-upsert builder (`build_precheck_upsert_query`,
#1508) emits **named `:pN`** placeholders. The tenant `QueryInterceptor` appends `tenant_id` when
the INSERT column list omits it (the upsert `insert_data = {**where, **create}` never carries
`tenant_id`), but `_detect_placeholder_style` did not recognise the `:pN` style and defaulted to
`qmark`, appending a lone `?` into a `:pN` query. Downstream `_convert_to_named_parameters`
renumbered that `?` to `:p0` (its counter restarts at 0), colliding with the existing `:p0`, so
`tenant_id` bound to the first value. The same `:pN`-blindness also corrupted the existing-row
`UPDATE` branch.

Fix: the interceptor now recognises the `:pN` (`colon`) style across the INSERT, UPDATE/DELETE, and
SELECT injection paths, so the injected query is pure `:pN` — structurally identical to the
already-correct non-tenant upsert. `:pN` is emitted by every upsert/bulk dialect builder, so the
machinery-level fix covers all of them. `_tenant_placeholder_for_style` now fails LOUD on `colon`
(callers must emit `:p{len}` inline). A caller-supplied `tenant_id` in `create` is overwritten to
the active tenant (no spoofing). Pre-existing on `main`; surfaced red-teaming #1508.

## Verification

- **Red-team CONVERGED** — Round 1: `reviewer` + `security-reviewer` (parallel), both
  MERGE-WITH-FIXES (core fix correct, index invariant verified, no live leak). Shared finding: the
  SELECT injection path was not `:pN`-aware (same bug class, unreachable today — no SELECT builder
  emits `:pN` — but asymmetric). Fixed in-session per `autonomous-execution.md` Rule 4. Round 2:
  adversarial verifier → CONVERGED on the delta (no off-by-one, no `:pN` collision, non-colon paths
  byte-identical, tests genuine).
- **Tests:** 11 Tier-2 + structural regression tests (`test_issue_1518_multi_tenant_upsert_tenant_id.py`,
  real SQLite, raw `sqlite3` read-backs) — new-row INSERT persists the active tenant regardless of
  create-dict column order; existing-row UPDATE stays tenant-scoped; a second tenant's same-key
  upsert fails closed (UNIQUE) and cannot corrupt the first tenant's row; caller-supplied `tenant_id`
  is overwritten; structural locks on colon detection + INSERT/UPDATE/SELECT `:p{N}` index invariants
  - the fail-loud helper. All fail without the fix. Broader gate: 115 passed / 9 skipped (PG-infra) /
    0 fail across tenancy + upsert suites; #1249 SELECT-injection + #1252 bulk + #1508 + #1498
    unregressed.
- **CI:** Fix PR #1525 full matrix GREEN (12 checks). Release-prep PR #1527 (`release/v2.13.8-dataflow`,
  `--admin`, matrix path-skipped).
- **Clean-venv (Python 3.12, published wheel + `aiosqlite`, after the documented PyPI/uv index-cache
  lag — 1 retry):** `dataflow.__version__ == "2.13.8"`; #1518 repro (multi_tenant upsert, tenant_id
  omitted from `create`) → raw row `tenant_id == "tenant-a"` (was the `id` value pre-fix). PASS.
  TestPyPI skipped per the patch human-approval precedent (user "approved").

## Scope / drift

Dataflow-only patch (security: cross-tenant leak class). No sibling drift — all 8 packages at PyPI
parity at release-time enumeration (kailash 2.45.3, nexus 2.11.0, kaizen 2.28.0, mcp 0.2.15, ml
2.2.2, align 0.7.4, pact 0.14.3 all == PyPI). Core `kailash` unchanged — no `kailash>=` floor bump
(the fix is entirely within the dataflow tenancy interceptor).

## Out of scope / filed separately

- **#1526** (design) — `multi_tenant` models use single-column `id` PK, so two tenants cannot share
  one natural key (a same-key cross-tenant upsert fails closed with a UNIQUE constraint — safe, no
  leak). The `#1518` AC "two tenants sharing a natural key get separate rows" needs composite
  `(tenant_id, id)` uniqueness — a schema-generation change (migrations, PG+SQLite, cross-SDK), too
  large for this shard. Filed for the maintainer's decision.
- **#1519** (HIGH) — `bulk_upsert` silently ignores `conflict_on` on SQLite. Design shard (3
  divergent bulk builders + orphaned dialect methods).
- **#1520** (MEDIUM) — PG `conflict_on` on a non-unique field raises a cryptic driver error; add an
  actionable up-front error.
