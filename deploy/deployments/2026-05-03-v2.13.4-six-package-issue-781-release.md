# 2026-05-03 — Six-package release closing issue #781 cleanup

## Scope

Patch release across 6 packages cutting PyPI for the issue #781 cleanup workstream (T1-T5 merged via PRs #804/#805/#806/#807/#808) plus sibling-package sweep per `build-repo-release-discipline.md` Rule 1.

| Package          | From   | To         | Tag                    | publish-pypi.yml result |
| ---------------- | ------ | ---------- | ---------------------- | ----------------------- |
| kailash (core)   | 2.13.3 | **2.13.4** | `v2.13.4`              | success                 |
| kailash-dataflow | 2.7.5  | **2.7.6**  | `dataflow-v2.7.6`      | success                 |
| kailash-kaizen   | 2.18.0 | **2.18.1** | `kaizen-v2.18.1`       | success                 |
| kailash-nexus    | 2.6.0  | **2.6.1**  | `nexus-v2.6.1`         | success                 |
| kaizen-agents    | 0.9.4  | **0.9.5**  | `kaizen-agents-v0.9.5` | success                 |
| kailash-mcp      | 0.2.10 | **0.2.11** | `mcp-v0.2.11`          | success                 |

## Per-package change classification

- **kailash 2.13.4** — T4 (core/runtime TODO-NNN comment-strip) + T5 (CI gate + regression test). Comment-only + new gate; no API changes.
- **kailash-dataflow 2.7.6** — T1 (89 markers stripped across `migrations/`, `core/`, `fabric/`, `features/`). Comment-only.
- **kailash-kaizen 2.18.1** — T2 (80 markers stripped across `research/`, `tools/native/`, `core/`, `core/autonomy/`, `mixins/`, `strategies/`, `execution/`, `session/`, `integrations/`, `docs/`) + #801 test fix. Comment-only.
- **kailash-nexus 2.6.1** — T4 (16 markers stripped in `nexus/core.py` public-API banners + `auth/` subsystem). Comment-only.
- **kaizen-agents 0.9.5** — T3 (69 markers stripped across `agents/autonomous/base.py`, `runtime_adapters/docs/`, `patterns/`, `api/shortcuts.py`). Comment-only. 3 commits bypassed pre-commit hooks per documented `git -c core.hooksPath=/dev/null` exception (Black + Ruff wanted `Optional[X] → X | None` modernization that exceeds T3's comment-only mandate; flagged as follow-up workstream).
- **kailash-mcp 0.2.11** — sibling sweep per Rule 1: W6-002 type fix (`ElicitationSystem._CapturingTransport.response_handler` typed as `Optional[Callable]`) + 10 Tier-2 ElicitationSystem integration tests + LOW-6 stub-marker triage already on main since 2026-04-28.

## SDK dep pin sweep

Per `/release` Step 2 SDK Dependency Pin Update Rule, every framework package's `kailash>=` pin bumped to `>=2.13.4`:

- Released this cycle: dataflow, kaizen, nexus, mcp, kaizen-agents
- Not released this cycle but pin updated for consistency: kailash-align (still 0.7.0), kailash-pact (still 0.11.0), kailash-ml (still 1.7.1)
- kaizen-agents `kailash-kaizen` pin also bumped to `>=2.18.1`

## Release pipeline

1. **Release-prep PR**: [#809](https://github.com/terrene-foundation/kailash-py/pull/809) on `release/v2.13.4` branch (`release/v*` convention auto-skips PR-gate matrix on metadata-only diff per `rules/git.md`).
2. **Pre-commit**: green on all 21 changed files (12 version anchors + 8 dep pin updates + 7 CHANGELOG entries; pre-commit isort auto-fix on align/ml init reverted to keep release-prep purely metadata-only).
3. **CI**: 18 skipped per `release/v*` auto-skip; 3 success (Build Documentation, Version Consistency × 2 from align + ml workflows); 1 CANCELLED build was concurrency-replaced by the success run.
4. **Merge**: admin override per BUILD-repo standing rule (review_required is the only blocker on metadata-only release-prep PRs).
5. **Tag push**: 6 tags pushed sequentially with `sleep 2` between each per `rules/deployment.md` § "Multi-Package Release Tags Pushed Individually" (batch push silently drops triggers).
6. **publish-pypi.yml**: all 6 workflows triggered + completed `success` via OIDC trusted publishing.
7. **Clean-venv install verification** (per `build-repo-release-discipline.md` Rule 2 — done gate, not merge):

   ```bash
   for pkg in kailash:2.13.4:kailash kailash-dataflow:2.7.6:dataflow kailash-kaizen:2.18.1:kaizen \
              kailash-nexus:2.6.1:nexus kaizen-agents:0.9.5:kaizen_agents kailash-mcp:0.2.11:kailash_mcp; do
     uv venv "/tmp/verify-${pkg%%:*}" --python 3.12 -q
     uv pip install --python "/tmp/verify-${pkg%%:*}/bin/python" --refresh -q "${pkg%:*}==${pkg#*:}"
     "/tmp/verify-${pkg%%:*}/bin/python" -c "import ${pkg##*:}; print(${pkg##*:}.__version__)"
   done
   ```

   All 6 returned correct version strings on first attempt (no PyPI cache lag).

## Closes

- Issue #781 (TODO-NNN cleanup workstream) — closed via merged commit on T6 (final audit) earlier in the same session.

## Follow-up workstreams (drafts in workspace, NOT auto-filed per upstream-issue-hygiene Rule 1)

- **kaizen pyright cleanup** — pre-existing diagnostics in `tools/native/*` BaseTool override mismatches + missing `research/` modules; SHA-grounded to `b511f186` (2026-03-19). Draft issue at `workspaces/issue-781-todo-nnn-cleanup/03-implementation/T2-followup-issue-kaizen-pyright.md`.
- **kaizen-agents auto-formatter sweep** — Black + Ruff modernization of `Optional[X] → X | None` etc.; rejected from T3 to keep diffs comment-only. Bypass documented in commit bodies.
- **nexus E2E port-mismatch** — `test_ai_agent_discovery_and_exploration` pre-existing failure, SHA-grounded to `b553104c` (2026-03-11).
