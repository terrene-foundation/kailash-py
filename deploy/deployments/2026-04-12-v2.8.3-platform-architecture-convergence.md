# Release: Platform Architecture Convergence — 2026-04-12

## Packages Released

| Package          | Version | Tag                    | PyPI Status     |
| ---------------- | ------- | ---------------------- | --------------- |
| kailash          | 2.8.3   | `v2.8.3`               | published       |
| kailash-dataflow | 2.0.5   | `dataflow-v2.0.5`      | publishing (CI) |
| kailash-ml       | 0.9.0   | `ml-v0.9.0`            | publishing (CI) |
| kaizen-agents    | 0.9.2   | `kaizen-agents-v0.9.2` | publishing (CI) |

## PRs Merged

- PR #425 — fix(ci): install trust+dev extras in cross-sdk conformance workflow
- PR #426 — chore(release): bump versions for v2.8.3 release

## What Shipped

### kailash 2.8.3 (core SDK)

- `EventLoopWatchdog` — deadlock detection for async workflows
- `ProgressUpdate` contract — structured streaming progress events
- `safe_log_value()` — CodeQL-compliant log sanitizer (py/clear-text-logging-sensitive-data)
- PACT N4/N5/N6 conformance — JSON schema, CBOR, byte-identical serialization vectors
- Cross-SDK conformance CI fix — `uv pip install -e ".[trust,dev]"` instead of `uv sync`

### kailash-dataflow 2.0.5

- Patch: watchdog loop= deprecation fix + progress orphan wiring (PR #423)

### kailash-ml 0.9.0 (MINOR — breaking)

- **BREAKING**: `DriftMonitor.set_reference()` renamed to `set_reference_data()` for API clarity

### kaizen-agents 0.9.2

- Patch: wiring fixes and cleanup from convergence session

## Version Bump Summary

| Package          | Before | After |
| ---------------- | ------ | ----- |
| kailash          | 2.8.2  | 2.8.3 |
| kailash-dataflow | 2.0.4  | 2.0.5 |
| kailash-ml       | 0.8.1  | 0.9.0 |
| kaizen-agents    | 0.9.1  | 0.9.2 |

## Publishing Method

Tag-triggered GitHub Actions via OIDC trusted publisher (`publish-pypi.yml`).
No manual twine uploads — all CI-automated.
