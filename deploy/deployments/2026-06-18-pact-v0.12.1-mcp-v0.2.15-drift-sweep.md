# Deployment Record — kailash-pact 0.12.1 + kailash-mcp 0.2.15 (drift sweep)

**Date:** 2026-06-18
**Packages:** kailash-pact (patch) + kailash-mcp (patch) — two-package sweep
**Purpose:** bring the package source trees and PyPI back in sync (pristine) by
releasing two previously-unreleased source commits left un-bumped by prior sessions.

## What shipped

Neither change is functional; both are released purely to eliminate
source-vs-PyPI drift (per `build-repo-release-discipline.md` Rule 1 sibling-sweep).

- **kailash-pact 0.12.0 → 0.12.1** — commit `7abe1a2c2`: reworded two
  `# type`-substring comments to dodge the `python-use-type-annotations` pygrep
  false positive (`rules/git.md` § Discipline) + removed one verified-dead
  duplicate local import in `pact/engine.py::verify_audit_chain`. No runtime
  behavior change; AST parses unchanged.
- **kailash-mcp 0.2.14 → 0.2.15** — commit `1e17d63df` (#1258): documented +
  pinned non-ASCII byte-vectors for the 4 MCP canonical encoders in
  `protocol/messages.py`. Documentation + test-vector pins only; zero runtime /
  API / behavior change.

## Pipeline

- Combined release-prep PR **#1363** (`release/pact-0.12.1-mcp-0.2.15`, merge
  `2ee743eae`) — version bumps (pyproject + `__init__`) + CHANGELOG for both;
  PR-gate matrix path-skipped per `release/`.
- Tags pushed individually (multi-tag discipline): `pact-v0.12.1` → publish-pypi
  run `27706314940` **SUCCESS**; `mcp-v0.2.15` → publish-pypi run `27706325566`
  **SUCCESS**.
- TestPyPI skipped per the patch human-approval exception (user: "sweep them please").

## Verification (clean-venv, live PyPI)

`uv pip install --refresh` (first attempt, no cache lag):

- `kailash-pact==0.12.1` → `import pact; pact.__version__ == "0.12.1"`
- `kailash-mcp==0.2.15` → `import kailash_mcp; kailash_mcp.__version__ == "0.2.15"`

## Pristine-state confirmation

Post-sweep drift enumeration: ALL packages (core kailash 2.38.2 + all 8
sub-packages) show 0 source commits since their latest release tag — main source
trees == PyPI across the entire monorepo.
