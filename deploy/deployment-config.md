# SDK Release Configuration

## Build System

- **Tool**: setuptools (pyproject.toml)
- **Python**: >=3.11

## Publishing

- **Registry**: PyPI (production), TestPyPI (validation)
- **Method**: Trusted Publisher (OIDC) via GitHub Actions
- **Trigger**: Git tags (`v*` for core, `kaizen-v*` for Kaizen, etc.)
- **Workflow**: `.github/workflows/publish-pypi.yml`
- **Strategy**: Tag-triggered automated publishing. No manual twine uploads.

## Packages

| Package          | Tag Pattern        | Current Version |
| ---------------- | ------------------ | --------------- |
| kailash (core)   | `v*`               | 2.45.5          |
| kailash-dataflow | `dataflow-v*`      | 2.14.5          |
| kailash-kaizen   | `kaizen-v*`        | 2.28.0          |
| kailash-nexus    | `nexus-v*`         | 2.9.0           |
| kailash-pact     | `pact-v*`          | 0.12.1          |
| kailash-ml       | `ml-v*`            | 2.2.2           |
| kailash-align    | `align-v*`         | 0.7.4           |
| kailash-mcp      | `mcp-v*`           | 0.2.15          |
| kaizen-agents    | `kaizen-agents-v*` | 0.9.11          |

Last updated: 2026-07-24 — single-package minor release `kailash-kaizen 2.41.0 → 2.42.0` closing **#1946** (RAG nodes silently mock-dispatched). 2.41.0 (#1943, same day) closed provider omission across the `Agent` deployment surface, but `kaizen.nodes.rag` was explicitly out-of-scope for that fix — its 26 `LLMAgentNode` construction sites across 7 files (`query_processing`, `agentic`, `conversational`, `evaluation`, `graph`, `similarity`, `multimodal`) still omitted `provider`, and because `LLMAgentNode`'s `provider` parameter defaults to `"mock"`, every RAG workflow stage silently mock-dispatched regardless of any configured `OPENAI_API_KEY` / `ANTHROPIC_API_KEY` (`workflows.py` already set a provider and was unaffected). Fix: every omitting site now resolves through the same shared `kaizen.core._provider_env.detect_provider_from_env()` helper 2.41.0 introduced (env-first: `OPENAI_API_KEY`→openai, `ANTHROPIC_API_KEY`→anthropic, else mock) — purely additive, zero-tolerance Rule 2/3. Fix PR #1949 (`fix/1946-rag-provider-sweep`, merge `d898ae4e4`; full PR-gate matrix GREEN — 3.11–3.14, DataFlow, PACT, kaizen LLM/cross-SDK-parity ×2, CodeQL, Version Consistency ×2, test-with-infrastructure, Analyze Python, all SUCCESS on the pinned head SHA `fcfbd6215`); redteamed to convergence pre-merge (reviewer + adversarial both CONVERGED; 1327 RAG tests pass; purely-additive diff). TestPyPI validated (minor-release rule) at `test.pypi.org/project/kailash-kaizen/2.42.0/`, clean-venv install (`uv pip install --refresh --index-url test.pypi.org ... "kailash-kaizen[rag]==2.42.0"`, attempt 2 after the documented TestPyPI/uv index-propagation lag) verified `kaizen.__version__ == "2.42.0"` + the fix present (`detect_provider_from_env` importable in all 7 touched RAG modules). Production tag `kaizen-v2.42.0` → publish-pypi run `30066114726` SUCCESS (Determine-package → Build → Publish-to-PyPI → Create-GitHub-Release, all green). Clean-venv install verified live from a FRESH venv (`uv pip install --refresh "kailash-kaizen[rag]==2.42.0"`, attempt 2 after the documented PyPI/uv index-propagation lag): `kaizen.__version__ == "2.42.0"` + `detect_provider_from_env()` resolves `"mock"` with no API keys present (correct fail-safe default) + `kaizen.nodes.rag` imports with zero `ImportError` (the `[rag]` extra — numpy/Pillow/networkx/requests — is the documented complete dependency set for that subpackage, not a new gap) + `detect_provider_from_env` present in the source of all 7 touched files (agentic, conversational, evaluation, graph, multimodal, similarity, query_processing). Docs/README version pin bumped 2.41.0→2.42.0 (`packages/kailash-kaizen/README.md`; `README.md` / `docs/getting_started.rst` / `docs/index.rst` carry no kaizen-specific version pin). No sibling drift — full 9-package enumeration confirmed all other packages AT-PARITY with PyPI at release-time (`kailash` 2.61.0, `kailash-dataflow` 2.19.1, `kailash-nexus` 2.14.0, `kailash-mcp` 0.4.3, `kailash-ml` 2.2.2, `kailash-align` 0.7.4, `kailash-pact` 0.18.0, `kaizen-agents` 0.12.0). Tracking issue #1946 (SDK-surface only, no workspace paths/session IDs).

Last updated: 2026-07-24 — single-package minor release `kailash-kaizen 2.40.0 → 2.41.0` closing **#1943** (provider-dispatch integrity). Two related defects: (1) a test-only "mock-upgrade" mechanism ran on the PRODUCTION agent response path with no provider guard — a genuine LLM answer opening with a generic phrase (e.g. "Based on the provided data and context…") could be misclassified as a mock template and silently replaced with fabricated content, even when a real provider was dispatched; (2) `LLMAgentNode`'s `provider` parameter defaults to `"mock"`, and many construction sites across the Agent surface omitted it, so they silently mock-dispatched (`Agent.compile_workflow`, the CoT/ReAct pattern executor, `communicate_with`/`broadcast_message`, `Agent.to_node_config`, `BaseAgent.to_workflow`, `NexusDeploymentMixin.to_workflow` — which additionally used the wrong key `llm_provider` — `DeploymentCache` cache-key resolution, and the multi-modal signature compiler). Fix: mock-upgrade substitution now gated on the actually-dispatched provider (real-provider output always returned verbatim); provider resolved consistently at every construction/cache site via new shared helper `kaizen.core._provider_env.detect_provider_from_env()` (env-first: `OPENAI_API_KEY`→openai, `ANTHROPIC_API_KEY`→anthropic, else mock); compiled-workflow memos rebuild when the resolved provider drifts instead of serving a stale cached provider; mock-upgrade WARN logs now fingerprint (length + sha256 prefix) instead of logging raw payloads. **Behavioral change**: agents/nodes that previously returned mock output because a provider was never set now dispatch to the real provider when API keys are present in the environment. Fix commit `31460f385` + version-bump commit `b38211bc2` + docs-version-bump commit `960f8a0eb`, merged via PR #1944 (`fix/kaizen-mock-upgrade-provider-guard`, merge `2bd8ec7f4`, full PR-gate matrix GREEN — the one non-SUCCESS check was a `cancel-in-progress` cancellation of the duplicate push-event "Build Documentation" run, the live `pull_request` run on the same head SHA passed); security-reviewer PASS (no CRIT/HIGH; two LOW/informational notes — fingerprint-prefix collision bound, a non-delegated-but-identical provider-resolution site). TestPyPI validated (minor-release rule) at `test.pypi.org/project/kailash-kaizen/2.41.0/`, clean-venv install (`uv pip install --index-url test.pypi.org ... kailash-kaizen==2.41.0`) verified `kaizen.__version__ == "2.41.0"` + `kaizen.core._provider_env.detect_provider_from_env` importable. Production tag `kaizen-v2.41.0` → publish-pypi run `30063060292` SUCCESS (Determine-package → Build → Publish-to-PyPI → Create-GitHub-Release, all green). Clean-venv install verified live from a FRESH venv (`uv pip install --refresh`, attempt 2 after the documented PyPI/uv index-propagation lag — `pypi.org/pypi/kailash-kaizen/2.41.0/json` returned metadata on attempt 1's failure, confirming publish had landed): `kaizen.__version__ == "2.41.0"` + `detect_provider_from_env()` resolves `"mock"` with no API keys present (correct fail-safe default) + the new module imports with zero `ImportError`. Docs/README version pins bumped 2.40.0→2.41.0 (`docs/getting_started.rst`, `docs/index.rst`, `README.md`, `packages/kailash-kaizen/README.md`). No sibling drift — full 9-package enumeration confirmed all other packages AT-PARITY with PyPI at release-time (`kailash` 2.61.0, `kailash-dataflow` 2.19.1, `kailash-nexus` 2.14.0, `kailash-mcp` 0.4.3, `kailash-ml` 2.2.2, `kailash-align` 0.7.4, `kailash-pact` 0.18.0, `kaizen-agents` 0.12.0). Tracking issue #1943 (SDK-surface only, no workspace paths/session IDs). Out of scope (separate follow-ups, explicitly not touched): `kaizen.nodes.rag`, the `LLMAgentNode` `provider="mock"` default itself.

Last updated: 2026-07-06 — two-package patch release closing **#1581** + **#1587**: `kailash 2.45.4 → 2.45.5` + `kailash-dataflow 2.13.20 → 2.13.21`, published core-first (dataflow `kailash>=` floor `2.45.4 → 2.45.5`). **#1581** (generated DataFlow CRUD nodes not transaction-aware — a silent data-integrity violation): each CRUD node ran every statement on its own cached `AsyncSQLDatabaseNode` in `transaction_mode="auto"` (per-statement autocommit; the kwarg was inert), so inside a `TransactionScopeNode` a CRUD write committed independently and **survived a later `TransactionRollbackNode`**. Core `kailash 2.45.5`: `_AdapterTransactionScope.transaction` property + `AsyncSQLDatabaseNode.async_run` reads `inputs["transaction"]` and threads it through `_execute_with_retry`/`_execute_with_transaction` (+ batch path) via a borrow-don't-own branch (param-threaded, no instance-state leak); also ships #1580's uniform `adapter.transaction()` contract + pool-safe commit/rollback (unreleased since the v2.45.4 tag). Dataflow `2.13.21`: a module-level `_run_sql_in_scope` reads `active_transaction` off the workflow context and injects `scope.transaction` at all 10 single-record CRUD sites (read-your-writes inside scope; discarded on rollback; **fail-closed** `NodeExecutionError` when a scope has no handle); the PG `$11` retry runs on the POOLED node (joins the scope); `TransactionSavepointNode` + `TransactionRollbackToSavepointNode` registered (were orphaned); savepoint-name `re.match(...$)` → `re.fullmatch(...)`. **Bulk CRUD nodes NOT yet scope-aware** — tracked #1585 (`xfail(strict=True)` pin, self-clearing). **#1587** (Express cache adapter thread-pool leak, pre-existing on `main`, invisible on `[dev]`-only CI): `DataFlow.close()`/`close_async()` never closed the `AsyncRedisCacheAdapter` (owns a `ThreadPoolExecutor`) → `ResourceWarning` at GC on every documented cleanup path. Adapter gains sync `close()`; `DataFlowExpress` gains `close()`/`close_async()`; both engine close paths tear down the Express instance AND `_cache_integration`; `close_async` offloads the blocking shutdown via `asyncio.to_thread`. Fix PRs #1586 (#1581, merge `02b1ca89b`; the CI blocker was the #1560 source-pin re-anchored at `b21e54f04`) + #1587 (merge `8d3a3eaf3`; reviewer + security-reviewer converged — MEDIUM loop-freeze → `asyncio.to_thread`, 2 coverage gaps → tests). Release-prep PR #1588 (`release/v2.45.5-dataflow-2.13.21`, merge `8adc4f8fe`, PR-gate matrix path-skipped) → tags `v2.45.5` (publish-pypi `28788037820` SUCCESS) + `dataflow-v2.13.21` (`28788240105` SUCCESS). TestPyPI skipped per the patch human-approval precedent ("approved"). Clean-venv verify live (Python 3.12, `uv pip install --refresh`, attempt 2 after the documented PyPI/uv index-propagation lag): `kailash.__version__ == "2.45.5"` + wheel smoke `_AdapterTransactionScope.transaction` + `async_run` reads `inputs["transaction"]`; `dataflow.__version__ == "2.13.21"` resolving `kailash 2.45.5` + wheel smoke `AsyncRedisCacheAdapter.close` + `DataFlowExpress.close_async` (the fixes shipped, not just the versions). Tests: `test_issue_1581_crud_transaction_awareness.py` 8 passed + 1 xfail-strict (bulk gap for #1585); `test_dataflow_close_cache_adapter_leak.py` 7 deterministic (no-Redis) close-wiring tests. No sibling drift — all 9 packages AT-PARITY with PyPI at release-time. Deployment record: `deploy/deployments/2026-07-06-v2.45.5-dataflow-2.13.21-1581-1587.md`.

Last updated: 2026-07-06 — dataflow-only patch `kailash-dataflow 2.13.19 → 2.13.20` closing **#1575** (sibling of #1572 — adapter pools on transient/owned event loops OUTSIDE the sync→async bridge leaked at GC: `RuntimeError: Event loop is closed` + `ResourceWarning: Unclosed connection`). Fix: schema-discovery inspect paths (`_inspect_database_schema_real` PG+SQLite) drain the adapter in a guarded `try/finally` on every exit path; transient loop-creating sites (`discover_schema`, `validate_schema`, `current_schema` fallback, model-registry workflow bridge) routed through `async_safe_run` (stamps `BRIDGE_LOOP_ATTR` → the #1572 `loop_pool_registry` drains before close; DEADLOCK guard preserved); the `SyncExpress` (`db.express_sync`) owned persistent loop now closes + drains its loop-bound connections on `DataFlow.close()`/`close_async()` (new `SyncExpress.close()` drains ON the owning loop then stop/join(5s)/close; `__del__` ResourceWarning — mirrors `SyncTransactionManager`, which was already correct); `_run_sync` hardened against concurrent/after-close use (typed closed-error not `AttributeError`; in-flight call settles with `CancelledError` bounded, not hung; submit-vs-close serialized under the existing pending-futures lock). **dataflow-only** — uses the existing `kailash.utils.loop_pool_registry` (kailash 2.45.4); no new core symbols, floor stays `kailash>=2.45.4`. Fix PR #1578 (merge `1946b8ec9`, fix commits `f141afcf0`…`589c1180e` + release-prep `857f3415f`; full matrix GREEN 20 SUCCESS / 4 SKIPPED / 0 fail on head `857f3415f`; #1575 auto-closed). Redteam CONVERGED — 5 rounds: R1 core CLEAN; R2 caught the `SyncExpress` owned-loop teardown gap (HIGH) → fixed; R3 caught fix-introduced concurrent-close hang + typed-guard gap + strict secret-log LOW → hardened; R4 CLEAN (TOCTOU unreproducible in 1500 stressed settlements; collateral warnings proven pre-existing); R5 CLEAN (main-loop: model-registry no-pool claim verified 33 sync / 0 async nodes; F2 concurrent-close stress 960 settlements, 0 hangs, 0 deadlocks). Tag `dataflow-v2.13.20` (publish-pypi `28760862262` SUCCESS, GitHub Release created). TestPyPI skipped per the patch human-approval precedent (user: "go"). Clean-venv verify live (Python 3.12, `uv pip install --refresh`, attempt 2 after the documented PyPI/uv index-propagation lag): `dataflow.__version__ == "2.13.20"` + wheel smoke `SyncExpress.close`/`_drain_loop_resources`/`__del__`/`_run_sync` closed-guard all present (the fix shipped, not just the version). Tier-2 regression on real PG:5434 + MySQL:3307 + file-SQLite (`test_issue_1575_transient_loop_pool_drain.py`): **11/11 green**, clean under `-W error::ResourceWarning`; deterministic pre-fix-fails/post-fix-passes bites for both inspect branches + the express owned-loop (PG + MySQL). Out-of-scope, tracked separately (SHA-verified pre-existing): 14 `tests/integration/transactions/` failures (`ProductionPostgreSQLAdapter` has no `transaction`, different subsystem); `#1552`-class driver-error `str(e)` schema-discovery logs (`engine.py` 4644/4805/7045/7068); cross-SDK #1575 rs inspection (needs user-authorized cross-repo action per repo-scope-discipline). Deployment record: `deploy/deployments/2026-07-06-dataflow-2.13.20-1575.md`.

Last updated: 2026-07-03 — single-package patch `kailash-dataflow 2.13.7 → 2.13.8` closing **#1518** (multi_tenant upsert mis-maps `tenant_id` — cross-tenant leak class). On a `multi_tenant=True` DataFlow a single-record upsert persisted the row's `id` value in `tenant_id` instead of the active tenant, so write-path tenant scoping was silently built against a bogus `tenant_id`. Root cause: the SQLite precheck-upsert builder (`build_precheck_upsert_query`, #1508) emits named `:pN` placeholders; the tenant `QueryInterceptor` appends `tenant_id` when the INSERT omits it (`insert_data = {**where, **create}` never carries it), but `_detect_placeholder_style` did not recognise `:pN` and defaulted to `qmark`, appending a lone `?` into a `:pN` query → downstream `_convert_to_named_parameters` renumbered that `?` to `:p0` (counter restarts at 0), colliding with the existing `:p0` so `tenant_id` bound to the first value (the same blindness also corrupted the existing-row `UPDATE` branch). Fix: the interceptor now recognises the `:pN` (`colon`) style across INSERT + UPDATE/DELETE + SELECT injection paths → pure `:pN`, structurally identical to the already-correct non-tenant upsert; `_tenant_placeholder_for_style` fails LOUD on `colon`; caller-supplied `tenant_id` in `create` is overwritten to the active tenant. Pre-existing on `main`; surfaced red-teaming #1508. Fix PR #1525 (full matrix GREEN 12 checks; red-team CONVERGED — Round 1 reviewer + security-reviewer both MERGE-WITH-FIXES, Round 2 adversarial verifier CONVERGED on the SELECT-path sibling fix landed in-session). Release-prep PR #1527 (`release/v2.13.8-dataflow`, `--admin`, matrix path-skipped) → tag `dataflow-v2.13.8` (publish-pypi `28657016873` SUCCESS). TestPyPI skipped per the patch human-approval precedent ("approved"). Clean-venv verify live (Python 3.12, published wheel + `aiosqlite`, after the documented PyPI/uv index-cache lag — 1 retry): `dataflow.__version__ == "2.13.8"` + #1518 repro raw row `tenant_id == "tenant-a"` (was the `id` value pre-fix). 11 Tier-2 + structural regression tests; 115 passed / 9 skipped (PG-infra) across tenancy + upsert suites; #1249/#1252/#1508/#1498 unregressed. Dataflow-only; no sibling drift (all 8 packages at PyPI parity at release-time enumeration); core `kailash` unchanged, no floor bump. Out-of-scope, filed separately: **#1526** (multi_tenant single-column `id` PK — two tenants can't share a natural key; needs composite `(tenant_id, id)` uniqueness — a schema-generation change). Deployment record: `deploy/deployments/2026-07-03-dataflow-v2.13.8-1518-multi-tenant-upsert-tenant-id.md`.

Last updated: 2026-07-03 — single-package patch `kailash-dataflow 2.13.6 → 2.13.7` closing **#1508** (SQLite upsert `conflict_on` on a non-UNIQUE field). The single-record SQLite upsert path already ran a `WHERE`-based existence pre-check (SQLite lacks PostgreSQL's `xmax`), then discarded the result and still emitted `INSERT ... ON CONFLICT (col)`, which SQLite rejects unless the conflict target is backed by a UNIQUE/PK constraint — so `conflict_on=["email"]` on a non-unique field raised `ON CONFLICT clause does not match any PRIMARY KEY or UNIQUE constraint`. Fix uses the pre-check `row_exists` to emit a plain `INSERT` / `UPDATE ... WHERE` via the new dialect-agnostic `SQLDialect.build_precheck_upsert_query` (values parameter-bound, identity/PK excluded from `SET`); PostgreSQL keeps its atomic `ON CONFLICT` + `xmax` form. Pure DML — no runtime DDL. `:memory:`-independent (reproduces on file-backed SQLite); surfaced not caused by the #1502 shared-cache fix (made the table exist so the ON CONFLICT was finally reached — the #1508 test was red the whole time behind a `no such table` error). Fix PR #1521 (merge, full matrix GREEN 12 checks; red-team CONVERGED 3 parallel reviewers — security SECURE, dataflow-specialist CORRECT+COMPLETE, reviewer MERGE after an in-shard tenant-scoping regression test + empty-`where` guard). Release-prep PR #1522 (`release/v2.13.7-dataflow`, `--admin`, matrix path-skipped) → tag `dataflow-v2.13.7` (publish-pypi `28651414046` SUCCESS). TestPyPI skipped per the patch human-approval precedent ("approved"). Clean-venv verify live (Python 3.12, published wheel + `aiosqlite`, attempt 2 after the documented uv index-cache lag): `dataflow.__version__ == "2.13.7"` + #1508 repro `created: True` with the create payload. Dataflow-only; no sibling drift (all packages at PyPI parity; 0 non-dataflow src commits since `v2.45.3`); core `kailash` unchanged, no floor bump. Pre-existing defects surfaced by the red-team (NOT introduced; filed separately): **#1518** (HIGH — multi-tenant upsert mis-maps `tenant_id`, proven pre-existing on `main` via file-swap — recommended next work), **#1519** (HIGH — bulk_upsert silently ignores `conflict_on` on SQLite, hardcodes `ON CONFLICT (id)`), **#1520** (MED — PG needs an actionable error). Origin trace: #1508 family is pre-existing test-coverage debt in the upsert subsystem (`conflict_on` feature `a96c942d1` 2025-11-02; ON CONFLICT/tenant-injection machinery predates the 2026-03-11 monorepo move) — a layered-masking cascade (#1498 → #1502 → #1508) unmasked by active repair, not new regressions. Deployment record: `deploy/deployments/2026-07-03-dataflow-v2.13.7-1508-upsert-conflict-on.md`.

Last updated: 2026-07-03 — two-package patch release closing **#1502** (bare `sqlite :memory:` multi-connection isolation): `kailash 2.45.2 → 2.45.3` + `kailash-dataflow 2.13.5 → 2.13.6`, published core-first (dataflow `kailash>=` floor `2.45.2 → 2.45.3`). Each consumer of a bare-`:memory:` DataFlow opened its own private in-memory DB → DDL and CRUD/registry landed in different DBs → `no such table`. Fix: ONE shared-cache URI per instance (`file:df_mem_<id>?mode=memory&cache=shared`, `_memory_db_uri`) + a lifetime anchor connection, injected at every SQLite connection-construction site (async CRUD node, 5 SyncDDLExecutor sites, migration system, migration_connection_manager, migration_test_framework, model registry, schema-state manager, `_get_async_sql_connection` fallback); `close()`/`close_async()` dispose the registry StaticPool for the URI (id-reuse aliasing guard). Core `kailash 2.45.3`: `SQLDatabaseNode` StaticPool + `check_same_thread=False` for memory (fixes the sync-registry cross-thread `ProgrammingError`) + `dispose_pools_for`. `config.database.url` unchanged → dialect detection intact; fires only for bare `:memory:` (file/PG/MySQL byte-unchanged). Implemented as 2 shards + a review-hardening commit: PR #1509 (merge `33d33e44a`; full matrix GREEN 12 checks incl. Postgres regression + CodeQL; security-reviewer CLEAN; code-reviewer's 2 connection-lifetime gaps fixed + regression-covered R7/R8). Release-prep PR #1511 (`release/v2.45.3-dataflow-2.13.6`, `--admin`) → tags `v2.45.3` (publish-pypi `28641958766` SUCCESS) + `dataflow-v2.13.6` (`28642024058` SUCCESS). TestPyPI skipped per the patch human-approval precedent. Clean-venv verify live (Python 3.12, published wheels + `aiosqlite`, pip `--no-cache-dir` after the documented index-cache lag): `kailash.__version__ == "2.45.3"` + `dataflow.__version__ == "2.13.6"` + bare-`:memory:` DDL+CRUD reach one DB + two instances isolated. `pytest -k sqlite`: 493 passed / 1 xfailed (#1503) / 0 failed. 6 Tier-2 regression tests. No sibling drift. Discovered pre-existing (NOT in scope): **#1508** (upsert `conflict_on` on a non-UNIQUE field; `:memory:`-independent, exposed not caused; test xfail-strict). Deployment record: `deploy/deployments/2026-07-03-v2.45.3-dataflow-2.13.6-1502-sqlite-memory.md`.

Last updated: 2026-07-03 — two-package patch release closing **#1498** (SQLite `pytest -k sqlite` failures): `kailash 2.45.1 → 2.45.2` + `kailash-dataflow 2.13.4 → 2.13.5`, published core-first (dataflow `kailash>=` floor bumped `2.28.0 → 2.45.2`). Two coupled product bugs: (1) the core `AsyncSQLDatabaseNode` SQLite adapter discarded `RETURNING` rows via a leading-keyword DML short-circuit (`src/kailash/nodes/data/async_sql.py`, two sites) — added `and "RETURNING" not in query.upper()` guards (PostgreSQL-adapter parity), so SQLite upsert returns the row instead of `{"rows_affected": 0}`; (2) `build_upsert_query` (SQLite + PostgreSQL) built `DO UPDATE SET col = EXCLUDED.col`, but `EXCLUDED` is the INSERT/`create` value, not the caller's separate `update` payload — now binds the `update` values as parameters, also fixing two pre-existing PostgreSQL upsert failures (verified against real Postgres in CI). `pytest -k sqlite`: 14 failed → 485 passed / 1 skipped / 3 xfailed / 0 failed. Fix PR #1505 (merge `c6a7cf8ea`; full matrix GREEN 20-pass incl. DataFlow Infra Regression Postgres/Redis + CodeQL; security-reviewer CLEAN — update-value binding is strictly safer than the prior `EXCLUDED`). Release-prep PR #1506 (`release/v2.45.2-dataflow-2.13.5`, merge `558c85c53`, PR-gate path-skipped, `--admin`) → tags `v2.45.2` (publish-pypi `28637285545` SUCCESS) + `dataflow-v2.13.5` (`28637338927` SUCCESS). TestPyPI skipped per the patch human-approval precedent. Clean-venv verify live (Python 3.12, published wheels + `aiosqlite`): `kailash.__version__ == "2.45.2"` + `dataflow.__version__ == "2.13.5"` + upsert INSERT returns the row (no `rows_affected` summary) + conflict path applies the `update` value (99.99, not the create 49.99). Regression test `tests/regression/test_issue_1498_sqlite_upsert_returning.py`. No sibling drift — all 6 other sub-packages AT-PARITY with PyPI. Deferred/tracked (xfail-strict, self-clearing): **#1502** (bare `:memory:` shared-cache URI orphaned — not wired to the CRUD/registry hot path; file-backed unaffected), **#1503** (`test_production_dataflow` mock-engine Tier-2 violation). Discovered pre-existing (NOT in scope): **#1504** (`test_concurrent_order_processing` PostgreSQL two-manual-transaction isolation fails on pristine `HEAD`, proven independent). Deployment record: `deploy/deployments/2026-07-03-v2.45.2-dataflow-2.13.5-1498-sqlite-upsert.md`.

Last updated: 2026-06-24 — single-package patch `kailash-align 0.7.3 → 0.7.4` closing **#1429** (`OnlineDPOAdapter.build()` surfaced an opaque `AlignmentError: Unknown training method 'online_dpo'` instead of an actionable error — the OPEN follow-up flagged in the 2026-06-23 entry below). The fix commit `de5863f3e` (merged in #1439) was the SOLE unreleased kailash-align src commit since `align-v0.7.3`; a full 9-package sibling-drift enumeration confirmed all 8 siblings AT-PARITY with PyPI (0 unreleased src commits) — no sibling drift. `build()` now raises a `TrainingError` byte-identical to `OnlineDPOConfig.to_trl_config()` ("Online DPO is unavailable: trl >=1.0 removed … use `method='dpo'` or `method='grpo'`"); `learn()` builds-first so it surfaces the same error; the unreachable post-build trainer-construction body is removed (dead code). Behavior-narrowing bug fix — no public dataclass-signature break. Release-prep PR #1441 (`release/v0.7.4-align`, merge `015a02ef0`, PR-gate matrix path-skipped on `release/*`, Version Consistency check passed; `--admin` cleared REVIEW_REQUIRED) → lightweight tag `align-v0.7.4` → publish-pypi run `28076457723` SUCCESS (Build → Publish-to-PyPI → Create-GitHub-Release). TestPyPI skipped per the patch human-approval exception ("approved"). Gates: 40 rl_bridge unit tests pass (incl. 2 behavioral + 1 adapter↔config byte-parity regression test); security-reviewer APPROVE + reviewer APPROVE (both zero findings). Clean-venv install verified live (`pip install --no-cache-dir kailash-align==0.7.4` on Python 3.12 — the pip fallback per `build-repo-release-discipline.md` Rule 2 after `uv pip install --refresh` hit the documented deeper-index-cache lag while `pypi.org/simple/kailash-align/` already listed the 0.7.4 wheel): `kailash_align.__version__ == "0.7.4"` + `OnlineDPOAdapter().build()` raises the informative `TrainingError` (not the opaque `AlignmentError`) + `learn(total_timesteps=1)` raises the byte-identical error. Cross-SDK: Python-only trl-version-floor consequence — kailash-rs has no trl dependency, no equivalent surface (`cross-sdk-inspection.md` 3a). Deployment record: `deploy/deployments/2026-06-24-align-v0.7.4-1429-online-dpo-error.md`.

Last updated: 2026-06-23 — two-package patch release closing **#1426** + **#1430**: `kailash-align 0.7.2 → 0.7.3` (the #1426 trl 1.x compatibility fix — 0.7.2 could not run ANY SFT/DPO/KTO/ORPO/Online-DPO fine-tune against ANY trl in its `>=1.0,<2.0` range; `to_trl_config()` now emits trl-1.x kwargs, pipeline drops the double-`peft_config` pass, orpo/online_dpo de-registered with informative redirects, sibling trl-absent trainers raise informative `TrainingError`, docs+spec reconciled) + `kailash-ml 2.2.1 → 2.2.2` (the #1430 `numba>=0.61` floor — fresh `pip install kailash-ml` on Python 3.12/3.13/3.14 backtracked to numba 0.53.1 → llvmlite 0.36.0 with no wheel → source-build failure). Published in dependency order (ml first): tags `ml-v2.2.2` (publish-pypi run `28014614692` SUCCESS) + `align-v0.7.3` (run `28014766241` SUCCESS); release-prep for align was PR #1432 (`release/v0.7.3-align`), ml bump landed in fix PR #1431. TestPyPI skipped per the patch human-approval exception (user "Release both now"). Verified vs trl 1.6.0 locally (246 tests) + CI Python-3.11 align cell green. Clean-venv install verified live on Python 3.14 (the previously-broken cell): `kailash_ml.__version__ == "2.2.2"` import OK; `kailash_align.__version__ == "0.7.3"` + orpo/online_dpo absent from `METHOD_REGISTRY` + `validate_method_name('orpo')` raises `ValueError`. reviewer (HIGH registry de-reg + MED sibling-guard, both fixed) + security-reviewer CLEAN + adversarial verification workflow `wf_062a74d9-854`. No sibling drift. Follow-ups: #1429 (rl_bridge OnlineDPOAdapter orphan, kailash-ml[rl]-gated, OPEN); pre-existing uv.lock staleness post-loom-2.45.0-sync (separate chore, no gate enforces it). Also this session: PR #1427 landed the uncommitted loom 2.45.0 `/sync-to-build` COC delivery (VERSION 2.35.0 → 2.45.0). Deployment record: `deploy/deployments/2026-06-23-ml-v2.2.2-align-v0.7.3-1426-trl-compat.md`.

Prior: 2026-06-22 — single-package core patch `kailash 2.44.0 → 2.44.1` closing the three implementable public-reachable gaps of the `#1406` Production/Stable stub-marker inventory (edge-monitoring `active_count` no-op, `validate_imports --include-tests` parsed-but-dropped, a stale `max_pages` comment). Fix PR #1420, release-prep PR #1421 (`release/v2.44.1`, lightweight tag `v2.44.1` → publish-pypi run `27934578989` SUCCESS, TestPyPI skipped per patch exception). reviewer surfaced one MED (unreachable `get_alerts(active_only=True)` no-history semantic flip) fixed-immediately; security-reviewer clear. Clean-venv install verified (`kailash.__version__ == "2.44.1"` + `is_alert_active` live/stale True/False + `validate_directory.include_tests` present). Core-only — all framework `kailash>=` floors (≤2.41.0) satisfied by 2.44.1, no pin bumps. Deployment record: `deploy/deployments/2026-06-22-v2.44.1-1406-stub-gaps.md`.

Prior: 2026-06-20 — single-package core patch `kailash 2.41.0 → 2.41.1` shipping two trust-plane correctness fixes from a holistic post-multi-wave redteam of the recently-shipped PACT KSP/Bridge governance epic. **#1393 (HIGH, `kailash.trust.pact`)**: YAML envelope `confidentiality_clearance` + `max_delegation_depth` were silently dropped on the #1386 YAML-apply path (`EnvelopeSpec` had no slot, `_parse_envelopes` never read them, `yaml_resolvers.resolve_envelope` never forwarded them into `ConstraintEnvelopeConfig`), so a YAML-authored `max_delegation_depth: 1` silently became `None` (UNLIMITED) at enforcement and an authored confidentiality ceiling never applied — no error. Both fields now parse (fail-closed on invalid level / non-positive-int depth / unhashable list-or-dict via a shared `_is_valid_level` guard) + forward to the runtime config, participating in monotonic-tightening end-to-end (widening child → `MonotonicTighteningError`). **#1394 (MEDIUM, `kailash.trust.revocation.cascade`)**: `cascade_revoke` returned `revoked_agents=[]` on a partial failure even when the best-effort rollback could NOT restore an already-soft-deleted chain (audit result diverged from store state); `_rollback_chains` now returns the un-restorable agents and `revoked_agents` reflects ground truth + a WARN names any chain left revoked; docstrings over-claiming transaction-backed "atomic chain invalidation" (which the code never did and the InMemory transaction context cannot provide — it snapshots only active chains) reconciled to the real best-effort-rollback contract; a pin test asserts `cascade_revoke` never calls `store.transaction()`. Fix PR #1395 (`fix/holistic-redteam-pact-trust-findings`, merge `50f3f3cd1`, 4 commits) — full PR-gate matrix GREEN (22 checks); discovery via holistic cross-shard redteam (5 finders → adversarial refute; initial concurrent fan-out hit a server-side rate-limit producing a false "0 findings" which the evidence gate CAUGHT, re-run sequentially → 2 confirmed + 1 refuted); PR redteam CONVERGED 2 evidence-gated rounds (3 lenses/round: code-correctness + security + PACT-domain, sequential under rate-limit back-off; R1 clean + 3 LOW/NIT resolved, R2 clean + 1 LOW resolved; PACT lens verified tightening live, code-correctness ran a pre-fix validity check). Issues #1393 + #1394 auto-closed. Regression tests `tests/regression/test_issue_139{3,4}_*.py` + YAML-path tightening tests + reconciled cascade atomicity tests; broad sweep 5360 passed. Release-prep PR #1396 (`release/v2.41.1`, merge `342346e53`, PR-gate heavy matrix path-skipped on `release/v*`; the one red Build-Documentation check was a `cancel-in-progress` cancellation of the duplicate push-event run, the live `pull_request` run passed; `--admin` cleared REVIEW_REQUIRED) → lightweight tag `v2.41.1` → publish-pypi run `27855845025` SUCCESS (Build → Publish-to-PyPI → Create-GitHub-Release). TestPyPI skipped per the patch human-approval exception ("approved"). Clean-venv install verified live (pip `--no-cache-dir` fallback after the documented uv deeper-index-cache lag; `pypi.org/pypi/kailash/2.41.1/json` returned wheel + sdist): `kailash.__version__ == "2.41.1"` + #1393 YAML envelope authoring `confidentiality_clearance: restricted` / `max_delegation_depth: 1` resolves to `ConfidentialityLevel.RESTRICTED` / `1` AND an unhashable clearance raises `ConfigurationError` + #1394 cascade summary reads "NOT a single atomic transaction" and `_rollback_chains` returns the restore-failure list. No sibling drift — all 7 sub-packages AT-PARITY with PyPI (dataflow 2.12.0 / nexus 2.11.0 / kaizen 2.28.0 / mcp 0.2.15 / ml 2.2.1 / align 0.7.2 / pact 0.14.0); the kailash-pact change in #1395 was test-only (carve-out, no pact release); pact's `kailash>=2.41.0` floor satisfied by 2.41.1 → no pin bump. Cross-SDK: both fixes are shared trust-plane (`src/kailash/trust/`), so kailash-rs may have the same governance-field-drop / revocation-audit gaps — SURFACED for the operator's human-gated scrubbed filing decision, NOT auto-filed (`repo-scope-discipline.md` + `upstream-issue-hygiene.md`). Deployment record: `deploy/deployments/2026-06-20-v2.41.1-1393-1394-trust-plane.md`.

Last updated: 2026-06-19 — single-package minor release `kailash-kaizen 2.27.1 → 2.28.0` closing #1388 (`LlmClient` had no `close()`/`aclose()`/`__aexit__` lifecycle method → its HTTP transport socket was released only at GC, emitting `ResourceWarning: unclosed <socket.socket ...>` that fails any run under `-W error::ResourceWarning`). 2.28.0 adds the lifecycle surface ADDITIVELY: `aclose()` (idempotent) + `__aenter__`/`__aexit__` close an OPT-IN pooled `LlmHttpClient`; `async with LlmClient.from_deployment(d)` eagerly pools ONE transport on entry (before any concurrent `embed()`), reuses it across every `embed()` in the scope (amortizing SSRF-resolver + connection-pool setup, surviving per-call errors) and closes it on exit; the pooled client still routes through the structurally-installed `SafeDnsResolver` (no SSRF bypass). Backward-compatible — `embed()` transport acquisition is 3-case (caller-injected / managed-pooled / UNCHANGED unmanaged fresh-per-call construct+close), so existing one-shot callers hold nothing, leak nothing, warn nothing. `__del__` is WARN-ONLY mirroring `LlmHttpClient.__del__` (never calls close from the finalizer per `patterns.md` § Async Resource Cleanup); no sync `close()` (would break inside event loops). Feature PR #1390 (`feat/llmclient-lifecycle-1388`, merge `441d3ead4`, change `1c9745c46`) — full PR-gate matrix GREEN (3.11–3.14 / DataFlow / PACT / Analyze / CodeQL / infrastructure). Redteam CONVERGED — 2 consecutive clean rounds, evidence-gated, 3 lenses/round (code-review + security + independent Bash verification); R1 surfaced one MEDIUM test-hygiene event-loop leak in the regression file (NOT a production bug — verify lens proved the production transport closes clean under `-W error::ResourceWarning`), fixed by converting the warns-at-del test to `@pytest.mark.asyncio` (no throwaway loops); re-run all-clean. 10 new tests (1 OPENAI-gated skip) + 1021 broader kaizen LLM suite, no regression; collect-only 13872, exit 0. Release-prep PR #1391 (`release/kaizen-2.28.0`, merge `b64154753`, PR-gate path-filter-skipped on `release/*`; `--admin` cleared REVIEW_REQUIRED) → lightweight tag `kaizen-v2.28.0` (→ main HEAD `b641547`) → publish-pypi run `27826801043` SUCCESS (Build → Publish-to-PyPI → Create-GitHub-Release). TestPyPI skipped per the minor-release human-approval exception ("Yes — release 2.28.0 (skip TestPyPI)"). Clean-venv install verified live (`uv pip install --refresh "kailash-kaizen==2.28.0"`, attempt 1, no cache lag): `kaizen.__version__ == "2.28.0"` + `LlmClient` lifecycle surface `['__aenter__','__aexit__','aclose']` + managed `async with` pools-then-closes the transport (`_http_client` non-None in-scope, None after exit) + `aclose()` idempotent no-op when unmanaged (no network). No sibling drift — all 9 packages AT-PARITY with PyPI at release-time (kailash 2.41.0 / dataflow 2.12.0 / nexus 2.11.0 / mcp 0.2.15 / ml 2.2.1 / align 0.7.2 / pact 0.14.0 / kaizen-agents 0.9.11); `kaizen-agents` floor satisfied by 2.28.0 → no coupled release. Cross-SDK: `LlmClient` lifecycle-method gap on the kailash-rs sibling `LlmClient` is a candidate EATP-D6 parity item — SURFACED for the operator's human-gated scrubbed filing decision, NOT auto-filed (`repo-scope-discipline.md` + `upstream-issue-hygiene.md`). Deployment record: `deploy/deployments/2026-06-19-kaizen-v2.28.0-1388-llmclient-lifecycle.md`.

Last updated: 2026-06-18 — single-package core patch `kailash 2.38.2 → 2.38.3` closing the F1 sibling of #1356 (`MiddlewareAuthManager` had NO token-revocation capability — no `jti`, no `verify_token` revocation consultation, no `revoke_token`; surfaced by #1356's R1 security-reviewer). Wires the SAME pluggable `TokenRevocationStore` into the legacy nodes-based verifier: `jti` claim + post-decode `is_revoked(jti=,token=)` rejection + new `async revoke_token` + injectable `revocation_store`/`enable_blacklist=True`; shared-store cross-worker propagation; decode-failure fallback TTL-capped at `token_expiry_hours`. Purely additive (pre-2.38.3 tokens still verify) → patch, matching the #1356 precedent. ALSO fixed a pre-existing bug in the same file (zero-tolerance Rule 1): three `security_logger.execute(severity="warning")` sites passed an invalid `SeverityLevel`, so `SecurityEventNode` raised `ValueError` and every invalid/expired token escaped `verify_token` as an internal error instead of a 401 — fixed to `MEDIUM` + best-effort `_emit_security_event` helper (logging failure can no longer turn a 401 into a 500). Code PR #1365 (`feat/middleware-auth-token-revocation`, merge `405f5c23b`; full PR-gate matrix 11-pass/3-skip/0-fail). 3-round /redteam CONVERGED (L5; 4 lenses reviewer/security/closure/bypass; concurrency capped at 3 per worktree-isolation Rule 4 after a 4-wide synchronized throttle): R1 0-confirmed-MED+ (1 MED refuted) + audit-path LOW fixed, R2 all-APPROVE + logging-failure LOW fixed, R3 all-APPROVE; bypass-proof across 9 vectors every round; 14 regression tests. Release-prep PR #1366 (`release/v2.38.3`, merge `792a6e9b7`, PR-gate path-skipped) → lightweight tag `v2.38.3` → publish-pypi run `27732055401` SUCCESS. TestPyPI skipped per the patch human-approval exception ("Yes — release 2.38.3 (patch)"). Clean-venv install verified live (`uv pip install --refresh "kailash[server]==2.38.3"`, attempt 2 after uv index-cache lag): `kailash.__version__ == "2.38.3"` + `revoke_token` async + ctor carries `enable_blacklist`/`revocation_store` + two managers sharing one store demonstrate cross-worker revocation propagation. No sibling drift — all 8 sub-packages AT-PARITY with PyPI. Cross-SDK F2 (kailash-rs `MiddlewareAuthManager`-equivalent inspection) surfaced, human-gated, NOT acted. Deployment record: `deploy/deployments/2026-06-18-v2.38.3-1356-sibling-middleware-auth-revocation.md`.

Last updated: 2026-06-18 (drift sweep) — two-package patch sweep bringing the monorepo to pristine main==PyPI: `kailash-pact 0.12.0 → 0.12.1` (commit `7abe1a2c2` — comment reword + dead-import removal; no behavior change) + `kailash-mcp 0.2.14 → 0.2.15` (commit `1e17d63df` / #1258 — doc + byte-vector pins; doc/test only). Both were previously-unreleased src commits left un-bumped by prior sessions. Combined release-prep PR #1363 (`release/pact-0.12.1-mcp-0.2.15`, merge `2ee743eae`, PR-gate path-skipped) → tags pushed individually → publish-pypi runs `27706314940` (pact) + `27706325566` (mcp) SUCCESS. TestPyPI skipped per the patch human-approval exception ("sweep them please"). Clean-venv verified live: `pact.__version__ == "0.12.1"` + `kailash_mcp.__version__ == "0.2.15"`. Post-sweep enumeration confirms ALL packages (core 2.38.2 + 8 sub-packages) at 0 unreleased src commits — main source == PyPI monorepo-wide. Deployment record: `deploy/deployments/2026-06-18-pact-v0.12.1-mcp-v0.2.15-drift-sweep.md`.

Last updated: 2026-06-18 — single-package core patch `kailash 2.38.1 → 2.38.2` closing #1356 (MEDIUM — `JWTAuthManager` token revocation was process-local, silently inert in multi-worker deployments). Introduces a synchronous pluggable `TokenRevocationStore` (+ `InMemoryTokenRevocationStore` default) exported from `kailash.middleware.auth`; a shared injectable backend via `JWTAuthManager(revocation_store=...)` propagates revocation across workers; `verify_token`/`revoke_token` stay sync (no API break), revocation checked post-decode keyed on `jti`; decode-failure path TTL-bounded. Fix PR #1360 (`fix/issue-1356-jwt-revocation-store`, merge `41cb30480`, 3 commits + convergence docs) — full PR-gate matrix green (the only red checks were `cancel-in-progress` cancellations of the duplicate push-event run). 3-round /redteam CONVERGED (L5): R1 M2/M3 fixed + H1 deferred as a separate shard, R2+R3 0 CRIT/HIGH/MED. Release-prep PR #1361 (`release/v2.38.2`, merge `7891a1d1d`, PR-gate path-skipped) → lightweight tag `v2.38.2` → publish-pypi run `27704218183` SUCCESS. TestPyPI skipped per the patch human-approval exception ("Full release"). Clean-venv install verified live (`uv pip install --refresh "kailash[server]==2.38.2"`, attempt 2 after uv index-cache lag): `kailash.__version__ == "2.38.2"` + `TokenRevocationStore`/`InMemoryTokenRevocationStore` import + two managers sharing one store demonstrate cross-worker revocation propagation. 10 regression tests. Documented residuals: refresh-token tracking + rate-limiting stay process-local (disclosed in docstring); `MiddlewareAuthManager` has no revocation at all (H1 — follow-up issue drafted, human-gated). Sibling drift (pre-existing, version-matches-PyPI, NOT released this cycle): `kailash-pact` `7abe1a2c2` (comment-only) + `kailash-mcp` `1e17d63df` (#1258 doc/byte-pins) — surfaced for operator decision. Deployment record: `deploy/deployments/2026-06-18-v2.38.2-1356-jwt-revocation-store.md`.

Last updated: 2026-06-17 — two independent patch bug-fix releases closing two same-day-filed issues: `kailash-kaizen 2.27.0 → 2.27.1` (#1352) + `kaizen-agents 0.9.10 → 0.9.11` (#1351). **#1352** (HIGH): a Kaizen `Signature` defined under `from __future__ import annotations` (PEP 563) stored each output field's type as the annotation STRING (`'List[dict]'`/`'str'`), so `JSONOutputParser._convert_to_type`'s `isinstance(value, 'str')` raised `TypeError` — swallowed by the parser's own `except (json.JSONDecodeError, TypeError)`, silently degrading a valid JSON response to `{}`. Fixed at root (`SignatureMeta` resolves string annotations to real type objects after class creation via `typing.get_type_hints` against the class's module globals — the reporter-suggested fix; best-effort, never raises) + defense-in-depth (parser returns value unchanged if a string type survives construction). The initial `eval`-based draft was replaced with `get_type_hints` (no `eval`, public API, no 3.13 deprecation, passes the `python-no-eval` pre-commit gate). PR #1357 (`fix/issue-1352-pep563-signature-parser`, merge `c5f544e30`, change `662bdc182`); regression `tests/regression/test_issue_1352_pep563_signature_parser.py` (6). **#1351** (MED): Gemini adapters imported the end-of-life `google.generativeai` declared only in `[dev]` — a normal `pip install kaizen-agents` raised ImportError on the Gemini path. Migrated `google_adapter.py` + `gemini_cli.py` to the supported `google.genai` SDK (`genai.Client` + `client.aio.models.generate_content_stream` + `types.GenerateContentConfig`/`Tool`/`ToolCodeExecution`; per-request config; chunks via `candidates[0].content.parts`); fixed the packaged `03-external-adapters.md` doc that taught the removed `_generative_model` API; declared `google-genai` AND the sibling `anthropic` (identical undeclared-runtime defect) at `[project.dependencies]`, removed the deprecated pin. PR #1358 (`fix/issue-1351-google-genai-migration`, merge `858a9af8a`, change `909ed5470`); regression `tests/regression/test_issue_1351_google_genai_migration.py` (6) + the 2 Anthropic adapter tests that failed on a no-`[dev]` venv now pass. Gate reviews: reviewer 1 MED (packaged doc taught removed API — fixed same change) + 2 LOW; security-reviewer no blocking findings (`get_type_hints` carries no injection surface; the untrusted `from_brief` path sets real type objects via a closed allowlist). Full PR-gate matrix green on both (3.11–3.14 / DataFlow / PACT / CodeQL / infrastructure). TestPyPI skipped per the patch human-approval exception ("Merge on green + release both"). Lightweight tags pushed individually → publish-pypi runs `27697261489` (`kaizen-v2.27.1`) + `27697274565` (`kaizen-agents-v0.9.11`) SUCCESS. Clean-venv install verified live (`uv pip install --refresh`): `kaizen.__version__ == "2.27.1"` + a PEP 563 Signature resolves `items`→`typing.List[dict]` / `label`→`<class 'str'>` and valid JSON parses `success=True`; `kaizen_agents.__version__ == "0.9.11"` + a no-`[dev]` clean install pulls `google-genai` 2.8.0 + `anthropic` 0.109.2, `google.generativeai` absent, `GoogleStreamAdapter._client` is a `google.genai.client.Client`. No sibling drift — all 7 other packages AT-PARITY at release-time enumeration. Cross-SDK: #1352 PEP 563 is Python-intrinsic (no kailash-rs equivalent); #1351 kaizen-agents is Python-only. Deployment record: `deploy/deployments/2026-06-17-kaizen-v2.27.1-agents-v0.9.11-1352-1351.md`.

Last updated: 2026-06-13 — two-package coordinated minor release completing the M2 feature store (FM2 Wave 3 of 3, closes #693): `kailash 2.30.0 → 2.31.0` + `kailash-ml 2.1.1 → 2.2.0`. **Core**: new public `OnlineStoreUnavailableError` in `kailash.ml.errors` (`FeatureStoreError` subclass, `src/kailash/ml/errors.py:758`) — purely additive, supports the ml online-store adapter. **ml**: Redis-backed `OnlineFeatureStore` (graduates spec §11.4a) + self-healing model re-registration on fresh-instance read + spec graduation (`dataflow-ml-integration.md` 1.0.0-draft → 2.0.0; `ml-feature-store.md` §6/§11 citations re-derived); core pin floored `kailash>=2.31.0`. Shipped via feature PR #1310 (`feat/fm2-wave3`, merge `86f190b1f`, 10 commits — 3 shards + §11.7 conflict-resolution merge + version bumps + redteam fix + CI redis-`[dev]` fix). Holistic terminal /redteam (3 parallel reviewers across Waves 1/2/3): closure-parity CONVERGED (all 7 §11.7 `errors.py:NNN` citations exact), security APPROVE (tenant isolation structural via cache-key embedding; credentials masked; no raw SQL), code-review CHANGES-REQUIRED→resolved (`OnlineFeatureStore` missing `__del__`/`ResourceWarning` per patterns.md § Async Resource Cleanup — fixed `77cc0d7` warn-and-return-only + regression tests; defense-in-depth schema-name re-validation same commit). First CI run surfaced a real red — `Base (Python 3.12)` Coverage, 7 `OnlineFeatureStore` tests failing at construction with the loud `redis`-missing ImportError; diagnosed as CI-config gap (Base installs `[dev]`; `redis` was `[online-store]`-only) and fixed by declaring `redis>=6.2.0` in `[dev]` (`Declared = Imported`, matching the pandas/onnxruntime test-time entries) so the wiring tests RUN in Base; coverage 76% > 60%. PR-gate matrix green (25 pass / 0 fail; CUDA-GPU smoke non-required + queued, irrelevant to the Redis/spec blast radius). Lightweight tags pushed individually → publish-pypi runs `27459453161` (`v2.31.0`) + `27459460095` (`ml-v2.2.0`) SUCCESS. TestPyPI skipped per the minor-release human-approval exception ("approve release"). Clean-venv install verified live (`pip install --no-cache-dir` — `uv --refresh` hit the documented deeper-index-cache lag while `…/2.31.0/json` + `…/2.2.0/json` returned HTTP 200; pip fallback per `build-repo-release-discipline.md` Rule 2): `kailash.__version__ == "2.31.0"` + `kailash_ml.__version__ == "2.2.0"` + `kailash.ml.errors.OnlineStoreUnavailableError is kailash_ml.errors.OnlineStoreUnavailableError` (re-export identity) + `issubclass(..., FeatureStoreError)` + `kailash_ml.features.OnlineFeatureStore` importable. No sibling drift — only kailash + kailash-ml had main ahead of PyPI; all 7 other framework packages AT-PARITY. Two non-blocking follow-ups deferred (consolidate `_mask_redis_url` onto shared `kailash.utils.url_credentials` masker; correct two pre-existing stale citations in `ml-registry.md` historical audit block — `engines/engine.py:75-80` no longer exists, `model_registry.py` write at `:537`). Outstanding user action — **F-YANK**: `kailash-ml` 2.1.0 still live on PyPI with broken `kailash>=2.16.0` pin (ships ImportError to fresh installs); yank from PyPI (web-only). Deployment record: `deploy/deployments/2026-06-13-v2.31.0-ml-v2.2.0-fm2-wave3.md`.

Last updated: 2026-06-11 (latest) — single-package minor release `kailash-kaizen 2.26.0 → 2.27.0` (F31 Wave 4 — RAG node honesty). Five `kaizen.nodes.rag` nodes that advertised ML capabilities they only simulate are made honest: `VisualQuestionAnsweringNode` + `ImageTextMatchingNode` (keyword-derived placeholders, no real backend) DEPRECATED via warn-on-construction shim (kept importable per `zero-tolerance.md` Rule 6a; hard-removal scheduled 2.28.0) + CHANGELOG migration; `MultimodalRAGNode` / `FederatedRAGNode` / `ColBERTRetrievalNode` (real LLM/aggregation/lexical cores) docstrings corrected to stop advertising CLIP/BLIP-2 / real distributed federation / BERT-based ColBERT. Also fixes a hardcoded `model="gpt-4-vision"` → env-resolved `OPENAI_VISION_MODEL` (`env-models.md`) and deletes ColBERT's dead, never-called `_create_workflow` (random `np.random.randn(768)` embeddings); its orphaned None-content test ported to a behavioural `run()` test. PR #1297 (`fix/kaizen-rag-strip-overclaims`, merge `18cdfa699`, change commit `eeeac37ce`) all-green PR-gate matrix (Analyze/CodeQL/Test 3.11–3.14/DataFlow-Unit/PACT/infrastructure) + security-reviewer APPROVE + reviewer CHANGES-REQUESTED→resolved-in-change (orphaned-test port + Example-block over-claim) + 235 RAG tests → lightweight tag `kaizen-v2.27.0` → publish-pypi run `27355844513` SUCCESS. TestPyPI skipped per the minor-release human-approval exception ("yes please" to drive through `/release`). Clean-venv install verified live (`pip install --no-cache-dir 'kailash-kaizen[rag]==2.27.0'` — `uv --refresh` hit the documented deeper-index-cache lag while `…/2.27.0/json` returned 200; pip fallback per `build-repo-release-discipline.md` Rule 2): `kaizen.__version__ == "2.27.0"`, both deprecated nodes fire `DeprecationWarning`, `MultimodalRAGNode` does not warn + carries the "no CLIP, no BLIP-2" disclaimer, `ColBERTRetrievalNode` "0.92" claim absent + `run()` ranks the relevant doc first. `kaizen.nodes.rag` requires the `[rag]` extra (numpy/Pillow/networkx/requests) — pre-existing intended optional-extra design, prior release 2.26.0 identical, top-level `import kaizen` unaffected. No sibling drift — only `kailash-kaizen` ahead of PyPI; `kaizen-agents` 0.9.8-vs-0.9.10 was the `info.version` aggregate-endpoint lag (version-specific `…/0.9.10/json` HTTP 200), all 8 other packages AT-PARITY. Deployment record: `deploy/deployments/2026-06-11-kaizen-v2.27.0-f31-wave4-rag-honesty.md`.

Last updated: 2026-06-11 (later) — single-package minor release `kailash-kaizen 2.25.1 → 2.26.0` closing FNEW-5 (provider-config intrinsic default constants). The nine `get_*_config()` functions in `kaizen.config.providers` now resolve their final fallback from documented module-level `DEFAULT_<PROVIDER>_MODEL` constants (provider-intrinsic, overridable via `KAIZEN_<PROVIDER>_MODEL`, deliberately NOT chained to `KAIZEN_DEFAULT_MODEL` to avoid the 2.25.0 provider/model mismatch) instead of inline literals; the stale Anthropic default `claude-3-haiku-20240307` is refreshed to `claude-haiku-4-5` (user-visible, non-breaking — final-fallback value only; signature + resolution order unchanged). Minor bump chosen because the default-model value change is user-observable. Fix landed on main under CHANGELOG `[Unreleased]` via PR #1292 (`fix/kaizen-fnew5-provider-intrinsic-defaults`, merge `ea0da1e0f`, converged R1 3-agent CLEAN + R2 193 passed `-W error`); release-prep PR #1294 (`release/kaizen-2.26.0`, merge `99c86498e`, PR-gate path-filter-skipped on `release/v*`; `--admin` cleared REVIEW*REQUIRED) → lightweight tag `kaizen-v2.26.0` → publish-pypi run `27346215627` SUCCESS. TestPyPI skipped per the minor-release human-approval exception (in-session "Yes — release 2.26.0"). Clean-venv install verified live (`uv pip install --refresh` from published wheel, attempt 2 after uv index-cache lag): `kaizen.__version__ == "2.26.0"` + 9 `DEFAULT*<PROVIDER>\_MODEL`constants present +`DEFAULT_ANTHROPIC_MODEL == "claude-haiku-4-5"`. `kaizen-agents`floors`kailash-kaizen>=2.25.1`, satisfied by 2.26.0 → no coupled release. No sibling drift — all other framework packages AT-PARITY with PyPI at release-time enumeration (kaizen/kaizen-agents `info.version`aggregate-endpoint lag on 2.25.1/0.9.10 was confirmed cosmetic via the version-specific`…/2.25.1/json`+`…/0.9.10/json`HTTP-200 check). Deployment record:`deploy/deployments/2026-06-11-kaizen-v2.26.0-fnew5.md`. NOTE: the per-release deploy records remain the authoritative chain; the `Current Version` table above stays stale for other packages per their own sessions' deploy records.

Last updated: 2026-06-11 — coordinated kaizen release of the FNEW wave (PRs #1287/#1288) + a same-session slim-core hotfix the clean-venv done-gate caught. **Release 1**: `kailash-kaizen 2.24.6 → 2.25.0` (minor, BREAKING migration — AI-node model defaults resolve from `KAIZEN_DEFAULT_MODEL`; fail-closed `ProviderUndetectable` for unrecognized models; HuggingFace preset retargeted to the Inference Providers router off the decommissioned `api-inference.huggingface.co`; `get_multi_modal_adapter` kwarg filtering) + `kaizen-agents 0.9.8 → 0.9.9` (patch — slim-core `patterns` import, `datetime.now(UTC)`, `shutdown(graceful=False)`, ruff-clean test dirs). R1-R3 redteam CONVERGED-CLEAN (receipt `workspaces/kaizen-rag-node-coverage/04-validate/22-fnew-wave-2026-06-11.md`), corroborated live on main (FNEW-4 regression 24/24, fail-closed detection, kaizen-agents ruff exit 0). Release-prep PR #1289 (`release/kaizen-2.25.0-agents-0.9.9`, merge `a92dd0d06`, PR-gate path-filter-skipped; `--admin` cleared REVIEW_REQUIRED) → tags `kaizen-v2.25.0` + `kaizen-agents-v0.9.9` pushed individually → publish-pypi runs `27333074238` + `27333103383` SUCCESS. **Clean-venv done-gate CAUGHT a real defect**: `import kaizen_agents.patterns` raised `ModuleNotFoundError` on a slim-core install — `kaizen.core.autonomy.hooks` eagerly re-exported LoggingHook/MetricsHook/TracingHook, which import `structlog`/`prometheus-client`/`opentelemetry` (all `[observability]`-extra deps) at module scope; the exact slim-core contract kaizen-agents 0.9.9 claimed. **Release 2 (hotfix, same session)**: `kailash-kaizen 2.25.0 → 2.25.1` — lazy-load the three observability hooks via PEP 562 `__getattr__` at `hooks/__init__.py` + `hooks/builtin/__init__.py` (TYPE_CHECKING blocks per orphan-detection Rule 6b); `logging_hook` `import structlog` moved into the JSON-format branch (text format needs no extra); accessing an observability hook without the extra now raises a clear ImportError → `pip install 'kailash-kaizen[observability]'`. `kaizen-agents 0.9.9 → 0.9.10` — floor `kailash-kaizen>=2.25.1` (enforce the now-true contract; no source change). Fix PR #1290 (`fix/kaizen-2.25.1-slim-core-hooks-import`, merge `c9e2984eb`) all-green PR-gate matrix (Analyze/CodeQL/Test 3.11–3.14/DataFlow-Unit/PACT) + 119/119 autonomy hooks unit tests → tags `kaizen-v2.25.1` + `kaizen-agents-v0.9.10` pushed individually → publish-pypi runs `27334...` SUCCESS. TestPyPI skipped per the patch/minor human-approval exception ("yes" + "proceed with the publish"). Clean-venv install verified live (`pip install --no-cache-dir` from published wheels): `kaizen.__version__ == "2.25.1"` + `kaizen_agents.__version__ == "0.9.10"`, the full `import kaizen_agents.patterns` chain imports clean on slim core, 2.25.0 surface intact (fail-closed `detect_provider`), and the lazy observability-hook ImportError path fires correctly. Sibling-drift sweep: `kailash-mcp` (#1258) + `kailash-pact` drifted but are non-functional (docstrings/test-vectors/comments only — no wheel change), deferred to their next functional release. Deployment record: `deploy/deployments/2026-06-11-kaizen-v2.25.1-agents-v0.9.10-fnew-wave.md`. NOTE: the `Current Version` table above remains stale for other packages (ml/align/mcp/pact) per their own sessions' deploy records — the per-release deploy records are the authoritative chain, not this table.

Last updated: 2026-06-06 — two-package coordinated patch release: `kailash-dataflow 2.11.2 → 2.11.3` (#772 type-introspection consolidation) + `kailash 2.29.2 → 2.29.3` (#1258 canonical-encoder divergence docs + byte-vector pins). **#772**: consolidated two-spelling Optional/Union detection (`typing` + PEP 604) into the single `dataflow.core.type_introspection.union_non_none_args` primitive across 10 caller-methods + `strip_annotated` for `Annotated[T]` normalization; behavior-preserving except the latent `Annotated`-alias-leak fix (`_resolve_type(Annotated[int])` now returns `int`). Fresh adversarial /redteam round converged — reviewer `aff2a754be108f1f9` CONVERGED-APPROVE (main-vs-HEAD differential battery byte-identical detection, ruff clean, 74 tests) + security-reviewer `afff7e79fbeb1011e` APPROVE-0-blocking (SQL-type drift / PK-rejects-Optional / nullability / strip_annotated-metadata / injection all clean); 2 consecutive clean rounds (R2 prior + this). PR #1270 (`f15db7206`, merge `f97e6bbbb`) all-green CI (Analyze/CodeQL/Test 3.11–3.14/DataFlow-Unit/PACT/infrastructure) → release-prep PR #1271 (`release/dataflow-v2.11.3`, merge `5a3653e29`) → tag `dataflow-v2.11.3` → publish-pypi run `27063127240` SUCCESS. **#1258**: documentation + test-vector ONLY (zero runtime/API/behavior change) — closes the sibling drift where #1269 (`d46695d1e`) landed `src/kailash/trust/` docstring changes + cross-SDK byte-vector pins (`tests/test-vectors/{delegate,trust-plane}-canonical.json`) on main without a version bump; documents the intentional `ensure_ascii` divergence (delegate `canonical_json_dumps` raw-UTF8 vs trust-plane `serialize_for_signing` ASCII-escaped) + decision-not-to-unify; #1269 source diff verified pure-docstring. Release-prep PR #1272 (`release/v2.29.3`, merge `32004349f`) → tag `v2.29.3` → publish-pypi run `27063125341` SUCCESS. Tags pushed individually. TestPyPI skipped per the patch human-approval exception (user authorized "Both" + "approved"). Clean-venv install verified live (`uv pip install --refresh`, first attempt, no cache lag): `kailash.__version__ == "2.29.3"` (`canonical_json_dumps({"name":"漢字"})` → raw UTF-8) + `dataflow.__version__ == "2.11.3"` (`union_non_none_args(Optional[int])==[int]`, `(int|None)==[int]`, `(int) is None`; `strip_annotated(Annotated[int,...]) is int`). No sibling drift remaining — main == PyPI across all 9 packages. Deployment record: `deploy/deployments/2026-06-06-v2.29.3-dataflow-v2.11.3-772-1258.md`. F3 held drift (`.session-notes` + issue-643 journal) preserved throughout (explicit-path staging only). PRIOR CYCLE (2026-06-04, kailash 2.29.2): `kailash 2.29.1 → 2.29.2` single-package security patch closing #1260 (connection-pool-key credential masking). `AsyncSQLDatabaseNode` / Redis pool keys embed a raw connection string (`postgresql://user:pass@host/db` / `redis://:pass@host/dbN`) that leaked at WARN/ERROR log lines, Prometheus metric labels, `PoolExhaustedError`, and public diagnostic return values (`get_pool_info`/`get_pool_metrics`/`pool_keys`/`get_lock_metrics`; Redis pool-status/health-report/exec-result/cleanup) — all of which reach log/metric aggregators with broader access than the DB. New shared helper `kailash.utils.url_credentials.redact_pool_key` masks only the credential-bearing connection-URL segment via canonical `mask_url` (`postgresql://***@host`), preserving loop-id/db-type/pool-size segments for forensic correlation; deterministic, so correlation still works and label cardinality is bounded. Helper reconstructs the connection field from middle `|`-segments so a literal `|` in a password cannot leak the tail. Fix PR #1266 (4 commits `c997306a4`/`b11eb4825`/`b27efe991`/`5e36cf7cf`, merge `a41782ba1`) went through code-review (clean) + 4 security rounds (R1: 5 findings, R2: 1, R3: 1 runtime-confirmed, R4: CONVERGED) — same-bug-class return-value siblings surfaced in waves. Release-prep PR #1267 (`bd1b7481d`, `release/v2.29.2`) merge `a6bd3675e` (heavy PR-gate path-filter-skipped on `release/v*`; `--admin` cleared REVIEW_REQUIRED) → tag `v2.29.2` → publish-pypi.yml run `26958730049` SUCCESS. TestPyPI skipped per the patch human-approval exception ("publish 2.29.2 now"). Clean-venv install verified live: `kailash.__version__ == "2.29.2"`, `redact_pool_key` masks to `postgresql://***@db:5432/app` (no secret). 21-test regression suite `tests/regression/test_pool_key_credential_leak_1260.py`. No sibling drift — all 8 framework packages AT-PARITY with PyPI at release-time. Deployment record: `deploy/deployments/2026-06-04-v2.29.2-1260-pool-key-credential-masking.md`. PRIOR CYCLE (2026-06-04, kailash 2.29.1 + dataflow 2.11.2) — see `deploy/deployments/2026-06-04-v2.29.1-trust-json-sqlite-pool.md` + `deploy/deployments/2026-06-04-dataflow-v2.11.2-1250-sqlite-datetime.md` for the authoritative record. `kailash 2.29.0 → 2.29.1` bundles three fixes on structurally separate code paths: #1243 (canonical-JSON signing pre-image rejects `NaN`/`Infinity`/non-string keys — `canonical_json_dumps` + `serialize_for_signing` both pass `allow_nan=False` plus a cycle guard that raises `ValueError` instead of leaking `RecursionError`; the `serialize_for_signing` `ensure_ascii` half is deferred to #1258 as breaking, correct as-is against its fixture), #1245 (the three SQLite trust stores `PostureStore`/`SqliteChainStore`/`SqliteTrustPlaneStore` now track per-thread connections in `_all_conns` under `_conns_lock` and `close()` flips the closed-flag FIRST then drains every tracked connection — previously each store leaked all non-calling-thread connections until process exit; reviewer surfaced the 3rd store + flag-first race in-PR), and #1248 (`AsyncSQLDatabaseNode.clear_shared_pools(*, loop_id=None)` scopes pool/registry teardown to the disposing event loop so a `LocalRuntime` finishing on one loop no longer disposes another loop's in-use shared pools). PRs #1257 (#1243, `072fd0555`+`f416752dd`) / #1259 (#1248, `853126f98`+`8f777262b`) / #1261 (#1245, `f832c162c`+`8fb3aa65c`) admin-merged → release-prep PR #1263 (`b4d59f81b`, `release/v2.29.1`) merge `a3ab86ce8` → tag `v2.29.1` → publish-pypi.yml run `26899398624` SUCCESS. The #1248 branch's 26 dataflow failures PROVEN pre-existing via main-vs-branch (28 on main, 26 on branch; only the 2 new #1248 tests flip). `kailash-dataflow 2.11.1 → 2.11.2` closes #1250: Python 3.12 deprecated the implicit SQLite `datetime`/`date` adapters, so every DataFlow SQLite timestamp write emitted a `DeprecationWarning` (and would silently change storage format on 3.13+). `dataflow/adapters/sqlite.py` now registers explicit adapters at import using `isoformat(" ")` (SPACE separator — byte-matching the legacy implicit format so existing SQLite DBs stay readable; the T-separator default would silently re-format stored timestamps). PR #1262 (#1250, `d569fbb7c`+`016b49f91`) admin-merged → release-prep PR #1264 (`ef600f26f`, `release/dataflow-v2.11.2`) merge `569f0e485` (all 11 PR-gate checks path-filter-skipped; `--admin` cleared the REVIEW_REQUIRED protection) → tag `dataflow-v2.11.2` → publish-pypi.yml run `26899711644` SUCCESS. TestPyPI skipped on both per the patch human-approval exception ("publish 2.29.1 now" + "merge + release after CI green"). Clean-venv install verified live for both: `kailash.__version__ == "2.29.1"` with all 3 fix surfaces importable/behavioral, and `dataflow.__version__ == "2.11.2"` with the datetime adapter emitting `'2026-01-01 12:00:00'` (space separator preserved). The dataflow verify hit a uv deeper-resolver-cache lag (`--refresh` unsatisfiable while the simple index already listed 2.11.2); `python -m pip install --no-cache-dir` succeeded on first try, the documented `build-repo-release-discipline.md` Rule 2 fallback. Deployment records: `deploy/deployments/2026-06-04-v2.29.1-trust-json-sqlite-pool.md` + `deploy/deployments/2026-06-04-dataflow-v2.11.2-1250-sqlite-datetime.md`. No sibling drift — all other framework packages AT-PARITY with PyPI at release-time. NOTE: the intervening `kailash 2.29.0` (#1209, 2026-06-02), `kailash-dataflow 2.11.0`/`2.11.1` (#1249/#1252, 2026-06-03), and `kailash-ml 1.7.5`/`1.7.6` (#1241, 2026-06-03) releases shipped to PyPI with deploy-record files under `deploy/deployments/` but were not appended to THIS running log by their sessions; the per-release deploy records are the authoritative chain for those.

Prior 2026-06-01 — coupled release closing the three deferred #1174 Nexus FastAPI-parity follow-ups (PR #1235): `kailash 2.28.3 → 2.28.4` (patch, #1218 — `WorkflowAPI` gateway `/workflows/{name}/execute` now honors a typed HTTP status raised by a workflow node instead of collapsing to a generic 500; `_find_typed_status_exc` walks the `__cause__`/`__context__` chain and maps `status_code`+`body` only when BOTH present — the `NexusHandlerError` contract — with a `body`-presence gate per security-reviewer LOW so a stray `HTTPException(404, detail)` collapses to 500 rather than echoing `str(exc)`) and `kailash-nexus 2.8.0 → 2.9.0` (minor, #1216 WS handshake auth rejects pre-upgrade with HTTP 401/403 via `serve(process_request=...)` instead of post-upgrade WS-1008, fail-closed at two layers; #1217 subprotocol echoed via `Sec-WebSocket-Protocol` via `serve(select_subprotocol=...)`, reject-only allowlist preserved; floor bumped `kailash>=2.28.4`). Single shared `serve()`-callback rewire of `transports/websocket.py` addresses #1216+#1217; handshake-auth moved fully pre-upgrade, post-upgrade resolver removed. reviewer APPROVE (clean, 41 touched-area tests pass) + security-reviewer APPROVE (zero CRIT/HIGH; one LOW body-less-match fixed in-PR commit `4131edae8` + regression test). Admin-merged PR #1235 → `main` `9070ecf3f` (issues #1216/#1217/#1218 auto-closed) → tags `v2.28.4` + `nexus-v2.9.0` pushed individually → publish-pypi.yml runs `26754034027` + `26754046377` succeeded (TestPyPI skipped per minor-release exception with explicit human authorization; full Python 3.11–3.14 matrix + DataFlow/PACT/infrastructure CI green on PR #1235). Clean-venv install verified live (`uv pip install --refresh` both pinned, attempt 2 after ~60s PyPI cache lag): `kailash.__version__ == "2.28.4"` + `WorkflowAPI._find_typed_status_exc` body-gated; `nexus.__version__ == "2.9.0"` + `WebSocketTransport._process_request` (#1216) + `_select_subprotocol` (#1217) present. Deployment record: `deploy/deployments/2026-06-01-v2.28.4-nexus-v2.9.0-1216-1217-1218.md`. No sibling drift — all 7 other framework packages AT-PARITY with PyPI at release-time enumeration. Follow-up surfaced (not blocking): #1218's fix is in the Core SDK gateway, so kailash-rs likely has the same typed-handler-error→500 gap on its gateway-execute path — scrubbed cross-SDK issue drafted for the operator's per-issue filing gate. Session incident (recovered): a background reviewer sub-agent's stray `git stash pop` applied an old release-prep stash (1,600 files) onto the working tree mid-session; recovered with user authorization (conflict markers resolved, 1,599 stash-residue files restored to session-start HEAD, `.session-notes` + `stash@{0}` preserved, no work lost).

Prior 2026-06-01 — two-package patch release closing two externally-reported correctness bugs: `kailash 2.28.2 → 2.28.3` (PR #1226, #1200 — runtime classes `AsyncLocalRuntime`/`LocalRuntime`/`ParallelRuntime`/`ParallelCyclicRuntime` no longer drop `contextvars.Context` across their thread boundaries; six dispatch sites now snapshot `copy_context()` in the caller frame and run the thread-boundary callable through `ctx.run`, parallel paths using a fresh `.copy()` per concurrent dispatch; `distributed.py` excluded as queue-reconstructed) and `kailash-dataflow 2.10.0 → 2.10.1` (PR #1227, #1207 — `db.express` round-trips `Optional[list]`/`Optional[dict]` JSONB fields as Python `list`/`dict` instead of a JSON string via new `_unwrap_optional_type` collapsing `Optional[T]`/`Union[T,None]`/PEP604 `T|None` before the `in (dict,list)` check, routed through `_deserialize_json_fields` + `convert_datetime_fields`). Both admin-merged → tags `v2.28.3` + `dataflow-v2.10.1` pushed individually → publish-pypi.yml runs `26735122907` + `26735128446` succeeded (TestPyPI skipped per patch-release exception; full Python 3.11–3.14 matrix + CodeQL + infrastructure CI green on each PR). Clean-venv install verified live (`uv pip install --refresh` both pinned): `kailash.__version__ == "2.28.3"` + `copy_context` wiring present in async_local/local; `dataflow.__version__ == "2.10.1"` + `_unwrap_optional_type(Optional[list]) is list` AND `(list|None) is list`. Convergence done inline (independent reviewer/security subagents server-throttled, 0 tokens × 2 attempts — same infra block as the prior session). Deployment record: `deploy/deployments/2026-06-01-v2.28.3-1200-dataflow-v2.10.1-1207.md`. No sibling drift — all 7 other framework packages AT-PARITY with PyPI at release-time enumeration. Follow-up surfaced (not blocking): #1228 — PEP 604 `T | None` unions unrecognized in four other DataFlow type-introspection paths (distinct bug class from #1207).

Prior 2026-06-01 (#855 cut) — three-package patch release closing issue #855 + a core fix surfaced during its validation: `kailash 2.28.1 → 2.28.2` (PR #1221 — `Node.__init_with_capture` no longer crashes with `TypeError: not iterable` when a Node subclass replaces `self.config` with a typed config object like kaizen `BaseAgentConfig`; the dict-only param-capture now skips for non-dict configs; recovered ~93 kaizen-agents Pipeline orchestration test errors; reviewer + security-reviewer APPROVED), `kailash-kaizen 2.24.3 → 2.24.4` (PR #1219 — `DataFlowMemoryBackend` column `tags` → `tag_list` to dodge the reserved `NodeMetadata.tags` `set[str]` collision that raised on first warm-tier store; store()→get() round-trips content AND tags), `kaizen-agents 0.9.7 → 0.9.8` (PR #1220 — `Agent(memory="persistent"|"learning")` no longer raises `TypeError: unexpected keyword argument 'storage_path'`; both shortcut factories now build a real warm-tier-backed `HierarchicalMemory` via a `removeprefix`-hardened, `?`/`#`/null-rejecting 4-slash SQLite DSN helper + `tag_list` model, degrading to hot-tier-only with a logged warning when DataFlow is absent; plus pyright type-drift fixes in `state_manager.py` + `journey/core.py`, 19 errors/14 warnings → 0/0). All three PRs merged via admin → three tags (`v2.28.2`, `kaizen-v2.24.4`, `kaizen-agents-v0.9.8`) pushed individually from `main` `fbdff857a` → publish-pypi.yml runs `26731952285` + `26731958370` + `26731965177` all succeeded (TestPyPI skipped per patch-release exception; both gate reviews clean on each originating PR; CI GitHub-hosted ubuntu-latest matrix green). Clean-venv install verified live (`uv pip install --refresh` all three pinned): `kailash.__version__ == "2.28.2"` + Node non-dict-config guard present + `kaizen.__version__ == "2.24.4"` + `DataFlowMemoryBackend` `tag_list` read-back present + `kaizen_agents.__version__ == "0.9.8"` + repaired persistent-shortcut + `removeprefix` DSN hardening present. Deployment record: `deploy/deployments/2026-06-01-855-3package-release.md`. No sibling drift — no other framework package had `main` ahead of PyPI at release-time enumeration. Follow-up surfaced (not blocking, pre-existing): `kaizen.memory.__init__` eagerly imports `aiosqlite` (an optional extra) via `persistent_tiers.py:21`, so `import kaizen.memory` fails on a clean `pip install kailash-kaizen` without the memory extra (top-level `import kaizen` works); recommend a kailash-kaizen 2.24.5 patch guarding the import per `dependencies.md`.

Prior 2026-05-31 — single-package minor release closing issue #1174 (PR #1214, Shard 5): `kailash-nexus 2.7.0 → 2.8.0` — Nexus FastAPI parity, final AC (migration guide + version owner). Shards 1–4 (PRs #1210/#1211/#1212/#1213) shipped the `nexus.extractors` sub-module (`Depends`/`Request`/`UploadFile`/`Multipart`/`Bytes`/`Headers`/`NexusHandlerError` + `DependencyOverrideMap`), `Nexus.handler_extract` resolver chain, `Nexus.dependency_overrides` test-injection map, `Nexus.register_sse` primitive (`register_sse_endpoint` now a shim), and the `register_websocket` callback overload — all backwards-compatible, no new top-level `fastapi` dep (Starlette re-exports). Shard 5 adds `docs/migration-fastapi.md` (474 lines, 9 sections). Stale-plan correction caught at implement: the outline documented a `Body[T]` typed-body extractor + `register_decoder` that were DEFERRED at the Q3 gate (absent from shipped `__all__`); guide §2 documents the shipped flat-input + `Bytes` story and §9 lists `Body[T]`/`Query` as not-yet-ported (no phantom imports per `spec-accuracy.md` MUST-1). reviewer APPROVE (6/6 mechanical sweeps) + security-reviewer APPROVE (zero findings; bounded file reads, fail-closed auth samples, `Body[T]`-deferred claim verified against source). Full Python 3.11–3.14 matrix + DataFlow/PACT/infrastructure CI green on PR #1214. Admin-merge → `main` `37f543643` (issue #1174 auto-closed) → lightweight tag `nexus-v2.8.0` → publish-pypi.yml run `26711302363` succeeded (TestPyPI skipped per minor-release exception with explicit human approval — matrix CI + both gates clean). Clean-venv install verified live: `nexus.__version__ == "2.8.0"` + extractors + `handler_extract`/`register_sse`/`register_websocket` all importable. SDK pin `kailash>=2.28.1` already PyPI-resolvable; no framework pin sweep needed. Deployment record: `deploy/deployments/2026-05-31-nexus-v2.8.0.md`. No sibling drift — all 7 other framework packages AT-PARITY with PyPI at release-time enumeration.

Prior 2026-05-08 — single-package minor release closing issue #881 (PR #888): `kailash 2.16.1 → 2.17.0` — `DurableExecutionEngine.workflow_blob` JSON serialization contract. `_enqueue_for_run` previously enqueued `Task(workflow_blob=b"", ...)` because the engine had no built-in serializer; cross-process workers couldn't reconstruct without out-of-band registry access. Both producer surfaces (`WorkflowScheduler` + `DurableExecutionEngine`) now route through new shared helper `runtime/_workflow_blob.py::serialize_workflow_to_blob` so they emit byte-identical JSON for the same workflow. Workers reconstruct via `Workflow.from_dict(json.loads(blob.decode("utf-8")))`. Producer-boundary 8 MiB cap prevents worker OOM. Additive contract (workers ignoring `workflow_blob` keep working). Side-benefit: W6 redaction consumer's `_redact_workflow_blob` now actually fires for engine-dispatched paths (pre-fix `json.loads("")` raised non-fatally and redaction never ran). 6 Tier-1 + 3 regression tests. Both gates approved on PR #888 (reviewer + security-reviewer, no findings; one LOW addressed in CHANGELOG addendum). Release-prep PR #889 auto-skipped PR-gate matrix per `release/v*` convention; admin-merge → tag `v2.17.0` → publish-pypi.yml run `25537573033` succeeded (TestPyPI skipped per minor-release exception with explicit human approval — Python 3.11–3.14 matrix CI on PR #888 pass + reviewer/security gates clean). Clean-venv install verified live: `kailash.__version__ == "2.17.0"` + `serialize_workflow_to_blob` importable + `MAX_WORKFLOW_BLOB_BYTES == 8388608` from both helper and scheduler back-compat alias + `DurableExecutionEngine` importable. Deployment record: `deploy/deployments/2026-05-08-v2.17.0.md`. No sibling drift — all 8 framework packages AT-PARITY with PyPI at release-time enumeration; framework `kailash>=2.16.0` pins admit 2.17.0 (minor backward-compat) so no framework-package release required. Follow-up surfaced (not blocking): #891 — `HybridSearchNode` name collision between `kailash-dataflow` and `kailash-kaizen` (both register the same string name into the global node registry; pre-existing since 2026-03-11 monorepo refactor `b553104c`).

Prior 2026-05-06 — single-package patch release closing issue #835 (PR #842): `kailash-dataflow 2.7.8 → 2.7.9` — `db.transactions.transaction()` no longer raises `RuntimeError: Event loop is closed` when invoked from an event loop different from the one that constructed the DataFlow instance. `TransactionManager._get_adapter` now resolves the asyncpg pool via the same per-loop `_get_or_create_async_sql_node(db_type)._get_adapter()` priority chain used by `db.express.*` (priority chain `_shared_pools` → runtime pool → `_PROCESS_POOL_REGISTRY` → fallback under per-key creation locks; WeakValueDictionary-based reaping on loop close). `_get_adapter_from_context` (used by `TransactionScopeNode` / `TransactionSavepointNode`) converted to `async`; every caller awaits it. `ConnectionManager.initialize_pool` converted to transient reachability check (no long-lived `_connection_manager._adapter` retention); the init-time fail-fast contract from `dataflow-pool.md` Rule 2 preserved exactly. `_PoolWrapper` (internal, dead branch) deleted. Internal API removal carries no deprecation shim per `rules/zero-tolerance.md` Rule 6a internal-only carve-out. 9 Tier-2 regression tests at `tests/regression/test_issue_835_transaction_cross_loop.py` against real PostgreSQL (cross-loop `begin()`, nested savepoint/rollback, `TransactionScope` async-cm, concurrent two-loop `begin()`, WeakValueDictionary reaping, pool-cap stress 50 sequential loops). Spec: `specs/dataflow-cache.md` §12.1 (loop-affinity contract) + §13.4 (async transaction participation) updated. Admin-merge → tag `dataflow-v2.7.9` → publish-pypi.yml run `25420043122` succeeded (TestPyPI skipped per patch-release exception with human authorization — Python 3.11–3.14 matrix CI on PR #842 all pass + CodeQL + spec drift + infrastructure tests). Clean-venv install verified live: `dataflow.__version__ == "2.7.9"` + `TransactionManager._get_adapter` source contains `_get_or_create_async_sql_node` AND no `_PoolWrapper` reference + `ConnectionManager._adapter` field absent (no `self._adapter:` annotation). Deployment record: `deploy/deployments/2026-05-06-dataflow-v2.7.9.md`. No sibling drift — all 7 other framework packages AT-PARITY with PyPI at release-time enumeration.

Prior 2026-05-06 — single-package minor release closing issue #829 (PR #836): `kailash-kaizen 2.19.0 → 2.20.0` — replaces `Kaizen._generate_role_based_traits` keyword classifier with LLM-first `Signature`-driven derivation per `rules/agent-reasoning.md` Rule 1. New private `RoleToTraitsSignature` (`role: str → traits_csv: str`) constructed in a per-call `BaseAgent` with `temperature=0`. Per-Kaizen-instance bounded LRU cache (`OrderedDict`, max 256 entries, `popitem(last=False)` eviction) keyed by `role.strip().lower()`. Security hardening landed same-shard per security-reviewer: output sanitization regex `^[a-z0-9_ ]{1,32}$` (prompt-injection defense — protects downstream `_generate_role_based_prompt` system-prompt rendering); hashed `role_hash=<sha256[:8]>` + `raw_len=<int>` in WARN logs (PII-leakage defense per `rules/observability.md` Rule 8 spirit); bounded LRU (DoS defense). Failure mode change: `KAIZEN_DEFAULT_MODEL` unset OR LLM call failure → `RuntimeError` naming both escape hatches (`behavior_traits=...` config arg OR `.env` provider key). Test sweep per `rules/orphan-detection.md` Rule 4: rewrote `tests/regression/test_issue_822_behavior_traits_render.py::test_behavior_traits_default_from_role` to shape-only Tier-2 + `tests/unit/test_kaizen_multi_agent_coordination.py::test_specialized_agent_role_based_behavior_traits` to shape-only; new `tests/unit/conftest.py` autouse fixture stubs `Kaizen._generate_role_based_traits` for Tier-1 (replaces 36 individual call-site edits with one fixture); 6 new Tier-2 tests across `test_role_to_traits_llm_derivation.py` (4) + `test_role_traits_cache_wiring.py` (2). Spec: `specs/kaizen-core.md` §7.5 (Trait Derivation) added with full contract. Admin-merge → tag `kaizen-v2.20.0` → publish-pypi.yml run `25393777596` succeeded (TestPyPI skipped per minor-release exception with explicit human approval — Python 3.11–3.14 matrix CI on PR #836 all pass + CodeQL Python pass). Clean-venv install verified live: `kaizen.__version__ == "2.20.0"` + `RoleToTraitsSignature` importable + `Kaizen._TRAIT_CACHE_MAX == 256` + keyword classifier residue absent in `inspect.getsource` of `_generate_role_based_traits`. Deployment record: `deploy/deployments/2026-05-06-kaizen-v2.20.0.md`. No sibling drift — all 8 other framework packages AT-PARITY with PyPI at release-time enumeration.

Prior 2026-05-05 — single-package minor release closing issue #822 (PRs #825 + #826): `kailash-kaizen 2.18.2 → 2.19.0` (Shard 1 — Optional/None pyright cascade fix + `signature_programming` silent no-op gate, restoring the LLM-first signature-programming behavior; Shard 2 — dead MCP integration surface deletion, ~1155 LOC removed, 12 methods + 1 property on `kaizen.core.agents.Agent` and `kaizen.core.framework.Kaizen` that imported `..mcp.registry::get_global_registry` / `..mcp::AutoDiscovery` / `..mcp::MCPConnection` — modules that never existed in the kaizen source tree at any commit since the original `apps/`→`packages/` move `b553104c`; per `rules/zero-tolerance.md` Rule 2 + `rules/orphan-detection.md` Rule 3, deletion is the only valid disposition; LOC invariant regression test at `tests/regression/test_issue_822_loc_invariant.py`). Release-prep PR #827 auto-skipped PR-gate matrix per `release/v*` convention; admin-merge → tag `kaizen-v2.19.0` → publish-pypi.yml run `25382388831` succeeded (TestPyPI skipped per minor-release exception with human approval — structural deletion + type-only fixes covered by Python 3.11–3.14 matrix CI on Shards). Clean-venv install verified live: `kaizen.__version__ == "2.19.0"` + all 12 deleted surfaces absent (`hasattr` returns False on `expose_as_mcp_server` / `connect_to_mcp_servers` / `mcp_registry` / etc.) + canonical Agent/CoreAgent/Kaizen surfaces live. Deployment record: `deploy/deployments/2026-05-05-kaizen-v2.19.0.md`. No sibling drift — only kailash-kaizen needed release; the other 7 of 8 packages were AT-PARITY with PyPI.

Prior 2026-05-04 — single-package patch release closing issue #814 (PRs #818 + #820): `kailash-kaizen 2.18.1 → 2.18.2` (Shard 1 — 22 BaseTool override conformance + 12 Optional/None safety + Cluster D `ResearchAdapter` runtime bug; Shard 2 — vestigial research-integration subsystem deleted, ~5,019 LOC removed across 14 files including 5 src + 8 tests + 1 examples dir, `[research]` + `[web-search]` optional extras declared, `WebFetchTool._extract_text` bs4 silent-degradation converted to loud `ImportError`). publish-pypi.yml run `25323887979` succeeded (TestPyPI skipped per patch-release exception). Clean-venv install verified live: `kaizen.__version__ == "2.18.2"` + new `kaizen.research` 8-symbol surface imports + deleted orphan symbols (`FeatureManager`, `IntegrationWorkflow`) raise `ImportError`. Two follow-up issues filed: #821 (kaizen-agents test parity for `research_patterns/*`) + #822 (kaizen Optional/None typing in `core/`). Deployment record: `deploy/deployments/2026-05-04-kaizen-v2.18.2.md`. No sibling drift — only kailash-kaizen needed release.

Prior 2026-05-03 — six-package release closing the issue #781 cleanup workstream (T1-T5 merged via PRs #804/#805/#806/#807/#808): `kailash 2.13.3 → 2.13.4` (T4 core/runtime TODO-NNN comment-strip + T5 CI gate `.pre-commit-config.yaml::no-untracked-todo-nnn` + `tests/regression/test_no_untracked_todo_nnn.py` + `scripts/check_no_untracked_todo_nnn.sh`), `kailash-dataflow 2.7.5 → 2.7.6` (T1, 89 markers stripped), `kailash-kaizen 2.18.0 → 2.18.1` (T2, 80 markers stripped + #801 test fix), `kailash-nexus 2.6.0 → 2.6.1` (T4, 16 markers stripped), `kaizen-agents 0.9.4 → 0.9.5` (T3, 69 markers stripped), `kailash-mcp 0.2.10 → 0.2.11` (sibling sweep per `build-repo-release-discipline.md` Rule 1: W6-002 type fix + 10 ElicitationSystem Tier-2 tests + LOW-6 triage). All six PRs through admin-merge → 6 tags pushed sequentially → publish-pypi.yml (OIDC) workflow runs all `success`. Clean-venv install + import verification documented in `deploy/deployments/2026-05-03-v2.13.4-six-package-issue-781-release.md`. SDK dep pin sweep `kailash>=2.13.4` across all 8 framework packages (released 5 + align/pact/ml unreleased-this-cycle for consistency). Issue #781 closed.

Prior 2026-05-02 — paired release closing #761 + #762 + #763 + #764 + #779: `kailash 2.13.2 → 2.13.3` (catch-up release for #779's auth Rule 2 cleanup that landed without a same-PR version bump per `build-repo-release-discipline.md` Rule 5 — 7 NotImplementedError stubs eliminated across `src/kailash/{nodes,middleware}/auth/` per `zero-tolerance.md` Rule 2 § "Fake dispatch"), `kailash-kaizen 2.14.0 → 2.16.0` (two-minor jump combining PR #780's #761/#762 LlmDeployment.openai_compatible + anthropic_compatible escape-hatch presets + preset_name field, AND PR #783's #763 LlmDeployment.supports() capability matrix + #764 register_bedrock_region runtime override). Cross-SDK parity with kailash-rs PR #722/#724/#725/#726. Both via admin-merge → tag → publish-pypi.yml (OIDC) → workflow runs `25237480388` + `25237487960` succeeded. TestPyPI gate skipped on both with explicit human approval (kailash 2.13.3 is recap-only; kailash-kaizen 2.16.0 surfaces unit-tested with parametrized cross-SDK byte-match coverage and dual-reviewer APPROVED). Clean-venv install + import verification documented in `deploy/deployments/2026-05-02-v2.13.3-kaizen-v2.16.0.md`.

Prior 2026-05-01 — paired patch cut closing #767 + #768: `kailash 2.13.1 → 2.13.2` (durability_middleware short-circuits before draining `StreamingResponse` / `text/event-stream`, restoring SSE + cache-replay semantics for `EventSource` clients on `EnterpriseWorkflowServer`/`Nexus()` for #767), `kailash-dataflow 2.7.1 → 2.7.3` (skipping 2.7.2 — never tagged; `ListNodeCacheIntegration` invalidation patterns aligned with producer key shape for #750, plus `FieldTypeProcessor._resolve_type` now strips parameterized generics + handles PEP 604 `T | None` so `@db.model` accepts `list[str]` / `dict[str, Any]` on Python 3.11+ for #768). Both PRs through admin-merge → tag → publish-pypi.yml (OIDC) → clean-venv install verified live (`kailash==2.13.2` + `kailash-dataflow==2.7.3` import; `_resolve_type(list[str]) is list` + `durability_middleware` source contains `StreamingResponse`). Cross-SDK loop closure for #759/#767/#768 still pending explicit human approval per `upstream-issue-hygiene.md` Rule 1.

Prior 2026-04-30 — the v2.13.0 cluster cut: `kailash 2.12.0 → 2.13.0` (adds `kailash.utils.lifespan` shared FastAPI helpers + patches 3 sibling FastAPI lifespan sites for #712), `kailash-nexus 2.4.1 → 2.5.0` (adds `Nexus.add_startup_handler` / `add_shutdown_handler` public API for #712), `kailash-dataflow 2.5.0 → 2.6.0` (lazy `runtime` `@property` + setter + `runtime=` kwarg for #713 + 6 subsystem captures → lazy lookups + DDL connection-reuse via `SyncDDLExecutor.execute_ddl_batch_per_statement` for #714). Three tags pushed individually (per multi-tag discipline), three `publish-pypi.yml` runs triggered. Specs landed in PR #728 (`specs/dataflow-core.md` §1.4/1.5/1.6.5 + `specs/nexus-core.md` §10.2/10.3 + `specs/nexus-services.md` §29). Cross-SDK: kailash-rs uses axum + Tokio (no equivalent custom-lifespan footgun for #712, no module-import event-loop binding for #713); analogous DDL connection thrash for #714 may exist — companion issue to be filed.

Prior 2026-04-28 (kailash-dataflow 2.3.3 patch — closes merged-but-unreleased gap from PRs #684 / #689 / #690: `not_null_handler.py` pyright cleanup + 8 migration unit-test mock-method-drift repairs. No production behavior change beyond type-checking cleanup; 13 pre-existing pytest warnings in the migrations suite remain on separate workstreams). kailash-ml 1.5.1 (PR #679) routes the `engines/model_registry.py` `ModelNotFoundError` through the canonical `kailash.ml.errors.ModelNotFoundError` (subclass of `ModelRegistryError → MLError`), removing the divergent local `class ModelNotFoundError(Exception)` that surfaced when the W7-001 LineageGraph walker raised canonical while `ModelRegistry.get_model` raised local. 6 raise sites converted to canonical kwargs; 3 regression tests pin class-identity + AST invariant. PR #679 also adds `scripts/development/find-venv-python.sh` — wrapper that resolves `.venv/bin/python` via `git rev-parse --git-common-dir` so pre-commit hooks run from inside `.claude/worktrees/<X>/` without the `core.hooksPath=/dev/null` bypass; 3 regression tests pin the worktree-safe invariants. Clean-venv install verified live: `kailash-ml==1.5.1` + `ModelNotFoundError canonical identity: OK` + `__all__ count: 50 (AST-derived)`. No sibling drift.

Prior 2026-04-27 — W7 portfolio remediation: kailash-ml 1.5.0 (PR #677 closes #657) ships the cross-engine `LineageGraph` engine module; kailash-dataflow 2.3.2 (PR #678) wires `format_error_for_event` through `emit_train_end` at the emitter.

## Release Runbook

1. Version bump in `pyproject.toml` + `__init__.py`
2. Update CHANGELOG.md
3. Run full test suite
4. Security review
5. Create release branch + PR
6. Merge PR (admin bypass)
7. Tag on main → triggers publish-pypi.yml
8. Verify PyPI install
9. Update COC template dependency pins
10. Document in `deploy/deployments/`

## Documentation

- **Build**: `cd docs && python build_docs.py`
- **Deploy**: Auto via `docs-deploy.yml` on push to main
- **Hosting**: GitHub Pages

## Versioning

- Lockstep: core SDK version bumps require framework dependency pin updates
- SemVer: major (breaking), minor (features), patch (fixes)

## Tag Convention

**IMPORTANT**: Use lightweight tags (NOT annotated) for release triggers.

```bash
# CORRECT — lightweight tag triggers GitHub Actions reliably
git tag v2.5.0
git push origin v2.5.0

# WRONG — annotated tag may NOT trigger publish-pypi.yml
git tag v2.5.0 -m "Release message"
```

GitHub Actions `push.tags` webhook processing handles lightweight tags
more reliably than annotated tags pushed after creation.

## Multi-Tag Release — Push Individually

When releasing multiple packages at once (coordinated patch release), tags
MUST be pushed individually, NOT in a single batch. Batch pushes of 3+ tags
fail to trigger the publish workflow reliably.

```bash
# CORRECT — push tags one at a time
git push origin v2.8.6
git push origin dataflow-v2.0.8
git push origin kaizen-v2.7.4
git push origin nexus-v2.0.2
git push origin mcp-v0.2.4

# WRONG — batch push silently skips workflow triggers
git push origin v2.8.6 dataflow-v2.0.8 kaizen-v2.7.4 nexus-v2.0.2 mcp-v0.2.4
# ↑ observed on 2026-04-14: ZERO of 5 tags triggered publish-pypi.yml.
# Required manual workflow_dispatch for each package.
```

**Why:** GitHub Actions' `push.tags` webhook delivery has undocumented
rate-limiting/batching behavior when multiple tags arrive in a single push
event. The first observed failure mode (2026-04-14): 5 tags pushed at once
triggered zero workflow runs. Individual pushes with a brief pause (≥1s)
between them trigger reliably.

**Recovery if a batch push was already done**: use `workflow_dispatch` to
manually trigger publishing for each affected package. The tags themselves
are still valid — only the auto-trigger was missed.
