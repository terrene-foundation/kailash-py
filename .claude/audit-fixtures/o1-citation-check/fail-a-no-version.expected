ok=false reason=no-version-token failed=(a)
predicate: (a) FAILED — standard named (ISO 27001) + clause + derivation present, but NO version token (no ":2022" / "v4.0" / "Rev. N"). (a) is evaluated before (b)/(c), so the most fundamental gap surfaces first. Note: "27001" is a 5-digit catalog number, NOT a version; the dotted "8.24" inside the clause id is deliberately NOT counted as a version.
