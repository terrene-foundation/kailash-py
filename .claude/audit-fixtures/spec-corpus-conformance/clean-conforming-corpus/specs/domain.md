# Domain spec

## Governed behavior

The rule `rules/alpha.md` governs this behavior, and the module
`mod.mjs::realSymbol` carries the constant the rule depends upon at runtime.

## Out of scope

Cross-ecosystem federation is TBD and is deliberately outside this spec's perimeter;
per spec-accuracy.md Exception 1 an out-of-scope section may bound coverage this way.
