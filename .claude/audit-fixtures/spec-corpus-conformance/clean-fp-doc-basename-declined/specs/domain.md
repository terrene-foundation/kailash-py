# Domain spec

## Governed behavior

The rule `rules/alpha.md` governs this behavior. A bare `some-design-doc.md` is
spelled the same whether it names a rule or a workspaces design doc, so it is
declined rather than resolved against a guessed directory.
