{
  "_predicate": "public-authority-O1 NEGATIVE — an attacker-controlled authority string that PREFIX-COLLIDES a public-allowlist token ('ISO-Acme-Internal-Policy' starts with 'iso') MUST NOT be carved out; the word-boundary match (_tokenBoundaryOk) refuses an identifier-continuation char ('-'), so the write falls to the disclosure scan and BLOCKS on the fork-identifying findings (#584 R2 leak fix)",
  "upstreamCanon": { "remote": "canon-origin", "url": "ssh://canon/loom.git" },
  "targetEcosystem": "canon-origin",
  "o1": true,
  "authority": "ISO-Acme-Internal-Policy",
  "findings": ["acme-corp tenant slug", "src/acme-internal/billing.py"]
}
