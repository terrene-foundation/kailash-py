# posture-gate audit fixture: flag — MultiEdit attempt on protected state file
#
# Predicate (F14 LOW-2): posture-gate.js's R6-C-02 defense-in-depth check
# MUST cover MultiEdit and NotebookEdit tools in addition to Edit/Write.
# Without the extension, a MultiEdit invocation against
# .claude/learning/posture.json bypasses the secondary fence; only the
# settings.json::permissions.deny primary fence catches it. If settings is
# malformed or overridden, the agent can silently mutate posture state.
#
# Input shape (PreToolUse):
#   tool_name = "MultiEdit"
#   tool_input.file_path = ".claude/learning/posture.json"
#   tool_input.edits = [{old_string: "L1_PSEUDO_AGENT", new_string: "L5_DELEGATED"}, ...]
#
# Expected gate evaluation:
#   posture-gate's R6-C-02 branch matches MultiEdit
#   reason cites "posture.json / violations.jsonl writes are reserved for hooks"
#   halt = true (halt-and-report)
#
# Same predicate covers NotebookEdit (no fixture variant — same code path).
