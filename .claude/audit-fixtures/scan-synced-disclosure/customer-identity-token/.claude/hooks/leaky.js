// FIXTURE (synthetic): the next line embeds the synthetic tenant identifier in
// LOWERCASE so the match locks the shape's case-insensitive `i` flag. Exactly
// one occurrence in this file -> exactly one finding (the count-lock).
// tenant-ref: faketenant
const x = 1;
