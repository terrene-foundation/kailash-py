# Generic governance vocabulary — MUST NOT flag

This line uses the GENERIC, jurisdiction-neutral labor-relations concept terms
that identify no customer: works-council / co-determination agreement. These
are the safe replacements the journal/0214 scrub uses; they are deliberately
NOT denylist tokens, so this file MUST produce zero findings even while the
customer-identity shape is active.
