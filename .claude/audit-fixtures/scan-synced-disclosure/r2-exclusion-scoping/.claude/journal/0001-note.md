# Genuine journal/ entry (MUST stay EXCLUDED — directory scope intact)

This file lives under the `journal/` DIRECTORY (a path segment exactly
equal to `journal`). The R2 fix scopes the exclusion to this exact
directory — it MUST still be excluded. A synthetic leak here MUST NOT
flag; if it does, the must-fix #5 narrowing over-corrected and broke
the accepted-history journal exclusion.

Synthetic planted shape that MUST NOT flag:
ran on Fakename-MacStudio at /Users/notesperie/repos/loom,
runner globex-linux-arm64, org acme-corp/loom#3.

All tokens SYNTHETIC.
