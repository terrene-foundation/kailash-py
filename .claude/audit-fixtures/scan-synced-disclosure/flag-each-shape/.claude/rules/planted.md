# Planted-disclosure fixture (ALL synthetic, NO real tokens)

Every token below is INVENTED for this fixture. None is a real operator
hostname, org slug, runner label, home path, or service label.

1. operator-hostname shape:
   The build ran on Fakename-MacStudio overnight.
   A second box, Buildbot-Mini, handled the arm leg.

2. nonfoundation-org-slug shape:
   See https://github.com/acme-fixture/loom for the mirror.
   The org slug acme-enterprise is a synthetic enterprise example.

3. org-derived-runner-label shape:
   Self-hosted label: acme-linux-arm (invented; not example-linux-arm).

4. operator-home-path shape:
   Config lives at /Users/fakeuser/repos/loom/.claude here.

5. operator-service-label shape:
   launchctl bootout gui/501 com.fakeco.runner.alpha
