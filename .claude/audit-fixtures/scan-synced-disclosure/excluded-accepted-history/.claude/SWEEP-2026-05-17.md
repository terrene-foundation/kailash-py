# Accepted-history sweep report (MUST be EXCLUDED from the scan)

This file deliberately contains a synthetic disclosure shape:

ran on Fakename-MacStudio at /Users/fakeuser/repos/loom

It MUST NOT flag, because SWEEP-\*.md is accepted-history and is on the
scanner's exclusion list. If this fixture flags, the exclusion broke.
