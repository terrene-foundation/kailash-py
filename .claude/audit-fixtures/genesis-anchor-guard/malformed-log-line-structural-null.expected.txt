structural-NULL negative: malformed JSONL lines in coordination-log.jsonl MUST be skipped by loadLogRecords() — they cannot be folded (no signature to verify), they contribute neither to the trust root nor to the peer high-water, and the hook MUST NOT crash on them. Per cc-artifacts.md Rule 9 + hook-output-discipline.md MUST-4: insufficient information is structural-NULL → return null from the relevant predicate, fail-CLOSED on the SEMANTIC question (no verifying anchor present), NOT on the syntactic question (malformed line).

In this fixture: line 1 is truncated (no closing brace), line 2 is prose, line 3 is well-formed (but does NOT verify against a real key — the sig is illustrative). A real fold would skip lines 1+2 (parse error → skipped) and reach line 3 (parse-succeeds but verify-fails because the sig is fake) → no verifying anchor found → fail-CLOSED with the structured emit() payload (severity: block, what_happened: "no verifying owner-bound genesis-anchor", agent_must_report: instructs the agent to run /whoami --enroll-genesis).
