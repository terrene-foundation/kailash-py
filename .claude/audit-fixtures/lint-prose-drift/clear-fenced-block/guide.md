# Fixture — E1 fenced-code-block exemption (CLEAR)

A layout literal inside a fenced example is display, not an assertion:

```bash
# DO NOT — positional guess
ls ~/repos/loom/kailash-coc-claude-py
cd ../kailash-py && git status
```
