# Fixture — E3 historical-section-fence exemption (CLEAR)

## Migration from ~/repos (historical — completed 2026-03-29)

<!-- ssot-lint-allow:historical (one-time migration, complete; provenance only) -->

The management commands that were at `~/repos/.claude/` moved to loom/. After
migration, `~/repos/.claude/` is reduced to a thin settings.json.
