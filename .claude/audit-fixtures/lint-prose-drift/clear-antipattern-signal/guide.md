# Fixture — E5 anti-pattern / HINT framing exemption (CLEAR)

Tooling resolves every target through the resolver — never a positional `~/repos/<name>` / `../kailash-py` guess (the literal is the anti-pattern, not an assertion).

The recommended sublayout `~/repos/kailash/{build,use}/<slug>` is a HINT, not a MUST.
