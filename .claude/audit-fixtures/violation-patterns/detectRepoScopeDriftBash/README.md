# detectRepoScopeDriftBash audit fixtures

Per `rules/cc-artifacts.md` Rule 9 + `rules/hook-output-discipline.md` MUST-4. Each fixture pins one scope-restriction predicate the detector relies on. Inputs are Bash command strings; expected outputs are the JSON returned by `detectRepoScopeDriftBash(input, cwd)` — `null` (no flag) or a violation object.

| Fixture                         | Expects           | Predicate locked                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ------------------------------- | ----------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `clean-current-repo.txt`        | `null`            | `gh ... --repo X` where X contains cwd basename → in-scope, no flag                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `flag-explicit-other-repo.txt`  | `halt-and-report` | `gh ... --repo X` where X is a SYNTHETIC, never-authorized off-repo string (`other-org/other-repo`) → flag with halt-and-report severity (NOT block, per MUST-2). MUST be a synthetic slug: the detector's condition-4 receipt allowance (`hasCrossRepoAuthorizationReceipt`) returns null when a REAL cross-repo-authorization receipt for the target exists on disk in `.claude/cross-repo-authz/`, so a real repo slug (e.g. a genuine sibling repo) would be suppressed the moment a real session authorizes a read/write to it — a slug that can never have a real remote/receipt keeps the flag fixture deterministic. |
| `skip-shell-variable.txt`       | `null`            | `gh ... --repo "$REPO"` — pre-expansion shell variable, detector cannot evaluate, MUST skip (MUST-3)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `skip-command-substitution.txt` | `null`            | `gh ... --repo $(...)` — command substitution, detector cannot evaluate, MUST skip (MUST-3)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `skip-braced-variable.txt`      | `null`            | `gh ... --repo "${REPO}"` — braced shell variable, MUST skip (MUST-3)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `skip-embedded-in-echo.txt`     | `null`            | `echo '...gh ... --repo X...'` — `gh` embedded as a substring inside an echo / JSON payload (preceded by a quote, NOT a command-segment boundary). The SEGMENT-ANCHORED regex `(?:^\|[;&\|\n(])\s*gh` MUST skip so the guide-first PreToolUse ceremony does not halt on a command that merely MENTIONS a cross-repo gh invocation.                                                                                                                                                                                                                                                                                           |
| `skip-embedded-in-grep.txt`     | `null`            | `grep -rn "gh ... --repo X" ...` — same substring-embedding class; `gh` follows a quote, not a segment boundary → MUST skip.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `skip-embedded-heredoc-multiline.txt` | `null`      | #1320 — a `gh … --repo other` quoted as a DOCUMENTATION example on its OWN line inside a doc-carrier's multi-line `--body "$(cat <<'EOF' … EOF)"` heredoc payload. `maskDocCarrierPayloads` neutralizes the heredoc BODY before the split, so the example is opaque data, not a segment-leading cross-repo command → MUST skip. (Without the mask the newline makes the prose `gh … --repo` segment-leading → false halt.)                                                                                                                                                                                              |
| `skip-embedded-fenced-codeblock.txt`  | `null`      | #1320 — same class, with the cross-repo example inside a fenced ```sh code block inside the heredoc body → MUST skip.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `flag-real-multiline-chain.txt`       | `halt-and-report` | #1320 DISCRIMINATOR — a GENUINE second cross-repo command (`gh … --repo other-org/other-repo`) on its OWN real newline (NOT inside a quoted doc-carrier payload) MUST STILL flag. Proves the mask distinguishes by ARGUMENT CONTEXT, not by pattern-matching the line: a real chained cross-repo write is never masked, only a doc-carrier's payload is.                                                                                                                                                                                                                                                             |
| `flag-body-flag-token-in-quoted-prose-then-real-crossrepo.txt` | `halt-and-report` | #1321 redteam CRITICAL (PoC C) — `echo "x -F "; gh issue create --repo other-org/other-repo …`. A body-flag TOKEN (`-F`) sits INSIDE the quoted prose `"x -F "`. The pre-fix Pass-2 masker matched `-F` anywhere and ran an ad-hoc quote scan that misread the string's CLOSING quote as the value's OPENING quote, masking the real trailing `; gh … --repo other` (incl. the `;`) to EOL → a BLOCK→PASS bypass. The quote-aware Pass-2 honors a body flag ONLY at an unquoted word boundary, so the `-F` in prose is data and the real chained cross-repo write STILL flags. |
| `skip-interpreter-body-gh.txt` | `null` | #1319/#1320 systemic (F-B) — a `gh … --repo other-org/other-repo` slug quoted as DATA inside a `node -e '…'` interpreter body. `maskQuotedSpans` masks the body so the `gh` never leads a segment → MUST skip. Distinct from the doc-carrier (`--body`) path above; the LIVE lock is inline in `tests/integration/multi-operator/doc-carrier-mask-1319-1320.test.js` § "F-B interpreter body". |
| `skip-interpreter-body-gh-multiline.txt` | `null` | #1319/#1320 systemic (F-B) — same class, MULTI-LINE `python3 -c "…"` body with `gh … --repo` on its own line inside the quoted body. maskQuotedSpans masks in-quote newlines too, collapsing to one masked segment → MUST skip. |
| `flag-real-gh-after-interpreter-body.txt` | `halt-and-report` | #1319/#1320 systemic (F-B) DISCRIMINATOR — a GENUINE `gh … --repo other-org/other-repo` chained after an interpreter body (`node -e '…' && gh …`) MUST STILL flag. Proves the interpreter-body quote-mask distinguishes by CONTEXT, not by matching `gh --repo`: a real chained cross-repo write is never masked. |
| `flag-ansic-desync-before-crossrepo.txt` | `halt-and-report` | F-B SECURITY regression (fb-security adversarial redteam, HIGH, #1321-class flag→PASS) — `echo $'\'' ; gh issue create --repo other-org/other-repo`. maskQuotedSpans does NOT honor `$'…'` ANSI-C escaping, so the mask ended stuck in an OPEN single-quote → the real `;` separator was masked → the cross-repo `gh` was not segment-leading → MISSED (returned null). Closed by the `EXECUTES_INSIDE_QUOTES_RX` raw-fallback (parity with detectStateFileMutation's per-line fallback): any command carrying `$(`/backtick/`$'…'`/`${ …}` segments on the RAW doc-masked string (pre-#1319, fail-closed) → the real `; gh --repo` still leads a segment and flags. LIVE lock: doc-carrier-mask-1319-1320.test.js § "F-B SECURITY". |

| `flag-quoted-sep-exec-crossrepo.txt` | `halt-and-report` | #1325 per-segment EXECUTES gating — `gh issue create --title "x;y" --repo other-org/other-repo --body "$(true)"`. A quoted separator in a NON-doc-carrier flag value (`--title "x;y"`) PLUS a benign executing construct (`--body "$(true)"`). The pre-fix whole-command EXECUTES gate (`!EXECUTES_INSIDE_QUOTES_RX.test(masked)`) downgraded the ENTIRE command to the pre-#1319 raw `[;&\|\n]` split, which FRACTURED the quoted `;` → the `gh` lead lost its `--repo` → MISS (returned null). The fix applies the EXECUTES fail-close PER SEGMENT, gated on the QUOTE-MASKED segment: a `$(…)` masked away inside a quoted value no longer downgrades its segment (so the sibling quoted `;` is not re-fractured), while a `$'…'` desync — whose `$'` survives the mask — still fails closed to a raw re-split. LIVE lock: `doc-carrier-mask-1319-1320.test.js` § "#1325" (incl. a benign in-scope NO-FP row). **Known bound (residual, origin parity — NOT a regression):** an UNQUOTED top-level `$(…)` SHARING a segment with a quoted `;` and the `--repo` (`--title "a;b" --arg $(true) --repo other-org/other-repo`) still re-fractures — pre-expansion the `$(…)` could itself contain a `;`, so a raw re-split of that segment is the fail-closed choice. This equals origin/`main` behavior; closing it needs exec-aware tokenization (INCREMENTAL, out of #1325 scope). |

The cwd for every fixture is `/Users/esperie/repos/loom` (basename `loom`). `other-org/other-repo` is the canonical SYNTHETIC off-repo string for the FLAG case (it can never resolve to a real remote or accumulate a real authorization receipt, so the flag stays deterministic — see the `flag-explicit-other-repo.txt` row). The two `skip-embedded-*` fixtures deliberately keep a realistic-looking slug inside the echo/grep payload, since they assert `null` via the segment-anchor skip regardless of any receipt. The segment-anchored regex (`gh` MUST lead a command segment — start-of-string OR after `;` `&` `|` newline `(`) is what distinguishes a real cross-repo invocation from one embedded as a substring; the two `skip-embedded-*` fixtures pin that boundary.
