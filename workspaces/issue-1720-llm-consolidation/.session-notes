# Session Notes — 2026-07-17 (creds-in-logs security sweep → 2.34.1)

## Where we are

Continued #1720. This session ran an **adversarial creds-in-logs redteam to
convergence** (6 rounds of parallelized workflows) — the MED-1 sibling class the
2.34.0 release fix only partially closed — and prepared **kaizen 2.34.1**.

## Executed this session (durable receipts)

- **Creds-in-logs class CLOSED across the whole package.** 22 source files, 85
  sanitize/mask sites, 10 behavioral regression tests
  (`tests/regression/test_issue_1720_creds_in_logs_sweep.py`). Every
  credential-bearing exception log + URL log routes through
  `sanitize_provider_error` / new `_mask_redis_url` / new `_mask_webhook_url`;
  `exc_info=True` dropped on provider/MCP/connection paths (it resurfaced the raw
  provider exception via the implicit `__context__` chain).
- **Redteam CONVERGED** — 6 rounds (task IDs in
  `journal/creds-in-logs-convergence.md`). Final round: 0 findings across
  verify-all-fixes + exc-info-chain + return/metric lenses. Exhaustive grep over
  all 54 credential-bearing modules → zero remaining raw-exception logs.
- **Gate review done** — reviewer + security-reviewer both ran; found 3 real
  issues (registry.py return/log-asymmetry sibling; `_mask_webhook_url` userinfo
  + query leak; `_mask_redis_url` multi-URL leak) — ALL fixed + 2 more regression
  tests. async_single_shot LLM-gen error also sanitized.
- **PR #1797** open (branch `fix/1720-creds-in-logs-sweep`, 3 commits: fix +
  review-fixes + `release: kailash-kaizen 2.34.1`). Version bumped atomically
  (pyproject 2.34.1 + `__init__` 2.34.1). pre-commit clean.

## Read first (next session)

1. **`04-validate/sweep-2026-07-17.md`** — the full repo-wide /sweep + the
   **clustered wave plan** (Wave 1 parallel / Wave 2 provider-surface / Wave 3
   soak-gated Wave C). START HERE for next-session scope.
2. `journal/creds-in-logs-convergence.md` — the 6-round convergence receipt.
3. This file's "Outstanding" below.

## Wave plan (from the 2026-07-17 /sweep — value-ranked, clustered for parallel)

- **WAVE 1 (parallel, run now):** 1A example-gallery/doc test fixes ·
  1B fastapi eager-import hygiene · 1C spec-drift triage (harden sweep-redteam
  allowlist) · 1D #1779 governance-egress `/analyze`-only. 1A/1B/1C file-disjoint
  → true parallel (cap 3); 1D is analysis-only.
- **WAVE 2 (provider surface, serial, after W1):** 2A #1727 max_completion_tokens
  (VERIFY-then-fix — may be no-op) · 2B #1779 impl (per 1D design).
- **WAVE 3 (soak-gated, own cycle):** #1720 Wave C legacy-providers delete —
  subsumes the ollama test cleanup. Only after 2.34.1 shims soak.

## Outstanding (in priority order)

| ID | Item | Status |
| -- | ---- | ------ |
| R1-R3 | **PR #1797 merged + kaizen 2.34.1 released** | **DONE ✓** — merged (main `920003b3c`), tag `kaizen-v2.34.1`, publish-pypi run 29572288787 SUCCESS. Clean-venv `pip install kailash-kaizen==2.34.1` + import verified; fix surface (redis/webhook mask, provider sanitizer, 0 raw exc_info in shipped providers/llm) confirmed in the wheel. Sibling-drift sweep clean (all pkgs main==pypi). |
| R4 | **Cross-SDK issue** — Rust SDK creds-in-logs sibling | **DONE ✓** — user approved; filed `esperie-enterprise/kailash-rs#1908` (2026-07-17). Grant receipt: `cross-repo-authorization-1720-creds-in-logs.md` (all 5 conditions, journaled before acting). Advisory MUST-NOT-1 tripwire fired post-hoc, adjudicated in-scope (same as byok rs#1881). |
| PRE-EXISTING NOTE | `fastapi` eager-import in `kaizen/monitoring/__init__` → `dashboard` | Pre-existing (2.34.0 had it); `import kaizen.monitoring` fails on clean venv w/o fastapi. NOT from this PR. Candidate for a `deployment.md` eager-transitive-dep follow-up (monitoring optional-extra vs core dep). |
| F1 | **Wave C** — partial delete of legacy `providers/llm/` | SOAK-DEFERRED (unchanged; 2.34.0 released today, zero soak). |
| PRE | **8 pre-existing test failures** (ollama vision-setup, example-gallery docs) | Confirmed failing on clean HEAD; outside CI gate; unrelated to this PR. Separate shard. |

## Traps

- kaizen tests: `cd packages/kailash-kaizen && ../../.venv/bin/python -m pytest ... -k "not mcp and not discover"`.
- pre-commit shim has a stale interpreter path — invoke via `.venv/bin/python -m pre_commit run --files <files>`.
- pyright false-positive storm for kaizen.* (possibly-unbound, dict-vs-str, Node.execute override) — pytest authoritative.
- framework-first hook emits FALSE "raw SQL detected" on `llm_agent.py` edits — no SQL.
- Release branch carrying CODE must NOT use `release/*` (auto-skips PR-gate matrix) — this PR is on `fix/`.
- CI-check + merge are SEPARATE steps pinned to head SHA (git.md).
