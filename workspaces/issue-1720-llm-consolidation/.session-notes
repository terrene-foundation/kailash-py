# Session Notes — 2026-07-17

## Where we are

#1720 (retire legacy `providers/llm/` → four-axis `LlmClient`). Waves 1+2+A, **B1
(consumer cutover) and B2 (delete-gate prep) all MERGED to main**. Live LLM/embed
paths fully consolidated (except the `azure_ai_foundry` scope-out). Phase 05-codify.
Wave C (DELETE) is a HELD irreversible human gate — user approved the hold this session.

## Read first

1. `02-plans/02-wave-b-c-todos-plan.md` — Wave-B1/B2 DELIVERED sections + refined Wave-C scope + hold conditions
2. `packages/kailash-kaizen/src/kaizen/nodes/ai/llm_agent.py::_legacy_provider_chat` — the SOLE azure_ai_foundry live residual for Wave C
3. `packages/kailash-kaizen/tests/regression/test_issue_1720_b2_metrics_registry_decoupled.py` — B2 runtime-decoupling invariant (Wave-C delete-safety)

## In-flight state

- Nothing uncommitted; all session work merged (PRs #1789–#1792).

## Executed this session

- None — no external actions this session (all PRs are in this repo's `git log`).

## Outstanding ledger (forest)

| ID  | Item | Value-anchor (MUST-1 source) | Status |
| --- | ---- | ---------------------------- | ------ |
| F1 | Wave C — DELETE `providers/llm/` + `registry.py` + `base.py` + barrels (KEEP azure_ai_foundry); sweep ~35 legacy test files; re-base LLMProvider subclassers (folded B2c) | #1720 program goal (terminal delete); user-approved Wave-B/C plan | BLOCKED on user go-ahead + F2 |
| F2 | kaizen PyPI release carrying Waves 1–2/A/B1/B2 | zero-tolerance 6a — B2 deprecation shims MUST ship ≥1 minor before Wave C; prior "release deferred to Wave-3" user call | queued (prereq for F1) |
| F3 | azure_ai_foundry four-axis wire (Decision-2B) + extend CRLF/non-ASCII api_key guard to its legacy `.chat()` | plan Decision-2 (2A ratified; 2B optional → makes Wave C a full delete) | queued (optional) |
| F4 | PR #1782 (loom Gate-2 governance-artifact sync) | rule-corpus sync; explicitly left to user | in-flight, user's call |

Closed this session: `F-waveB1` → PR #1789; `F-waveB2` → PR #1791; wave-close docs → #1790, #1792.

## Unreleased packages

- kailash-kaizen — Waves 1–2 + A + B1 + B2 merged, NOT on PyPI. A release is the F1 prerequisite; recommend `/release` before Wave C.

## Traps

- kaizen tests: `cd packages/kailash-kaizen && ../../.venv/bin/python -m pytest ... -k "not mcp and not discover"`.
- Worktree editable-install: shared `.venv` resolves `kaizen` to MAIN checkout; use `PYTHONPATH=<wt>/packages/kailash-kaizen/src` or verify post-merge from main.
- pyright false-positive storm for `kaizen.llm.*`/`kaizen.providers.*` + merge-pending import noise — pytest is authoritative.
- CI duplicate `pull_request` runs saturate self-hosted runners → Python 3.11/3.12 queue latency; check-then-merge as SEPARATE steps pinned to the head SHA.
