# Session Notes — 2026-07-18

## Where we are

**#1779 governance_required posture: SHIPPED, RELEASED, CODIFIED, CLOSED.**
kailash 2.54.0 / kailash-kaizen 2.35.0 / kaizen-agents 0.10.0 are live on PyPI
(TestPyPI-validated in dependency order, then prod-published, full-chain
clean-venv verified). Docs landed (`docs/core/trust.rst` § "Governance-Required
Posture"). Issue #1779 closed with release refs. Repo is **clean & fully
released — zero version drift (all 9 packages main == PyPI)**. Working tree
clean. Workspace issue-1720-llm-consolidation, phase 05-codify.

**Fork RESOLVED = DELETE** (owner-approved): retire legacy `providers/llm` onto
four-axis (#1720 Wave C), which subsumes #1803. Wave 1 landed; **Wave 2 (the
delete) is scoped + deferred to a FRESH session** per owner (irreversible, 2-wave
split, 2 releases — too large for this session's tail). Cross-SDK rs#1945 filed.

Next session: START the Wave 2 handoff section below.

## Read first

1. `01-analysis/1720-wave2-delete-scoping.md` — **the Wave 2 /analyze; START HERE.** Consolidation already done → this is the ~90% surgical DELETE; azure_ai_foundry kept; deprecation gate resolved (no hard break); missed consumer `kaizen-agents/streaming_agent.py`.
2. `journal/0003-DISCOVERY-release-hazards-1779.md` — 3 reusable release hazards (pin-floor / CI-order / TestPyPI).
3. `journal/0004-DECISION-codify-1779-docs.md` — #1779 codify record + violations triage.
4. `docs/core/trust.rst` § "Governance-Required Posture" — the shipped #1779 posture + its non-coverage boundary (what #1803 covered; now subsumed by the delete).

## In-flight state

- **NONE.** All session PRs merged (#1804 pins+CI, #1805 fail-closed test, #1807
  codify docs, #1808 gitignore). Working tree clean. No uncommitted work.
- Unreleased packages: **NONE** — all 9 main == PyPI (sweep-verified this session).

## Executed this session

- **Released** kailash 2.54.0 + kailash-kaizen 2.35.0 + kaizen-agents 0.10.0 to
  PyPI (TestPyPI-first per user; dependency-order; clean-venv chain verified).
- **PR #1804** — tightened dependency pins (fail-closed-guard hazard) + fixed the
  kaizen CI local-editable install ordering it exposed.
- **PR #1805** — fail-closed posture-read-branch test (security-reviewer gap).
- **PR #1807** — /codify docs: trust.rst posture section, CHANGELOG → [2.54.0]
  dated, stale doc version badges 0.12.5 → 2.54.0.
- **PR #1808** — gitignore kaizen-agents task/session/memory test-scratch.
- **Filed** rs#1932 (max_completion_tokens defect) + rs#1933 (posture parity) on
  the Rust SDK repo (fresh-confirmed cross-repo grant; journal/0002).
- **Closed** #1779 with release refs.
- **F-FORK resolved = DELETE** (owner-approved) → #1720 Wave C; subsumes #1803.
- **Wave 1B** — PR #1782 loom Gate-2 sync merged (safety-verified: no roster/settings/coordination-enable changes; brought in newer rule corpus incl. wave-loop/product-completion-first).
- **Wave 1C** — PR #1810 FRECON: 4 stale workspaces → `workspaces/_archive/`.
- **Wave 2 /analyze** — PR #1811 persisted `01-analysis/1720-wave2-delete-scoping.md`.
- **Filed** rs#1945 (cross-SDK #1720 legacy-provider retirement + BYOK parity; grant journal/0005).
- **Wave 1A** (tests→tmp_path) — agent hit a session limit mid-investigation; DEFERRED as INCREMENTAL (real fix is src-level discovered-root redirect, captured in `04-validate/f-testhyg-followup.md`; orphaned worktree cleaned; `.gitignore` #1808 mitigates the symptom).

## Outstanding ledger (forest) — value-ranked

| ID | Item | Value-anchor (MUST-1) | Status |
| --- | --- | --- | --- |
| **F1720C** | **#1720 Wave 2 — DELETE legacy `providers/llm` (subsumes #1803)** | #1720 tracker (terminal goal) | **SCOPED (01-analysis/1720-wave2-delete-scoping.md); DEFERRED to fresh session — see Wave 2 handoff below** |
| F1803 | gate legacy/multimodal/azure provider layer | #1779 follow-up | **SUBSUMED by F1720C delete** (deleted layer needs no gate) |
| F1782 | loom Gate-2 rule sync | sync-artifact policy | **DONE** — merged (safety-verified) |
| F-TESTHYG | kaizen-agents test-scratch root writes | test-hygiene | **DEFERRED** (INCREMENTAL) — Wave 1A agent hit session limit; found the real fix is src-level (discovered-root writes, not just CWD); chdir-fixture partial captured. See `04-validate/f-testhyg-followup.md`; `.gitignore` #1808 mitigates. Revisit `after-milestone:1720-wave2` |
| FRECON | archive 4 stale workspaces | value-prioritization MUST-4 | **DONE** — PR #1810 |
| F-LOOMCANON | Canonicalize release-hazards #1 (dependencies.md pin-floor) + #3 (deployment.md interdependent-minors TestPyPI) | journal/0003; loom rule fleet | OPEN — loom /codify (not this repo) |
| FVERT | Vertex-Claude/Gemini LIVE validation | #1717 headline path | BLOCKED — GCP creds not in .env |
| rs#1932 / rs#1933 / rs#1945 | Rust cross-SDK (max_completion_tokens / posture parity / #1720 legacy-provider+BYOK) | cross-sdk-inspection | filed; Rust-side, tracked |

Closed this session: **F1779** (shipped+released+closed), **F1782**, **FRECON**, **F-FORK** (=delete).

## Wave 2 handoff (fresh session — START HERE)

Read `01-analysis/1720-wave2-delete-scoping.md` first. Decisions already resolved:
- **D1 deprecation:** RESOLVED — shims shipped kaizen 2.34.0, live ≥1 minor → delete lands next kaizen minor, NO hard break.
- **D2 azure_ai_foundry:** KEEP as legacy (no four-axis wire) — ~90% delete, not 100%.
- **D3 cross-SDK:** rs#1945 filed.

Then: `/todos` (human gate) writing the **Wave 2a** (serial — 7 provider deletes converge on registry.py + provider_names.py `assert`) + **Wave 2b** (≤3 parallel: 2b-A barrel-shim removal, 2b-B embedding/azure-unified delete, 2b-C test sweep + `kaizen-agents/streaming_agent.py` cutover) shard plan. Then `/implement` → holistic `/redteam` → **two releases** (kailash-kaizen + kaizen-agents). Verify-at-todos: OpenAI vision content-block transform + Google finish_reason map (before deleting openai.py/google.py). Per build-repo-release-discipline: done = released, both packages.

## Parallelizable wave plan

**F-FORK decision (human — blocks Wave 2 ONLY).** Recommend **DELETE** — retire
the legacy `providers/llm` layer onto the four-axis client (#1720 Wave C).
Rationale: the legacy layer is redundant with the four-axis client (optimal
architecture, no shims); deleting it **closes #1803's non-coverage boundary
entirely** (nothing to gate once gone) rather than maintaining a second
enforcement surface. Con: public-API removal may need a deprecation cycle
(zero-tolerance 6a) — the /analyze scopes that. If instead you want the legacy
layer KEPT, do #1803 (gate it) and drop #1720 Wave C.

**Wave 1 — parallel NOW, no decision needed (disjoint file trees → no shared invariants):**
- **1A · F-TESTHYG** — convert kaizen-agents CWD-scratch tests to `tmp_path`
  (touches `packages/kaizen-agents/tests/**` only). Small.
- **1B · F1782** — review + merge (or hold) the loom Gate-2 rule sync (touches
  `.claude/**` only). Big review; land it BEFORE Wave 2 so implementation
  doesn't drift from freshly-synced rules.
- **1C · FRECON** — reconcile + archive the 4 stale workspaces (touches
  `workspaces/**` only). User-gated prune.
- 1A ∥ 1B ∥ 1C are fully independent (tests/ vs .claude/ vs workspaces/) → one
  parallel wave. Per wave-loop.md: Wave 1 invariant-surface is tiny (hygiene +
  artifact-sync), well under the bound-B ceiling.

**Wave 2 — provider-layer consolidation (post-F-FORK), internally sharded:**
- /analyze #1720 Wave C: enumerate legacy `providers/llm` public surface +
  consumers + deprecation-cycle needs.
- Sharded /implement across ≤3 worktrees (worktree-isolation R4 cold-start cap)
  by provider family: {openai, anthropic, google} ∥ {docker, perplexity} ∥
  {multimodal/document, azure backends} — each shard = migrate onto four-axis +
  remove legacy. Holistic /redteam across merged shards → /release.
- Subsumes F1803 (a deleted layer needs no gate). Wave-loop bound-B: the
  provider migrations share the four-axis-contract invariant set — if the
  shard-union exceeds ~10 invariants without a live harness, split at the
  provider boundary into Wave 2a/2b.

**Wave 3 — async at loom (not this repo):** F-LOOMCANON — promote release-hazard
#1 + #3 into fleet rules via loom /codify.

Overlap: Wave 1 can run concurrently with the F-FORK /analyze; Wave 2 /implement
starts after F-FORK decided AND ideally after 1B (rule sync) lands.

## Traps

- kaizen tests PER-PACKAGE (`cd packages/<pkg> && ../../.venv/bin/python -m pytest`); combined kaizen-agents+kailash-kaizen → `ImportPathMismatchError` (both ship `tests/conftest.py`) — rootdir artifact, not a defect.
- **CI install ordering:** local editable `kailash-kaizen` MUST install BEFORE `kaizen-agents` (else its `>=2.35.0` pin resolves from PyPI). Fixed in test-kailash-kaizen.yml this session.
- **TestPyPI for interdependent packages:** dispatch in dependency order + `--index-strategy unsafe-best-match` (deps live on prod PyPI, pkg-under-test on TestPyPI); capture uv's REAL exit code (a `| tail` pipe masks it).
- PyPI `info.version` lags minutes after publish; the `/pypi/<pkg>/<ver>/json` endpoint + clean install are ground truth (build-repo-release-discipline R2).
- Autonomy examples MUST compile on Python **3.11**. `Validate Autonomy Examples` job.
- pyright false-positive storm on `kaizen.*` (stale installed wheel) — editable `.venv` pytest is authoritative.
- `Test (Python 3.11)/(3.12)` core jobs run SLOW on the self-hosted queue (~15-18 min); earlier matrix jobs finish ~4-5 min. Not a hang. A .gitignore/docs-only PR with all other checks green MAY admin-merge (matrix is inert to it).
- CI shows a stale CANCELLED "Build Documentation"/build duplicate on most PRs (concurrency cancel-in-progress) — pin the head SHA's run; the current-head run is the real result.

## Open questions for the human

1. **F-FORK:** delete the legacy provider layer (#1720 Wave C, recommended — subsumes #1803) OR gate it (#1803)? Only blocker for Wave 2.
2. **F1782:** merge or hold the loom Gate-2 rule sync (152 files)?
3. **FRECON:** approve archiving the 4 stale workspaces?
