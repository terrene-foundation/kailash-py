# Session Notes — 2026-08-10 (session N: forest merged, security triage, #1995 closed out)

## Where we are

**PR #2016 merged** (admin, 341 commits) → main is `25973edb5`. That auto-closed
#1970/#1971/#1972/#1974/#1981, and #2005/#2008/#2007 were already closed or closed here.
**9 of the original 22 outstanding issues are now closed.**

Four NEW security issues were filed that **outrank everything on the original list**, plus
**PR #2028** (open) closing out #1995.

Workspace issue-1720-llm-consolidation, phase 05-codify.

## Read first

1. **The four new security issues — #2024, #2025, #2026, #2027.** Two are CRITICAL. All were
   verified against source by the orchestrator, not taken from a subagent report.
2. **PR #2028** — #1995 fix. Green and idempotent; needs merge.
3. The **"specs were wrong" section** below. This is the load-bearing finding of the session.

## In-flight state

**PR #2028 (open, chore/1995-isort-normalization)** — 2 commits, `c29a1fbce` + `c40a09a87`.
Acceptance criterion VERIFIED: clean tree → `pre-commit run --all-files` exit 0 → 0 files
modified. Not yet merged; CI not yet checked. Working tree clean, branch pushed.

**Nothing else in flight.** No uncommitted work.

## The four new security issues (higher priority than the original 22)

- **#2024 CRITICAL** — `FileSecretBackend` (`src/kailash/gateway/security.py:189`) documents
  "encrypted file storage" and writes plaintext (`f.write(secret)` at :205). The module imports
  and uses Fernet — in a *different* class. Fake-encryption stub, `zero-tolerance.md` Rule 2.
- **#2025 CRITICAL** — `workflow_server.py:761-770` registers an UNAUTHENTICATED `{path:path}`
  catch-all for GET/POST/PUT/DELETE/PATCH that forwards to the proxied backend and returns the
  body. Only middleware in the file is CORS (:326); no `Depends` anywhere. `Authorization` is
  stripped on forward (:779) so the backend can't re-authorize either.
  **CodeQL framed this as SSRF — that framing is wrong.** Host is not attacker-controlled; no
  authority pivot is constructible. Fixing "SSRF" closes nothing. The defect is the missing auth gate.
- **#2026 HIGH** — `sso.py:924` returns a FORGED successful token response when
  `"oauth.example.com" in token_url` (substring, on the *exception* path, so an attacker wants
  it to fail). `mfa.py:31` logs the OTP body at INFO and returns True having sent nothing.
- **#2027 HIGH/MED** — 5 sites: MD5 over AWS secret material as a cache key
  (`resource_resolver.py:295`), workflow parameter VALUES logged
  (`parameter_injector.py:411`), raw pre-sanitization value (`dataflow/core/nodes.py:1034`),
  rate-limit identifier at WARN (`nexus rate_limit/middleware.py:184`), and a **world-readable
  trust audit log** (`trust/plane/integration/cursor/hook.py:139`, `0o644`).

**CodeQL backlog: 2,353 open alerts — 1 critical, 35 high, 57 medium.** Of the 36 high/critical,
**11 are real, 25 are false positives.** The 25 are NOT yet dismissed — deliberately. Dismissing a
security alert on an un-reverified analysis is the failure this session spent its effort avoiding.
Spot-verify the two generic logging sinks (#11249, #11244) first; being wrong there hides a real leak.

## THE finding: five issues had wrong SPECIFICATIONS, not wrong code

A diligent session implements each faithfully and closes it wrongly. Verify before implementing.

- **#2004** — its own prescribed fix (route through `mask_error_text`) **provably does not redact**.
  That helper handles URL userinfo and query params; the threat is a CLI flag `--token=`. No
  scrubber in the monorepo covers CLI-flag credentials. Also a third leak surface the issue misses:
  `SpawnSecurityError` puts the raw command in `.data`, which `to_dict()` sends over the wire.
  **Correct fix is at the raise site** (`security.py:60-65`), storing a basename+hash, not at the
  three call sites.
- **#2006** — blames the `else` branch; a `ts: datetime = None` model (its own repro) takes the
  `elif "default" in field_info` branch. Patching only `else` does not fix the reported case.
  Real sites: `nodes.py:1780-1788` + `1845-1869`, not the stale numbers in the issue.
- **#2015** — "auth path worst" is FALSE. 1 auth site vs **10 in `visualization/api.py` on
  unauthenticated routes leaking DSNs**. Plus a 26th site the issue's grep cannot find
  (`durable_workflow_server.py:480` uses an f-string spelling).
- **#1997** — the binary is wrong. Coverage is **preset**-dependent: apparent groq/cohere coverage
  is incidental (an AWS 40+ rule in `_OPAQUE_SHAPE_PATTERNS`, OFF on the conservative preset).
  **All four vendors leak unconditionally on the conservative preset**, where nothing tests.
- **#2022** — names the missing kwarg; the real defect is the **swallow** at `base_agent.py:743`
  turning a `ConfigurationError` into "the LLM emitted a malformed plan".

## Decisions A–D — status

- **A (#1997 fix shape) — the recommended option-3 was REFUTED.** xAI is **not a provider in this
  codebase at all** (0 mentions in kaizen src, no `_FROM_ENV_PROVIDERS` entry), so half of it was
  unactionable. `gsk_` has **0 in-repo literals** — cite Groq's published docs or don't pin it.
  Cohere is NOT an easier class than mistral. See the #1997 comment for the corrected shape.
- **B (#2022 boundary) — needs re-deciding.** There are **THREE** provider resolvers, not one:
  `_provider_env.detect_provider_from_env` (env-keyed, what #1952 hardened),
  `_env_model.detect_provider` (model-keyed, what the fix needs), `providers.auto_detect_provider`.
  Publishing one of three without declaring which is canonical recreates the drift #1952 ended.
- **C (#2023 per-step)** — untouched. Note the issue says 5 steps; **there are 8**. Fix the title.
- **D (CodeQL #11400) — DONE.** Dismissed as false positive with evidence.

## Traps

- **A green subagent report is not a green run.** L8 reported "all 21 hooks Passed"; the verifying
  run exited 1 on two hooks. Always re-run the gate yourself.
- **`pre-commit run --all-files` and the commit-time hook can disagree.** Root cause was five
  competing isort configs (fixed in #2028). If it recurs, compare `known_first_party` across
  root + the three per-package `[tool.isort]` blocks.
- **`git status --porcelain` counts staged AND unstaged** — `'^ M\|^.M'` matches both; it will
  mislead you about what a hook just modified.
- **Bash working directory PERSISTS between calls.** A stray `cd packages/...` silently made an
  `isort --show-config` reading come from the wrong package; it produced a confident wrong
  conclusion that had to be retracted. Re-anchor with an absolute `cd` when measuring config.
- **`#2011` fires during pre-commit itself** — the Tier-1 hook reports "files were modified"
  because `packages/kailash-kaizen/scripts/test_examples_implementation.py` writes
  `kaizen_implementation_test.log` into the repo root at import. L6 identified the fix: RENAME the
  file (the `test_` prefix IS the bug — it collects zero tests) + move `logging.basicConfig` into
  `__main__`. Sibling `comprehensive_implementation_tester.py` has the same latent shape.
- **#2029** — `test_concurrent_execution_performance` is a load-sensitive flake in the Tier-1
  pre-commit hook (asserts 3x on a ~30ms workload). It will block commits on a busy machine.
  Do NOT widen the threshold.
- `.env` `OPENAI_API_KEY` is INVALID (live 401). Anthropic works.
- Tests: ALWAYS `.venv/bin/python -m pytest`. An errored run is zero evidence, not a failure.

## Next session

1. **Merge PR #2028** (check CI first, pinned to head SHA).
2. **#2025 and #2024** — the two criticals, ahead of the remaining correctness bugs. #2025 needs
   BOTH a correctness reviewer AND an adversarial security-reviewer (`agents.md`).
3. **#2013** — verified inert (`set_auth_manager` has ZERO definitions repo-wide). Treat as
   security-adjacent, not a correctness bug: `enable_auth=True` users believe they're authenticated.
   `enable_monitoring` at `core.py:4826` has the IDENTICAL dead-`hasattr` shape — fix both.
4. Then #2014 (reproduced; isolation dead on macOS/Windows under `spawn`), #2006, #2004, #2011,
   #2010 (a 3-line deletion), #2002, #2023.

## Outstanding ledger — remaining open from the original 22

#1995 (PR open) · #1997 · #2000 · #2002 · #2004 · #2006 · #2010 · #2011 · #2012 · #2013 · #2014 ·
#2015 · #2017–#2021 · #2022 · #2023 — plus new: #2024 #2025 #2026 #2027 #2029.
