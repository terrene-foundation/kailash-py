# Session Notes — 2026-07-20 (Wave-B/3 EATP + Wave-A, RELEASED)

## Where we are

Wave-B (EATP cross-SDK) + the in-repo Wave-3 items are COMPLETE + RELEASED to PyPI.
main @ origin/main (post release-prep #1875 merge = 3701b45d7 + 3 tags). 0 open PRs from this session.

**Released to PyPI (verified HTTP 200 + clean-venv import):**
- kailash 2.58.0 (v2.58.0) — EATP delegation-signing engine (#1841 S1/S2) + signed revocation ledger/HeadCommitment/verify (#1842 S1/S2/S3)
- kailash-pact 0.16.0 (pact-v0.16.0) — MCP tenant isolation (#1843); + 0.16.1 (pact-v0.16.1) follow-up: export McpCallerIdentity/McpTenantGrant/McpResourceContext at pact top level (#1876, reviewer-caught API-surface gap)
- kailash-kaizen 2.38.0 (kaizen-v2.38.0) — governance-gate over remaining provider egress (#1803)

## Read first
1. `workspaces/issue-1720-llm-consolidation/wave-ledger-2.md` — full per-shard ledger (8 feature PRs + release, redteam rounds incl. the CRITICAL #1842-S3 catch, all findings + dispositions).
2. `CLAUDE.md` — project rules.

## Issues closed this session
#1842 (EATP revocation: ledger + HeadCommitment + verify-consults-signed-ledger, security-hardened) · #1843 (MCP tenant isolation) · #1803 (kaizen governance-gate).
Filed: #1869 (kaizen-providers.md spec split, DQ). Merged: #1865/#1866/#1867/#1868/#1870/#1871/#1872 (features) + #1873 (authz receipt) + #1875 (release-prep).

## In-flight state
Nothing uncommitted of substance. Local main FF'd to origin/main. Cross-repo READ authz receipt (kailash-rs) upstreamed via #1873. Untracked spend-artifact junk under packages/kaizen-agents/ (pre-existing) — leave.

## CARRY-FORWARD (value-anchored, fresh session)
| ID | Item | Value | Status |
| -- | ---- | ----- | ------ |
| #1841-S2b | Enrich DelegationRecord (structured constraints/resource_limits/scope/multi-sig/trust_level) → records SIGN v3-complete → **quorum-integrity value** | HIGH — the #1841 headline fix; engine+version-gate scaffolding ALREADY merged | OPEN, ~12+ inv byte-CHANGING data-model + cross-SDK lockstep |
| #1846 | dataflow Vector byte-lockstep | feature parity | BLOCKED on rs#1990 (unmerged; no authored reference vectors). Re-check when rs#1990 lands. |
| DQ7 | kaizen-providers.md split (>300 lines) | maintainability | tracked #1869 |
| DQ9 | non-ASCII posture harmonize #1841/#1842 engines (one fail-closes, other trusts RFC-8785) | consistency | deferred-quality |
| DQ10 | key-zeroization (del b64_priv) both sign sites (signed_ledger + head_commitment) | defense-in-depth | deferred-quality |
| DQ11 | #1841 rotation.py:546 try/except (unknown-version record aborts rotation batch; fails closed) | robustness | fold into #1841-S2b |

## Traps / lessons (CODIFY candidates)
- **correctness-review-clean ≠ security-clean**: RT-verify-corr passed #1842-S3 CLEAN; RT-verify-sec (adversarial) caught a CRITICAL revocation-resurrection bypass (single-file head delete → all revoked delegations silently un-revoked) + a HIGH monotonic-regression + the revocation_verifier=None fail-open. BOTH adversarial lenses non-negotiable on security-critical shards.
- **secure-default recurrence**: same fail-open-default shape hit #1843 (require_caller_identity=False) AND #1842-S3 (revocation_verifier=None). New security features should default fail-CLOSED (or loud-WARN when a required verifier is unwired).
- **CodeQL orphan check**: repo has BOTH a "CodeQL Security Scanning" WORKFLOW (real scan) AND an orphaned "CodeQL" default-setup CHECK that hangs `queued`. Verify the WORKFLOW=success on the head, then admin-merge over the orphaned check (it never completes).
- **duplicate-run race** (git.md): a concurrency-CANCELLED duplicate `build` run shows as "fail" in `gh pr checks` while the real run succeeds — verify the SUCCESS run is on the current head SHA before admin-merging.
- EATP byte-lockstep: rs LEADS + authored the vectors (committed on rs default branch even though rs#1795/#1849/#1763 tracking issues stay OPEN); NEVER reason bytes — pin the exact rs vectors behind a PROVISIONAL tripwire; re-pin in lockstep.

## Release facts (for next release)
- Tag convention: core `v<X.Y.Z>`, `pact-v<X.Y.Z>`, `kaizen-v<X.Y.Z>`. Tag-triggered OIDC "Publish to PyPI" workflow. release/* branch auto-skips the heavy PR-gate matrix.
- Dep-pins this release: NONE needed (sub-pkg kailash>= floors already satisfied; grep-verified no new-core-symbol usage in pact/kaizen).
- nexus/mcp: no changes since nexus-v2.14.0 / mcp-v0.4.2 — NOT released.

## Codify complete (2026-07-20)
- /codify DONE on branch `codify/jack-hong-2026-07-20`: TWO rule clauses landed with canonical-8-field clause-scoped Trust Posture Wiring —
  - `security.md` § "Secure-Default For A New Security Feature — Fail-Closed Or Loud-WARN, Never A Silent No-Op" (recurred #1843 + #1842-S3).
  - `agents.md` § "MUST: Correctness-Review-Clean Is Not Security-Clean" (evidence #1842-S3).
- Self-referential Rule-1 gate (agents.md allowlisted): 3-lens PARALLEL redteam (CODIFY-rev/sec/arch) converged NO-BLOCKER; two non-blocking tightenings applied.
- posture.json `pending_verification = [security, agents]`; anchor advanced → 2026-07-20T07:41:49Z; BUILD→loom proposal appended (pending_review, Rule-10 headroom = loom Gate-1 action).
- Journal: `journal/0007-DECISION` + `journal/0008-DISCOVERY`. CodeQL-orphan/duplicate-run lessons remain captured in this notes file (not rule-worthy — CI-environment facts, not SDK/COC contracts).

## Open questions for the human
- Proceed with #1841-S2b (quorum-integrity data-model enrichment) next session? It's the last piece of the #1841 headline value.
