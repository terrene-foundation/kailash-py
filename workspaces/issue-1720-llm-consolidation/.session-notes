# Session Notes — 2026-07-17

## Where we are

Wave 1 code-health CONVERGED → merged (#1799) → kaizen 2.34.2 released + verified →
post-Wave-1 `/sweep` done. Repo is clean & fully released. Next: **`/autonomize`
Wave 2 (#1779 + #1727 in PARALLEL) → `/redteam` to convergence**, then Wave 3
(#1720, soak-gated). Workspace issue-1720-llm-consolidation, phase 05-codify.

## Read first

1. `workspaces/issue-1720-llm-consolidation/04-validate/sweep-2026-07-17-post-wave1.md` — live forest + ranked next-session items. START HERE.
2. `workspaces/issue-1720-llm-consolidation/01-analysis/1779-governance-egress-analysis.md` — #1779 impl design (gate at construction, NOT openai.py; parallel-safe w/ #1727).
3. `tools/sweep-redteam.py` — drift tool, hardened this session (CapWords-tail contract carve-out).

## In-flight state

- PR #1801 (the `/sweep` report) may still be merging — verify it landed; admin-merge if open.
- Unreleased packages: **NONE** — all 8 main==PyPI (sweep Sweep-8 verified).

## Executed this session

- Released **kaizen 2.34.2** to PyPI (tag `kaizen-v2.34.2`, publish run SUCCESS, clean-venv bare-install import verified). External to this repo's git log.

## Outstanding ledger (forest)

| ID | Item | Value-anchor (MUST-1) | Status |
| --- | --- | --- | --- |
| F1779 | #1779 governance_required posture | #1779 user-filed; EATP D6 parity | Wave 2 — design done, ready |
| F1727 | #1727 max_completion_tokens (four-axis openai) | #1727 user-filed | Wave 2 — VERIFY-FIRST (may be no-op), PARALLEL w/ F1779 |
| F1720C | #1720 Wave C legacy `providers/llm` prune | #1720 tracker (session terminal goal) | SOAK-GATED (2.34.2 just shipped) |
| FVERT | Vertex-Claude/Gemini LIVE validation | #1717 headline path | BLOCKED — GCP creds not in `.env` |
| F1782 | PR #1782 loom Gate-2 rule sync | sync-artifact policy (user gate) | user merge/hold decision |
| FRECON | Reconcile+archive 2 stale workspaces | sweep F5; value-prioritization MUST-4 | queued — user-gated prune of 5 verified-CLOSED-issue rows (issue-1717-vertex-claude, sdk-backlog) |

Closed this session: `WAVE1` → PR #1799 + tag `kaizen-v2.34.2`.

## Traps

- kaizen tests PER-PACKAGE (`cd packages/<pkg> && ../../.venv/bin/python -m pytest`); combined kaizen-agents+kailash-kaizen pytest → `ImportPathMismatchError` (both ship `tests/conftest.py`) — rootdir artifact, not a defect.
- Autonomy examples MUST compile on Python **3.11** (no f-string backslash; 3.12+ only). `Validate Autonomy Examples` job.
- pyright false-positive storm on `kaizen.*` (resolves stale installed wheel) — pytest via editable `.venv` is authoritative.
- pre-commit shim stale → `.venv/bin/python -m pre_commit run --files <files>`; isort/ruff/black auto-fix reports "Failed" on modify → re-stage to idempotency. `# type` comment fragment trips `python-use-type-annotations` → reword.
- uv index-cache lag on fresh PyPI publish → pip `--no-cache-dir` fallback (build-repo-release-discipline R2).
- **Wave 2 #1779 gate MUST live at construction/selection (`get_provider`/LlmClient/Agent), NEVER inside `providers/llm/openai.py` `chat()`** — else it collides with #1727 (2A) and loses the parallel guarantee.
- Worktree wave cap ~3 cold-start (worktree-isolation R4); each shard carries curated governance slices (governed-throughput).

## Open questions for the human

- Merge or hold PR #1782 (loom Gate-2 rule sync)?
