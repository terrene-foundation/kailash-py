# Session Notes — 2026-07-17 (foundation redteam converged + Wave-B plan drafted)

## Where we are

#1720 (retire legacy `providers/llm/` → four-axis `LlmClient`). **Waves 1 + 2 + A
merged.** This session: a **holistic redteam of the merged foundation** (before
drafting Wave-B) found + fixed **3 defects** in already-shipped code, converged
(reviewer + security-reviewer; round-1 found an F1 HIGH, round-2 CLEAN), and
shipped as **PR #1784 (merged, main @ `923709a83`)**. Also drafted the **Wave-B/C
`/todos` plan** — awaiting the human structural gate.

**User APPROVED the Wave-B/C plan (2026-07-17)** with both recommended decisions —
Google `finish_reason` full value-map parity, and `azure_ai_foundry` scoped OUT
(partial Wave-C). **The fresh session starts Wave B1** (see `02-plans/02-wave-b-c-todos-plan.md`).
The cross-SDK BYOK issue was filed (rs#1881). PR #1782 left to the user.

## Read first

1. `02-plans/02-wave-b-c-todos-plan.md` — the Wave-B/C plan + 2 decisions (THIS is the gate)
2. `02-plans/01-wave3-refined-decomposition.md` — the original refined wave plan
3. `packages/kailash-kaizen/tests/regression/test_issue_1720_wave_a_foundation_redteam.py` — the 17 redteam regression tests
4. `04-validate/wave-a-parity-findings.md` — earlier parity gate evidence

## Shipped this session (PR #1784)

Foundation-redteam fixes (merged code the Wave-B cutover would inherit):
- **F1** — `legacy_tool_choice_default` made stream-aware (kept for Wave-B). Redteam
  HIGH: threading it into the dual-run **shadow** was wrong (the node always calls
  legacy `.chat()`, never `stream_chat`, so the shadow must reproduce `.chat()`'s
  "required") — shadow threading reverted; helper retained.
- **F2** — `EmbedOptions.normalize` now reaches the HF embed wire via `client.embed`
  (was a silent no-op, zero-tolerance 3c). Uniform-normalize on ollama/cohere/openai
  DEFERRED → tracked in the plan as B1b.
- **BYOK** — `resolve_deployment_for` now validates a caller-supplied `api_key`
  (CRLF/control-char/non-ASCII) at enforcement-surface parity with `complete()`.

## Also shipped (non-gated Wave-B prep, PR #1786 merged)

- **Parity coverage** — plane-B response-parse parity extended to the OpenAI-compat
  wires **docker + perplexity** (both byte-identical to openai on shared canned
  bytes). Closes the "extension pending" gap from `04-validate/wave-a-parity-findings.md`.
  `drive_legacy_openai_family` gained an optional `api_key` passthrough (offline
  Perplexity credential check).
- **Cross-SDK BYOK issue FILED** — esperie-enterprise/kailash-rs#1881 (`cross-sdk`),
  filed 2026-07-17 under the User-Authorized Exception (grant receipt:
  `cross-repo-authorization-1720-byok.md`). Requests the Rust four-axis client
  validate per-request BYOK keys for header-injection at parity across both BYOK
  entry points. `cross-sdk-byok-draft-UNFILED.md` is the scrubbed source body.

## NEXT SESSION — start here (Wave B1, APPROVED)

The user approved the Wave-B/C plan. Begin Wave B1 (consumer cutover) per
`02-plans/02-wave-b-c-todos-plan.md`, with the two ratified decisions baked in:
- **Google `finish_reason`**: apply the FULL legacy value-map in the four-axis
  google wire (STOP→stop, MAX_TOKENS→length, SAFETY→content_filter, tool→tool_calls) —
  NOT a bare lowercase. Folded into B1a.
- **`azure_ai_foundry`**: SCOPED OUT of the cutover. Keep legacy
  `AzureAIFoundryProvider` alive; Wave-C is a PARTIAL delete (everything EXCEPT the
  azure_ai_foundry path + its registry entry). No four-axis Foundry wire this program.

Wave B1 shards (parallel, behind the parity harness): B1a `LLMAgentNode._provider_llm_response`
(promote shadow to primary + google value-map) ‖ B1b `EmbeddingGeneratorNode._generate_provider_embedding`
(+ uniform-normalize F2-MEDIUM + timeout kwarg) ‖ B1c `BaseAgent._simple_execute_async` (openai-only).
Run the wave-loop inter-wave gate (redteam→converge→feed-forward) before Wave B2 (delete-gate prep) / Wave C.
Wave C (DELETE) stays a separate human gate (irreversible).

## STILL OPEN — user's call

- **PR #1782** (loom Gate-2 governance-artifact sync) — open, green, mergeable.
  Left to the user (rewrites the rule corpus). Merge at your discretion.

## Unreleased

- **kailash-kaizen**: Waves 1–2 + A + this foundation-fix set merged, NOT released to
  PyPI. `/release` DEFERRED to the Wave-3 milestone (prior user call).

## Traps

- kaizen tests: `cd packages/kailash-kaizen && ../../.venv/bin/python -m pytest ...`.
- `iterative_llm_agent` MCP-discovery tests HANG offline (need a live MCP server) —
  exclude with `-k "not mcp and not discover"`; unrelated to LLM-consolidation.
- pyright shows a false-positive storm for `kaizen.llm.*` imports (stale index) —
  runtime + pytest are authoritative.
- `cd` persists across Bash calls in this harness; use absolute paths or re-`cd`.
