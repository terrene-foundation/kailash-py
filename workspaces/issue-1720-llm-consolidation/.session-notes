# Session Notes — 2026-07-17 (Wave B1 DELIVERED + CONVERGED + MERGED)

## Where we are

#1720 (retire legacy `providers/llm/` → four-axis `LlmClient`). **Waves 1 + 2 + A
merged. Wave B1 (consumer cutover) MERGED this session** as **PR #1789**
(main @ `7ba9caf1f`). Three parallel-worktree shards (B1a/B1b/B1c), holistic
redteam CONVERGED (2 consecutive clean rounds), post-merge re-verified from main
(72 regression+parity green).

## Shipped this session — Wave B1 (PR #1789)

- **B1a** — `LLMAgentNode._provider_llm_response` promoted the four-axis shadow to
  the PRIMARY live path (`resolve_deployment_for`→`complete`→`to_legacy_shape`);
  shadow dispatch removed from the success path; folded Decision-1A (full legacy
  google `finish_reason` value-map into the four-axis google wire). `azure_ai_foundry`
  scoped OUT (routes legacy `_legacy_provider_chat`).
- **B1b** — `EmbeddingGeneratorNode._generate_provider_embedding` → `LlmClient.embed`;
  added `timeout` kwarg; `EmbedOptions.normalize` now UNIFORM client-side (removed
  HF-only copy). ollama/docker embed stays legacy fallback (needs node `base_url`/`api_key`).
- **B1c** — `BaseAgent._simple_execute_async` → four-axis (openai-only).
- **Redteam fixes (`d87ecf799`)** — HIGH: embed silently returned a MOCK on an
  unresolvable credential provider → restored legacy loud raise (sanctioned mock
  path stays `provider=="mock"` only). 2× MED: sanitize-provider-error parity on
  embed + base_agent paths. LOW: google `_map_finish_reason(None)`→"stop"; 3 sibling
  embed-shaper docstrings corrected.

## NEXT SESSION — start here (Wave B2, then surface Wave C)

Per `02-plans/02-wave-b-c-todos-plan.md` (updated with the B1-delivered status +
precise residual list). Wave B2 = delete-gate prep, three shards:
- **B2a** — `nodes/ai/__init__` + `providers/__init__` barrels → DeprecationWarning
  shims (≥1 minor cycle) + CHANGELOG migration entry (zero-tolerance 6a). No hard drop.
- **B2b** — extract the provider-name registry (`PROVIDERS` + `_MODEL_PREFIX_MAP`)
  off `providers/registry.py` so `production/metrics.py` no longer imports it.
  (Coupling is LESS than the census implies — only metrics.py + barrels + azure
  fallback import the registry; `llm/deployment_resolver.py` + `llm/client.py` do NOT.)
- **B2c** — re-base `providers/base.py::LLMProvider` subclassers
  (`unified_azure_provider`, cohere/hf embedding providers) before the base is deleted.
Run the inter-wave gate (redteam→converge) after B2, THEN surface Wave C.

**Wave C (DELETE) is a HUMAN structural gate — IRREVERSIBLE.** Residual after B2:
azure_ai_foundry chat + ollama/docker embed fallback (both documented partial-delete
residuals) + 35 test files to sweep (orphan-detection Rule 4). Do NOT execute Wave C
without explicit user confirmation.

## Non-blocking follow-ups (surfaced by B1 redteam, NOT introduced by B1)

- Legacy `azure_ai_foundry` `.chat()` lacks the CRLF/non-ASCII api_key guard the
  four-axis path now has (enforcement-parity asymmetry, one legacy provider). Fold
  into the Foundry four-axis-wire capability item.
- `_run_async_in_sync_context` (unchanged since 2026-03-11) latent RuntimeError-remap
  edge; B1a grows its risk surface but unreachable (LLM taxonomy is Exception-based).
- Pre-existing `structured_output_mode='auto'` FutureWarning (`async_single_shot.py`)
  — separate structured-output deprecation workstream.

## STILL OPEN — user's call

- **PR #1782** (loom Gate-2 governance-artifact sync) — open, left to the user.

## Unreleased

- **kailash-kaizen**: Waves 1–2 + A + B1 merged, NOT released to PyPI. `/release`
  DEFERRED to the Wave-3 milestone (prior user call).

## Traps

- kaizen tests: `cd packages/kailash-kaizen && ../../.venv/bin/python -m pytest ...`.
- Exclude MCP-discovery hangs with `-k "not mcp and not discover"`.
- pyright false-positive storm for `kaizen.llm.*` + line drift — runtime + pytest authoritative.
- Worktree editable-install gotcha: the shared `.venv` resolves `kaizen` to the MAIN
  checkout; run worktree tests with `PYTHONPATH=<wt>/packages/kailash-kaizen/src` OR
  verify post-merge from main (worktree-isolation Rule 2a/3a).
