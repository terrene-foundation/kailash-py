# Orchestration Launch Ledger — cont-30

Baseline SHA: `8a53023367f9c1c103596726250b6af78de5e319` (origin/dev)
Worktree parent: `/Users/esperie/repos/kailash/build/.kailash-py-wt/`
Standing order: D0 — land into `dev` same session, then drain (reap wt, delete branch, re-census).
Priority (owner, this session): 1 drain+land backlog · 2 burn down aggressively · 3 keep the fleet refilled.

## Lanes

| track           | agent         | branch                               | scope (exclusive)                                         | status                                      |
| --------------- | ------------- | ------------------------------------ | --------------------------------------------------------- | ------------------------------------------- |
| 2226+2227       | L1-govproxy   | fix/2226-2227-govproxy               | `trust/pact/**` + pact/kaizen-agents (widened, see below) | **REFUTED → reworking HIGH-1** (see below)  |
| 2222            | L2-accessctl  | fix/2222-access-control              | `middleware/auth/access_control.py`, `access_control*`    | **LANDED** `59efc6f7b`, drained             |
| 2220            | L3-provider   | fix/2220-provider-routing            | `packages/kailash-kaizen/**`                              | **LANDED** `34f69fd95`, drained             |
| 2220-residual   | L8-provresid  | fix/2220-residual-provider-callsites | `packages/kailash-kaizen/**`                              | in-flight                                   |
| promote+release | orchestrator  | (dev→main, tags)                     | version anchors, CHANGELOG, tags                          | **DONE — both published**                   |
| 2107            | L4-finalizers | fix/2107-finalizer-deadlock          | `src/kailash/**` minus L1's paths                         | **LANDED** `070a447d8`, drained             |
| 2211+2075       | L5-poollife   | fix/2211-2075-pool-lifecycle         | `packages/kailash-dataflow/**`, `nodes/data/**`           | in-flight                                   |
| 2171+2056       | L6-nexus      | fix/2171-2056-nexus                  | `packages/kailash-nexus/**`                               | in-flight                                   |
| 2000            | L7-perfcode   | fix/2000-pythoncode-import-cost      | `src/kailash/nodes/code/**`                               | **R2 fixed `732d26c5` — re-review running** |

Scopes are **fully disjoint** — verified by file-collision analysis before each dispatch, not assumed.

## L1 (#2226/#2227) DONE but GATED — security-critical, so it does NOT land on the lane's say-so

L1's fix is thorough and it self-redteamed well (found + pinned a `FrozenMapping` base-class-call
residual in its OWN fix, confined to non-verdict mapping dims). But self-redteam is not an
independent lens, and this is a fail-closed AUTHORIZATION boundary — `agents.md`
§ Correctness-Review-Clean-Is-Not-Security-Clean applies. An adversarial security-reviewer ran
against `dce46e761` and **REFUTED it** — L1 is reworking, does NOT land.

**HIGH-1 (the refutation): the containment boundary is defeated by an allowlisted member of the
proxy it returns.** `_ProtectedInnerProxy._ALLOWED_ATTRS` includes `to_workflow_node`, and
`BaseAgent.to_workflow_node()` returns `self` — the raw agent. So
`governed.inner.to_workflow_node().run()` — and, through the `innermost` boundary L1's own #2227
fix added, `streaming.innermost.to_workflow_node().run()` — execute UNGOVERNED by plain reads + a
call. Same class as #2226 and the #2000 review: an allowlisted member hands back a live handle.
L1's own tests missed it — they inspect the forwarder's `__self__` but never CALL
`to_workflow_node()`. The #2227 "closed" claim is false; the walk stops at the proxy but the proxy
hands back the raw agent.

**Other findings sent back:** MEDIUM-2 `_inner` is a one-hop plain-attribute bypass (was accepted
as documented-non-boundary when the reviewer was hypothetical; HIGH-1 makes the boundary
load-bearing, so reconsider sealing it); LOW-3 the `immutable.py` docstring's "not read by a verdict
path" is FALSE (`ConfidenceThresholdConfig.per_action` IS, at engine.py:1268 — safe only by
REACHABILITY, correct the stated reason); LOW-4 the anti-staleness test never exercises `check_access`
or a populated clearance. **MUST-VERIFY carried to the rework:** the envelope INTERSECTION path —
the reviewer's one unclosed #2226 gap — if it rebuilds `OperationalConstraintConfig` via
`model_construct` with a list, the tuple-based sequence fix reopens on the intersected envelope.

**Confirmed CLEAN by the review (not re-litigate):** #2226 sequence dimensions (tuple, no
`__setitem__`); `governance_callback` #2227 Route B (stores the view, not the raw engine); the
bound-method `__self__` leak closed on all proxies; no PactEngine public accessor leaks the raw
engine. So the refutation is narrow and precise — HIGH-1 + the intersection gap are the load-bearing
items, not a wholesale redesign.

**This is the FIRST INDEPENDENT round** (the lane's own self-redteam was round 0). Security-bearing
surface ⇒ iterates to convergence, not one-and-done.

**L1 widened scope, flagged and verified disjoint from siblings.** The two issues' `engine.py`
files are different: `PactEngine` (#2227 Route B) lives in `packages/kailash-pact/src/pact/engine.py`,
NOT `src/kailash/trust/pact/`. L1 touched pact + kaizen-agents (`governed_agent`, `wrapper_base`,
`_sdk_compat`) — verified disjoint from L8's kaizen-agents finalizer targets (`governance/storage`,
`integrations/nexus/storage`) and from L5/L6. 3-dot diff = 16 files, 0 forbidden. (L1 also caught
that a two-dot `origin/dev..HEAD` diff reported 42 files including forbidden ones — the wrong
instrument, because `origin/dev` advanced under it as siblings landed. The same two-dot trap this
project has hit before.)

**Landing L1 unblocks wave 2** — it touches `trust/pact/{config,suspension}.py`, the exact files
#2225 and #2221 were held for. Dispatch them once L1 lands clean.

**L1 residuals it reported rather than closed:** #2227 `_inner` (BYPASS B) is still the raw agent —
closing it means substituting the proxy on a link 5 call sites traverse, one reading `_system_prompt`
which no allowlist can expose, so it would degrade SILENTLY to None — a cross-module rewrite beyond
this shard, documented as non-boundary rather than left implying safety. Plus a third route
`Delegate.core_agent` in neither issue. Both carried forward, named, not booked clean.

## L7 (#2000) ROUND 2 — F1–F6 fixed at `732d26c5`, second independent review running

The lane answered the refutation and, notably, CORRECTED the reviewer's own payload: `type(plotly)`
does NOT yield the proxy class, because naming a module in user code makes the token scan import it
for real (so `type(x)` is `ModuleType` and the payload dies looking like "blocked"). The LIVE escape
was via `__globals__` with no module named — confirmed reached, then closed. `_LazyModule` now
validates against `_LAZY_BINDABLE_MODULES` at construction AND resolve; F2 both subclasses
`ModuleType` and is named in the egress filter; F4 verdict measured RESTORED across base/mine/HEAD
without re-introducing the eager import; F5 documented; F6 no longer caches a failed resolution. The
"verdict-preserving/only-narrower" claim is WITHDRAWN.

**Gate not cleared:** round 1 refuted, round 2 is the first answer — a security-bearing surface needs
TWO consecutive clean independent rounds, so a fresh reviewer is running against `732d26c5` with the
round-1 not-reached surfaces as its primary target (`async_python.py`, `FunctionWrapper`/`ClassWrapper`,
the test suite's pinned verdicts under F6).

### TRAP — `git checkout <base> -- <file>` on a DIRTY tree destroys uncommitted work

L7 self-reported destroying its own uncommitted F1/F2 fix mid-session with
`git checkout <base> -- python.py` while the tree was dirty — it had verified clean-vs-HEAD the FIRST
time it did that and not the second. The `git.md` destructive-working-tree class: `git checkout --`
reads the index and has NO reflog for unstaged work. Recovered by redoing the edits and committing
each fix as a checkpoint before any subsequent checkout. Nothing lost from the pushed result, but the
discipline is: commit-as-checkpoint before any `checkout --`/`restore`, and re-verify the tree is
clean EVERY time, not once.

## L7 (#2000) round 1 — an adversarial security round REFUTED it. Correctness ≠ security-clean.

L7's own correctness case was strong (cold start 8.485s → 1.387s, measured both poles) and it
correctly reported that NO independent review had returned (its reviewers died in the quota event).
That is ZERO evidence, not a clean round, so I dispatched a fresh adversarial security-reviewer
against its FINAL pushed SHA `39dae98e5`. It **REFUTED** "security-neutral":

- **F1 — HIGH, sandbox WIDENING (the fix INTRODUCED it).** The new `_LazyModule` is an unrestricted
  importer; its CLASS is reachable via `type(proxy)` (allow-listed) / `proxy.__class__`, which
  resolves through normal lookup NOT `__getattr__`, so the `_kailash_` guard misses it. User code
  with no `import` statement reaches `type(_p)("subprocess")` — a COMPLETELY_BLOCKED_MODULES entry.
  Before the fix the slot held a real `ModuleType`, so the same payload built an empty module and
  FAILED. A genuine regression, not a pre-existing hole.
- **F2 — MEDIUM.** The output filter strips `types.ModuleType` to stop live modules leaking into
  node outputs; `_LazyModule` is a `__slots__` class, not a `ModuleType`, so it now escapes.
- **F3/F4/F5 — the "verdict-preserving" claim is FALSE.** Several accept/reject verdicts changed
  (pandas-DataFrame allow now needs pandas LOADED not installed; coverage-gated sklearn). Fail-
  CLOSED, so not vulnerabilities — but the claim asserted as evidence was wrong.

Sent back to the lane to fix F1+F2 (blocking) and correct the false verdict claims, with the F1
escape payload and the F2 egress case as discriminating regression tests shown RED first. **The
gate does not clear on one round** — I re-review before landing.

**The reviewer had NO Bash tool** (Read/Grep/Glob only), so F1's payload is constructed from code,
not observed running. That does not lower the bar — a plausible HIGH sandbox-widening on a security
gate blocks landing regardless — but the rework MUST execute the payload to confirm the fix, and the
reviewer's own "NOT reached" list (the real `git diff`, `async_python.py`, the test suite's pinned
verdicts) is carried forward, not booked clean.

## QUOTA PAUSE → RESUMED (not death) — 3 agents, account swapped

An account session-limit (HTTP 429) stopped three agents mid-run. Per
`orchestration-launch-ledger.md` MUST-4 a quota failure is a **PAUSE, not a death**: the agents
retain their transcripts and resume when addressed by name. **No replacement was launched into any
of their worktrees** — two live agents on one directory under one git identity is the collision
`--author` cannot untangle, and it silently sweeps a sibling's mid-edit work into someone else's
commit.

| lane                                     | state at pause                                            | transition                             |
| ---------------------------------------- | --------------------------------------------------------- | -------------------------------------- |
| L7-perfcode (#2000)                      | "both reviews still in flight, waiting before I finalize" | in-flight → quota-paused → **resumed** |
| L8-provresid (#2220 residual)            | "now adding the shared predicate to `_provider_env.py`"   | in-flight → quota-paused → **resumed** |
| security-reviewer (grandchild of a lane) | "checking sandbox allow-lists and runtime checks"         | in-flight → quota-paused → **resumed** |

L7's resume RELEASED one self-assigned sub-goal — the wait on its own reviews, which died in the
same 429 — and instructed it to state plainly if no independent review ran rather than let the gap
read as a pass. Its worktree was NOT reaped and was confirmed present before the resume.

**The 10 idle teammates from ~21h ago (cont-29) were deliberately NOT resumed.** They are idle, not
fallen: their work already landed and drained last session, so their context is stale by a full
release cycle. `drain-stash` in particular would act on the 5 stashes that still need the owner's
prune authorization (D4). Resuming a stale teammate is not recovery, it is re-running a finished
session against a tree that has moved.

## OWED: a kaizen 2.47.0 BREAKING release decision

L3's #2220 fix is a **breaking behaviour change** and is currently unreleased on `dev`. The package
documents that it does NOT follow SemVer — "a MINOR release MAY contain breaking changes" — so the
lane's recommendation is `2.46.1 → 2.47.0`. **No version was bumped and no tag was cut**: a breaking
minor is a release-authorization decision, which is a structural human gate, and the owner's
standing instruction named core 2.65.0 specifically, not kaizen.

Who breaks: callers relying on the env-credential guess for a model whose name misses the prefix
registry — `chatgpt-4o-latest`, `ft:...`, Azure deployment names. They were right BY LUCK; the
resolver cannot distinguish them from `llama-3.1`. Migration is one argument: `llm_provider="..."`.

**Do NOT cut this release until L8 lands** — L8 closes the residual half, and shipping 2.47.0 with
the headline harm still live at ~33 other entry points would publish a fix that does not fix it.

## L3 landed HALF-CLOSED, and said so — the residual is now L8

The primary #2220 fix has ONE production consumer. Measured WITH the fix applied,
`Agent(config={"model":"llama-3.1"})._get_provider_for_config()` still returns `'openai'`. ~33 sites
across ~13 files call `detect_provider_from_env()` directly and pair it with a model.

Per `autonomous-execution.md` Rule 4 + `product-completion-first.md` MUST-3, a warm same-class BUG
gap is fixed in the SAME SESSION, not filed as a follow-up issue — so it was dispatched as lane L8
rather than written down. L3's own honesty about being half-done is why that lane exists.

Second residual L3 measured: a prefix HIT is not safe either — `deepseek-r1:7b` → `'deepseek'`,
`gpt-oss:20b` → `'openai'`, both mainstream Ollama names that never reach the new guard. Closing
that needs a POSITIVE signal for local serving, not another name heuristic. **Not yet lane-assigned.**

Also corrected by L3, on the issue: the defect is NOT OpenAI-specific (with `ANTHROPIC_API_KEY` set,
`llama-3.1` resolved to `'anthropic'`), and "there CANNOT be an ollama row" was overstated — a
partial one exists at `nodes/_env_model.py:72`; what is impossible is a SOUND one.

## L4 landed: the finalizer guard was INERT, and hid a second deadlock

`except Exception: pass` in ten finalizers did **zero work** against the hazard that motivated it —
a deadlock is not an exception, so nothing is ever raised for the handler to catch. It bought no
safety and swallowed real release failures.

**The standard Rule 3 remedy is contraindicated here.** Adding a logging call to the except branch
takes the very lock whose re-entry IS the deadlock. The fix DELETES the cleanup call: no call, no
exception, no handler, no Rule 3 exposure. The stdlib C-level deallocators close sqlite3/socket
handles anyway, which a Python-level `close()` from a GC callback cannot do safely.

**Second deadlock, never documented in the issue, and deterministic (100% reproduction):** four
finalizers re-acquire a NON-REENTRANT `threading.Lock`. A finalizer fires at an arbitrary bytecode
boundary on whichever thread dropped the last reference — including a thread already inside one of
those locked sections, which then blocks on itself forever.

**#2107's acceptance criterion is too weak to close the class** — its `close()`-only predicate is
blind to a finalizer swallowing a differently-named cleanup call (`release()`, `shutdown()`,
`rollback()`), and five such sites exist. A shape-level guard test now walks EVERY `__del__` under
`src/kailash`, so a ninth site reds CI regardless of the method name.

Two issue line numbers had drifted (`local.py` 2061→2177, `async_sql.py` 7025→7475); correction
posted. 17 regression tests pass post-merge in the main checkout; 14 were shown RED pre-fix.

**One test the lane EXPECTED to discriminate and did not** — `test_async_local_runtime_warns_
exactly_once`: pre-fix the forced `close()` drove `_ref_count` to 0, so the count was also exactly 1
for an entirely different reason. The lane moved it out of the evidence list rather than leave a
false claim standing. That is the behaviour to keep rewarding.

### L4 residuals — recorded, NOT absorbed

| residual                                                                                                          | disposition                                                |
| ----------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------- |
| 2 finalizer sites in `packages/kailash-kaizen` (`governance/storage.py:652`, `integrations/nexus/storage.py:606`) | dispatch after L3 frees that package                       |
| 5 further sites the issue's own predicate cannot see (all `packages/`)                                            | same lane as above                                         |
| 10 pre-existing lone-`pass` handlers in `local.py` (none in a finalizer)                                          | separate sweep; out of this bug class                      |
| 2 inert `patch("networkx.topological_sort")` targets in `test_parallel_cyclic.py:245,571`                         | green-for-the-wrong-reason; separate audit                 |
| `test_watchdog_detects_stall` load-sensitive flake                                                                | NOT L4's — 3/3 green in isolation at base and with the fix |
| `access_control/managers.py:170` `evaluate_rules()` DeprecationWarning                                            | `zero-tolerance` Rule 1 — queue a fix                      |

## RELEASE DONE — kailash 2.65.0 + kailash-dataflow 2.20.1 are on PyPI

Promotion PR #2228 merged at `4a98750d8` (16 SUCCESS / 4 skipped / 0 fail on the pinned head).
Tags `v2.65.0` + `dataflow-v2.20.1` pushed; both `publish-pypi.yml` runs `success`. Verified live
with a DISCRIMINATING instrument — `pypi.org/pypi/<pkg>/<ver>/json` returns 200 for both and the
`2.99.99` control returns 404. **D1 and D2 discharged.**

### NEW TRAP — a `dev → main` promotion DELETES the trunk branch

`delete_branch_on_merge` is `true` on this repo (measured via `gh api ... --jq 'has(...)'`). The
promotion PR's head ref IS `dev`, so merging it deleted `dev` on the remote. The next `git push
origin dev` printed `* [new branch]` and silently recreated it. Nothing warned.

**Next promotion: cut a `promote/<date>` branch from `dev` and open the PR from THAT**, so the
auto-delete consumes a disposable ref instead of the trunk. Re-verify `dev` exists after any
promotion merge before assuming the trunk survived.

### NEW TRAP — `pip install` cannot tell "unpublished" from "index cache lag"

Immediately post-publish, `pip install kailash==2.65.0` returned `No matching distribution
found` — and so did the `kailash==2.99.99` control. The instrument returns the same string under
both hypotheses, so it answers neither. Use the version-pinned JSON endpoint with a 404 control.

### L2 correction: #2222 under-reported by two sites, and missed a second failure

Five `AccessDecision` construction sites, not three: `:183` (`check_api_access`) and `:639`
(`authorize_request`'s default-deny) were unrecorded — and `:639` is the fail-closed branch, so
the one path whose job is to refuse safely could not produce a refusal. Separately,
`_emit_access_event` read the same three absent fields, so with an event stream wired EVERY
decision raised, including one that never touched a broken constructor. A kwarg-only fix would
have left the module broken one line later. Correction posted to #2222.

**Open for the co-owner:** whether `AccessDecision` gains `user_id`/`resource_id`/`permission`,
and which of the two same-named classes is canonical. L2 did the non-breaking half only (identity
now rides `reason`, a field that exists) and deliberately did NOT pick a schema.

**Surfaced, not yet owned:** `access_control/managers.py:170` emits `DeprecationWarning:
evaluate_rules() is deprecated, use evaluate()` on the now-live path. `zero-tolerance` Rule 1
makes that a fix, not a note — queue it.

## Wave 2 — held back deliberately, NOT forgotten

| track | why held                                                                                             |
| ----- | ---------------------------------------------------------------------------------------------------- |
| 2225  | touches `trust/pact/engine.py` (:3216, :3305) — SAME-file collision with L1. Launch after L1 lands.  |
| 2221  | touches `trust/pact/suspension.py` — collides with L1's `get_suspension` fix. Launch after L1 lands. |

## Refill queue (dispatch as lanes free up)

| track              | content                                                                                  |
| ------------------ | ---------------------------------------------------------------------------------------- |
| 2116-pr2098        | 6 verified-unfixed findings: silent ephemeral-key fallback, swallowed auth raises        |
| llm-wire           | 2209, 2215 (2120/2121 landed lane L16)                                                   |
| doc-drift          | `packages/kailash-kaizen/docs/a2a_http_service.md:319,447` stale handler signature       |
| 2203-w2/w3         | MCP → Modern 2026-07-28; conformance suites (approved)                                   |
| a2a-residuals      | trust.verify oracle, jti replay, TTL cap                                                 |
| fingerprint-rename | durable fix for the CodeQL name-heuristic class (51 sites, 13 files, 4 pkgs) — not filed |

BLOCKED, do not dispatch: #2141 / #2166 / #2194 — co-owner schema decision owed.

## Burn-down baseline (measured this session, instruments named)

| axis                    | instrument                                                            | @ 8a5302336 |
| ----------------------- | --------------------------------------------------------------------- | ----------- |
| open issues             | `gh issue list --state open --limit 300 --json number -q '.\|length'` | 51          |
| open PRs                | `gh pr list --state open --json number -q '.\|length'`                | 0           |
| local unlanded branches | `git branch -vv`                                                      | 0           |
| worktrees               | `git worktree list`                                                   | 1           |
| stashes                 | `git stash list`                                                      | 5           |
| archive refs            | `git ls-remote origin 'refs/archive/*'`                               | 51          |
| promotion gap           | `git rev-list --count --first-parent origin/main..origin/dev`         | 5           |

## Release state (measured, not inherited)

- core `kailash`: `pyproject.toml` + `__init__.py` both `2.65.0`; latest core tag `v2.64.0` → **2.65.0 UNRELEASED**.
- `kailash-dataflow`: `pyproject.toml` `2.20.1`; latest tag `dataflow-v2.20.0` → **2.20.1 untagged**.
- Only a pushed tag publishes (`publish-pypi.yml`, OIDC, no manual twine).

## Carried corrections — do NOT re-derive

- Release scope anchors at the **PyPI upload time**, never a version-bump commit and never the core
  tag. Script: `measure_content_parity.py`. Strongest instrument: diff the published wheel.
- "Eight packages overdue, none ever published" was FALSE. Every package was already on PyPI at its
  declared version.
- `gh issue close`: ONE per Bash command, full 40-char SHA inline. Batching trips the closure-evidence
  detector; so does passing the SHA via a shell variable.

## Instrument-scope discipline (the dominant error class across cont-29 and cont-28)

Before any measurement is trusted: **fire the check at a case whose answer you already know, and
confirm it can return the OTHER answer.** Fourteen instances across the last two sessions were the
same shape — an instrument returning a plausible value for a question nobody had checked it could
answer.
