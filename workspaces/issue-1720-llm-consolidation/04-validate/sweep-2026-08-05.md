# /sweep — Management Decision Report — 2026-08-05 (session E close)

Repo `terrene-foundation/kailash-py` (`coc-build`, py). Workspace
`issue-1720-llm-consolidation`, phase 05-codify. Branch
`fix/issue-1720-forest-drain` @ `c9f3d2124` — **87 commits ahead of main, 26
UNPUSHED.** Tree clean, no stray worktrees.

Supersedes `sweep-2026-08-04b.md`. **That report's §1 completion table and §6
recommendation are REFUTED by this session's evidence** (§1 below); its §3/§4
triage largely stands and is carried forward.

---

## 1. Completion status

Every "complete" claim cites a durable receipt (`verify-resource-existence.md`
MUST-4 — no self-attested completion).

| Milestone                                   | State                           | Durable receipt                           |
| ------------------------------------------- | ------------------------------- | ----------------------------------------- |
| 3rd tool-schema emitter (anon disclosure)   | **COMPLETE**                    | `19c8cfb33`                               |
| Async-wrapper auth bypass (no-cred execute) | **COMPLETE**                    | `14d9d8b31`                               |
| Falsy `auth_provider` disables authz        | **COMPLETE**                    | `14d9d8b31`                               |
| Credentials in Redis key names + logs       | **COMPLETE**                    | `71eb63790`, `ee6d5b8bb` (hashed)         |
| Cross-principal cache sharing               | **COMPLETE**                    | `ee6d5b8bb`                               |
| Discovery fail-closed + falsy checker       | **COMPLETE**                    | `393f33e27`, `784f92462`                  |
| Advisory-label over-correction (own regr.)  | **COMPLETE**                    | `b8d60577e`                               |
| Scrub local/remote split + prose regression | **COMPLETE**                    | `b2d3acce5`, `eee4fa59b`                  |
| Release-blocking cross-package floor        | **COMPLETE**                    | `269038fd9`                               |
| #1996 trust-plane FastMCP guard             | **COMPLETE — issue still open** | `45ccac417`                               |
| kaizen security+trust suite 9 failed → 0    | **COMPLETE**                    | `2f7d56807`, `9791892fa` (test-only)      |
| Redteam to convergence                      | **NOT MET — counter ZERO**      | rounds 1–5 all un-clean                   |
| Version anchors (5 packages)                | **NOT DONE**                    | all unbumped; measured §8                 |
| CHANGELOGs                                  | **NOT DONE**                    | zero CHANGELOG files touched this session |
| Push / `/release`                           | **NOT DONE**                    | 26 unpushed; 116 commits since kaizen tag |

### The refutation, stated plainly

`sweep-2026-08-04b.md` §6 recommended releasing on **abnormal-termination
evidence** — severity fell monotonically, nothing in three rounds changed what
the module leaked. **Five further rotated-lens rounds found five CRITICAL/HIGH
defects**, including an unconditional auth bypass (no credentials ⇒ a
permission-gated tool EXECUTES). That release would have shipped them.

The trend was real; treating it as evidence was the error. Each round found
defects on an axis the prior lens could not see. **Three of the five were
regressions introduced BY fixes**, one of them this agent's.

**Walking skeleton stands. No shipped path is known-wrong.** What is NOT done is
the release, and it is gated on convergence — a decision (§5-A), not on code.

## 2. ETA to completion

**2–3 autonomous cycles** to a released, visible product.

| Item                                       | Cycles | Basis                                        |
| ------------------------------------------ | ------ | -------------------------------------------- |
| F3 adjudication + two clean redteam rounds | 1–2    | 1 MEDIUM + 3 LOW open; rounds are now narrow |
| Version anchors + CHANGELOGs + PR          | 0.5    | 5 packages; targets verified §8              |
| `/release` to PyPI                         | 1      | 6 packages, lockstep                         |

Excludes the INCREMENTAL backlog (§4) and the unowned nexus 500s (§5-D), neither
of which gates release.

## 3. Prioritized immediate queue — BUG + INVEST-NOW

Classified per `product-completion-first.md` MUST-1 (category gates, severity
ranks; ambiguous resolves ON-LIST).

| #   | Item                                                        | Cat        | Value-anchor                                                                                                        |
| --- | ----------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------- |
| 1   | **#1998** production stdio bypasses every gate              | **BUG**    | gated tools disclose full schema + `Args:`; disabled tools EXECUTE, `isError=false`. Anonymous reach. Architectural |
| 2   | **#1997** Mistral key redaction probabilistic per key       | **BUG**    | a credential survives redaction depending on key shape                                                              |
| 3   | **#1974** error_sanitizer gaps (non-http DSN, Slack, JWT)   | **BUG**    | same class as the leaks fixed this session, unswept surface                                                         |
| 4   | **#1981** A2A scores always 0.0 without structured output   | **BUG**    | every real score is 0.0 ⇒ ranking is arbitrary; made LOUD but not fixed                                             |
| 5   | **F3** advisory labels grant `permission_level="execute"`   | INVEST-NOW | two lanes flagged; unadjudicated. **Gates convergence**                                                             |
| 6   | **#2000** eager torch/sklearn ⇒ 11.4s first execution       | INVEST-NOW | fails nexus e2e `test_concurrent_requests` 3/3                                                                      |
| 7   | **F5/F6/F7** warn-scoping, Mapping-len grant, echo doctrine | INVEST-NOW | bounded gaps in working guards; documented                                                                          |

**Why 1–4 are BUG, not INVEST-NOW:** each is a live wrong-answer or disclosure on
a shipped path, not a bounded gap in a working guard. #1998 in particular is
reachable anonymously and the stdio hardening this session landed sits on
`run_stdio`, which has **zero production callers** — the hardening is certified
on a path nobody runs.

**Nothing in this queue was relabelled to escape convergence.** Every BUG-class
finding _surfaced this session_ is fixed and regression-pinned; 1–4 are
pre-existing, filed, and were correctly out of this session's shards.

## 4. Deferred-quality backlog — INCREMENTAL

### `after-milestone:forest-release`

| Item                                              | Blocking-safety                          | Acceptance                              |
| ------------------------------------------------- | ---------------------------------------- | --------------------------------------- |
| **#1995** pre-commit rewrites 2,022 files (isort) | no workflow runs isort/black on any path | `pre-commit run --all-files` modifies 0 |
| 3 dependabot PRs (#1988/#1989/#1990)              | CI green; action-version bumps only      | merged or closed                        |

### `on-demand`

| Item                                             | Blocking-safety                                       | Acceptance                            |
| ------------------------------------------------ | ----------------------------------------------------- | ------------------------------------- |
| **#1970** acceptance names a non-existent symbol | fix landed in `KaizenAIModelNode`; delivered          | acceptance text corrected             |
| **#1971** AC1 delivered opposite direction       | fail-closed validation shipped + tested               | acceptance re-scoped                  |
| **#1972** nexus: 2 of 3 are STALE TESTS          | tests assert behaviour production deliberately blocks | tests ported, **not** product changed |
| F-STUBS marker baseline (29 prod TODO)           | pre-existing                                          | **do-not-requeue** (standing call)    |

### Sweep-N revisit gate — items deferred ≥2 sessions

- **#1995** — filed 2026-08-03, now spans sessions D **and** E. **Crosses the
  ≥2-session line: a "still wanted?" gate IS owed this cycle** (see §5-E).
  (`sweep-2026-08-04b.md` recorded it under threshold at 1 session; that is no
  longer true.)
- **F-STUBS** — standing user call 2026-06-26, `do-not-requeue`. **Not
  re-surfaced.**

## 5. Decision points

### A — Release gate: convergence is NOT met, and the shortcut is now refuted

Rounds 1–5 were all un-clean; the clean-round counter is at **ZERO**.
`commands/redteam.md` requires two consecutive clean rounds.

- **Recommend: adjudicate F3, then run rounds to two-clean before `/release`.**
  Basis: the identical "severity is falling, ship it" reasoning was tried at
  session D and five CRITICAL/HIGH defects followed, three of them introduced by
  fixes. The remaining rounds are narrow — F3 plus three LOWs — not open-ended.
- **Pros:** the release is defensible on convergence rather than on a trend that
  has already been falsified once on this exact branch.
- **Cons, stated honestly:** 1–2 more cycles on a branch already 26 commits
  deep, and what remains is MEDIUM/LOW, not the CRITICAL class that justified
  rounds 1–4. A reasonable reader could call the marginal return low.
- **Alternative:** draw the line at "all known CRITICAL/HIGH fixed, MEDIUM/LOW
  documented" and release. Defensible — but the release notes MUST then say
  which basis was used, because that is a materially weaker claim than two clean
  rounds and the next reader cannot tell them apart otherwise.

### B — #1996 is fixed but still OPEN; close it with the SHA

`45ccac417` delivers all four acceptance criteria. **Recommend: close citing the
SHA** (`git.md` — closure without a code reference is BLOCKED).

Also worth recording: `sweep-2026-08-04b.md` §5-A judged #1996's premise FALSE
after reading `nexus/transports/mcp.py`. **That was the wrong file** — the issue
names `src/kailash/trust/plane/mcp_server.py:31`, which was genuinely unguarded
at module scope. The re-scope recommendation should NOT be actioned.

### C — Cross-SDK: prior grants exist but are action-scoped; this session's classes need a NEW one

Correcting an over-broad statement in the session-E notes: cross-SDK work on
this workspace **has** been authorized and filed before — five
`cross-repo-authorization-1720-*.md` receipts in this workspace, two drafts
marked FILED against the Rust SDK BUILD repo.

But `repo-scope-discipline.md` condition 5 scopes a grant to **the named action**.
None covers this session's two new classes (the credential-scrubber
under-redaction family, and the identity-vs-truthiness authz-gate family).

- **Recommend: run `/cross-repo-authorize` for the two new classes specifically.**
  Neither is Python-specific and `cross-sdk-inspection.md` Rule 1 binds.
- **Con:** two more filings on a private sibling that is not this session's
  scope. Mitigated — the drafts are cheap and the classes are exactly the kind
  that recur silently across bindings.

### D — The 3 nexus 500s are UNOWNED

Every request returns 500 on `POST /workflows/process/`. **Consistent across
runs, not flaky** — discriminated with the MCP changes fully reverted. Different
bug class, outside every package touched this session, and **not filed**.

- **Recommend: file it and assign an owner.** It is a live failure with no
  tracker, which is the state most likely to be lost at `/clear`.

### E — #1995 crosses the Sweep-N line: "still wanted?" gate owed

Deferred across sessions D and E. Per `value-prioritization.md` MUST-3 the
disposition is user-gated: **implement** / **re-defer with a fresh anchor** /
**close with the gate** — never auto-close as `not_planned`.

- **Recommend: re-defer with a fresh anchor** to `after-milestone:forest-release`.
  It is genuinely blocked on a decision nobody has needed yet (whether isort runs
  repo-wide at all), and no workflow runs isort/black on any path today.

### F — spec-drift (session D Decision C): the BLOCKER is now RESOLVED

Session D held this pending "does `specs/spec-drift-gate.md` subsume
`tools/sweep-redteam.py`?" **Answered this session:** partial overlap; the mature
gate (86 specs, 116-entry baseline, pre-commit + CI, 23 test files) subsumes the
axis that generates sweep-redteam's noise, but NOT its `coverage_gap` and `stub`
checks. Also measured: `sweep-redteam.py --all` scans `workspaces/*/specs/`,
which holds **zero** files here — the documented Sweep-5 protocol resolves to
N/A, so the 70 findings came from an off-protocol run.

- **Recommend: RETIRE `sweep-redteam.py`; port `coverage_gap` + `stub` into the
  gate as new FRs.** Removes 55–63 of 70 rows by construction rather than tuning.
- **Con, and it is the real cost:** `rules/sweep-completeness.md` names
  `tools/sweep-redteam.py` as the mandated Sweep-5 tool, so retiring it is a
  coordinated COC-artifact change, not a file deletion.

## 6. Recommendation — next steps

1. **Adjudicate F3** (§3 item 5) — the one open item that gates convergence.
2. **Two clean redteam rounds**, rotating the lens (Decision A).
3. **Close #1996** with `45ccac417`; **file the nexus 500s** (Decisions B, D).
4. **Version anchors + CHANGELOGs**, one PR. **TRAP: `kailash-ml` keeps its
   version in `_version.py`, NOT `__init__.py`** — the obvious bump ships a split
   version state (zero-tolerance Rule 5). Several entries are BREAKING: discovery
   fail-closed, the falsy-checker enforcement flip (presents as a sudden
   access-denial wave), and the renamed log event
   `discovery.permission_check_failed_open` → `..._failed_closed`.
5. **Push** (26 commits) and open the PR.
6. **`/release`**, stating the convergence basis used.
7. Then the BUG queue in §3 order — **#1998 first**, and note its fix is
   architectural, not a filter.

---

## Sweep-by-sweep raw results

| Sweep                         | Result                                                                                                                                                                                           |
| ----------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1 — active todos              | 0 active, 0 completed (workspace todo dirs empty)                                                                                                                                                |
| 2 — pending journal           | 0 in `journal/.pending/`                                                                                                                                                                         |
| 3 — open issues               | 10 (#1970/#1971/#1972/#1974/#1981/#1995/#1996/#1997/#1998/#2000)                                                                                                                                 |
| 4 — open PRs / branches       | 3, all clean dependabot (#1988/#1989/#1990)                                                                                                                                                      |
| 5 — redteam vs specs          | blocker RESOLVED — retire `sweep-redteam.py`, port 2 checks (§5-F)                                                                                                                               |
| 6 — workspace/worktree/ledger | tree clean; 1 worktree (main); no active todos                                                                                                                                                   |
| 7 — process hygiene           | 87 ahead of main, **26 unpushed**, 0 dirty                                                                                                                                                       |
| 8 — release readiness         | 116 commits since `kaizen-v2.45.0`; **5 of 6 packages unbumped**; kailash 2.62.0 / kaizen 2.45.0 / kaizen-agents 0.12.0 / dataflow 2.19.1 / nexus 2.16.0 / ml 2.2.2; **zero CHANGELOGs touched** |
| 9 — cross-ecosystem           | N/A — `type: coc-build`                                                                                                                                                                          |
| 10 — deferred-quality         | 1 labelled (#1995) — **Sweep-N gate OWED** (§5-E) + 4 enumerated §4                                                                                                                              |
