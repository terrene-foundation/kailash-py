# /sweep — Management Decision Report — 2026-08-07 (session H close)

Repo `terrene-foundation/kailash-py` (`.claude/VERSION::type = coc-build`, variant `py`).
Workspace `issue-1720-llm-consolidation`, phase 05-codify. Branch
`fix/issue-1720-forest-drain` — **155 ahead of `origin/main`, 6 behind, 0 UNPUSHED.**

Supersedes `sweep-2026-08-07.md` (session G close). Its §5-A blocker is CLOSED; its §3 queue
is re-triaged below; its branch-position numbers were measured against a stale local `main`
and are corrected here.

> **THE HEADLINE HAS CHANGED. The 94 unpushed commits are PUSHED.** Sessions F and G both
> closed on that risk. It is gone: `git rev-parse HEAD` == `origin/fix/issue-1720-forest-drain`
> == `7b3d9ee77`, unpushed = 0, verified from the remote rather than from an exit code.
> **The new at-risk surface is the UNCOMMITTED #2007 fix in the working tree.**

---

## 1. Completion status

Every "complete" claim below cites a durable receipt (`verify-resource-existence.md` MUST-4).

| Milestone                                 | State                      | Durable receipt                                                           |
| ----------------------------------------- | -------------------------- | ------------------------------------------------------------------------- |
| Session F's 63-file tree committed        | **COMPLETE**               | 13 slices, `23ff5cbf2`..`dc69cb786`                                       |
| Rounds 2/3/4 findings fixed               | **COMPLETE**               | per-finding RED→GREEN; `8f8577c36` `b1ac06e5b` `9f3e69de8` `98e83dfbe`    |
| #1720 closed on the public route          | **COMPLETE**               | `568036906` + `2d2563d81`                                                 |
| 9 packages version-anchored + CHANGELOG'd | **COMPLETE**               | 9/9 `pyproject` == imported `__version__`; 9/9 CHANGELOG headings present |
| **Branch PUSHED**                         | **COMPLETE**               | `03795208d..7b3d9ee77`; unpushed = 0                                      |
| **All 3 dependabot PRs merged**           | **COMPLETE**               | `876a3bf18`, `1be52aafd`, `c11dd4b5d`; PR board EMPTY                     |
| **All 5 test trees ESTABLISHED**          | **COMPLETE**               | §6 — no UNESTABLISHED tree remains                                        |
| **#1998 / #2001 closed with receipts**    | **COMPLETE**               | SHAs confirmed ancestors of the pushed branch BEFORE citing               |
| **#2007 filed + fixed**                   | **FIXED, UNCOMMITTED**     | 2605 passed / 0 failed; 13 new tests; NOT yet committed                   |
| **Round 5 / convergence**                 | **NOT MET — counter ZERO** | one lens never ran; hypothesis D unresolved                               |
| **PR opened**                             | **NOT STARTED**            | body asserts a false convergence claim (§5-C)                             |
| **`/release`**                            | **NOT STARTED**            | gated on convergence + the nexus hang (§3 item 2)                         |

**Walking skeleton stands.** No shipped path is known-wrong. What is missing is convergence,
the PR, and the release.

## 2. ETA to completion

**3–5 autonomous cycles** to a released, visible product (was 4–7 at session G close; the push,
the merges, the tree establishment and one BUG closed since).

| Item                                                             | Cycles | Basis                                                   |
| ---------------------------------------------------------------- | ------ | ------------------------------------------------------- |
| Commit #2007 + finish round 5 (3 of 4 lens tasks + hypothesis D) | 1      | lens tasks are mechanical; D is one focused read        |
| One clean round after it                                         | 0.5–1  | counter is ZERO and #2007 voided the nexus surface      |
| Nexus post-summary hang (116 threads)                            | 0.5–1  | needs a `main` baseline + a root-privileged thread dump |
| PR body rewrite + open                                           | 0.2    | numbers re-derived; convergence claim must go           |
| `/release` — 7 packages, ordered + extras floors                 | 1      | sequence manual, CI-unenforced                          |
| BUG queue (§3 items 3–8)                                         | 2–3    | #2006 is architectural                                  |

Excludes the INCREMENTAL backlog (§4), which does not gate release.

## 3. Prioritized immediate queue — BUG + INVEST-NOW

Classified per `product-completion-first.md` MUST-1 (category gates, severity ranks, ambiguous
resolves ON-LIST). Value-anchored per `value-prioritization.md` MUST-1.

| #   | Item                                                             | Cat        | Value-anchor                                                                                                                                           |
| --- | ---------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | **Commit the #2007 fix** (5 sources + 1 test, uncommitted)       | **BUG**    | verified work with no second copy IS the risk class this session just spent itself closing                                                             |
| 2   | **Nexus suite hangs post-summary — NOT FILED**                   | **BUG**    | 116 leaked threads; a green suite that never exits HANGS a CI job and reads as infra flake, so `/release` cannot trust its own gate                    |
| 3   | **#2006** dataflow CREATE sends explicit NULL                    | **BUG**    | defeats every column DEFAULT on every unset optional field                                                                                             |
| 4   | **#1997** Mistral key redaction probabilistic                    | **BUG**    | a credential survives redaction depending on key shape                                                                                                 |
| 5   | **#2004** discovery health-check leaks untrusted cmd             | **BUG**    | one site of its class outside `server.py`, found by a general instrument                                                                               |
| 6   | **#1974** error_sanitizer gaps (non-http DSN, Slack, JWT)        | **BUG**    | same class as the leaks closed this session; surface still unswept                                                                                     |
| 7   | **#1970 / #1981** provider errors leak / A2A scores 0.0          | **BUG**    | raw provider exception to user surface; every real A2A score 0.0 ⇒ ranking arbitrary                                                                   |
| 8   | **Discovery `except Exception` grants access — UNTRACKED**       | **BUG**    | an authz fail-open deliberately deferred in-code with sound reasoning, but `zero-tolerance.md` Rule 1b wants a TRACKING ISSUE, not only a code comment |
| 9   | **#2002** root `tests/regression/` has no CI                     | INVEST-NOW | **1,564 of 1,591 never run.** Would have caught round 4's two regressions before a reviewer                                                            |
| 10  | **~37 UNAUDITED test files** + round-5 lens Tasks 2/3/4          | INVEST-NOW | incl. 3 under `kailash-kaizen/tests/security/` no lane has ever read                                                                                   |
| 11  | **#1972** nexus 3 failures (2 are STALE tests)                   | INVEST-NOW | fixing the product to green them RE-INTRODUCES the defect — port the tests                                                                             |
| 12  | **#2000 / #1971** 11s first execution / 63-char id               | INVEST-NOW | fails a nexus e2e concurrency test; wrong-dialect id selection                                                                                         |
| 13  | **kaizen `BaseAgent(description=…)` stale E2E — NOT FILED**      | INVEST-NOW | git-proven pre-existing (both files unchanged vs `origin/main`); a stale test, not a live defect                                                       |
| 14  | **`kaizen_implementation_test.log` repo-root write — NOT FILED** | INVEST-NOW | a test dirties the working tree: voids the fingerprint protocol and leaves a CI checkout dirty                                                         |

**Nothing was relabelled to escape convergence.** Items 3–8 are pre-existing or newly-surfaced
BUGs correctly out of this branch's shards. Items 2, 8, 13, 14 are **NOT YET FILED** — filing
them is part of item 1's cycle.

## 4. Deferred-quality backlog — INCREMENTAL

### `after-milestone:forest-release`

| Item                                              | Blocking-safety                           | Acceptance                              |
| ------------------------------------------------- | ----------------------------------------- | --------------------------------------- |
| **#1995** pre-commit rewrites 2,022 files (isort) | no workflow runs isort/black on any path  | `pre-commit run --all-files` modifies 0 |
| **#2003** envelope doubles run-history storage    | nothing breaks, nothing leaks; 2x storage | persisted ratio ≈ 1.0 vs raw control    |

### `on-demand`

| Item                                                 | Blocking-safety                                                                          | Acceptance                                      |
| ---------------------------------------------------- | ---------------------------------------------------------------------------------------- | ----------------------------------------------- |
| **#2005** tenant key falls open to global namespace  | not cross-tenant; degrades to GLOBAL, not sibling; multi-tenant-enabled deployments only | intent documented + pinned                      |
| MCP enumerator-guard AST blind spots (4 named forms) | latent only — all four verified ABSENT today                                             | matcher covers Name/Call receivers + bare `for` |
| Cross-CLI skill drift (`.claude` 127 vs 235 lines)   | COC artifact; not release-blocking                                                       | proposal filed, not in-branch                   |

### Sweep-N revisit gate — items deferred ≥2 cycles

- **#1995 — filed 2026-08-03; now spans sessions D, E, F, G, and H. FIFTH cycle.**
  Gate OWED and re-surfaced at §5-D below. It was the ONLY labelled item until this session.
- **#2003 / #2005 — labelled 2026-08-07 (this session), cycle 1.** No gate owed yet; they now
  ride the same anti-forgetting hooks as #1995 instead of drifting unlabelled.

## 5. Decision points

### A — The #2007 fix is verified but UNCOMMITTED. This is now the only at-risk work.

5 modified nexus sources + `test_issue_2007_rate_limit_client_key.py`, full nexus tree at
2605 passed / 0 failed.

- **Recommend: commit it.** Branch is clean of anything else, tests are green, and the
  session's entire lesson was that verified-but-unreplicated work is the risk.
- **Pros:** closes the last single-copy surface; lets #2007 be closed with a resolvable SHA.
- **Cons, stated plainly:** BUILD-repo policy reserves commits to the co-owner, so this needs
  an explicit go-ahead rather than agent discretion — that is the only thing holding it.

### B — Convergence is NOT met, and the #2007 fix ENLARGED the gap

Round 5 ran two rotated lenses: the security lens resolved hypotheses A/B/C but **D is
unresolved**, and the test-contract lens **ran Task 1 of 4**. Separately, `completion-criterion.md`
MUST-3: the #2007 fix TOUCHED the nexus surface, so any clean-round credit that surface held is
**void**.

- **Recommend: finish round 5's four open tasks, then one clean round.**
- **Cons, honestly:** this branch is 155 commits deep and each round returns less — round 2
  found 7 HIGH, round 3 found 3, round 4 found 2, round 5 found 1 (latent). A reasonable reader
  could call the marginal return low.
- **Counter-argument I find stronger:** round 5's one finding was a dead security control that
  had been dead since before this branch, found only because a lens was rotated onto it. The
  yield is dropping on the OLD lenses, not on new ones.

### C — The PR body asserts a FALSE convergence claim

`04-validate/pr-body-draft.md` § Verification states "Convergence was reached by rotated-lens
redteam rounds." That is false, and publishing it would put an unverified durable claim on a
public surface (`verify-claims-before-write.md` MUST-1). It also carries stale counts, a stale
"round 3" gate, an incomplete filed-issues list, and mis-classifies #1998 as carried.

- **Recommend: rewrite AFTER convergence, from re-derived numbers.** Do not open the PR first.
- **Con:** the branch stays un-reviewed longer. Mitigated — it is now PUSHED, so it is no longer
  at risk while it waits.

### D — #1995 crosses Sweep-N for the FIFTH time (D, E, F, G, H)

Per `value-prioritization.md` MUST-3 the disposition is user-gated: **implement** /
**re-defer with a fresh anchor** / **close with the gate** — never auto-close.

- **Recommend: close it with the gate.** Four sweeps ago the honest read was "genuinely blocked
  on a decision nobody has needed." At five, the more honest read is that nobody intends to do
  it: no workflow runs isort/black on any path, so the 2,022-file rewrite has no consumer.
  **This reverses session G's recommendation** (which was re-defer), on the strength of one more
  cycle of evidence.
- **Con, stated because it cuts the other way:** closing loses the record that the repo has
  repo-wide import-order drift. If that matters, re-defer WITH a fresh anchor is the honest
  alternative — but a sixth silent cycle is not.

### E — Branch is 6 commits BEHIND `origin/main`

Self-caused: exactly the three dependabot merges landed this session. They touch only
`.github/workflows/**`, which this branch does not modify — so there is no conflict.

- **Recommend: do nothing now; merge `origin/main` at PR time.** Rebasing 155 commits to pick up
  three workflow bumps is churn with no benefit.
- **Con:** the PR will show a merge commit. Acceptable.

### F — Semver: BREAKING changes in MINOR bumps (carried, still unanswered)

kaizen 2.46.0 and nexus 2.16.0 carry breaking changes while their CHANGELOGs claim semver
adherence. Pre-existing project-wide pattern (same shape at kaizen 2.40.0/2.38.0/2.37.0).

- **Recommend: ship as-is; treat versioning policy as its own decision.**
- **Con:** a consumer pinning `>=2.x` auto-upgrades into a break.

## 6. Verification status — ALL FIVE TREES ESTABLISHED

The fingerprint protocol (`git status --porcelain` before AND after; numbers VOID unless both
match) held on every run below.

| Tree                             | Result                                | Verdict                                   |
| -------------------------------- | ------------------------------------- | ----------------------------------------- |
| root `tests/unit/`               | 4798 passed, 4 skipped                | **VALID**                                 |
| root `tests/regression/`         | 1567 passed, 2 skipped, 22 deselected | **VALID**                                 |
| `packages/kailash-mcp/`          | 649 passed, 1 skipped                 | **VALID**                                 |
| `packages/kailash-nexus/`        | **2605 passed, 14 skipped, 0 failed** | **VALID** (post-#2007; was 2592 + 13 new) |
| `packages/kailash-kaizen/tests/` | 156 passed, 10 failed, 48 skipped     | **VALID**                                 |
| `packages/kaizen-agents/tests/`  | 3943 passed, 13 failed, 89 skipped    | **VALID**                                 |

**All 23 failures are Tier 2/3 infra-dependent — ZERO outside `tests/deployment/`,
`tests/e2e/`, `tests/integration/`.** No LLM key, no Docker daemon, no `config/*.env` on this
host. One "failure" is the SDK working: its SSRF guard correctly rejected a private-IP ollama
endpoint. The single non-environmental failure is git-proven pre-existing (§3 item 13).

**READ THE SUMMARY LINE, NEVER THE EXIT CODE, for the nexus tree** — a run that passed 2592
tests recorded `EXITCODE=143`, which was the SIGTERM sent to the post-summary hang.

## 7. Recommendation — next steps, in order

1. **Commit the #2007 fix** (§5-A, needs the co-owner's go-ahead), then close **#2007** with the
   SHA.
2. **File the four unfiled items** — the nexus hang, the untracked authz fail-open, the kaizen
   stale-E2E test, the repo-root log write (§3 items 2, 8, 13, 14).
3. **Finish round 5** — lens Tasks 2/3/4 + security hypothesis D — then ONE clean round.
   Counter is ZERO and the nexus surface was re-touched.
4. **Fix the nexus post-summary hang** before `/release`; capture a `main` baseline and a
   thread dump (needs root on macOS).
5. **Rewrite the PR body** from re-derived numbers, drop the convergence claim, merge
   `origin/main`, open the PR.
6. **`/release`** — mcp 0.5.0 → kaizen 2.46.0 → kaizen-agents 0.13.0 → dataflow/nexus/ml →
   kailash 2.63.0 LAST. Raise the **FIVE** extras floors between the sub-package publishes and
   the core tag (`kaizen-agents 0.12.0→0.13.0` is the one a `kailash-`-prefixed pattern misses).
7. **Co-owner gate on #1995** (§5-D) and the semver question (§5-F).
8. BUG queue in §3 order — **#2006 first**.

---

## Sweep-by-sweep raw results

| Sweep                         | Result                                                                                           |
| ----------------------------- | ------------------------------------------------------------------------------------------------ |
| 1 — active todos              | 0                                                                                                |
| 2 — pending journal           | 0 in `journal/.pending/`; **no root `journal/` dir** — 16 entries live in the workspace          |
| 3 — open issues               | **14** (#1970 #1971 #1972 #1974 #1981 #1995 #1997 #2000 #2002–#2007) — was 15; 2 closed, 1 filed |
| 4 — open PRs / branches       | **0 open PRs** (board cleared); 167 local branches; 1 worktree                                   |
| 5 — redteam vs specs          | rounds 2/3/4/5 run; **round 5 INCOMPLETE** — 1 lens ran ¼, hypothesis D unresolved               |
| 6 — workspace/worktree/ledger | 1 worktree (main); session-H ledger current at `04-validate/launch-ledger-sessionH.md`           |
| 7 — process hygiene           | 155 ahead, **6 behind** (own dependabot merges), **0 unpushed**, 9 dirty paths                   |
| 8 — release readiness         | 9/9 version anchors consistent; 9/9 CHANGELOG entries present; extras floors PENDING BY DESIGN   |
| 9 — cross-ecosystem           | N/A — `.claude/VERSION::type = coc-build`                                                        |
| 10 — deferred-quality         | **3 labelled** (#1995 5th cycle GATE OWED §5-D; #2003 + #2005 cycle 1) + 3 enumerated §4         |
