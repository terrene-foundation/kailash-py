# /sweep — Management Decision Report — 2026-08-08 (session H final)

Repo `terrene-foundation/kailash-py` (`.claude/VERSION::type = coc-build`, variant `py`).
Workspace `issue-1720-llm-consolidation`, phase 05-codify. Branch
`fix/issue-1720-forest-drain` @ `d6c6cbb83` — **160 ahead of `origin/main`, 6 behind, 0 UNPUSHED.**

Supersedes `sweep-2026-08-07b.md` (same session, earlier). Its §5-A (uncommitted work) and
§5-D/§5-F (the two co-owner gates) are all RESOLVED.

> **Nothing is at risk.** Working tree holds one untracked test-generated log and nothing else;
> every commit is on the remote, verified by comparing `rev-parse` on both sides rather than
> reading an exit code. This is the first sweep in three sessions that can say that.

---

## 1. Completion status

| Milestone                                 | State                      | Durable receipt                                    |
| ----------------------------------------- | -------------------------- | -------------------------------------------------- |
| Branch pushed                             | **COMPLETE**               | `d6c6cbb83` on both sides; unpushed = 0            |
| All 3 dependabot PRs merged               | **COMPLETE**               | `876a3bf18`, `1be52aafd`, `c11dd4b5d`; board EMPTY |
| All 5 test trees established              | **COMPLETE**               | §6                                                 |
| #1998 / #2001 closed with receipts        | **COMPLETE**               | SHAs confirmed on the remote BEFORE citing         |
| **#2007 filed, fixed, documented**        | **COMPLETE (on branch)**   | `ca56bf501` + `d85fa80f6`; 2605 passed / 0 failed  |
| **Semver claim corrected (Option A)**     | **COMPLETE**               | `805d68179` — 2 packages, correctly scoped         |
| **#1995 root-caused; prep landed**        | **COMPLETE**               | `d6c6cbb83`; diagnosis corrected on the issue      |
| 9 packages version-anchored + CHANGELOG'd | **COMPLETE**               | 9/9 anchors match; 9/9 changelog headings present  |
| **Round 5 / convergence**                 | **NOT MET — counter ZERO** | one lens ran ¼; hypothesis D unresolved            |
| **PR opened**                             | **NOT STARTED**            | body asserts a false convergence claim (§5-B)      |
| **`/release`**                            | **NOT STARTED**            | gated on convergence + the nexus hang (§3 item 1)  |

**Walking skeleton stands.** No shipped path is known-wrong.

## 2. ETA to completion

**3–4 autonomous cycles** to a released, visible product (was 3–5 at the previous sweep).

| Item                                               | Cycles | Basis                                            |
| -------------------------------------------------- | ------ | ------------------------------------------------ |
| Finish round 5 (lens Tasks 2/3/4 + hypothesis D)   | 1      | mechanical sweeps + one focused read             |
| One clean round after it                           | 0.5–1  | counter ZERO; #2007 re-touched the nexus surface |
| Nexus post-summary hang                            | 0.5–1  | needs a `main` baseline + a root thread dump     |
| PR body rewrite + merge `origin/main` + open       | 0.2    | numbers re-derived; convergence claim must go    |
| `/release` — 7 packages, ordered + 5 extras floors | 1      | sequence manual, CI-unenforced                   |
| BUG queue (§3)                                     | 2–3    | #2006 architectural                              |

## 3. Prioritized immediate queue — BUG + INVEST-NOW

Per `product-completion-first.md` MUST-1 (category gates, severity ranks, ambiguous → ON-LIST).

| #   | Item                                                             | Cat        | Value-anchor                                                                                               |
| --- | ---------------------------------------------------------------- | ---------- | ---------------------------------------------------------------------------------------------------------- |
| 1   | **Nexus suite hangs post-summary — NOT FILED**                   | **BUG**    | 116 leaked threads; a green suite that never exits HANGS a CI job, so `/release` cannot trust its own gate |
| 2   | **#2006** dataflow CREATE sends explicit NULL                    | **BUG**    | defeats every column DEFAULT on every unset optional field                                                 |
| 3   | **#1997** Mistral key redaction probabilistic                    | **BUG**    | a credential survives redaction depending on key shape                                                     |
| 4   | **#2004** discovery health-check leaks untrusted cmd             | **BUG**    | one site of its class outside `server.py`                                                                  |
| 5   | **#1974** error_sanitizer gaps (non-http DSN, Slack, JWT)        | **BUG**    | same class as the leaks closed this session; surface still unswept                                         |
| 6   | **#1970 / #1981** provider errors leak / A2A scores 0.0          | **BUG**    | raw provider exception to user surface; every real A2A score 0.0 ⇒ ranking arbitrary                       |
| 7   | **Discovery `except Exception` grants access — UNTRACKED**       | **BUG**    | authz fail-open deferred in-code with sound reasoning, but Rule 1b wants a TRACKING ISSUE                  |
| 8   | **#2002** root `tests/regression/` has no CI                     | INVEST-NOW | 1,564 of 1,591 never run; would have caught round 4's regressions                                          |
| 9   | **Round-5 lens Tasks 2/3/4 + ~37 unaudited test files**          | INVEST-NOW | incl. 3 under `kailash-kaizen/tests/security/` no lane has read                                            |
| 10  | **#1972** nexus 3 failures (2 are STALE tests)                   | INVEST-NOW | fixing the product to green them RE-INTRODUCES the defect — port the tests                                 |
| 11  | **#2000 / #1971** 11s first execution / 63-char id               | INVEST-NOW | fails a nexus e2e concurrency test; wrong-dialect id selection                                             |
| 12  | **kaizen `BaseAgent(description=…)` stale E2E — NOT FILED**      | INVEST-NOW | git-proven pre-existing; a stale test, not a live defect                                                   |
| 13  | **`kaizen_implementation_test.log` repo-root write — NOT FILED** | INVEST-NOW | a test dirties the tree: voids the fingerprint protocol, leaves a CI checkout dirty                        |

Items 1, 7, 12, 13 remain **UNFILED** — filing them is one cycle's opening move.

**#2007 is correctly still OPEN.** `Fixes #2007` auto-closes only when the PR merges to the
default branch; the fix is on the branch, not on `main`. Verified with
`git merge-base --is-ancestor ca56bf501 origin/main` → false. No action.

## 4. Deferred-quality backlog — INCREMENTAL

### `after-milestone:forest-release`

| Item                                           | Blocking-safety                                                      | Acceptance                              |
| ---------------------------------------------- | -------------------------------------------------------------------- | --------------------------------------- |
| **#1995** repo-wide formatter drift            | no workflow runs isort; black gate is scoped to `examples/autonomy/` | `pre-commit run --all-files` modifies 0 |
| **#2003** envelope doubles run-history storage | nothing breaks, nothing leaks; 2x storage                            | persisted ratio ≈ 1.0 vs raw control    |

### `on-demand`

| Item                                                 | Blocking-safety                                         | Acceptance                                      |
| ---------------------------------------------------- | ------------------------------------------------------- | ----------------------------------------------- |
| **#2005** tenant key falls open to global namespace  | not cross-tenant; multi-tenant-enabled deployments only | intent documented + pinned                      |
| MCP enumerator-guard AST blind spots (4 named forms) | latent only — all four verified ABSENT today            | matcher covers Name/Call receivers + bare `for` |
| Cross-CLI skill drift (`.claude` 127 vs 235 lines)   | COC artifact; not release-blocking                      | proposal filed, not in-branch                   |

### Sweep-N revisit gate

- **#1995 — GATE DISCHARGED this session.** It crossed five sweeps (D–H). Rather than a sixth
  deferral OR the close the previous sweep recommended, the co-owner approved scope and the
  root cause was investigated: **the issue's own diagnosis is refuted by measurement** (its
  proposed fix moves the drift 1397 → 1746). Prep landed (`d6c6cbb83`); the repo-wide reformat
  is **re-deferred with a fresh, concrete trigger** — it overlaps 57 files with this branch's
  diff, so it must land AFTER the branch merges. Corrected diagnosis posted to the issue.
- **#2003 / #2005 — cycle 2.** No gate owed until cycle 3.

## 5. Decision points

### A — Convergence is NOT met, and the gap is now the ONLY thing between here and release

Round 5's security lens resolved hypotheses A/B/C; **D is unresolved**. Its test-contract lens
**ran Task 1 of 4**. And `completion-criterion.md` MUST-3: the #2007 fix TOUCHED the nexus
surface, so any clean-round credit it held is **void**.

- **Recommend: finish the four open tasks, then one clean round.** Everything else on the
  critical path is now done.
- **Cons, honestly:** yield is falling — round 2 found 7 HIGH, round 3 found 3, round 4 found
  2, round 5 found 1 (latent).
- **Counter-argument I find stronger:** round 5's one finding was a security control dead since
  before this branch, found only because the lens rotated onto it. The yield is falling on the
  OLD lenses, not on new ones — and two of round 5's own lenses have not finished.

### B — The PR body still asserts a FALSE convergence claim

`04-validate/pr-body-draft.md` states "Convergence was reached." It has not been. Publishing
it would put an unverified durable claim on a public surface.

- **Recommend: rewrite AFTER convergence, from re-derived numbers; merge `origin/main` at the
  same time** (6 behind, all three dependabot merges, no conflict — this branch touches no
  workflow file).
- **Con:** the branch stays un-reviewed longer. Mitigated — it is pushed, so it is not at risk.

### C — Four findings are unfiled

The nexus hang, the untracked authz fail-open, the kaizen stale-E2E test, the repo-root log
write. All four have full evidence in `launch-ledger-sessionH.md`.

- **Recommend: file all four at the start of the next cycle**, before any new work. Evidence
  decays; three of the four were found by instruments that took real effort to get right.

## 6. Verification status — ALL FIVE TREES ESTABLISHED

| Tree                             | Result                                | Verdict                               |
| -------------------------------- | ------------------------------------- | ------------------------------------- |
| root `tests/unit/`               | 4798 passed, 4 skipped                | **VALID**                             |
| root `tests/regression/`         | 1567 passed, 2 skipped, 22 deselected | **VALID**                             |
| `packages/kailash-mcp/`          | 649 passed, 1 skipped                 | **VALID**                             |
| `packages/kailash-nexus/`        | **2605 passed, 14 skipped, 0 failed** | **VALID** (post-#2007: 2592 + 13 new) |
| `packages/kailash-kaizen/tests/` | 156 passed, 10 failed, 48 skipped     | **VALID**                             |
| `packages/kaizen-agents/tests/`  | 3943 passed, 13 failed, 89 skipped    | **VALID**                             |

**All 23 failures are Tier 2/3 infra-dependent** — zero outside `tests/deployment/`,
`tests/e2e/`, `tests/integration/`. No LLM key, no Docker, no `config/*.env` on this host.
One "failure" is the SDK working: its SSRF guard correctly rejected a private-IP endpoint.

**READ THE SUMMARY LINE, NEVER THE EXIT CODE** for the nexus tree.

## 7. Recommendation — next steps, in order

1. **File the four unfiled findings** (§5-C) — evidence is freshest now.
2. **Finish round 5** — lens Tasks 2/3/4 + security hypothesis D — then ONE clean round.
3. **Fix the nexus post-summary hang**; capture a `main` baseline + a thread dump (root on macOS).
4. **Rewrite the PR body**, merge `origin/main`, open the PR.
5. **`/release`** — mcp 0.5.0 → kaizen 2.46.0 → kaizen-agents 0.13.0 → dataflow/nexus/ml →
   kailash 2.63.0 LAST. Raise the **FIVE** extras floors between sub-package publishes and the
   core tag (`kaizen-agents 0.12.0→0.13.0` is the one a `kailash-`-prefixed pattern misses).
6. **After the branch merges:** the #1995 repo-wide reformat, as its own PR.
7. BUG queue in §3 order — **#2006 first**.

---

## Sweep-by-sweep raw results

| Sweep                         | Result                                                                                    |
| ----------------------------- | ----------------------------------------------------------------------------------------- |
| 1 — active todos              | 0                                                                                         |
| 2 — pending journal           | 0 in `journal/.pending/`; no root `journal/`; 16 entries in the workspace                 |
| 3 — open issues               | **14** (#1970 #1971 #1972 #1974 #1981 #1995 #1997 #2000 #2002–#2007)                      |
| 4 — open PRs / branches       | **0 open PRs**; 1 worktree                                                                |
| 5 — redteam vs specs          | rounds 2/3/4/5; **round 5 INCOMPLETE** — 1 lens ran ¼, hypothesis D unresolved            |
| 6 — workspace/worktree/ledger | 1 worktree; session-H ledger + this report current                                        |
| 7 — process hygiene           | 160 ahead, 6 behind (own dependabot merges), **0 unpushed**, 1 untracked test artifact    |
| 8 — release readiness         | 9/9 anchors consistent; 9/9 changelog entries; 2 semver claims remain, both 0.x = CORRECT |
| 9 — cross-ecosystem           | N/A — `type: coc-build`                                                                   |
| 10 — deferred-quality         | 3 labelled (#1995 gate DISCHARGED; #2003/#2005 cycle 2) + 3 enumerated §4                 |
