# /sweep — Management Decision Report — 2026-08-06 (session F close)

Repo `terrene-foundation/kailash-py` (`coc-build`, py). Workspace
`issue-1720-llm-consolidation`, phase 05-codify. Branch `fix/issue-1720-forest-drain`
@ `3a642d188` — **96 ahead of main, 28 UNPUSHED, and 62 files DIRTY.**

Supersedes `sweep-2026-08-05.md`. That report's §5-A release-gate recommendation still
stands (convergence not met); its §5-B/§5-F dispositions are updated below.

> **THE SINGLE MOST IMPORTANT FACT IN THIS REPORT: session F produced ~62 files of
> work and committed NONE of it.** Every "complete" row below is complete _in the
> working tree_. A `git checkout`/`reset`/`clean` loses the entire session.

---

## 1. Completion status

Every claim cites a durable receipt (`verify-resource-existence.md` MUST-4). **Receipts
this session are orchestrator-run test evidence, NOT commit SHAs** — that is the caveat
above, stated per-row rather than assumed.

| Milestone                                   | State                      | Durable receipt                                      |
| ------------------------------------------- | -------------------------- | ---------------------------------------------------- |
| 7 packages version-anchored atomically      | **COMPLETE (uncommitted)** | `tomllib`+regex re-derivation, 0 split-state         |
| kailash-mcp added to the release set        | **COMPLETE (uncommitted)** | was MISSING from session E's list entirely           |
| Core multi-channel parity (`get_inputs`)    | **COMPLETE (uncommitted)** | parity suite 7 passed                                |
| Envelope binding across ALL entry points    | **COMPLETE (uncommitted)** | shared binder + AST guard, 16 passed; 7th site found |
| `rate_limit=None` unconditional 500         | **COMPLETE (uncommitted)** | 12 passed; documented behaviour had never worked     |
| Rate-limit WARN predicate/runtime mismatch  | **COMPLETE (uncommitted)** | red-then-green, both directions                      |
| Rate-limiter IP-dimension unbounded         | **COMPLETE (uncommitted)** | RED at 10500 entries vs 10000 cap                    |
| discovery len/items guards (both branches)  | **COMPLETE (uncommitted)** | orchestrator RED: 11 failed pre-fix / 640 pass post  |
| advisory-memo saturation flood              | **COMPLETE (uncommitted)** | now rate-limits, not floods                          |
| 9 stale mcp permission tests ported         | **COMPLETE (uncommitted)** | `tests/unit/mcp_server/` 645 passed (was 9 failed)   |
| `specs/mcp-server.md` blessed the bypass ×2 | **COMPLETE (uncommitted)** | both `:141` and `:694` fixed; `mcp-auth.md` too      |
| Private org slug in `specs/`                | **COMPLETE (uncommitted)** | 5 files, 2 spelling variants; specs now clean        |
| **Redteam to convergence**                  | **NOT MET — counter ZERO** | round 1 found 2 HIGH + 2 release-blocking            |
| **Commit / push / PR**                      | **NOT DONE**               | 28 unpushed + 62 dirty                               |
| **`/release`**                              | **NOT DONE**               | gated on convergence                                 |

**Walking skeleton stands. No shipped path is known-wrong.** What is NOT done is
convergence, then the release.

## 2. ETA to completion

**4–6 autonomous cycles** to a released, visible product.

| Item                                         | Cycles | Basis                                                  |
| -------------------------------------------- | ------ | ------------------------------------------------------ |
| Commit the 62-file tree in reviewable slices | 0.5    | mechanical; must precede everything                    |
| Round 2 over the NEW surface + round 3 clean | 1–2    | surface changed materially ⇒ counter genuinely 0       |
| `/release`, 7 packages, ordered              | 1      | sequence is manual + CI-unenforced                     |
| BUG queue (#1998 architectural, +4)          | 2–3    | #1998 is a fix at the registration layer, not a filter |

Excludes the INCREMENTAL backlog (§4), which does not gate release.

## 3. Prioritized immediate queue — BUG + INVEST-NOW

Classified per `product-completion-first.md` MUST-1 (category gates, severity ranks,
ambiguous resolves ON-LIST). Value-anchored per `value-prioritization.md` MUST-1.

| #   | Item                                                      | Cat        | Value-anchor                                                                                    |
| --- | --------------------------------------------------------- | ---------- | ----------------------------------------------------------------------------------------------- |
| 1   | **Commit the working tree**                               | **BUG**    | ~62 files of verified security work exist ONLY in the tree; any reset destroys the session      |
| 2   | **#1998** production stdio bypasses every gate            | **BUG**    | gated tools disclose full schema + `Args:`; `disable_tool()`'d tools EXECUTE. Anonymous reach   |
| 3   | **#1997** Mistral key redaction probabilistic             | **BUG**    | a credential survives redaction depending on key shape                                          |
| 4   | **#1974** error_sanitizer gaps (non-http DSN, Slack, JWT) | **BUG**    | same class as the leaks closed this session, surface still unswept                              |
| 5   | **#1970** MultiProviderNode leaks raw provider errors     | **BUG**    | raw provider exception reaches the user surface                                                 |
| 6   | **#1981** A2A scores always 0.0 without structured output | **BUG**    | every real score is 0.0 ⇒ ranking arbitrary; made LOUD in a prior session, not fixed            |
| 7   | **`bash_tool.py:65,77-79`** unscrubbed `command` echo     | INVEST-NOW | model-authored string may carry `Authorization: Bearer …` into ToolResult + logs. **NOT FILED** |
| 8   | **20 regression failures CI never runs**                  | INVEST-NOW | `unified-ci.yml:140-143` omits `tests/regression/`. Proven pre-existing. **NOT FILED**          |
| 9   | **#2000** eager torch/sklearn ⇒ 11.4s first execution     | INVEST-NOW | fails nexus e2e `test_concurrent_requests`                                                      |
| 10  | **#1972** nexus 3 failures                                | INVEST-NOW | 2 of 3 are STALE TESTS; port them — fixing the product re-introduces the defect                 |

**Nothing was relabelled to escape convergence.** Items 2–6 are pre-existing and were
correctly out of session F's shards. Items 7–8 are NEW findings from round 1 and are
unfiled — filing needs the co-owner gate (§5-C).

## 4. Deferred-quality backlog — INCREMENTAL

### `after-milestone:forest-release`

| Item                                              | Blocking-safety                            | Acceptance                              |
| ------------------------------------------------- | ------------------------------------------ | --------------------------------------- |
| **#1995** pre-commit rewrites 2,022 files (isort) | no workflow runs isort/black on any path   | `pre-commit run --all-files` modifies 0 |
| 3 dependabot PRs (#1988/#1989/#1990)              | CI green; action-version bumps only        | merged or closed                        |
| 31 residual mypy/pyright findings in nexus        | pre-existing; none introduced this session | checker delta ≤ 0 vs HEAD               |

### `on-demand`

| Item                                                        | Blocking-safety                                        | Acceptance                      |
| ----------------------------------------------------------- | ------------------------------------------------------ | ------------------------------- |
| 35 bare `kailash-rs` mentions in `specs/`                   | names a sibling, not where it lives — org slug is gone | n/a — may be correct as-is      |
| `cli_channel.py:805-815` `__del__` calls `close()`          | pre-existing; `patterns.md` blocks it                  | finalizer no longer calls close |
| **#1996** trust-plane FastMCP guard — **FIXED, still open** | delivered by `45ccac417`                               | closed citing the SHA (§5-B)    |
| F-STUBS marker baseline (29 prod TODO)                      | pre-existing                                           | **do-not-requeue** (standing)   |

### Sweep-N revisit gate — items deferred ≥2 sessions

- **#1995** — filed 2026-08-03; now spans sessions **D, E, and F**. Crossed the ≥2-session
  line last cycle and was re-deferred; **still owed a "still wanted?" gate** (§5-E).
- **F-STUBS** — standing user call 2026-06-26, `do-not-requeue`. **Not re-surfaced.**

## 5. Decision points

### A — Release gate: convergence is NOT met, and round 1 proved why

Round 1 (3 fresh reviewers, rotated lenses, all with genuine ran-signals) found **2 HIGH +
2 release-blocking + 6 MEDIUM/LOW**. Two of the HIGHs were defects in fixes made _this
session_ — bringing this branch to **five defects introduced by corrections that looked
right**.

- **Recommend: commit, then run round 2 over the new surface, then a clean round 3.**
  The counter genuinely resets — the surface changed materially (a new shared binder, six
  rebound entry points, the rate-limit wrapper, both discovery guards).
- **Pros:** the release rests on convergence rather than on a trend that has now been
  falsified twice on this exact branch.
- **Cons, honestly:** 2 more cycles on a branch already 96 commits deep, and what remains
  after round 1's fixes is MEDIUM/LOW, not the CRITICAL class that justified rounds 1–5 in
  session E. A reasonable reader could call the marginal return low.
- **Alternative:** draw the line at "all known HIGH+ fixed, MEDIUM/LOW documented" and
  release. Defensible — but the release notes MUST then state which basis was used, because
  it is materially weaker than two clean rounds and the next reader cannot otherwise tell.

### B — #1996 is FIXED and still OPEN; close it with the SHA

`45ccac417` delivers it. **Verified this session with a discriminating instrument** — a
`find_spec`-based blocker proved to discriminate first (`import mcp.server` → ImportError),
then: module imports with `mcp` absent, and `get_server()` raises the typed, actionable
error naming the extra. (An earlier attempt used `find_module`, removed in Python 3.12,
and was INERT — its result was discarded, not reported.)

**Recommend: close citing `45ccac417`** (`git.md` — closure without a code reference is
BLOCKED). Needs the co-owner gate; issue writes are shared state.

### C — Two NEW round-1 findings are UNFILED

`bash_tool.py`'s unscrubbed `command` echo (queue item 7) and the 20-regression-failures/CI-gap
(item 8) have no tracker. **That is the state most likely to be lost at `/clear`.**

- **Recommend: file both.** Con: two more issues on an already-10-deep backlog. Mitigated —
  an unfiled finding that took a full review round to surface is far more expensive to
  rediscover than to track.

### D — The private org slug is in 21 tracked files of a PUBLIC repo

`terrene-foundation/kailash-py` is PUBLIC. `specs/` is now clean (5 files, 2 spelling
variants). **21 files remain** — deploy notes, cross-SDK test vectors, 6 root sweep reports
— on `main` since ~2026-06-16 and **in git history**.

- **Recommend: do NOT fold this into the release.** History retains it, so file edits are
  partial mitigation; it is unrelated to this branch; and whether an org NAME (not a
  credential) is sensitive is the co-owner's call.
- **Con, stated plainly:** it is the exact token `cross-sdk-inspection.md` Rule 6 keeps out
  of world-readable artifacts, and "already leaked" is how disclosures stay leaked.

### E — #1995 crosses Sweep-N again (sessions D, E, F)

Per `value-prioritization.md` MUST-3 the disposition is user-gated: **implement** /
**re-defer with a fresh anchor** / **close with the gate** — never auto-close as
`not_planned`.

- **Recommend: re-defer with a fresh anchor** to `after-milestone:forest-release`. It is
  genuinely blocked on a decision nobody has needed yet (whether isort runs repo-wide), and
  no workflow runs isort/black on any path today.

### F — spec-drift / `sweep-redteam.py` retirement (carried from session E §5-F)

Unchanged and **not actioned this session**. Recommendation stands: retire
`sweep-redteam.py`, port its `coverage_gap` + `stub` checks into the mature spec-drift gate.
Cost: `rules/sweep-completeness.md` names it as the mandated Sweep-5 tool, so retiring it is
a coordinated COC-artifact change.

## 6. Recommendation — next steps, in order

1. **Commit the working tree** in reviewable slices (security fixes / release artifacts /
   specs / tests). Nothing else is safe until this happens.
2. **Round 2** over the new surface, rotated lenses; then **round 3** to clean (Decision A).
3. **Close #1996** with `45ccac417`; **file the two unfiled findings** (Decisions B, C).
4. **Push** (28 + the new commits) and open the PR. **Do not open it before the 9 ported
   mcp tests are in** — `tests/unit/` IS in CI and they would red it.
5. **`/release`** in the mandated order — mcp 0.5.0 → kaizen 2.46.0 → kaizen-agents 0.13.0
   → dataflow/nexus/ml → kailash 2.63.0 last. Run the pre-tag floor script first; it
   **currently FAILS correctly** (2 floors unsatisfiable until mcp publishes).
6. **Raise the extras floors** — REQUIRED between the sub-package publishes and the core tag.
7. Then the BUG queue in §3 order — **#1998 first**.

---

## Sweep-by-sweep raw results

| Sweep                         | Result                                                                                                                                         |
| ----------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| 1 — active todos              | 0                                                                                                                                              |
| 2 — pending journal           | **5** in `journal/.pending/` — unresolved, carried                                                                                             |
| 3 — open issues               | 10 (#1970/#1971/#1972/#1974/#1981/#1995/#1996/#1997/#1998/#2000)                                                                               |
| 4 — open PRs / branches       | 3, all clean dependabot (#1988/#1989/#1990)                                                                                                    |
| 5 — redteam vs specs          | round 1 run, NOT clean; `sweep-redteam.py` retirement still open (§5-F)                                                                        |
| 6 — workspace/worktree/ledger | 1 worktree (main); ledger + release-order note current                                                                                         |
| 7 — process hygiene           | 96 ahead of main, **28 unpushed, 62 DIRTY**                                                                                                    |
| 8 — release readiness         | 250 commits since `mcp-v0.4.3`; 180 since `kaizen-agents-v0.12.0`; 118 since `kaizen-v2.45.0` and `v2.62.0`; all 7 anchors bumped, uncommitted |
| 9 — cross-ecosystem           | N/A — `type: coc-build`                                                                                                                        |
| 10 — deferred-quality         | 1 labelled (#1995) — **Sweep-N gate OWED, 3rd session** (§5-E) + 7 enumerated §4                                                               |
