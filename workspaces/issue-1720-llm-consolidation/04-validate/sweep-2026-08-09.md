# /sweep — Management Decision Report — 2026-08-09 (session I close)

Repo `terrene-foundation/kailash-py` (`.claude/VERSION::type = coc-build`, variant `py`).
Workspace `issue-1720-llm-consolidation`, phase 05-codify. Branch
`fix/issue-1720-forest-drain` @ `b220704b5` — **212 ahead of `origin/main`, 6 behind,
0 UNPUSHED, 0 uncommitted.**

Supersedes `sweep-2026-08-08.md`. Classification per `product-completion-first.md` MUST-1
(category gates, severity ranks, ambiguous → ON-LIST).

> **Nothing is at risk.** Every commit is on the remote — verified by comparing `rev-parse`
> on both sides, not by reading a push exit code (a piped push reports the pipe's status;
> that is how a rejected push once read as success here).

---

## 1. Completion status

| Milestone                                    | State                      | Durable receipt                                     |
| -------------------------------------------- | -------------------------- | --------------------------------------------------- |
| 51 commits landed + pushed                   | **COMPLETE**               | `b220704b5` on both sides; unpushed = 0             |
| Credential-leak class closed (kaizen-agents) | **COMPLETE**               | `689f9ebd8` `04f7c6258` `934d5f8ae` `d6030aefe`     |
| Sink scanner taught its 2 blind shapes       | **COMPLETE**               | `6a6e54541` — found 6 more sinks on first run       |
| Shutdown wedge (382-day production defect)   | **COMPLETE**               | `a50fb78c6`; nexus tree now exits 0 unaided         |
| False-success family (4 sites)               | **COMPLETE**               | `7333e90de` `62d6cf6f3` `4772d0c48` `bb8a3f966`     |
| Metrics percentiles scrapeable               | **COMPLETE**               | `4036b4c96` — `prometheus_client` as the oracle     |
| Six findings filed                           | **COMPLETE**               | #2008–#2013, co-owner approved                      |
| 9 packages version-anchored                  | **COMPLETE**               | root 2.63.0 == `__init__`; 8 packages consistent    |
| PR body written, every figure SHA-pinned     | **COMPLETE**               | `eb7f9ae01` content, frozen                         |
| Codify evidence §0–§3, provenance-graded     | **COMPLETE**               | `b417a83f9` `ce96848ae` `95b142320` `31de87487`     |
| **Clean round / convergence**                | **NOT MET — counter ZERO** | first attempt died on quota; re-dispatched, running |
| **PR opened**                                | **NOT STARTED**            | gated on convergence                                |
| **`/release`**                               | **NOT STARTED**            | gated on convergence + PR                           |

**Walking skeleton stands.** No shipped path is known-wrong. Two paths were known-wrong at
session start and are now fixed (credentials in logs; a suite that hung after passing).

## 2. ETA to completion

**2–3 autonomous cycles** to a released, visible product (was 3–4 at the previous sweep).

| Item                                          | Cycles | Basis                                         |
| --------------------------------------------- | ------ | --------------------------------------------- |
| Clean round to convergence                    | 0.5–1  | two lenses dispatched; counter ZERO           |
| PR body count re-derive + merge `origin/main` | 0.2    | 6 behind, zero overlap with the 376-file diff |
| `/release` — 7 packages, ordered + 5 floors   | 1      | sequence manual, CI-unenforced                |
| BUG queue (§3 ranks 1–3)                      | 1–1.5  | #2013 needs a security-reviewed partition     |

## 3. Prioritized immediate queue — BUG + INVEST-NOW (gates convergence)

Value-anchored per `value-prioritization.md` MUST-1.

| #   | Item                                                      | Cat        | Value-anchor                                                                                                                                                                         |
| --- | --------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | **#2013** `enable_auth` facade INERT                      | **BUG**    | A documented production security control that installs nothing. README tells DevOps readers to enable it. ≥140 days, still in the latest tag. Highest user-facing harm on the board. |
| 2   | **#2006** dataflow CREATE sends explicit NULL             | **BUG**    | Defeats every column DEFAULT on every unset optional field — silent data-shape corruption                                                                                            |
| 3   | **#2012** 390 un-triaged exception sinks                  | **BUG**    | Un-triaged surface; an unknown subset are live credential channels. Start at the 8 half-swept files (28 sites).                                                                      |
| 4   | **#1997** Mistral key redaction probabilistic             | **BUG**    | A credential survives redaction depending on key shape                                                                                                                               |
| 5   | **#2004** discovery health-check leaks untrusted cmd      | **BUG**    | One site of its class outside `server.py`                                                                                                                                            |
| 6   | **#1974** error_sanitizer gaps (non-http DSN, Slack, JWT) | **BUG**    | Same class as the leaks closed this session; surface still unswept                                                                                                                   |
| 7   | **#1970 / #1981** provider errors leak / A2A scores 0.0   | **BUG**    | Raw provider exception to a user surface; every real A2A score 0.0 ⇒ ranking arbitrary                                                                                               |
| 8   | **#2009** discovery `except Exception` grants access      | **BUG**    | Fail-open authz — deliberately deferred WITH the Rule-1b tracking receipt now filed                                                                                                  |
| 9   | **#2002** root `tests/regression/` has no CI              | INVEST-NOW | 1,564 of 1,591 never run; would have caught round-4 regressions                                                                                                                      |
| 10  | **#2011** a test writes into the repo root                | INVEST-NOW | Voids the run-fingerprint protocol; dirties CI checkouts                                                                                                                             |
| 11  | **#2010** stale `BaseAgent(description=)` E2E             | INVEST-NOW | Git-proven pre-existing; a stale test, not a live defect                                                                                                                             |
| 12  | **#1972** nexus 3 failures (2 are STALE tests)            | INVEST-NOW | Fixing the product to green them RE-INTRODUCES the defect — port the tests                                                                                                           |
| 13  | **#2000 / #1971** 11s first execution / 63-char id        | INVEST-NOW | Fails a nexus e2e concurrency test; wrong-dialect id selection                                                                                                                       |

**#2007 and #2008 are FIXED on this branch and correctly still OPEN** — `Fixes` auto-closes
only at merge to the default branch.

## 4. Deferred-quality backlog — INCREMENTAL

Three items carry the `deferred-quality` label with the four generalized Rule-1b conditions.

### `after-milestone:forest-release`

| Item                                           | Blocking-safety                                                   | Acceptance                              |
| ---------------------------------------------- | ----------------------------------------------------------------- | --------------------------------------- |
| **#1995** repo-wide formatter drift            | no workflow runs isort; black gate scoped to `examples/autonomy/` | `pre-commit run --all-files` modifies 0 |
| **#2003** envelope doubles run-history storage | nothing breaks, nothing leaks; 2x storage                         | persisted ratio ≈ 1.0 vs raw control    |

### `on-demand`

| Item                                                | Blocking-safety                                         | Acceptance                 |
| --------------------------------------------------- | ------------------------------------------------------- | -------------------------- |
| **#2005** tenant key falls open to global namespace | not cross-tenant; multi-tenant-enabled deployments only | intent documented + pinned |

### Sweep-N revisit gate — **TWO ITEMS ARE NOW OWED A "STILL WANTED?" GATE**

- **#2003 and #2005 have crossed THREE sweep cycles** (2026-08-07b, -08-08, this one).
  `value-prioritization.md` MUST-3 requires a re-validation gate at ≥2 cycles. **Surfaced in
  §5-C below** — not silently re-deferred for a fourth time.
- **#1995 — gate DISCHARGED in session H** (its own diagnosis was measurement-refuted; prep
  landed at `d6030aefe`'s predecessor `d6c6cbb83`). Its trigger is concrete and unfired:
  the reformat overlaps 57 files with this branch's diff, so it lands AFTER merge. No gate owed.

## 5. Decision points

### A — Convergence is NOT met, and it is the only thing between here and release

The first clean round was dispatched and **both lenses died on a session quota limit before
delivering**. A failed agent is ZERO evidence, never a clean round (`agents.md` § Redteam
Reviewer Dispatch; `evidence-first-claims.md` MUST-3). Re-dispatched at `b220704b5`.

- **Recommend: let the re-dispatched round finish, then judge.** Everything else on the
  critical path is done and pushed.
- **Cons, honestly:** this is the second attempt, and a round that has to be re-run costs a
  cycle. Nothing about the code changed between attempts, so the re-run is pure repetition.
- **Counter-argument I find stronger:** the alternative is recording a convergence that no
  instrument produced — which is precisely the class this session spent itself removing.

### B — #2013 is the highest-value open item and it is NOT this branch's to fix

`enable_auth` is inert: a README-documented production security control that installs nothing.
Pre-existing (≥140 days), so it does not gate this branch's convergence.

- **Recommend: interim WARN now, full fix as its own shard.** A loud one-time WARN at
  `enable_auth()` naming it a no-op and pointing at `NexusAuthPlugin` is one line, is
  `security.md` § Secure-Default's prescribed path when backward-compat forbids fail-closed,
  and stops the README from promising a protection that is not installed.
- **Cons:** a WARN is not protection; a deployment that reads it still has to act. And landing
  even one line on an auth surface inside a 212-commit release branch widens the blast radius
  of a PR that is otherwise converged.
- **Alternative if you prefer zero risk here:** file-only, fix in the first post-release cycle.
  The exposure is already ≥140 days; one more cycle changes little.

### C — #2003 and #2005 are owed a "still wanted?" gate (Sweep-N, cycle 3)

Both have crossed three sweep cycles without pickup. `value-prioritization.md` Origin records
7-of-7 deferred items decaying rather than being picked up; the gate exists to stop that.

- **Recommend: re-defer BOTH with fresh anchors, do not close.** #2003 (2x storage) has a
  concrete acceptance test and no safety impact; #2005 is explicitly a Question, and its
  blocking-safety note holds (not cross-tenant). Neither has decayed in value — they have
  simply never been the most valuable thing available.
- **Cons of re-deferring:** this is the third deferral, which is exactly the pattern the gate
  is designed to interrupt; a fourth would be indistinguishable from forgetting.
- **The disposition is yours** — `value-prioritization.md` MUST-4 blocks the agent from
  closing either as `not_planned` without your gate.

## 6. Recommendation — next steps, in order

1. **Let the clean round finish**; judge convergence across both lenses.
2. **Re-derive the git counts** (perishable; suite numbers are stable), merge `origin/main`
   (6 behind, zero overlap with the 376-file diff), **open the PR**.
3. **`/release`** — mcp 0.5.0 → kaizen 2.46.0 → kaizen-agents 0.13.0 → dataflow/nexus/ml →
   kailash 2.63.0 LAST. Raise the FIVE extras floors between sub-package publishes and the
   core tag (`kaizen-agents 0.12.0→0.13.0` is the one a `kailash-`-prefixed pattern misses).
4. **#2013 interim WARN** (§5-B), or file-only if you prefer zero branch risk.
5. **After merge:** the #1995 repo-wide reformat, own PR.
6. **BUG queue in §3 order** — #2013 full fix, then #2006.

---

## Sweep-by-sweep raw results

| Sweep                         | Result                                                                               |
| ----------------------------- | ------------------------------------------------------------------------------------ |
| 1 — active todos              | 0                                                                                    |
| 2 — pending journal           | 0 in `journal/.pending/`; 16 in the workspace                                        |
| 3 — open issues               | **20** — 6 filed this session (#2008–#2013)                                          |
| 4 — open PRs / branches       | **0 open PRs**; 1 worktree                                                           |
| 5 — redteam vs specs          | rounds 2/3/4/5 complete; **clean round RE-DISPATCHED, not yet returned**             |
| 6 — workspace/worktree/ledger | 1 worktree; session-I ledger + codify evidence §0–§3 + this report current           |
| 7 — process hygiene           | 212 ahead, 6 behind, **0 unpushed, 0 uncommitted**, 1 untracked (the #2011 artifact) |
| 8 — release readiness         | 9/9 anchors consistent; 5 extras floors pending by design until sub-packages publish |
| 9 — cross-ecosystem           | N/A — `type: coc-build`                                                              |
| 10 — deferred-quality         | 3 labelled (#1995 gate discharged; **#2003 + #2005 owed a cycle-3 gate — §5-C**)     |
