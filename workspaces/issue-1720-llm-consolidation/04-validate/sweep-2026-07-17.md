# /sweep — kailash-py — 2026-07-17

Repo-wide outstanding-work audit (Sweeps 1–9) after the #1720 creds-in-logs
release (kaizen 2.34.1). BUILD repo; run from `main @ 920003b3c`.

## Sweep results

| #   | Sweep                      | Result                                                                                                                                                       |
| --- | -------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | Active todos               | **0** across all workspaces — clean                                                                                                                          |
| 2   | Pending journals           | 3 found → 1 discarded (codified), 2 belong to sdk-backlog cadence (see F7)                                                                                   |
| 3   | Open GH issues             | **3**, all `cross-sdk`: #1720, #1727, #1779 (see F1–F3)                                                                                                      |
| 4   | Open PRs / branches        | **1**: #1782 (loom Gate-2 sync, CI green) — user's call (F8)                                                                                                 |
| 5   | Redteam gaps (tool)        | `sweep-redteam:v1:OK specs=79 symbols=233 orphans=222 coverage_gaps=9 stubs=0` — 222 dominated by FALSE POSITIVES (F6)                                       |
| 6   | Workspace/worktree hygiene | 5 orphan worktrees (all merged) → **FIXED (pruned)**; no stale notes/pending                                                                                 |
| 7   | Process hygiene            | in sync with origin/main (0/0); uncommitted = this session's workspace artifacts (committed in this report's PR)                                             |
| 8   | Release readiness          | **Nothing genuinely unreleased** — all 8 packages main==PyPI (sibling-drift sweep clean); 76 commits since `v2.53.0` all shipped under package-prefixed tags |
| 9   | Cross-ecosystem            | **N/A** — BUILD repo, not the loom orchestration root                                                                                                        |

## Findings (value-ranked)

### F1 · [HIGH] #1720 Wave C — legacy `providers/llm/` partial delete

- **Anchor (user):** #1720 tracker — the terminal goal of the consolidation driven all cycle.
- **Status:** SOAK-DEFERRED (2.34.0/2.34.1 released today; zero soak elapsed). Irreversible delete → re-confirm at fire point.
- **Scope:** surgical prune (keep `azure_ai_foundry`) — `base.py`/`registry.py` pruned + `provider_names.py` parity, 3 OPEN Qs, ~35–69 test-file sweep, barrel-importer migration. Own analyze→shard→redteam mini-cycle (≥5 invariants).
- **Files:** `providers/llm/*`, `providers/registry.py`, `provider_names.py`, `nodes/ai` barrels, ~35–69 tests.

### F2 · [MED-HIGH] #1779 — opt-in `governance_required` posture for direct LLM egress

- **Anchor (user):** #1779 (user-filed feature).
- **Status:** open; needs `/analyze` (design + surface).
- **Files (est.):** `governance/`, PACT integration, possibly `nodes/ai/llm_agent.py` egress point.

### F3 · [MED] #1727 — `max_completion_tokens` for GPT-5/o-series on four-axis openai

- **Anchor (user):** #1727 (user-filed correctness).
- **Status:** open — **VERIFY FIRST**: `providers/llm/openai.py` already has `_filter_reasoning_model_params` + `_requires_temperature_1` (gpt-5) + `max_tokens→max_completion_tokens` translation; the gap may be partially/fully closed on the four-axis path. `/analyze` confirms real gap before implementing.
- **Files:** `providers/llm/openai.py`, four-axis wire/`deployment_resolver`.

### F4 · [MED] Pre-existing test failures (8) — unrelated to #1720

- **Anchor:** code-health (SECONDARY) — clean test run.
- **Confirmed pre-existing on clean HEAD (stash test).** Two sub-areas:
  - (a) `ollama_model_manager` vision-setup + `ollama_availability` (5) — `providers/ollama_*`.
  - (b) `example_validation` gallery-docs (3) — `tests/examples/`, gallery docs.
- Outside the CI gate (2.34.x released green with them present).

### F5 · [MED-LOW] `fastapi` eager-import in `kaizen.monitoring`

- **Anchor:** code-health (SECONDARY) — clean-venv installability (`deployment.md` § eager-transitive-deps).
- **Status:** pre-existing (2.34.0 had it). `import kaizen.monitoring` → `dashboard` → `fastapi` fails on a clean venv without fastapi. Fix = guard the eager import (optional-extra) OR declare fastapi core.
- **Files:** `monitoring/__init__.py`, `monitoring/dashboard.py`, `pyproject.toml`.

### F6 · [LOW] Repo-wide spec-vs-code drift — 222 flags, mostly tool false-positives

- **Anchor:** code-health (SECONDARY) — spec hygiene.
- **Status:** `tools/sweep-redteam.py` flags spec prose that references stdlib (`secrets.compare_digest`, `hmac.compare_digest`, `torch.distributed`), config files (`pytest.ini`, `conftest.py`), instance attributes (`self._lock`), and illustrative snippets as "orphans". Real signal (if any) is buried. Disposition: harden the tool's allowlist (skip stdlib/config/instance-attr/illustrative), THEN triage the residual. NOT a mass code fix.
- **Files:** `specs/*.md`, `tools/sweep-redteam.py`.

### F7 · [LOW] sdk-backlog pending journals (2)

- 2 RISK entries (mcp-oauth SSRF #1712, mcp tool-result conformance #1712-A) belong to the **sdk-backlog** workspace's cadence — disposition when that workstream resumes, not here.

### F8 · [user-gate] PR #1782 — loom Gate-2 governance-artifact sync

- CI green; modifies `.claude/` rules. Not auto-merged (policy). Your call to merge or hold.

---

## Consolidated plan — clustered for parallel waves

Clustering principle: shards in one parallel wave touch **non-overlapping file
domains** (`worktree-isolation.md` Rule 4). The provider/LLM surface (F1/F3, and
F4a ollama) overlaps heavily → serialized; everything else is independent.

### WAVE 1 — parallel, independent domains (run now, 3 concurrent worktree shards + 1)

| Shard | Item                                                                             | File domain                     | Invariants                 | Notes                                   |
| ----- | -------------------------------------------------------------------------------- | ------------------------------- | -------------------------- | --------------------------------------- |
| 1A    | F4b example-gallery + doc-structure test fixes                                   | `tests/examples/`, gallery docs | low                        | mechanical; no src                      |
| 1B    | F5 fastapi eager-import hygiene                                                  | `monitoring/`, `pyproject.toml` | low (1: clean-venv import) | guard optional-extra                    |
| 1C    | F6 spec-drift triage — harden `sweep-redteam` allowlist + triage residual        | `tools/`, `specs/*.md`          | low-med                    | tool FP cleanup first                   |
| 1D    | F2 #1779 governance egress — **`/analyze` only this wave** (design, no impl yet) | (analysis)                      | n/a                        | de-risk overlap w/ nodes/ai before impl |

→ 1A/1B/1C are file-disjoint → true parallel (cap 3 cold-start). 1D is analysis-only (no file writes) → safe alongside.

### WAVE 2 — provider/LLM surface (serial; after Wave 1; NON-soak)

| Shard | Item                                                                | Gate                                       |
| ----- | ------------------------------------------------------------------- | ------------------------------------------ |
| 2A    | F3 #1727 max_completion_tokens — **verify-then-fix** (may be no-op) | `/analyze` proves a real gap first         |
| 2B    | F2 #1779 governance egress — implement per Wave-1 1D design         | only if 1D scoped it clear of Wave-C files |

→ 2A and 2B may parallelize IF 1D confirms #1779 doesn't touch `providers/llm`/`nodes/ai` egress that 2A touches; else serial.

### WAVE 3 — #1720 Wave C (soak-gated, own mini-cycle)

- F1 — fires only when 2.34.1 shims have soaked a real cadence. Own analyze→todos→shard→redteam. **Subsumes F4a** (Wave C deletes the legacy ollama providers; fix the top-level `ollama_model_manager` tests within Wave C's test sweep to avoid double-work). Re-confirm the irreversible delete at fire point.

### Not in a wave (housekeeping / user-gate)

- F8 PR #1782 — your merge/hold decision.
- F7 sdk-backlog journals — that workspace's cadence.

## Recommended next-session pick (ranked)

1. **Wave 1 (parallel)** — highest throughput-per-session; 3 disjoint low-invariant shards + the #1779 analysis. Clears the code-health backlog (F4b/F5/F6) and de-risks F2 in one parallel wave.
2. **Wave 2** — after Wave 1; verify #1727 (likely small), implement #1779.
3. **Wave 3 (Wave C)** — only after soak; the #1720 terminal delete.

Sweep-5 sentinel: `<!-- sweep-redteam:v1:OK specs=79 symbols=233 orphans=222 coverage_gaps=9 stubs=0 -->`
Sweep-9 sentinel: `<!-- sweep-ecosystem:v1:N/A reason=not-orchestration-root -->`
