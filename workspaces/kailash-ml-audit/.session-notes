# Session Notes — 2026-04-22

## Where we are

kailash-ml 1.0.0 `/implement` phase, branch
`feat/kailash-ml-1.0.0-m1-foundations`, local only. W19 partial-slice
in progress. 20 of 38 waves landed (W16–W18 complete, M5 Milestone
DONE). **W19** split into two sub-shards:

- **W19.a (this session)** — MUST 1b env authority chain wire +
  Tier-1 invariant test coverage for 9 W19 invariants against the
  existing engine surface. 20 new tests (20/20 green).
- **W19.b (next session)** — MUST 2 full six-primitive default
  construction (ConnectionManager + ExperimentTracker +
  ModelRegistry + FeatureStore + ArtifactStore + TrainingPipeline
  auto-wired on first async entry). Currently lazy paths exist for
  registry/ConnectionManager via `_ensure_registry_for_read`;
  unifying into `_ensure_default_primitives_async()` is a separate
  200+ LOC shard.

## W19.a — env authority chain + invariant tests (done, this session)

- **W19.a edit** — `kailash_ml/engine.py::_default_store_url` now
  delegates to `kailash_ml._env.resolve_store_url(None)` (MUST 1b
  single-site env-var plumbing). Hand-rolled `os.environ.get(...)`
  at the engine site removed; the engine now shares the exact
  authority chain with tracker/registry/feature-store: explicit
  kwarg → `KAILASH_ML_STORE_URL` → legacy `KAILASH_ML_TRACKER_DB`
  (DEBUG log once; raises `EnvVarDeprecatedError` when
  `KAILASH_ML_STRICT_ENV=1`) → `sqlite:///~/.kailash_ml/ml.db`.
- **W19.a tests** — `tests/unit/test_w19_engine_surface.py` with
  20 Tier-1 tests covering 7 of the 9 invariants:
  - Inv 1: zero-arg construction + `engine.store_url` matches the
    shared resolver output (with `KAILASH_ML_STORE_URL` /
    `KAILASH_ML_TRACKER_DB` monkeypatch-unset for determinism).
  - Inv 2: all 7 DI slots present on `__init__` signature; a
    sentinel object flows through every slot.
  - Inv 3: DI overrides honored literally (identity `is` check)
    for tracker / registry / feature_store / artifact_store /
    connection_manager.
  - Inv 4: setup() idempotent per (df_fingerprint, target, ignore,
    feature_store_name) — same input → same `schema_hash`; different
    target or different ignore → different hash.
  - Inv 5: compare() dispatches every family through `self.fit()`
    (monkeypatched stub counts invocations; exercises the
    Lightning-spine invariant at the dispatch layer).
  - Inv 6: setup() raises `TargetNotFoundError`, ValueError on
    empty target, ValueError on zero-feature ignore list.
  - Inv 7: 8-method surface exactly (setup, compare, fit, predict,
    finalize, evaluate, register, serve) with a guard against 9th-
    method drift via public-callable enumeration.
  - Inv 9: `tenant_id` plumbed onto `SetupResult` (echoes
    `engine.tenant_id`; `None` single-tenant).
- **Skipped this shard** — Inv 2 (full MUST-2 six-primitive default
  composition) deferred to W19.b per capacity budget; Inv 8
  (async-first + sync variant) deferred to a later wave since the
  sync variant lives at `kailash_ml.sync.Engine` which this shard
  does not touch.
- **Totals** — 20/20 new tests green; 59/59 green across W19 unit +
  env resolver + existing compare integration tests.

## W18 — alias + lineage + query ops (done)

Single commit on the branch:

- **W18** (`7a0f6ad9`) — 2766 LOC / 7 files.
  - `ModelRegistry.set_alias` / `clear_alias` / `promote_model` /
    `demote_model` — tenant-scoped alias mutations; `promote_model`
    defaults to `force=False` (raises `AliasOccupiedError`),
    `demote_model` auto-sets `@archived` when no other alias holds
    the demoted version.
  - `ModelRegistry.get_model` / `list_models` / `search_models` /
    `diff_versions` — polars-native listing, restricted filter DSL
    (allowlist cols + ops, `LIKE` / subquery / function-call BLOCKED),
    structural diff (signature add/removed/changed + lineage + format
    + onnx_status).
  - `ModelRegistry.get_lineage_parent` — one-hop DAG walk with
    cross-tenant refusal (`CrossTenantLineageError` when parent
    resolves into a different tenant).
  - `register_model(alias=...)` kwarg now validates + applies
    post-registration; invalid alias blocks registration before the
    version row lands.
  - New dataclasses: `ModelHandle` (lazy `.load()` via attached
    artifact store), `ModelDiff`, `{Set,Clear}AliasResult`,
    `{Promote,Demote}Result`.
  - New errors: `InvalidAliasError`, `AliasOccupiedError`,
    `AliasNotFoundError`, `ModelNotFoundError`,
    `CrossTenantLineageError`, `FilterParseError`.
  - Storage: `experiment_registry_aliases` table added to SQLite +
    Postgres (soft delete via `cleared_at`, LWW via `sequence_num`,
    one row per `(tenant_id, model_name, alias)`). Audit rows route
    through existing `experiment_audit` with `resource_kind="model_alias"`.
  - Tests: 41 Tier-1 unit (regex + reserved aliases + DSL grammar +
    log-poisoning defense) + 26 Tier-2 wiring against real SQLite.
    All 166 W16+W17+W18 tests green; 446 tracker regression passes.

## Read first

1. `workspaces/kailash-ml-audit/02-plans/01-master-34-wave-plan.md`
   §250 — W19 split; W19.b is the next shard.
2. `specs/ml-engines-v2.md §2.1 MUST 2 + §2.2` — the six-primitive
   composition invariant (FeatureStore + ModelRegistry +
   TrainingPipeline + ExperimentTracker + ConnectionManager +
   ArtifactStore) constructed on default construction.
3. `packages/kailash-ml/src/kailash_ml/engine.py::_ensure_registry_for_read`
   (lines ~2479-2506) — existing lazy-construction pattern to
   generalise into `_ensure_default_primitives_async()` covering
   every DI slot uniformly.
4. `packages/kailash-ml/tests/unit/test_w19_engine_surface.py` —
   W19.a Tier-1 invariant tests landed this session. W19.b should
   add a class `TestInvariant2DefaultPrimitivesConstructed` that
   asserts every slot is non-None after `engine._ensure_default_primitives_async()`
   against a fresh SQLite store.

## In-flight state

- `ModelRegistry` now exposes the full §4 + §8 + §9 surface;
  register_model's `alias=` kwarg is live. `_validate_alias` lives
  at `ALIAS_REGEX` (`^@[a-zA-Z][a-zA-Z0-9_-]{0,63}$`); reserved set
  is `RESERVED_ALIASES`.
- Audit emission routes every mutation through
  `_emit_alias_audit(..., resource_kind="model_alias",
  resource_id="<name>@<alias>")` — a promote also emits a
  `set_alias` sibling row because the helper flow is
  promote → set_alias → audit; the spec's §8.1 requires both.
- `MLEngine.register` convenience (W21) still unowned; the current
  shard's register_model accepts bytes or explicit URI per W17.
- `allow_pickle_fallback=True` disposition still deferred — pickled
  formats WARN at registration but do not route through §5.6.2
  `"legacy_pickle_only"` yet.
- Schema unification (`experiment_*` → `_kml_*`) still deferred.
- `.pending/` journal entries still unflushed from prior sessions.
- Pre-existing segfault-on-import tests remain
  (`test_automl_engine.py`, `test_w09_lightning_adapters.py`,
  `test_xgboost_oom_fallback.py`, `test_device_report.py`,
  `test_auto_logging.py`). Not W18 regressions.

## Traps

- `list_registry_versions` parameters MUST be appended in final-SQL
  order: JOIN(alias)? → WHERE(tenant_id) → WHERE(name)? →
  LIMIT/OFFSET. The first iteration appended `tenant_id` before the
  JOIN param and silently returned zero rows (no type error,
  because both are strings). The inline comment locks the ordering.
- `demote_model` auto-archive walks
  `list_aliases_for_version(tenant_id, model_version_id)` AFTER the
  clear — the just-cleared alias is excluded by the
  `cleared_at IS NULL` predicate. If you add aliases that survive
  the clear, this invariant breaks.
- `ModelHandle.load()` forwards `tenant_id=` to
  `AbstractArtifactStore.get(uri, tenant_id=...)`. W17's CAS store
  enforces tenant isolation on disk, so a handle copied across
  tenants (which you cannot do through the public API) would fail
  loudly — not silently return someone else's bytes.
- The alias-audit resource_id format is
  `"<model_name>@<alias>"` (e.g. `"fraud@@production"`). The double
  `@` is the alias's `@` prefix, NOT a typo.
- Promote emits BOTH a `set_alias` row (from the inner set_alias
  helper) AND a `promote` row (from promote_model itself). Demote
  is symmetrical: `clear_alias` sibling + `demote` row. Audit
  consumers filtering by action MUST accept either.
- `search_models` DSL accepts ONLY `<col> <op> <literal>` clauses
  joined by `AND`. Quoted strings OR unsigned ints — no NULL, no
  list, no subquery, no function call. Rejection messages do NOT
  echo the raw filter (log-poisoning defense aligned with the
  dialect identifier helper).
- `test_alias_mutations_emit_audit_rows` depends on the
  `resource_id` format being exactly `"fraud@@production"`; a rename
  of the format breaks the audit-trail grep in CI.
