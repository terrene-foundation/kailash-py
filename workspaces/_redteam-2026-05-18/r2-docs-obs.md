# Redteam Round 2 — Documentation + Observability Drift Axis

Scope: closed delta of #1083 / #1084 / #1085 (post-2.9.18 + root 2.21.3 / 2.22.0).
Method: mechanical greps on user-facing docs (README + CLAUDE.md), observability scan of `protection.py` + `protection_middleware.py`, CHANGELOG quality read, closure-parity journal SHA verification, specs/\_index.md drift check, agent-doc grep for removed symbols.
Mode: read-only.

## Findings

| SEV | finding                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | file:line                                                                                              | evidence                                                                                                                                                                                                                                     | recommended fix                                                                                                                                                                                                       |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| MED | `packages/kailash-dataflow/README.md` has ZERO mention of the protection surface (`ProtectedDataFlow`, `WriteProtectionConfig`, `enable_write_protection`, `read_only_global`). Five releases (2.9.13 → 2.9.18) materially changed the enforcement story on the async hot path + workflow-runtime path + import_file branch; README still describes only "SQL operations with injection protection" / "SSRF protection" surfaces.                                                                                                                                                                                                                                                                                                                                                                                                                                   | `packages/kailash-dataflow/README.md` (lines 118 / 121 / 416 / 454 are the only "protection" matches)  | `grep -n "ProtectedDataFlow\|WriteProtection\|enable_write_protection\|read_only" packages/kailash-dataflow/README.md` → empty for all four queries                                                                                          | Add a "Write Protection" subsection citing `ProtectedDataFlow` + `WriteProtectionConfig.read_only_global()` + the express-path + workflow-runtime path wiring; cross-reference `specs/dataflow-protection.md` §2 + I5 |
| MED | `specs/_index.md` does NOT list `specs/dataflow-protection.md` even though the file exists, is the authoritative source for invariants I1–I9 cited in CHANGELOG 2.9.14/2.9.16 and the protection.py docstring (`protection.py:184`), and is referenced from `tests/integration/test_issue_1058_shards_3_4_*.py`. `specs-authority.md` MUST-1 requires every spec file in `_index.md`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `specs/_index.md` (DataFlow section lines 14-22)                                                       | `ls specs/dataflow-protection.md` → exists; `grep -n "dataflow-protection\|protection" specs/_index.md` → empty                                                                                                                              | Add `dataflow-protection.md` row under the DataFlow section of `_index.md` with a one-line description naming invariants I1/I5/I7                                                                                     |
| LOW | `_handle_violation` (`protection.py:459-470`) calls `auditor.log_violation` for ALL severities but only emits a direct `logger.warning` on `WARN` level (line 468). For `BLOCK`/`AUDIT` it relies on `auditor.log_violation` → `logger.warning("protection.protection_violation")` at `:227`. Inspection confirms WARN+ IS emitted on every block path — this is correctly wired. Per `observability.md` Rule 3 (every block path SHOULD log a tag operators can grep), the existing `extra={"event": event}` carries `operation`, `level`, `model`, `field`, `context` — operationally sufficient. **No finding** on the WARN-emit; flagging as LOW only because the BLOCK path's WARN is two indirections deep (engine → auditor → logger), one extra hop than a reader expects from the rule's phrasing. Not a defect — note for documentation-readability only. | `packages/kailash-dataflow/src/dataflow/core/protection.py:227, 459-470`                               | `grep -n "_handle_violation\|logger" protection.py` confirms `logger.warning("protection.protection_violation", extra={"event": event})` at :227 + `logger.warning("protection.protection_warning", extra={"violation": violation})` at :468 | Optional: add docstring at `_handle_violation` noting "WARN emitted via auditor.log_violation"                                                                                                                        |
| LOW | Audit event dict (`auditor.log_violation`, `protection.py:215-227`) does NOT carry a `tenant_id` field. Per `tenant-isolation.md` MUST-5 ("Every audit row written by the trust plane / governance layer MUST persist `tenant_id` as a column, indexed"). DataFlow's write-protection auditor is governance-adjacent but is in-memory (`self.events: List[Dict[str, Any]]`, line 210) and not persisted to a DB table. The audit-row obligation in MUST-5 targets persisted audit stores; the in-memory list is a process-local diagnostic. Marking LOW because the rule's text says "audit row written" and operators reading the event JSON in logs may expect `tenant_id` for forensic correlation.                                                                                                                                                              | `packages/kailash-dataflow/src/dataflow/core/protection.py:215-227`                                    | Inspect `log_violation`: event keys are `timestamp, message, operation, level, model, field, connection, context` — no `tenant_id`. `context` is a free-form dict; callers MAY put tenant_id there but the schema does not pin it            | Add `tenant_id: violation.tenant_id` (or pull from `context.get("tenant_id")`) to the event dict; document the field as optional but expected when DataFlow is multi-tenant                                           |
| LOW | `auditor.log_violation` emits `model` and `field` at WARN per `observability.md` Rule 8 ("Schema-Revealing Field Names MUST Be DEBUG Or Hashed"). The rule says WARN with raw `model="users"` / `field="ssn"` bleeds schema to aggregators. Protection's `model` is the user's @db.model class name and `field` is the column-protection name — both schema-revealing. Rule 8 prescribes either DEBUG OR a hashed `field_hash` for WARN. Current shape ships raw names at WARN.                                                                                                                                                                                                                                                                                                                                                                                     | `protection.py:215-227`                                                                                | Same `log_violation` event dict: `model=violation.model, field=violation.field` at WARN; no hash, no DEBUG downgrade                                                                                                                         | Either downgrade `model`/`field` keys to DEBUG and keep an operational WARN counter, OR replace `model`/`field` with `model_hash`/`field_hash` (first 8 chars sha256) per `observability.md` Rule 8 DO example        |
| LOW | CHANGELOG 2.9.16 documents the #1058 same-bug-class chain (UPSERT enum + protection-before-validation + import_file swallow) but does NOT cross-reference 2.9.15's Shard 1 (`AsyncSQLProtectionWrapper` removal) or 2.9.14's Shard 2 (workflow-runtime path + bulk_update bypass) — users upgrading 2.9.13 → 2.9.16 see four independent fixes without the "this is one same-bug-class chain that closed under #1050 redteam" narrative. Per `communication.md` "Report in Outcomes, Not Implementation", the 4-version chain should be summarized at the 2.9.16 entry's intro so the cumulative security delta is visible in a single grep.                                                                                                                                                                                                                        | `packages/kailash-dataflow/CHANGELOG.md` lines 5 (2.9.18) / 11 (2.9.17) / 17 (2.9.16) / 93 / 143 / 230 | Each entry describes its own fix; 2.9.16 entry intro (line 17) names "(#1058 Shards 2 + 3 + 4)" but does not name the prior 2.9.14/2.9.15 closures or the #1050 origin                                                                       | Add a one-sentence "Cumulative chain: closes #1050 + #1058 — 2.9.13 (middleware deprecation) → 2.9.14 (async hot path + bulk_update) → 2.9.15 (dead wrapper purge) → 2.9.16 (UPSERT enum + import_file swallow)"      |
| LOW | `specs/dataflow-protection.md` exists but is not referenced from `packages/kailash-dataflow/README.md` either. Per `specs-authority.md` Rule 9 (workspace specs reference canonical artifacts) the README should at least name the spec as the authoritative source for the protection surface.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | `packages/kailash-dataflow/README.md`                                                                  | `grep -n "dataflow-protection\|specs/" packages/kailash-dataflow/README.md` → empty                                                                                                                                                          | When adding the README "Write Protection" subsection (MED finding above), include a one-line "See `specs/dataflow-protection.md` for invariants I1–I9" pointer                                                        |

## Verified clean (no finding)

| Axis                                                                                                                               | Evidence                                                                                                                                                                                                                                                            |
| ---------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Agent-facing docs (`.claude/`, `specs/`) reference removed `AsyncSQLProtectionWrapper`                                             | `grep -rln "AsyncSQLProtectionWrapper" .claude/ specs/` → **zero hits**. Pure-deletion per orphan-detection Rule 3 confirmed across both surfaces.                                                                                                                  |
| `packages/kailash-dataflow/tests/unit/CLAUDE.md` citation of `specs/testing-tiers.md` § Tier-1 Contract Rule 1 (added by #1085 S6) | `tests/unit/CLAUDE.md:102` reads "Per `specs/testing-tiers.md` § Tier-1 Contract Rule 1, the following MUST NOT appear at module scope…". `specs/testing-tiers.md:26` is "### 1. No external infrastructure at import or run time". Title matches; semantics match. |
| Closure-parity journals 0006/0008/0009 — every cited PR SHA resolves                                                               | `git cat-file -e` on all 11 cited SHAs (8a327f467, d7ef67055, ff1477517, dcfd626bf, cf81fe7d2, 38463d62e, 3013a004d, 50cb5c825, 88dc590e0, fb43fb394, 81b1a4b5a) — all exit 0. Per verify-resource-existence MUST-4: receipts are durable + resolvable.             |
| Issue close-comments on #1083 / #1084 / #1085 cite SHAs                                                                            | `gh issue view {1083,1084,1085} --comments` returns close comments citing the same SHA set as the journals. SHAs resolve via `git cat-file -e`. Per `git.md` § Discipline (Issue closure SHA-required).                                                             |
| Root `CLAUDE.md` protection-narrative                                                                                              | Root CLAUDE.md does not enumerate per-package security stories; correctly defers to per-package docs. No drift introduced by closed delta.                                                                                                                          |
| `specs/_index.md` has `testing-tiers.md` listed                                                                                    | Line 177: "[testing-tiers.md](testing-tiers.md) \| 3-tier testing contract…" present.                                                                                                                                                                               |

## R1 inheritance check

R1 (reviewer / security / spec-coverage / testing) reported 0 CRIT / 0 HIGH / 4 MED / 5 LOW across code/security/spec/testing axes. R2 axes (docs + observability) are orthogonal — R2 inherits no R1 finding and adds 2 MED + 5 LOW that R1 could not detect from its mechanical-sweep + LLM-judgment surface (R1 reviewer F6 confirmed `AsyncSQLProtectionWrapper` source-level removal but did not check user-facing docs / specs index).

## Round 2 docs/observability verdict

```
Round 2 docs/observability verdict: 0 CRIT / 0 HIGH / 2 MED / 5 LOW
NEW findings (not present in R1): 7 (2 MED + 5 LOW — all in user-facing-doc + observability axes R1 did not scan)
Convergence: NO (action required on the 2 MED findings before next release)
```

The closed delta ships clean on enforcement correctness (R1) but ships with a documentation + observability lag:

- **MED-1** (README protection narrative missing): users running `pip install kailash-dataflow==2.9.18` have no README path to discover the enforcement surface they just received. Fix is one subsection.
- **MED-2** (`specs/_index.md` missing `dataflow-protection.md`): the spec exists and is cited by code + tests + CHANGELOG, but `_index.md`'s manifest contract (`specs-authority.md` MUST-1) is violated.

Five LOW findings are bounded:

- L1 (auditor.log_violation indirection) — readability note only, not a defect.
- L2 (audit-row tenant_id) — in-memory list, not persisted store; tenant-isolation MUST-5 targets persisted stores.
- L3 (schema-revealing model/field at WARN) — Rule 8 violation but operationally established surface; fix is one commit (DEBUG downgrade or hash).
- L4 (CHANGELOG 2.9.16 cumulative-chain narrative) — entries cite each fix individually but lack the cross-version security-delta summary.
- L5 (README→spec cross-reference) — pairs with MED-1; closing MED-1 closes L5.

Recommended disposition: ship MED-1 + MED-2 as a single docs/index sweep PR (fits one shard per `autonomous-execution.md` MUST-1: ≤500 LOC, 2 invariants, single-file edits). LOW-3 (Rule 8 schema-name WARN) should ride in the same PR as the docs sweep since it is one-line in `protection.py` (or `auditor.log_violation`) and same-class as the operability surface MED-1 documents.
