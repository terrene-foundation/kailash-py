# Outstanding-Work Sweep — 2026-06-25

Repo: `terrene-foundation/kailash-py` · main `aeb6c4915` · working tree clean (only `.session-notes`).
Run after the 2026-06-24 all-clear sweep (PR #1452). This sweep surfaces **new actionable work** that landed
on the tracker after the prior session closed.

## Headline

Two new `cross-sdk` issues (#1455, #1456) were filed **today (2026-06-25)** — both genuinely actionable
**in this repo**, both framed as verify-and-reconcile against a kailash-rs sibling fix. Two Dependabot
GitHub-Actions bumps (#1453, #1454) are open with all-green CI. Everything else is clean.

## Findings by sweep

### Sweep 1 — Active todos · CLEAN

No `workspaces/*/todos/active/*.md`. No in-flight workspace todos.

### Sweep 2 — Pending journal · CLEAN

No `workspaces/*/journal/.pending/*.md`.

### Sweep 3 — Open GitHub issues · 2 ACTIONABLE (new)

- **[HIGH] #1456 — MCP governance: clearance-before-cost-flag ordering** (`cross-sdk`)
  Location: `pact.mcp.enforcer.McpGovernanceEnforcer.evaluate`.
  Python sibling of kailash-rs#1492. Potential **authorization-ordering bypass**: a caller with
  absent/insufficient clearance landing in the `(0.8·max_cost, max_cost]` cost band may receive
  `flagged` (allowed) instead of `blocked` if the cost-ceiling soft-flag short-circuits before the
  clearance check. Severity MEDIUM (security-relevant, fail-closed correctness). Scope is bounded:
  verify ordering → reorder if bug present (`0.80` threshold value unchanged) → regression test. **Fits one shard.**
  Disposition: **ACTIONABLE — verify-first**. Acceptance criteria in issue body.

- **[HIGH] #1455 — RFC 3161 external TSA timestamping for the CARE chain** (`cross-sdk`)
  Location: EATP CARE-chain `TrustBlock.rfc3161_timestamp`.
  Python sibling of kailash-rs#1431. The `rfc3161_timestamp` field may be a permanent placeholder
  (always `None` / unconstructable) — if so, the audit-grade non-repudiation guarantee EATP D6 advertises
  is not met in py. Severity MEDIUM-HIGH. Step 1 is verify-and-reconcile (does the placeholder gap exist?);
  if it does, the implementation (DER `TimeStampReq`, TSA POST, fail-closed token verify across RSA+ECDSA,
  messageImprint/genTime/nonce/EKU checks, attach-after-signing invariant) is multi-invariant and **may need
  decomposition** per the per-session capacity budget.
  Disposition: **ACTIONABLE — verify-first; decompose if the gap is real**. Acceptance criteria in issue body.

Both issues self-declare "no kailash-py source was read" — so the first task for each is a source-grounded
verification before any code change.

### Sweep 4 — Open PRs / stale branches · 2 GREEN DEPENDABOT

- **[LOW] #1453 — Bump actions/checkout 6→7** — all checks pass. Remote branch `dependabot/.../checkout-7`.
- **[LOW] #1454 — Bump actions/cache 5→6** — all checks pass (RL job `skipping` is expected). Remote branch `dependabot/.../cache-6`.

Disposition: **RECOMMEND MERGE** (CI-green dependency hygiene). Both modify `.github/workflows/`; per
`feedback_no_auto_cicd` (CI/CD changes are user-gated) these are surfaced for approval rather than
auto-merged. No other orphan/stale remote branches.

### Sweep 5 — Redteam gaps vs specs · N/A

`<!-- sweep-redteam:v1:N/A reason=orchestration-mode no_specs=true no_tool=false -->`
`tools/sweep-redteam.py` is present, but there are zero `workspaces/*/specs/` directories (no active
workspace carries per-workspace specs). Structural N/A — no proxy substituted.

### Sweep 6 — Workspace / worktree hygiene · CLEAN

Single worktree at `main` (HEAD). No `.session-notes` >30d. No `.pending` >14d. The four STALE workspace
notes flagged at session-start (issue-1035-delegate-py, issue-855-kaizen-agents-pyright,
nexus-fastapi-parity-py, from-brief-1125) are all completed/merged prior work — no active state, no `todos/active`.

### Sweep 7 — Process hygiene · CLEAN (pre-existing markers only)

- Working tree clean except `.session-notes` (session-hook touch, no code bytes).
- `origin/main...HEAD` = `0 0` (in sync).
- Stub-marker grep: every `NotImplementedError` hit is an **abstract-method contract**
  (`base_async.py`, `base.py`, `base_with_acl.py`, `edge_*.py`, `async_sql.py:1104 # pragma: no cover - abstract`)
  — intentional "subclass must implement" raises, NOT stubs. The handful of `# TODO` comments
  (`middleware/communication/events.py`, `monitoring/deadlock_detector.py`, `nodes/data/bulk_operations.py`)
  are pre-existing feature-gated notes ("when X node available") present across every prior all-clear sweep.
  Disposition: **DEFER-WITH-REASON** (pre-existing, not introduced this session, not in active-work scope).

### Sweep 8 — Release readiness · RELEASE-CLEAN

Latest stable core tag: `v2.44.1`. Shippable core diff (`src/kailash`) since v2.44.1 = **1 commit**:
`f24589831 docs(trust): accurate per-encoder serde claims on witness/envelope default=str sites (#1403)` —
docstring/comment accuracy on source files, no behavior change. Sub-package release commits in the broader
`src/`+`packages/*/src` diff (pact 0.14.2, align 0.7.4, ml numba floor) were each already published under
their own prefixed tags (`pact-v0.14.2`, etc.). No behavior-shippable unreleased core work; **no release needed**.

## Cross-cutting observations

- The two new issues are the **same cross-SDK pattern**: kailash-rs fixed/implemented something, filed the
  py sibling as verify-and-reconcile. Neither has been source-verified in py yet. Both have crisp acceptance
  criteria. #1456 is a possible authorization bypass (fail-closed correctness); #1455 is a possible dead-field
  capability gap.
- This is an in-repo session: both issues are filed against THIS repo and are squarely in scope (the
  `cross-sdk` label means "check both SDKs", not "work belongs elsewhere").

## Recommended next-session items (ranked)

1. **#1456 — verify the MCP governance clearance/cost ordering** (HIGH). Highest value: potential
   authorization bypass, bounded to one shard (verify → reorder if needed → regression test). Start with a
   source-grounded read of `McpGovernanceEnforcer.evaluate` to confirm whether the bypass exists.
2. **#1455 — verify the RFC 3161 placeholder gap** (HIGH). Confirm whether `rfc3161_timestamp` is dead in py.
   If real, decompose the TSA-client implementation into value-anchored shards before building.
3. **Merge Dependabot #1453 + #1454** (LOW). CI-green Actions bumps; merge on your approval (CI/CD = user-gated).

The report is the deliverable; the pick is a human call. Say the word and I'll start with #1456 (verify-first),
or merge the Dependabot PRs, or both.
