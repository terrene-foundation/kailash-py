---
type: DECISION
status: pending
source_commit: 8cd1d2b0d633cb6e0fe47cddca772585977975dd
session_id: 6da54879-77b8-4550-8790-a34082c34c8b
created: 2026-04-20T09:20:50.540Z
---

# feat(ml): implement MLEngine.finalize() — full-fit retrain (shard-B)

<!-- Generated by SessionEnd hook from a commit matching a journal-worthy pattern.
     Next session: review, then promote to a real journal entry OR delete. -->

**Commit**: `8cd1d2b0d633` — feat(ml): implement MLEngine.finalize() — full-fit retrain (shard-B)

**Body**:

Replaces the Phase 3 NotImplementedError stub with a production
implementation per specs/ml-engines.md §2.2.

Candidate resolution: accepts either a TrainingResult directly or a
model-URI string ('models://<name>/v<version>'). URI strings are
parsed via the new `_parse_model_uri` helper and loaded from the
registry; the result is wrapped into a read-through TrainingResult
so callers can reach family + hyperparameters uniformly.

Behavior:
- full_fit=True (default): re-train the candidate's family on the
  combined train+holdout set by dispatching through self.fit() so the
  Lightning-spine invariant holds (§2.1 MUST 7). Escape-hatch `data=`
  and `target=` let finalize run without a prior setup() — same
  contract as compare() so sibling shard A's setup() landing is not a
  blocker.
- full_fit=False: re-wrap the candidate without retraining, useful
  when the caller wants to mark a candidate as finalized for audit
  without paying the retrain cost.

tenant_id propagates onto FinalizeResult AND the inner
training_result per §4.2 MUST 3.

Adds `_ensure_registry_for_read()` — the read-side registry-loading
helper. Writes (register()) still belong to sibling shard A; this
method is a bounded read-only bootstrap that keeps finalize() and
evaluate() functional against a pre-populated registry.

Why: finalize() is the last gate before promoting a candidate to
production. Without it, users who ran a compare() sweep have no
first-class API to retrain on the full dataset — they'd hand-build
a fresh fit() call with the candidate's hyperparameters, which is
exactly the kind of ad-hoc glue the Engine is meant to eliminate.

Pre-commit bypassed (core.hooksPath=/dev/null): the hook resolves
.venv/bin/python relative to the worktree cwd and the worktree has
no local venv. Per rules/git.md § Pre-Commit Hook Workarounds.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>

## Context to fill in (memory-based, no verification tool calls)

- Why was this change made? What alternative was considered?
- What does it unlock or block for the next session?
- Is this worth a full journal entry, or is the commit message sufficient?

**Promote**: rename to `journal/NNNN-DECISION-slug.md`, fill in context, remove the pending frontmatter.
**Discard**: `rm` this file.
