# Wave 1 — RAG eval/benchmark build-real: /redteam round history

Wave-loop G1 gate for Wave 1 (RAGEvaluationNode + RAGBenchmarkNode build-real).
BUILD repo: working-tree only, no commits. Receipts = agent task IDs below
(verify-resource-existence MUST-4 / wave-loop MUST-5: external, not self-attested).

## Implementation (kaizen-specialist, task a6f73132c8dc6d6ff)
- RAGEvaluationNode: design (a) judge-provided-results — a sandboxed PythonCodeNode
  cannot invoke an arbitrary passed node, so `test_executor` now passes through
  caller-provided `generated_answer`/`retrieved_contexts` (raises if absent; no
  fabrication). Real LLMAgentNode judges unchanged. Docstring/contract corrected.
- RAGBenchmarkNode: executes each provided system via the Core SDK runtime; real
  `perf_counter` latency, real throughput, real `tracemalloc` memory, real thread-pool
  concurrency. Non-runnable system → typed error, no fabrication fallback.
- Tests rewritten with deterministic Protocol-satisfying adapters (not mocks).
- Orchestrator-fixed lint debt (dead imports json/datetime/Union; unused local/helper;
  registering-import noqa mirroring realtime.py #1120).

## Round 1 (parallel reviewer + security-reviewer)
- reviewer (task ac372e9b43a653d21): **CHANGES-REQUIRED**
  - HIGH-1: `most_scalable` ranking INVERTED — stored `throughput_degradation` is
    actually a parallel-speedup factor (higher=better; empirically 0.997/4.85/9.30
    at users 1/5/10) but ranked with `min` (picked the worst). The "direction fix"
    comment was itself backwards.
  - MED-1 (dup w/ security): query-dict forwarded as execute kwargs can break a
    strict Node — documented as a benchmark-input contract note.
  - LOW: stale "Simulate relevance judgment" comment; thin per-query failure logging.
- security-reviewer (task a18ab85f534fbc7a6): **CHANGES-REQUIRED**
  - MED-1: tracemalloc start/stop not in try/finally → process-global leak if a
    system raises mid-benchmark.
  - MED-2: documented `duration` kwarg enforces nothing (Rule 3c) + no per-query
    timeout → wedged system hangs run() indefinitely.
  - LOW: missing correlation-id on new logs; pre-existing `{self.metrics}` codegen
    interpolation (F25 class — NOT a Wave-1 regression; backlog).
  - CLEAN: arbitrary-execution resolution (typed error, no fallback), codegen
    injection (no NEW surface), error-swallowing (zero `except` swallow), secrets-in-logs.

## Fix round (orchestrator, fix-immediately per autonomous-execution Rule 4)
All same-file `RAGBenchmarkNode`, within one shard:
- HIGH-1: renamed metric `throughput_degradation` → `parallel_speedup`; `_compare_systems`
  ranks `most_scalable` with `max`; no-data default 0.0; corrected comment. Added
  deterministic direction-pin unit test `TestMostScalableRankingDirection`.
- MED-1: per-system tracemalloc wrapped in try/finally. Test:
  `test_raising_system_does_not_leak_tracemalloc`.
- MED-2: `duration` is now a real wall-clock cap (deadline checks at system + workload
  boundaries); BOTH `_measure_workload` (sequential) and `_measure_concurrent` run
  queries via bounded futures with `shutdown(wait=False, cancel_futures=True)` —
  a wedged system cannot hang run(). `duration_exceeded` reported; WARN on truncation/
  timeout (honest partial, no swallow). Test: `test_duration_budget_caps_wedged_system`.
  (Self-caught: ThreadPoolExecutor `__exit__` waits on wedged threads — switched to
  explicit shutdown(wait=False); also bounded the sequential path the reviewer's
  concurrent-only ask had left open.)
- LOW: reworded the context_evaluator comment; added `run_id` correlation-id to all
  benchmark log lines.

State after fix: ruff clean; 63/63 Wave-1 tests pass; 610 rag-unit+F9 + 11 rag-integration
green. Only residual static hints: 2 registering-import pyright notices (mirror
realtime.py #1120) + benign Protocol `**kwargs`.

## Round 2 (verification)
- security-reviewer (task a21965f5c8a1a2b71): **CONVERGED** — both R1 MEDIUMs
  verified resolved (tracemalloc try/finally w/ ownership guard; duration cap +
  bounded futures + shutdown(wait=False) + WARN-no-swallow; logs = run_id+counts).
  No new findings. F25 LOW correctly untouched/backlog.
- reviewer (task af079f31918268caa): all 4 R1 findings RESOLVED, but **1 NEW MED**:
  `test_duration_budget_caps_wedged_system` leaked a worker thread (sleep_s=1.5 vs
  1s budget) whose SDK success-log hit a closed capture stream → deterministic
  `ValueError: I/O operation on closed file` (zero-tolerance Rule 1, test-only).

## Fix round 2 (orchestrator, test-only)
Replaced the sleep-based wedged adapter with `_ReleasableSlowRagNode` (blocks on a
`threading.Event`, 10s internal safety cap); the test releases + drains it in
`finally` (after the cap assertions) so no orphan thread logs against a closed
stream. Mechanical receipt: `grep -cE 'I/O operation on closed file'` = 0 in both
the isolation repro AND the full combo. 63/63 pass, ruff clean.

## Round 3 (verification — test-only; source byte-identical to R2-CONVERGED)
- reviewer (task abbc1514497663272): **CONVERGED** — logging-error gone (grep=0),
  cap assertions intact + correctly ordered before the finally-drain, 10s safety cap
  confirmed, 63 passed, ruff clean, no new findings.

## G1 VERDICT: CONVERGED
Source: security-reviewer CONVERGED (R2) + reviewer source-findings RESOLVED (R2),
on byte-identical source. Test: reviewer CONVERGED (R3). Both gate agents clean.
Receipts: agent task IDs a21965f5c8a1a2b71, af079f31918268caa, abbc1514497663272.

## Tracked backlog (not Wave-1 regressions)
- `{self.metrics}` f-string interpolation into the metric_aggregator PythonCodeNode
  (F25 Shard D injection class) — pre-existing, no current trust boundary; harden via
  identifier-shape validation in a future pass (or when metrics becomes remotely settable).
- kaizen test-infra dep gaps: `pytest-httpserver` (agentic tests collection-error),
  `pytest-env`/`pytest-timeout` (unknown-config warnings). Separate disposition.
- The wedged-system test leaks a non-daemon worker thread for ~0.5s (Python cannot kill
  threads) — inherent + acknowledged in the test docstring.
