# RAG Provably-Correct Remediation — Wave Plan (2026-06-08, session 4)

/ autonomize + /redteam to convergence. Continues F31 (APPROVED prior session).
BUILD repo: working-tree edits only, commits stay with the user.

## Value-anchor re-validation (value-prioritization MUST-3)

- Recorded anchor (F31): brief verbatim "provably correct, not merely importable" +
  user approval 2026-06-08.
- Re-validation: user said "continue from last session" THIS turn (same day) →
  anchor holds, no decay. Disposition: resume.

## Ground-truth correction vs the 06-/07- reports

An intervening **F9** fix cycle (#1112–#1126, PR #1198 era) landed AFTER the
redteam reports were written. F9 wired the **real `LLMAgentNode` judges** into
`RAGEvaluationNode` (faithfulness/relevance/answer-quality are genuine LLM calls
now). The redteam QUARANTINE verdict for the two eval nodes is therefore PARTIAL:
the *judge* half is real; the *system-under-test* half is still fabricated.

Residual defects confirmed by direct read (`evaluation.py`):
- `RAGEvaluationNode.test_executor` (L145-155) fabricates `rag_response`
  (`f"Generated answer for: {query}"` + hardcoded contexts) — never runs the
  `rag_system` it advertises evaluating.
- `RAGBenchmarkNode.run()` (L619-668) fabricates EVERY metric via `time.sleep()`
  + `random.randint()` — never calls `system.run(query)`. Latency/throughput/
  memory invented. `_compare_systems` then ranks fabricated numbers.

Test situation: unit tests assert SHAPE only (pass `rag_systems={"system_a": {}}`,
a non-runnable empty dict). F9 regression `test_issue_1117` pins module-scope
`result =` binding in each eval codegen node.

## Waves (value-ranked, each a wave-loop gate boundary)

| Wave | Scope | Disposition | Anchor |
| ---- | ----- | ----------- | ------ |
| **1** | RAGEvaluationNode + RAGBenchmarkNode | build-real (run the system-under-test; real measured metrics; eliminate fabrication) | brief "provably correct" + approved-plan §1 (highest blast radius — fabricated quality/perf metrics) |
| 2 | PrivacyPreservingRAGNode + SecureMultiPartyRAGNode | strip over-claims (DP/ε / homomorphic-encryption docstrings → document the real regex PII redaction) | approved-plan §2 |
| 3 | ToolAugmentedRAGNode, ReasoningRAGNode, query-intent classifiers | build-real via LLMAgentNode (eval nodes = reference pattern) | approved-plan §3 |
| 4 | multimodal CLIP/BLIP, federation network, ColBERT BERT | strip-or-experimental-flag (do NOT build) | approved-plan §4 |

Per wave: G1 /redteam to convergence → G2 journal the delta → G3 spec/test update →
G4 re-rank remaining waves → G5 next wave. Inter-wave gate is an EXECUTION gate
(autonomous). Structural gates remain the user's commit + /release.

## Wave-1 disposition detail

- **RAGBenchmarkNode**: feasible to build-real. Execute each provided system via the
  Core SDK runtime, measure real wall-clock latency (`time.perf_counter`), real
  throughput, real memory (`tracemalloc`); real concurrency via `asyncio.gather`.
  No `time.sleep`/`random` in the metric path.
- **RAGEvaluationNode.test_executor**: a sandboxed PythonCodeNode cannot invoke an
  arbitrary passed node. Honest options for kaizen-specialist to pick within the
  build-real disposition: (a) judge user-provided `generated_answer`/
  `retrieved_contexts` carried in `test_queries` (pass-through, no fabrication), or
  (b) restructure so the system runs via a real node-execution step. Pick the one
  that fits the Node model; eliminate the fabricated branch either way.

Receipt: this file + `06-`/`07-` reports (uncommitted, workspace cadence).

## Wave-loop boundary after Wave 1 (G2 learning → G4 re-rank) — 2026-06-09

**G1: Wave 1 CONVERGED** (receipt: `09-wave1-redteam-2026-06-08.md` + agent task IDs).

**G2 (learning delta — what the wave's todos CLAIMED vs what redteam FOUND):**
- Claim drift: the 06-/07- reports predated F9; the eval nodes' *judges* were
  already real — only the *system-under-test* execution was fabricated. Lesson:
  re-verify report claims against current source at wave start (the wave-plan's
  ground-truth-correction section did this).
- Design lesson (reused by Wave 3): a sandboxed `PythonCodeNode` cannot run an
  arbitrary passed node → "build-real" for a judge node = honest-judge-of-provided-
  results; "build-real" for a runner node (benchmark) = execute via Core SDK runtime.
  Wave 3's LLM-fixable cluster picks per-node which shape applies.
- **Behavior change to flag at commit:** `RAGBenchmarkNode.duration` went from
  accepted-but-ignored → real wall-clock cap (default 60s) that truncates + sets
  `duration_exceeded: true`. Honest + tunable; surfaced for user commit review.

**G3 (specs/todos):** no `specs/` dir for this workspace; Wave 2-4 todos unchanged
by Wave 1 (independent files). The build-real-vs-strip feasibility test is the
carried pattern.

**G4 (re-value-rank remaining waves):** anchor unchanged (approved plan + brief
"provably correct"). Rank holds: Wave 2 (privacy over-claims) > Wave 3 (LLM-fixable
cluster) > Wave 4 (strip-or-flag un-buildable). Wave 2 next.

**Backlog (tracked, not in any wave): kaizen test-infra dep gaps** (`pytest-httpserver`,
`pytest-env`, `pytest-timeout` undeclared) + the pre-existing `{self.metrics}`
codegen-interpolation hardening (F25 class). Separate disposition.

## MAJOR NEW FINDING (Wave 2 redteam) — F9 #1117-class "codegen never publishes result"

**Discovered during Wave-2 redteam** (security-reviewer flagged; orchestrator VERIFIED
in source 2026-06-09): several `kaizen.nodes.rag` PythonCodeNode codegen blocks DEFINE
an inner function that binds `result = {...}` **function-locally** but **never call it
at module scope** — so the node publishes NO `result` end-to-end. PythonCodeNode
publishes only the module-scope `result`. F9 #1117 fixed this for the eval nodes
(test_executor/context_evaluator/metric_aggregator) + privacy `pii_detector`, but NOT
for the rest.

- **CONFIRMED broken (privacy.py):** `dp_noise_injector` (`perturb_scores` def @302, no
  module-scope call), `secure_aggregator` (`aggregate_results` @347), `result_formatter`
  — the latter is the TERMINAL node, so `PrivacyPreservingRAGNode`'s workflow output is
  effectively empty at runtime. The honest flags Wave 2 just derived compute inside
  functions that never run in the assembled workflow.
- **Why tests didn't catch it:** unit/integration tests exercise the codegen functions
  via an AST body-exec helper, NOT the end-to-end `LocalRuntime.execute(workflow.build())`
  path. No DOCS-EXACT end-to-end regression (testing.md "End-to-End Pipeline Regression").
- **SUSPECTED systemic:** other rag codegen nodes (advanced/agentic/conversational/
  query_processing/...) likely share the defect — NEEDS AN AUDIT to bound scope.

**Value-anchor:** brief verbatim "provably correct, NOT merely importable" — a node whose
workflow publishes nothing is the exact failure the brief targets (it imports + constructs
fine, but does not actually work). This is arguably the HIGHEST-value remaining item.

**Disposition:** distinct bug class from the strip wave + exceeds the strip shard (3+
codegen nodes + workflow wiring + end-to-end Tier-2 tests, potentially tree-wide) → NEW
WAVE per autonomous-execution Rule 4 bound. Recorded here (not deferred-and-forgotten).

## G4 re-rank (post-Wave-2, pending user gate — reshapes the forest)

This finding may outrank Waves 3-4. Provisional value-rank (USER to confirm — forest
prioritization is the user's call per value-prioritization):
1. **NEW: F9 #1117 systemic audit + fix** (HIGH — "nodes publish nothing" = the core
   provably-correct gap; audit all rag codegen nodes, fix module-scope calls, add
   end-to-end Tier-2 tests asserting workflows publish non-empty results).
2. Wave 3 (LLM-fixable cluster — ToolAugmented/Reasoning/query-intent build-real).
3. Wave 4 (strip-or-flag un-buildable — multimodal/federation/ColBERT).
Plus: `SecureMultiPartyRAGNode` removal decision (flagged, pure simulation — user gate).
