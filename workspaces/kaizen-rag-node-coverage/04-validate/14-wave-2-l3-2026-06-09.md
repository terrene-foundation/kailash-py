# Wave 2 — L3 context-delivery fix across the REAL RAG nodes (2026-06-09)

Value-anchor: brief "provably correct, NOT merely importable" + user APPROVED Wave 2 (session
notes 2026-06-09). L3 = make every LLMAgentNode stage actually CONSUME retrieved context via the
valid `messages` port (LLMAgentNode reads only `messages`+`system_prompt`; any other wired port is
silently dropped). Reference template: `conversational.py` (Wave 1) — `from_function` messages-
composers → `messages` port. BUILD repo: working-tree only; commits stay with the user.

Execution model: SERIAL in the main working tree (worktree isolation infeasible — the held
#1117/Wave-1 shards are uncommitted working-tree edits that must be preserved; a worktree from
HEAD `2f125ca4c` would lose them). One node-file = one shard + redteam-to-convergence.

Value-rank (re-ranked from the decision doc's listing by blast-radius): eval (sharpest correctness
failure — a judge scoring evidence it never sees fabricates the score) → query_processing (7 stages,
feeds all retrieval) → agentic (6 stages) → graph (3) → workflows (1).

---

## Shard 1 — evaluation.py (RAGEvaluationNode, 3 LLM judges) — CONVERGED 2026-06-09

**Defect:** faithfulness / relevance / answer_quality judges were wired `test_executor → judge.test_data`
(a phantom port) → each scored from `system_prompt` alone, never seeing the query / retrieved
contexts / generated answer / reference it grades. Fabricated evaluation scores.

**Fix (kaizen-specialist, task ac2c12128390382e1):** 3 module-level `from_function`-wrapped composers
(`compose_faithfulness_messages` / `compose_relevance_messages` / `compose_answer_quality_messages`
+ `_render_contexts` / `_render_test_block` / `_normalize_test_results`) rendering the REAL query +
retrieved contexts (`content`+`score`) + generated answer (+ reference for answer_quality) into a
`messages` list wired to each judge's `messages` port. Phantom `test_data` wiring removed.

Two same-bug-class defects caught + fixed in the same pass (autonomous-execution Rule 4):
- **Nested-port fake-green:** held wiring read `test_executor."test_results"` as a top-level port,
  but PythonCodeNode publishes a single `result` port → corrected to `result.test_results` /
  `result.context_metrics`. Without it the new composers would themselves have read nothing.
- **#1118 NameError:** `test_executor` codegen referenced `_datetime_class` at module scope while
  PythonCodeNode exec passes separate (globals, locals) → moved import into the function body. This
  blocked ANY end-to-end run.

**Honesty (zero-tolerance Rule 2):** a single LLMAgentNode call publishes ONE `response` dict, but
`metric_aggregator` indexes `faithfulness_scores[i]` per test — genuinely cannot produce per-test
indexable scores without fabricating an N-way split. NOT papered: pinned via
`@pytest.mark.xfail(strict=True)` (`test_aggregator_list_shape_limitation_is_surfaced`); a future
judge-fan-out redesign flips it green and the strict-xfail then fails loudly, forcing guard removal.
No random/sleep/hardcoded scores introduced.

**End-to-end test:** `TestEvaluationContextReachesLLM` (integration, real LocalRuntime). Exercises the
PRODUCTION delivery path — `test_queries` delivered as a top-level workflow input, auto-distributed
via the parameter-injector (`parameter_injector.py:428-449`), NOT node-keyed harness injection (the
Wave-1 MED-2 false-green trap). Red-pre verified by stripping a judge's composer→messages edge →
the structural assertion goes RED. All 3 judges checked (playbook item 1).

**HIGH-1 (R1 reviewer) — held-shard stale regression tests swept (zero-tolerance Rule 1 / orphan
Rule 4a):** the held `RAGBenchmarkNode._resolve_runner` hardening (correctly raising a typed error on
non-runnable "systems" instead of fabricating random latencies — zero-tolerance Rule 2) left two
regression tests red in the working tree:
- `test_compare_systems_picks_lowest_latency_as_fastest`: rewritten to pass two REAL runnable
  callables with a deterministic latency gap (`fast_system` near-instant vs `slow_system`
  `time.sleep(0.01)`/query), asserting `fastest_system == "sys_a"` against the real measured numbers.
  R2 mutation-verified load-bearing (flip `_compare_systems` `min`→`max` → RED).
- `test_conversational_all_branches_off_constructs_minimal_graph`: node count `5 → 6` (the held
  Wave-1 `response_messages_composer` is a mandatory L3 node, present even with all optional branches
  off). R2 verified honest (real mandatory node, not an orphan).

**Gates:** ruff clean (all touched files); rag suite **877 passed + 1 xfailed**; 2 pre-existing
`test_agentic_nodes.py` pytest-httpserver fixture errors OUT OF SCOPE (undeclared dep). base.py NOT
edited.

**Redteam convergence receipts:**
- R1 security — task `abe7c38bdc5ff7801` — **APPROVE**, zero findings (5 surfaces: untrusted RAG
  content reaches no code-exec surface; composers passed by reference to `from_function`, not
  stringified into exec bodies; no log leak; guarded `except NameError` not a swallow; numeric-only
  aggregation sink).
- R1 reviewer — task `aaccffa9f0b05c58b` — CHANGES-REQUESTED (1 HIGH = the two stale tests). All 5
  mechanical sweeps + both L3 playbook checks + all honesty checks PASS; red-pre verified by the
  reviewer independently.
- R2 reviewer — task `af9ad2953b3f91444` — **APPROVE (convergence)**. HIGH-1 closed; mutation-proved
  load-bearing; node-count honest; no new production change since security APPROVE; no new findings.

**Files (working-tree only, held uncommitted):** `src/kaizen/nodes/rag/evaluation.py`,
`tests/integration/rag/test_evaluation_nodes.py`, `tests/unit/rag/test_evaluation_nodes.py`,
`tests/regression/test_issue_f8b9b_eval_conversational_defects.py`.

Status: **Shard 1 DONE + held-uncommitted.** Next: Shard 2 — query_processing.py.

---

## Shard 2 — query_processing.py (6 classes, 6 LLM stages) — CONVERGED 2026-06-09

**Distinction:** these stages run PRE-retrieval, so "context" = the user query (+ upstream
query-processing output), not retrieved docs.

**Fix (kaizen-specialist, task a2df0c7c6904773c4):** 6 `from_function` composers
(`compose_expansion/decomposition/analysis/rewrite/intent/hop_plan_messages`) rendering the real
query into `messages` wired per stage — `llm_expander` (QueryExpansionNode), `query_decomposer`
(QueryDecompositionNode), `query_analyzer`+`query_rewriter` (QueryRewritingNode 2-stage chain),
`intent_classifier` (QueryIntentClassifierNode), `hop_planner` (MultiHopQueryPlannerNode). The
rewriter composer ALSO renders the upstream `query_analyzer.response` analysis. Removed the phantom
`query_analyzer.response → query_rewriter."analysis"` edge (LLMAgentNode dropped it); analysis now
reaches the rewriter via its composer's `messages`.

**Honesty:** `AdaptiveQueryProcessorNode` owns NO direct LLMAgentNode stage (its embedded
`intent_analyzer` runs its deterministic `run()` under LocalRuntime, not an inner LLM workflow) —
documented + pinned by `test_adaptive_owns_no_direct_llm_stage_query_reaches_embedded` (asserts zero
LLM captures). No fabrication.

**End-to-end test:** `TestQueryProcessingContextReachesLLM` (8 tests, real LocalRuntime). Query
delivered as top-level workflow input (auto-distributed), NOT node-keyed. `_MessageCapturingLLMAgent`
is a Protocol-Satisfying Deterministic Adapter (subclass of the legal Tier-2 `_DeterministicLLMAgent`),
not a mock. Red-pre verified (strip expansion composer→messages edge → RED).

**Test-sweep (orphan Rule 4):** 5 unit graph-shape assertions updated to the real new node-sets +
`result.messages → messages` edges; rewriting assertion asserts the phantom edge is GONE.

**Gates:** ruff clean; RAG suite (unit+integration) **885 passed + 1 xfailed** (877→885, +8 new);
2 pre-existing `test_agentic_nodes.py` pytest-httpserver fixture errors OUT OF SCOPE. base.py NOT edited.

**Redteam convergence receipts (both APPROVE, zero findings → converged at R1):**
- R1 security — task `adaa44c6c6574c0d6` — **APPROVE**, zero findings (5 surfaces clean; query reaches
  no code-exec surface; 6 pre-existing `code=` blocks have no runtime `{...}` interpolation; no
  query/secret logging; prior M1 exception-logging hardening preserved).
- R1 reviewer — task `aafb76856a94bfc5c` — **APPROVE**, zero findings (all 5 mechanical sweeps + both
  L3 checks + all honesty checks PASS; red-pre verified by edge-strip; AdaptiveQueryProcessor honest).

**Files (working-tree only, held uncommitted):** `src/kaizen/nodes/rag/query_processing.py`,
`tests/unit/rag/test_query_processing_nodes.py`, `tests/integration/rag/test_query_processing_nodes.py`.

Status: **Shard 2 DONE + held-uncommitted.** Next: Shard 3 — agentic.py.

### Side finding (surfaced, not in L3 scope) — kaizen venv missing optional `kaizen-agents`
The kaizen venv (`packages/kailash-kaizen/.venv`) lacks the optional in-repo `kaizen-agents` package
→ 8 regression tests (`test_issue_1071/357/486/822`) fail for that environmental reason. Orthogonal to
RAG (kaizen.api/BaseAgent), proven pre-existing via clean-HEAD reproduction. A `--no-deps` editable
install left it unimportable (`No module named 'openai'`); a full install needs the agents dep tree
(openai, …). ROLLED BACK rather than perturb the in-flight venv. Recommend a deliberate `uv sync` of
the kaizen venv with the agents extra as a separate environment-setup step (user-gated — it changes
the venv dep tree). The RAG suite (885+1xfail) is the trustworthy Wave-2 gate.

---

## Shard 3 — agentic.py (3 classes, 6 LLM stages) — CONVERGED 2026-06-09

**Fix (kaizen-specialist, task a45b486080f7ab10a):** 6 `from_function` composers across AgenticRAGNode
(`compose_planner/react/verifier_messages`) + ReasoningRAGNode (`compose_decomposer/step_reasoner/
logic_verifier_messages`). 5 phantom inbound edges on the LLM stages removed (`additional_context`,
`answer_to_verify`, `reasoning_plan`, `reasoning_to_verify` — those names now legitimately target the
COMPOSER function params). react renders the REAL `state_manager.context_for_agent` ReAct transcript;
step_reasoner←real `problem_decomposer.response`; logic_verifier←real `step_reasoner.response`.
ToolAugmentedRAGNode owns NO LLMAgentNode stage (deterministic `run()` Node) — no composer needed.

**Honest dispositions (zero-tolerance Rule 2):**
- ReAct first-pass observations are genuinely empty in-graph (no tool has run yet) → rendered as an
  honest "No observations gathered yet — begin reasoning" note, NOT fabricated tool output.
- AgenticRAGNode has a GENUINE pre-existing cycle (`react_agent ↔ state_manager ↔ tool_executor`,
  confirmed on clean HEAD via `git show HEAD`) → its full graph cannot run under a plain unmarked
  LocalRuntime. The 3 AgenticRAG composers are therefore proven by a STRUCTURAL wiring assertion +
  a STANDALONE-composer production-delivery probe (load-bearing: strip a composer→messages edge → the
  structural guard goes RED, reviewer-verified). The acyclic ReasoningRAGNode runs its FULL graph
  end-to-end. `react_agent._continue_if_true` is the SDK loop-control gate (not a context port),
  correctly preserved + documented.

**End-to-end test:** `TestAgenticContextReachesLLM` (8 tests, real LocalRuntime, `_MessageCapturingLLMAgent`
Protocol-Satisfying Deterministic Adapter, top-level-input auto-distribute). Red-pre verified twice
(strip step_reasoner edge → both probe + structural guard RED; strip react edge → structural guard RED).

**Test-sweep (orphan Rule 4):** integration 6→9 (AgenticRAG) + 3→6 (ReasoningRAG); unit 6→9, 3→6,
verifier-omitted-when-disabled 5→7 (asserts the verifier composer is omitted too).

**Gates:** ruff clean; RAG suite **893 passed + 1 xfailed** (885→893, +8 new); 2 pre-existing
`test_agentic_nodes.py` pytest-httpserver fixture errors OUT OF SCOPE. base.py NOT edited.

**Redteam convergence receipts (both APPROVE, zero CRIT/HIGH/MED → converged at R1):**
- R1 security — task `acb15025770797d1d` — **APPROVE**, zero findings (5 surfaces clean; composers
  render only into chat content; no new tool-dispatch path; pre-existing `code=` blocks interpolate
  only dev-set constants; tool_executor calculate-branch is a hardened AST-walked safe-eval; no
  query/observation logging).
- R1 reviewer — task `a60999bead2e2611f` — **APPROVE**, zero CRIT/HIGH/MED. Cyclic-graph proof
  scrutiny verdict: ADEQUATE (cycle pre-existing per git HEAD; proof load-bearing; honestly surfaced).

**Files (working-tree only, held uncommitted):** `src/kaizen/nodes/rag/agentic.py`,
`tests/integration/rag/test_agentic_nodes.py`, `tests/unit/rag/test_agentic_nodes.py`.

Status: **Shard 3 DONE + held-uncommitted.** Next: Shard 4 — graph.py.

---

## Shard 4 — graph.py (GraphRAGNode, 3 LLM stages) — CONVERGED 2026-06-09

**Fix (kaizen-specialist, task a32612c162349514c):** 3 `from_function` composers —
`compose_entity_extraction_messages`←real source documents, `compose_query_analysis_messages`←real
query, `compose_summary_messages`←real `graph_retriever` output (entities/relationships/communities)
+ query. Removed the phantom `graph_builder.graph_data → summary_generator."graph_data"` edge.

**In-pass nested-port fix (#1117 class):** the existing edges read PythonCodeNode nested keys as
top-level source ports (`graph_builder."graph_data"`, `graph_retriever."graph_retrieval"`) → corrected
to `result.graph_data` / `result.graph_retrieval`. Latent because no test ran the full graph.

**Honest disposition (zero-tolerance Rule 2):** GraphRAGNode is acyclic BUT its full inner workflow
cannot run under a plain `LocalRuntime` — `graph_builder`'s `code=` block imports `networkx`, which the
PythonCodeNode sandbox blocks. This is PRE-EXISTING (3× `import networkx` on clean HEAD, reviewer-
confirmed; the pre-existing tests exercise `GraphBuilderNode.run`/`GraphQueryNode.run` directly, where
`.run` bypasses the sandbox, never the full graph). Surfaced honestly, NOT fixed (converting the
graph-algorithm `code=` blocks to `from_function` is an independent refactor, out of L3 scope). The 3
L3 composers are proven via a STRUCTURAL wiring assertion + STANDALONE-composer production-delivery
probes under real `LocalRuntime` (load-bearing: strip a composer→messages edge → guard RED, reviewer-
verified). Empty-retrieval rendered as an honest "No graph context was retrieved" note, not fabricated.

**End-to-end test:** `TestGraphContextReachesLLM` (6 tests: 4 standalone-composer probes + 2 structural
wiring guards; real LocalRuntime, top-level-input auto-distribute, Protocol-Satisfying Deterministic
Adapter). Red-pre verified.

**Test-sweep (orphan Rule 4):** unit `node_ids` 6→9 + `len` 5→7; integration `len` 6→9 + 5→7
(`summary_messages_composer` omitted when disabled). **+6 new graph tests, 0 removed** (corrected from
the implementer's inaccurate "+8" note — reviewer mechanically verified zero `-def test_` removals).

**Gates:** ruff clean; RAG suite **899 passed + 1 xfailed** (893→899, +6); 2 pre-existing
`test_agentic_nodes.py` pytest-httpserver fixture errors OUT OF SCOPE. base.py NOT edited.

**Redteam convergence receipts (both APPROVE → converged at R1):**
- R1 security — task `a4026b1e623ccdfc4` — **APPROVE**, zero security findings (5 surfaces clean;
  document/query/graph content reaches only LLM `messages`; pre-existing `code=` blocks — incl. the
  networkx-importing `graph_builder` — interpolate only dev-set constants; no logging leak; downstream
  sinks are pure in-memory networkx). 1 LOW non-security advisory: composers lack DIRECT unit tests.
- R1 reviewer — task `a04c691349d5639ae` — **APPROVE**. SCRUTINY #1 (orphaned output) → MED, orthogonal
  follow-up (see forest item below). SCRUTINY #2 (networkx) → ADEQUATE. All mechanical + L3 + honesty
  checks PASS.

**Files (working-tree only, held uncommitted):** `src/kaizen/nodes/rag/graph.py`,
`tests/integration/rag/test_graph_nodes.py`, `tests/unit/rag/test_graph_nodes.py`.

Status: **Shard 4 DONE + held-uncommitted.** Next: Shard 5 — workflows.py (last L3 shard).

### Forest item (NEW, surfaced by shard-4 redteam) — F31-FU1: summary_generator output orphaned
`GraphRAGNode.result_synthesizer` accepts `global_summaries` (wired from `summary_generator.response`)
but NEVER reads it in its `code=` body → the global-summary LLM output is DROPPED on the
`use_global_summary=True` path. **Value-anchor:** brief "provably correct, not merely importable" — an
LLM stage (summary_generator, now L3-fixed to consume context) whose OUTPUT is invisible to the user is
not provably correct end-to-end. **Disposition:** SEPARATE shard/issue — pre-existing (HEAD byte-
identical; this shard's diff never touched the synthesizer body), different bug class (Rule 3c
documented-input-unused, not the L3 messages defect), and exceeds the shard per autonomous-execution
Rule 4 (fixing it changes the synthesizer's published `graph_rag_results` shape → downstream consumers
may assert on it → needs its own test sweep). BUILD-repo issue filing is USER-GATED
(upstream-issue-hygiene MUST-1) — recommend at wave close.

### Wave-level hardening advisory (security R1 LOW, recurring across shards) — F31-FU2
The messages-composers across shards are proven mainly via integration message-capture / standalone
probes, not DIRECT composer unit tests asserting `{"messages":[...]}` output shape + the empty/None-
input branch (testing.md "One Direct Test Per Variant"). Non-blocking coverage hardening; best handled
as a wave-loop G2 codify/hardening pass, not per-shard.

---

## Shard 5 — workflows.py (1 direct LLM stage) — CONVERGED 2026-06-09

**Inventory correction (implementer + reviewer):** the lone direct `add_node("LLMAgentNode", ...)` stage
is `rag_strategy_analyzer` in **AdaptiveRAGWorkflowNode** (NOT AdvancedRAGWorkflowNode — that uses a
PythonCodeNode `quality_analyzer` + SwitchNode router). Simple/Pipeline classes have no direct LLM stage;
their composed `strategies.py` sub-pipelines are other shards' scope.

**Fix (kaizen-specialist, task a002ce0cd1e37edbd):** `compose_strategy_analyzer_messages` renders the real
in-graph `document_preprocessor` corpus characteristics (`document_count`/`avg_length`/`has_structure`/
`is_technical`/`content_types`) + the real query into `messages`. Removed the phantom
`document_preprocessor.result → rag_strategy_analyzer."input"` edge. The query reaches the composer via
`top-level query → input_mapping → preprocessor.result.query → composer.query` (wired edge; LOW-1 comment
corrected post-review to describe this accurately).

**Honest disposition:** structural + standalone-composer + message-capture proof under real LocalRuntime
(NOT full-graph — the inner `strategies.py` sub-pipelines need an out-of-graph configured vector DB,
pre-existing). Red-pre proven in-test (`test_red_pre_proof_phantom_input_edge_starves_analyzer`
reconstructs the pre-shard topology → analyzer starved).

**End-to-end test:** `TestWorkflowsContextReachesLLM` (4 tests, real LocalRuntime, Protocol adapter,
top-level-input delivery). Includes a permanent inventory invariant (exactly 1 direct LLM stage).

**Gates:** ruff clean; RAG suite **903 passed + 1 xfailed** (899→903, +4); 2 pre-existing httpserver
errors OUT OF SCOPE. base.py NOT edited.

**Redteam convergence receipts (both APPROVE → converged at R1):**
- R1 security — task `aa3d3a96e2d500880` — **APPROVE**, zero findings (composer renders only into chat
  content; `config_processor` f-string uses `repr()`-wrapped whitelist-validated dev constants; no
  logging; analyzer output feeds a fixed 4-value allowlist SwitchNode → fail-closed).
- R1 reviewer — task `a1651a6dda1e4e18b` — **APPROVE**. Output-side defect verdict: (a) orthogonal
  follow-up (see F31-FU3). Structural proof load-bearing. 1 LOW comment-accuracy nit → FIXED in-shard.

**Files (working-tree only, held uncommitted):** `src/kaizen/nodes/rag/workflows.py`,
`tests/integration/rag/test_workflows_nodes.py`.

### Forest item (NEW, surfaced by shard-5 redteam) — F31-FU3: workflows analyzer output mis-wired
`rag_strategy_analyzer` (LLMAgentNode) publishes on the `response` port, but the edges
`llm_analyzer."result" → strategy_executor.input_data` / `→ results_aggregator.llm_decision` read
`result`; AND the SwitchNode `condition_field: "recommended_strategy"` needs a PARSED `{recommended_strategy}`
dict that requires a response-parser PythonCodeNode between the LLM and the SwitchNode. **Value-anchor:**
brief "provably correct" — the L3-fixed analyzer now CONSUMES context, but its strategy DECISION never
drives the executor (output dropped + unparsed). **Disposition:** SEPARATE shard/issue — pre-existing
(HEAD byte-identical), different bug class (output port + JSON parse + SwitchNode dispatch reshape),
exceeds the shard per autonomous-execution Rule 4. Same CLASS as F31-FU1 (graph orphaned output).

---

## WAVE 2 — CONVERGED (all 5 shards) — 2026-06-09

**Delivered:** 19 LLM stages across 5 node files now CONSUME retrieved/query context via the valid
`messages` port (was system-prompt-only) — eval (3 judges), query_processing (6), agentic (6), graph (3),
workflows (1) — plus Wave-1 conversational (3). Each shard ships a real-LocalRuntime end-to-end test with
a verified red-pre proof; every shard converged via parallel reviewer + security-reviewer redteam (shards
2-5 at R1 both-APPROVE-zero-findings; shard 1 at R2 after a held-shard stale-test HIGH was fixed).

**In-pass same-class fixes (autonomous-execution Rule 4):** nested-port `result.<key>` hygiene (#1117
class) in eval + graph (latent because no full-graph test ran); 2 stale held-shard regression tests swept
in shard 1 (RAGBenchmark runnable-systems rewrite + conversational node-count 5→6).

**Final gate:** RAG suite (unit+integration) **903 passed + 1 xfailed**; RAG regression (f8b9b + f9) 32
passed. 2 pre-existing `test_agentic_nodes.py` pytest-httpserver fixture errors remain OUT OF SCOPE.
base.py untouched throughout. **BUILD repo — ALL of Wave 1 + Wave 2 + the held #1117 shards are held
UNCOMMITTED in the working tree; commit stays with the user.**

### G2 — Learning capture (wave-loop inter-wave gate, lightweight)
1. **Output-side wiring is a PARALLEL systemic defect class** (NEW). The L3 program fixed the INPUT side
   (LLM consumes context via `messages`). Shards 4+5 surfaced that several LLM stages' OUTPUT is then
   dropped or mis-wired: graph `summary_generator` output unread by `result_synthesizer` (F31-FU1);
   workflows `rag_strategy_analyzer` publishes `response` but downstream reads `result` + needs a parser
   (F31-FU3). A node is NOT provably-correct end-to-end if it consumes context but its decision/output is
   invisible. This is a coherent next workstream — likely more instances across the family.
2. **Structural+standalone-probe is a validated proof technique** for non-runnable-graph nodes (cyclic
   ReAct in agentic; networkx-sandbox in graph; vector-DB requirement in workflows). Reviewer-confirmed
   load-bearing in all three (strip composer→messages edge → structural guard RED).
3. **Composer-direct-unit-test gap (F31-FU2):** composers proven via integration capture/probes, not
   direct unit tests of `{"messages":[...]}` shape + empty-input branch (testing.md "one direct test per
   variant"). Wave-level hardening, best at a codify pass.
4. **Latent nested-port (#1117) defects persist** in the codegen base (found in-pass in eval + graph) —
   confirms the from_function migration (original Wave 3) will systematically close a real class.

### G4 — Re-value-rank of the remaining F31 program (user-gated)
Original program: Wave 3 = from_function migration of real-content codegen; Wave 4 = simulated nodes
(strip/flag/build — product call). NEW candidate: **Wave 2.5 (output-side wiring)** — make every L3-fixed
LLM stage's OUTPUT actually reach its real downstream consumer in the right port+shape. Recommended
re-rank (value-anchor: brief "provably correct, not merely importable"):
1. **Output-side wiring (F31-FU1 + F31-FU3 + family sweep) — HIGHEST.** Without it, the L3 input fix is
   necessary-but-not-sufficient: the node consumes context but its answer is dropped. Most direct
   "provably correct" completion of the work just done.
2. **from_function migration (orig Wave 3)** — mechanical de-risk; supersedes the held in-place #1117
   patches; closes the latent nested-port class. High value, large scope.
3. **Wave 4 simulated nodes** — PRODUCT CALL (user): strip / experimental-flag / build-real.
4. **F31-FU2 composer unit tests + the kaizen-agents venv `uv sync`** — hardening / env hygiene.
