# Issue #814: fix(kaizen): resolve pre-existing pyright diagnostics in tools/native + research

## Affected API

`kailash_kaizen.tools.native.*` (BaseTool subclass family) and `kailash_kaizen.research.*`.

## Symptoms

`uv run pyright src/kaizen/tools/native/ src/kaizen/research/` reports **29 errors, 8 warnings** (verified against pyright 1.1.371) across four categories:

### A. BaseTool.execute signature drift (7 sites — base has 2 params; overrides diverge)

| File                               | Line | Override params       |
| ---------------------------------- | ---- | --------------------- |
| `tools/native/skill_tool.py`       | 96   | 3 + `**kwargs`        |
| `tools/native/process_tool.py`     | 365  | 3                     |
| `tools/native/process_tool.py`     | 478  | 5                     |
| `tools/native/search_tools.py`     | 64   | 3 + `**kwargs`        |
| `tools/native/search_tools.py`     | 227  | 3 + `**kwargs`        |
| `tools/native/task_tool.py`        | 117  | 8 + `**kwargs`        |
| `tools/native/todo_tool.py`        | 278  | 3                     |

### B. Optional/None safety (4 sites)

- `tools/native/task_tool.py:283` — `for_specialist` accessed on possibly-None object.
- `tools/native/notebook_tool.py:245` — `result` possibly unbound.
- `research/parser.py:83` — `Search` accessed on possibly-None object (lazy `arxiv` import sentinel).
- `research/parser.py:138` — `None` callable (lazy import sentinel).

### C. Missing imports (5 sites — modules referenced but not on disk OR not declared in deps)

- `research/__init__.py:26` — `.advanced_patterns` (module not on disk).
- `research/__init__.py:35` — `.experimental` (module not on disk).
- `research/__init__.py:39` — `.intelligent_optimizer` (module not on disk).
- `tools/native/search_tools.py:54/101` — `duckduckgo_search` (not in `pyproject.toml::dependencies`).
- `tools/native/search_tools.py:298` — `bs4` (not in `pyproject.toml::dependencies`).
- `research/parser.py:20` — `arxiv` (not declared).
- `research/parser.py:28` — `pypdf` (not declared).

### D. Type-argument mismatch (2 sites at one line)

- `research/adapter.py:119` passes `dict[str, str]` where `List[str] | None` expected (×2 — `inputs` and `outputs` parameters).

## Reproduction

```bash
cd packages/kailash-kaizen
uv run pyright src/kaizen/tools/native/ src/kaizen/research/
# Reports: 29 errors, 8 warnings, 0 informations
```

## Expected vs actual

- **Expected:** Pyright reports zero errors on `kailash_kaizen.tools.native.*` and `kailash_kaizen.research.*`. `BaseTool` subclass methods type-check against the base contract; `__init__` imports resolve; Optional access is guarded; declared dependencies match imports per `rules/dependencies.md` § "Declared = Imported".
- **Actual:** 29 errors + 8 warnings as enumerated above. SHA-grounded as pre-existing per `git log --oneline packages/kailash-kaizen/src/kaizen/tools/native/notebook_tool.py | head -1` → `b511f186` (2026-03-19) `style(eatp,dataflow,kaizen): standardize code formatting...`.

## Severity

**MEDIUM** — diagnostics are static-analysis only; no runtime crash observed today. But:

- The BaseTool contract drift is a maintenance hazard — every new tool implementation inherits the inconsistency.
- The missing-module imports in `research/__init__.py` are silent stubs (`rules/zero-tolerance.md` Rule 2): the package advertises `advanced_patterns`, `experimental`, `intelligent_optimizer` but the modules don't exist; first user-import of any will raise `ModuleNotFoundError`.
- The undeclared deps (`duckduckgo_search`, `bs4`, `arxiv`, `pypdf`) violate `rules/dependencies.md` § "Declared = Imported" — clean-venv installs hit `ModuleNotFoundError` whenever those code paths run.

## Acceptance criteria

- [ ] **BaseTool contract ratified.** Either widen `BaseTool.execute` to accept all current call patterns OR keep narrow signature + `**kwargs` and update every subclass to match.
- [ ] All 7 `BaseTool` subclass `execute` overrides match the ratified signature.
- [ ] `kaizen/research/__init__.py` imports resolve — either by creating the missing modules (`advanced_patterns`, `experimental`, `intelligent_optimizer`) OR by removing the imports if those features are not on roadmap (apply `rules/orphan-detection.md` Rule 3: removed = deleted, not deprecated).
- [ ] Undeclared deps (`duckduckgo_search`, `bs4`, `arxiv`, `pypdf`) added to `packages/kailash-kaizen/pyproject.toml::dependencies` OR moved to optional-extras with import-time guard per `rules/dependencies.md` § "BLOCKED Anti-Patterns".
- [ ] Optional/None safety guards added at the 4 cited sites (`task_tool.py:283`, `notebook_tool.py:245`, `research/parser.py:83/138`).
- [ ] `research/adapter.py:119` type-argument mismatch resolved (correct call shape OR widen the callee's accepted type).
- [ ] `uv run pyright src/kaizen/tools/native/ src/kaizen/research/` reports 0 errors, 0 warnings.
- [ ] Regression test in `packages/kailash-kaizen/tests/regression/` calls each `BaseTool` subclass's `execute` directly per `rules/testing.md` § "One Direct Test Per Variant In Every Delegating Pair".
