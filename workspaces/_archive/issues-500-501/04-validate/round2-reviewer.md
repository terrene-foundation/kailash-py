# /redteam Round 2 — #500 + #501 Nexus lifespan fix

Date: 2026-04-18
Commits audited: `fc77bdd3`, `dea8c195`, `1ac214b3` (round-2 fixes on top of `1535f4be` / `7463a5fb`).

## Round 1 findings — verification

| Finding                                                  | Status                   | Evidence                                                                                                                                                                                                                                                                                                                                                                                                              |
| -------------------------------------------------------- | ------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| sec H1 (partial-startup leak)                            | FIXED                    | `src/kailash/servers/workflow_server.py:195-254` — `try:` now wraps `app.router.startup()` AND `startup_hook(...)`; `finally:` runs `shutdown_hook` + `router.shutdown()` + `shutdown_coordinator.shutdown()` on both normal AND aborted-startup paths. Each teardown step wrapped in its own `try/except` → WARN log. Test `tests/integration/nexus/test_partial_startup_teardown.py` exercises the path end-to-end. |
| sec H2 / rev H1 (idempotency TOCTOU + no test)           | FIXED                    | `packages/kailash-nexus/src/nexus/core.py:435-436` — `_shutdown_hooks_fired_lock: threading.Lock = threading.Lock()`. Check-and-set under the lock in BOTH paths: sync at L2037-2044, async at L2092-2095. `tests/integration/nexus/test_shutdown_idempotency.py` (250 LOC) covers BOTH orderings (lifespan-then-stop, stop-then-lifespan) against real uvicorn + real plugin counter.                                |
| rev M1 (f-string logs in new lifespan)                   | FIXED                    | `grep 'logger\.\w+\(f' src/kailash/servers/workflow_server.py` on the new lifespan block (L179-254) returns 0 matches. All lifespan log lines use `logger.info("workflow_server.lifespan.*", extra={...})` structured form. (Pre-existing f-string log lines outside the new lifespan remain at L366, L386, L427, etc. — Rule 1 pre-existing disposition: out of scope for this fix, not introduced in round 2.)      |
| rev M2 (`asyncio.iscoroutinefunction` sites)             | FIXED (core.py), NEW LOW | All 7 sites in `packages/kailash-nexus/src/nexus/core.py` now use `inspect.iscoroutinefunction` (L213, L1626, L2015, L2047, L2068, L2098). One residual site exists at `packages/kailash-nexus/src/nexus/auth/audit/backends/custom.py:32` — outside the round-1 scope but still a Python 3.14 DeprecationWarning source. See LOW-1 below.                                                                            |
| sec M2 (startup_hook timeout)                            | FIXED                    | `WorkflowServer.__init__` accepts `startup_hook_timeout: Optional[float] = None`; forwarded from `create_gateway` (gateway.py L39). Lifespan wraps `startup_hook()` in `asyncio.wait_for(..., timeout=startup_hook_timeout)` at workflow_server.py L210-220 when the kwarg is set. `tests/integration/nexus/test_startup_hook_timeout.py` (99 LOC) asserts a hung hook is aborted AND shutdown teardown still runs.   |
| rev M4 (create_gateway docstring)                        | FIXED                    | `src/kailash/servers/gateway.py:62-74` — `Args:` block documents `startup_hook`, `shutdown_hook`, `startup_hook_timeout` with full contract descriptions (what runs, when, exception handling, timeout defense rationale).                                                                                                                                                                                            |
| sec M1 (WorkflowServer `__del__`)                        | DEFERRED                 | Not addressed. Not an in-scope MUST for this round (round-1 recommended "consider **del**/close"); pre-existing architecture issue that interacts with M3 below but is lower priority than the H1 fix.                                                                                                                                                                                                                |
| sec M3 (shutdown hook exception aggregation)             | DEFERRED                 | Round-1 proposed "aggregate exceptions + emit summary + Counter"; current code uses `logger.exception("Shutdown hook failed: %s", hook)` per hook which satisfies zero-tolerance Rule 3 (log + continue in cleanup path, explicit carve-out). The aggregate-counter improvement is a nice-to-have, not a rule violation.                                                                                              |
| rev M3 (AsyncLocalRuntime `ref_count=1` ResourceWarning) | DEFERRED                 | Pre-existing leak; re-observed in round-2 suite output via `tests/integration/nexus/test_shutdown_idempotency.py` test-teardown path (not emitted by the round-2 tests themselves — only surfaces with full nexus integration run). Not re-surfaced by the 5 new tests run in isolation; status unchanged from round 1.                                                                                               |
| sec L1 (`_shutdown_hooks_fired` not thread-safe)         | FIXED (upgraded)         | Round-1 LOW → round-2 fix lifted to `threading.Lock`. Explicitly called out in the L2037 and L2092 comment blocks as closing the sec H2 TOCTOU.                                                                                                                                                                                                                                                                       |
| sec L2 (regression tests slow)                           | UNCHANGED                | 5 integration tests pass in 1.40s (prior: 3.84s). Shared-server fixture not applied; test cost still acceptable. Not a rule violation.                                                                                                                                                                                                                                                                                |
| rev L3 (cross-SDK: kailash-rs clean)                     | UNCHANGED                | Disposition held.                                                                                                                                                                                                                                                                                                                                                                                                     |

## Round 2 NEW findings

### HIGH

_None._

### MED

_None._

### LOW

**LOW-1 — residual `asyncio.iscoroutinefunction` site outside round-1 scope.**
`packages/kailash-nexus/src/nexus/auth/audit/backends/custom.py:32` still calls `asyncio.iscoroutinefunction(self._store_func)`. This path is not exercised by the round-2 suite but emits a `DeprecationWarning` on Python 3.14 and will be a hard error on 3.16. Disposition: follow-up one-line swap in a future commit on the same branch; rule-1 scanner-surface symmetry argues for fixing it now, but it is mechanically identical to the core.py swap and out of the lifespan-fix scope.

## Green (both rounds of mechanical checks)

- `pytest --collect-only -q tests/` on root: **15,969 tests collected, exit 0**, no ImportError / ModuleNotFoundError. (The combined `tests/ + packages/kailash-kaizen/tests/` invocation fails with `ImportPathMismatchError` on `tests.conftest` — pre-existing monorepo conftest-naming collision, not introduced by round-2 commits; each subtree collects cleanly in isolation.)
- 5 target tests (`test_shutdown_idempotency`, `test_startup_hook_timeout`, `test_partial_startup_teardown`, + 2 prior-round tests) all pass: `5 passed, 2 warnings in 1.40s`. The 2 warnings are the pre-existing upstream `websockets.legacy` + `websockets.server.WebSocketServerProtocol` DeprecationWarnings, dispositioned as Upstream in round 1.
- `threading.Lock` usage is correct in BOTH async and sync paths: check-and-set runs inside `with self._shutdown_hooks_fired_lock:` (L2037-2044 sync, L2092-2095 async). Hook-loop iteration runs OUTSIDE the lock — which is correct: the lock's job is to atomically claim the "I'm the one firing hooks" right, not to serialize the hook loop itself. No deadlock risk (lock is not held across `await hook()`). No `try/finally` around the set because the set happens before any exception-raising code (hook loop) under the lock.
- CHANGELOG.md entries retained for #500 + #501 under `[Unreleased]`. No cross-SDK follow-up required (kailash-rs verified clean in round 1).

## CONVERGED: yes

All 3 in-scope HIGH/MED findings from round 1 (sec H1 / H2+rev H1 / rev M1 / sec M2 / rev M4) are FIXED with landed code + landed test evidence. No new HIGH or MED findings introduced by the round-2 commits. The single new LOW (residual `asyncio.iscoroutinefunction` at `auth/audit/backends/custom.py:32`) is mechanically identical to the sweep already performed on core.py and can be closed out in a one-line follow-up without holding the PR.
