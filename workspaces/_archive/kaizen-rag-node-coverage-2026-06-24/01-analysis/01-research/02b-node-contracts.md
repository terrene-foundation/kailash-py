# 02b — Node Contract Deep-Dive (part B: modules 10–17 + summary)

Continues `02-node-contracts.md`. Same legend. Systemic findings S1–S7 are
defined in the part-A file and referenced here by tag.

## Per-class contracts — Part B (modules 10–17, alphabetical)

### query_processing.py

All 6 are pure `Node` subclasses. Each has a self-contained keyword/rule
`run()` AND an unused `_create_workflow()` (Shape-W LLM graph that is never
invoked by `run()`). Module hard-imports `LLMAgentNode` (`:23`, S1) and
`PythonCodeNode` (`:19`) but the `run()` paths never touch them.

| Class                        | reg? | **all**? | Contract (run signature; cite)                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Infra        | Backend                          | Breakage                                             |
| ---------------------------- | ---- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------ | -------------------------------- | ---------------------------------------------------- |
| `QueryExpansionNode`         | yes  | yes      | `run(**kwargs)` `query:str`; returns {original, expansions, keywords, concepts, all_terms, expansion_count} via static suffix templates (`query_processing.py:94-136`).                                                                                                                                                                                                                                                                                                                             | PURE_COMPUTE | string-only (run never uses LLM) | MED — S1 (module import of `LLMAgentNode`)           |
| `QueryDecompositionNode`     | yes  | yes      | `run(**kwargs)` `query:str`; returns {sub_questions, execution_order, composition_strategy, total_questions}; splits on " and "/" compare " (`:270-308`).                                                                                                                                                                                                                                                                                                                                           | PURE_COMPUTE | string-only                      | MED — S1                                             |
| `QueryRewritingNode`         | yes  | yes      | `run(**kwargs)` `query:str`; returns {original, issues_found, versions, recommended, all_unique_versions, improvement_count}; hardcoded `replace()` corrections (`:461-523`).                                                                                                                                                                                                                                                                                                                       | PURE_COMPUTE | string-only                      | MED — S1                                             |
| `QueryIntentClassifierNode`  | yes  | yes      | `run(**kwargs)` `query:str`; returns {query_type, domain, complexity, requirements, recommended_strategy, confidence}; keyword classification (`:692-791`). NOTE: this is deterministic intent classification — acceptable HERE because it is a RAG-strategy hint, not LLM-agent decision routing; but flag for `agent-reasoning.md` review (it IS keyword classification on user query — the implement phase / reviewer must confirm this node is a heuristic helper, not an agent decision path). | PURE_COMPUTE | string-only                      | MED — S1 + flag agent-reasoning posture for reviewer |
| `MultiHopQueryPlannerNode`   | yes  | yes      | `run(**kwargs)` `query:str`; returns {batches, total_hops, parallel_opportunities, combination_strategy, estimated_time}; "influence"/"impact" keyword branch (`:958-1055`).                                                                                                                                                                                                                                                                                                                        | PURE_COMPUTE | string-only                      | MED — S1                                             |
| `AdaptiveQueryProcessorNode` | yes  | yes      | `run(**kwargs)` `query:str`; returns {original_query, processing_steps, processed_query, processing_plan, expected_improvement}; char/keyword branch (`:1215-1263`). `_create_workflow` adds `"QueryIntentClassifierNode"` by string (`:1271`) — cross-node string dep.                                                                                                                                                                                                                             | PURE_COMPUTE | string-only                      | MED — S1                                             |

### realtime.py

| Class                      | reg?        | **all**? | Contract                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Infra                                                                                               | Backend                                                       | Breakage                                                                                                                                                                                                                                                                                   |
| -------------------------- | ----------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- | ------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `RealtimeRAGNode`          | yes         | yes      | `WorkflowNode`; `__init__(update_interval, relevance_decay_rate, max_buffer_size)` builds monitor/indexer/time-retriever/stream-formatter graph (all pure Python; time-decay = `decay_rate ** age_hours`) (`realtime.py:84-371`). ALSO `async start_monitoring(data_sources)` (`:373`, spawns `asyncio.create_task(self._monitor_loop())`), `stop_monitoring()` (`:397`) — non-`run` public async methods needing dedicated tests. No own run (inherits WorkflowNode).                                                                                              | CONSTRUCTION=PURE_COMPUTE; FULL-RUN=PURE_COMPUTE; `start_monitoring`=PURE_COMPUTE(async, self-task) | string `PythonCodeNode`; `EventStreamNode` import (`:24`, S5) | MED — S5 (`EventStreamNode` import-existence)                                                                                                                                                                                                                                              |
| `RealtimeStreamingRAGNode` | yes (alias) | yes      | `Node` with `@register_node(alias="RealtimeStreamingRAGNode")` (`realtime.py:402`) — THIS is the resurrection collision rename (was `StreamingRAGNode`, collided with `optimized.StreamingRAGNode`). Public surface is `async stream(**kwargs)` `query:str`,`documents:list`,opt `max_chunks:int` → AsyncIterator yielding {type:start/chunk/complete} (`:465-526`) PLUS sync `run(**kwargs)` returning first chunk only (`:528-552`). Two public entrypoints; BOTH need behavioral tests (async `stream` is the documented contract; sync `run` is a compat shim). | PURE_COMPUTE (keyword scoring)                                                                      | none                                                          | MED — verify the `alias=` decorator kwarg is still supported by `register_node` on `main` (2-month-stale decorator API; resurrection PR fixed THIS collision so it is the highest-scrutiny node — confirm registration works AND no double-registration with `optimized.StreamingRAGNode`) |
| `IncrementalIndexNode`     | yes         | yes      | pure `Node.run(**kwargs)` `operation:str`(add/remove/update/search),opt `documents:list`,`document_ids:list`,`query:str`; self-owned inverted index dict; returns op-specific stats (`realtime.py:628-762`). Operation dispatch = config-branch (permitted).                                                                                                                                                                                                                                                                                                        | PURE_COMPUTE (in-memory index)                                                                      | none (self-owned `self.index`)                                | LOW                                                                                                                                                                                                                                                                                        |

### registry.py

| Class                 | reg? | **all**? | Contract                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Infra        | Backend                                                            | Breakage                                                                                                                                                                               |
| --------------------- | ---- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------ | ------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `RAGWorkflowRegistry` | NO   | yes      | Plain class (NOT a `Node`, no `run()`). Public API: `list_strategies()`, `list_workflows()`, `list_utilities()`, `recommend_strategy(document_count, avg_document_length, is_technical, has_structure, query_type, performance_priority)` → {recommended_strategy, reasoning, confidence, alternatives, strategy_details} (`registry.py:199-299`), `recommend_workflow(...)`, `create_strategy(name, config=None, **kw)`, `create_workflow(...)`, `create_utility(...)`, `get_quick_start_guide()`, `get_strategy_comparison()`. Behavioral coverage = exercise each public method + assert `create_strategy("semantic")` actually returns a `SemanticRAGNode` instance and `create_workflow("configurable")` returns a `RAGPipelineWorkflowNode`. NOTE: `recommend_strategy` is rule-based scoring — documented as "recommendation logic", acceptable (not an LLM-agent path); flag for reviewer that this is deterministic-by-design. Module-level singleton `rag_registry` instantiated at import (`:547`) — that instantiation IS the import-smoke that the resurrection test exercises. | PURE_COMPUTE | none; `create_*` factory-instantiates the node classes it imported | LOW (the `:547` module-level instantiation already proves import; the factory methods that build Shard-W nodes inherit those nodes' construction risk but not at registry-import time) |

### router.py

| Class                       | reg? | **all**? | Contract                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Infra                                                  | Backend                                               | Breakage                                                                                                                                                                                                                |
| --------------------------- | ---- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------ | ----------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `RAGStrategyRouterNode`     | yes  | yes      | pure `Node.run(**kwargs)` `documents:list`,opt `query:str`,`user_preferences:dict`,`performance_history:dict`; returns {strategy, reasoning, confidence, fallback_strategy, document_analysis, llm_model_used, routing_metadata} (`router.py:67-116`). Constructs `LLMAgentNode(name=, model=, provider=, system_prompt=)` DIRECTLY (`:76`) and calls `.execute(messages=[...])` (`:92`); on exception falls back to rule-based `_fallback_strategy_selection` (`:417`, deterministic — acceptable as documented fallback). Doc-analysis (`_analyze_documents`) is pure keyword stats. | COMPOSITE(LLM golden path; PURE_COMPUTE fallback path) | DIRECT `LLMAgentNode` ctor (`:76`) — S1 high-scrutiny | **HIGH — S1: direct `LLMAgentNode(name=,model=,provider=,system_prompt=)` ctor at `:76`, 2-month-unverified kwarg contract. The fallback path is PURE_COMPUTE and testable with [rag]; the golden path needs live LLM** |
| `RAGQualityAnalyzerNode`    | yes  | yes      | pure `Node.run(**kwargs)` `rag_results:dict`,opt `query:str`,`expected_results:list`; returns {quality_score, quality_analysis, content_analysis, recommendations, passed_quality_check, analysis_timestamp}; pure stats (variance, diversity, coverage) (`router.py:479-519`).                                                                                                                                                                                                                                                                                                        | PURE_COMPUTE                                           | none                                                  | LOW                                                                                                                                                                                                                     |
| `RAGPerformanceMonitorNode` | yes  | yes      | pure `Node.run(**kwargs)` `rag_results:dict`,opt `execution_time:float`,`strategy_used:str`,`query_type:str`; returns {current_performance, metrics, insights, performance_history_size}; self-owned `self.performance_history` rolling list (`router.py:700-736`). Stateful — test must exercise ≥2 calls to assert history accumulation.                                                                                                                                                                                                                                             | PURE_COMPUTE (in-memory history)                       | none                                                  | LOW                                                                                                                                                                                                                     |

### similarity.py

7 pure `Node` subclasses. Each `run()` is self-contained
keyword/BM25/late-interaction/RRF math over `documents`. Several also have
an unused `_create_workflow()` (Shape-W, never called by `run()`). Module
imports only `numpy` (in `[rag]`), `WorkflowNode`, `WorkflowBuilder` — NO
`LLMAgentNode` module-scope import (so S1 does NOT apply to similarity.py).

| Class                           | reg? | **all**? | Contract (run signature; cite)                                                                                                                                                                                             | Infra                          | Backend | Breakage                                                                                     |
| ------------------------------- | ---- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------ | ------- | -------------------------------------------------------------------------------------------- |
| `DenseRetrievalNode`            | yes  | yes      | `run(**kwargs)` `query:str`,`documents:list`,opt `k:int`; returns {results, scores, retrieval_method, total_results}; keyword-overlap stand-in (docstring says embeddings, code does set-overlap `similarity.py:107-161`). | PURE_COMPUTE                   | none    | LOW; FLAG: docstring "dense embeddings" vs keyword-overlap impl (fidelity gap, not breakage) |
| `SparseRetrievalNode`           | yes  | yes      | `run(**kwargs)` `query`,`documents`,opt `k`; real BM25 with `np.log` IDF (`:244-323`). `_create_workflow` (LLM query-expander) unused by run.                                                                              | PURE_COMPUTE (numpy, in [rag]) | none    | LOW                                                                                          |
| `ColBERTRetrievalNode`          | yes  | yes      | `run(**kwargs)` `query`,`documents`,opt `k`; token max-sim stand-in (substring match, not BERT `:526-589`).                                                                                                                | PURE_COMPUTE                   | none    | LOW; FLAG fidelity (no BERT)                                                                 |
| `MultiVectorRetrievalNode`      | yes  | yes      | `run(**kwargs)` `query`,`documents`,opt `k`; weighted full/summary/keyword overlap (`:784-852`).                                                                                                                           | PURE_COMPUTE                   | none    | LOW                                                                                          |
| `CrossEncoderRerankNode`        | yes  | yes      | `run(**kwargs)` `query:str`,`initial_results:dict`,opt `k:int`; reranks `initial_results["results"]` by 0.4*init+0.3*cov+0.3\*prec (`:1087-1146`). DIFFERENT input contract (`initial_results` dict, not `documents`).     | PURE_COMPUTE                   | none    | LOW                                                                                          |
| `HybridFusionNode`              | yes  | yes      | `run(**kwargs)` `retrieval_results:list[dict]`,opt `fusion_method:str`,`k:int`; RRF/weighted fusion (`:1329-1395`). Input is a LIST of result dicts.                                                                       | PURE_COMPUTE                   | none    | LOW                                                                                          |
| `PropositionBasedRetrievalNode` | yes  | yes      | `run(**kwargs)` `query`,`documents`,opt `k`; sentence-split proposition overlap (`:1614-1687`).                                                                                                                            | PURE_COMPUTE                   | none    | LOW                                                                                          |

### strategies.py

| Class                 | reg? | **all**? | Contract                                                                                                                                                                                                                                                                                                                                                        | Infra                                                                                                                                                                                | Backend                                                          | Breakage                                                                                                                                                                                                                                             |
| --------------------- | ---- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `RAGConfig`           | NO   | yes      | `@dataclass` (chunk_size=1000, chunk_overlap=200, embedding_model="text-embedding-3-small", embedding_provider="openai", vector_db_provider="postgresql", retrieval_k=5, similarity_threshold=0.7) (`strategies.py:20-30`). The exported config; behavioral "test" = construct, assert defaults, assert it threads into a strategy node.                        | PURE_COMPUTE                                                                                                                                                                         | n/a                                                              | LOW (but S3 name-collision context)                                                                                                                                                                                                                  |
| `SemanticRAGNode`     | yes  | yes      | `run(**kwargs)` requires `documents:list`,opt `query:str`,`operation:str`(default "index"); lazily builds via `create_semantic_rag_workflow(self.config)` then `self.workflow_node.execute(**kwargs)` (`strategies.py:409-415`). The built WorkflowNode wires string `SemanticChunkerNode`→`EmbeddingGeneratorNode`→`VectorDatabaseNode`→`HybridRetrieverNode`. | CONSTRUCTION=PURE_COMPUTE; FULL-RUN=COMPOSITE(EMBEDDING + VECTOR_STORE — `EmbeddingGeneratorNode`+`VectorDatabaseNode` need a real embedding model + vector index ABSENT from [rag]) | optional `config:RAGConfig`; backend = string kailash node-types | **HIGH — built graph's `EmbeddingGeneratorNode`/`VectorDatabaseNode` string node-types must still be registered in kailash after 2 months; `.execute()` golden path needs embedding+vector infra. Construction is the [rag]-only testable contract** |
| `StatisticalRAGNode`  | yes  | yes      | same shape, `create_statistical_rag_workflow` (adds `StatisticalChunkerNode`, `PythonCodeNode` keyword extractor) (`strategies.py:456-461`).                                                                                                                                                                                                                    | CONSTRUCTION=PURE_COMPUTE; FULL-RUN=COMPOSITE(EMBEDDING+VECTOR)                                                                                                                      | optional config                                                  | HIGH (same as Semantic)                                                                                                                                                                                                                              |
| `HybridRAGNode`       | yes  | yes      | `run(**kwargs)` `documents`,opt `query`,`operation`,`fusion_method`; nested WorkflowNodes (semantic+statistical sub-workflows + RRF PythonCodeNode) via `create_hybrid_rag_workflow` (`strategies.py:512-520`). Rebuilds if `fusion_method` changes.                                                                                                            | CONSTRUCTION=PURE_COMPUTE; FULL-RUN=COMPOSITE(EMBEDDING+VECTOR, nested)                                                                                                              | optional config + `fusion_method`                                | HIGH (nested graph; same infra dep)                                                                                                                                                                                                                  |
| `HierarchicalRAGNode` | yes  | yes      | same shape, `create_hierarchical_rag_workflow` (multi-level chunker + 3 `VectorDatabaseNode` collections) (`strategies.py:561-566`).                                                                                                                                                                                                                            | CONSTRUCTION=PURE_COMPUTE; FULL-RUN=COMPOSITE(EMBEDDING+3×VECTOR)                                                                                                                    | optional config                                                  | HIGH (3 vector collections; same infra dep)                                                                                                                                                                                                          |

### workflows.py

| Class                     | reg? | **all**? | Contract                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Infra                                                               | Backend                                   | Breakage                                                                                                                                                      |
| ------------------------- | ---- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------- | ----------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `SimpleRAGWorkflowNode`   | yes  | yes      | `WorkflowNode` subclass; `__init__(name, config=None)` = `super().__init__(workflow=create_semantic_rag_workflow(config).workflow, ...)` (`workflows.py:36-49`). Construction wraps the semantic graph. No own run.                                                                                                                                                                                                                                                                                  | CONSTRUCTION=PURE_COMPUTE; FULL-RUN=COMPOSITE(EMBEDDING+VECTOR)     | optional `config:RAGConfig` (strategies') | HIGH (same string-node infra dep as SemanticRAGNode)                                                                                                          |
| `AdvancedRAGWorkflowNode` | yes  | yes      | `WorkflowNode`; `_create_advanced_workflow` builds quality-analyzer/`SwitchNode` router/4 strategy sub-workflows/validator (`workflows.py:53-204`). No own run.                                                                                                                                                                                                                                                                                                                                      | CONSTRUCTION=PURE_COMPUTE; FULL-RUN=COMPOSITE                       | optional config                           | HIGH (4 nested strategy graphs)                                                                                                                               |
| `AdaptiveRAGWorkflowNode` | yes  | yes      | `WorkflowNode`; `__init__(name, llm_model="gpt-4", config=None)` builds `LLMAgentNode` strategy-analyzer + preprocessor + `SwitchNode` + 4 strategy pipelines + aggregator (`workflows.py:208-448`). S4: hardcoded `llm_model="gpt-4"` default at `:219` + `"model": "gpt-4"` in aggregator code (`:402`). No own run.                                                                                                                                                                               | CONSTRUCTION=PURE_COMPUTE; FULL-RUN=COMPOSITE(LLM+EMBEDDING+VECTOR) | optional config; string `LLMAgentNode`    | HIGH (LLM router + 4 strategy graphs); S4 hardcoded model                                                                                                     |
| `RAGPipelineWorkflowNode` | yes  | **NO**   | `WorkflowNode`; `__init__(name, default_strategy="hybrid", config=None)` builds config-processor + `SwitchNode` dispatcher + 4 strategy nodes + formatter (`workflows.py:452-577`). **Decorated + registered but ABSENT from `__init__.__all__`** (the 56-vs-55 not-exported delta). `registry.py` imports + uses it as the `"configurable"` workflow. Coverage MUST exercise it via `RAGWorkflowRegistry.create_workflow("configurable")` since direct public import is not advertised. No own run. | CONSTRUCTION=PURE_COMPUTE; FULL-RUN=COMPOSITE                       | optional config + `default_strategy`      | HIGH (4 strategy graphs); + the `__all__` omission is itself an `orphan-detection.md` §6-class finding to flag (decorated public-ish symbol not in `__all__`) |

## Contract summary table (all 58 — one row per class)

`tier` here = the dominant behavioral-coverage tier. For Shape-W nodes,
`[rag]-alone?` is split CONSTRUCT/RUN. "broken" in risk = a provable
runtime defect (not merely "needs infra").

| #   | Class                         | Module           | infra-tier                      | [rag]-alone?               | breakage                          |
| --- | ----------------------------- | ---------------- | ------------------------------- | -------------------------- | --------------------------------- |
| 1   | RAGConfig (dc)                | strategies       | PURE_COMPUTE                    | yes                        | LOW                               |
| 2   | SemanticRAGNode               | strategies       | COMPOSITE(EMB+VEC)              | construct:yes / run:no     | HIGH                              |
| 3   | StatisticalRAGNode            | strategies       | COMPOSITE(EMB+VEC)              | construct:yes / run:no     | HIGH                              |
| 4   | HybridRAGNode                 | strategies       | COMPOSITE(EMB+VEC)              | construct:yes / run:no     | HIGH                              |
| 5   | HierarchicalRAGNode           | strategies       | COMPOSITE(EMB+VEC)              | construct:yes / run:no     | HIGH                              |
| 6   | SimpleRAGWorkflowNode         | workflows        | COMPOSITE(EMB+VEC)              | construct:yes / run:no     | HIGH                              |
| 7   | AdvancedRAGWorkflowNode       | workflows        | COMPOSITE                       | construct:yes / run:no     | HIGH                              |
| 8   | AdaptiveRAGWorkflowNode       | workflows        | COMPOSITE(LLM+EMB+VEC)          | construct:yes / run:no     | HIGH                              |
| 9   | RAGPipelineWorkflowNode       | workflows        | COMPOSITE                       | construct:yes / run:no     | HIGH (+**all** omission)          |
| 10  | RAGStrategyRouterNode         | router           | COMPOSITE(LLM); fallback PURE   | fallback:yes / golden:no   | HIGH (S1 direct ctor)             |
| 11  | RAGQualityAnalyzerNode        | router           | PURE_COMPUTE                    | yes                        | LOW                               |
| 12  | RAGPerformanceMonitorNode     | router           | PURE_COMPUTE                    | yes                        | LOW                               |
| 13  | SelfCorrectingRAGNode         | advanced         | COMPOSITE(LLM+broken stub)      | no (run broken)            | HIGH (S2)                         |
| 14  | RAGFusionNode                 | advanced         | COMPOSITE(LLM+broken stub)      | no (run broken)            | HIGH (S2)                         |
| 15  | HyDENode                      | advanced         | COMPOSITE(LLM+broken stub)      | no (run broken)            | HIGH (S2)                         |
| 16  | StepBackRAGNode               | advanced         | COMPOSITE(LLM+broken stub)      | no (run broken)            | HIGH (S2)                         |
| 17  | RAGConfig (fallback)          | advanced         | PURE_COMPUTE                    | yes                        | MED (S3)                          |
| 18  | DenseRetrievalNode            | similarity       | PURE_COMPUTE                    | yes                        | LOW                               |
| 19  | SparseRetrievalNode           | similarity       | PURE_COMPUTE                    | yes                        | LOW                               |
| 20  | ColBERTRetrievalNode          | similarity       | PURE_COMPUTE                    | yes                        | LOW                               |
| 21  | MultiVectorRetrievalNode      | similarity       | PURE_COMPUTE                    | yes                        | LOW                               |
| 22  | CrossEncoderRerankNode        | similarity       | PURE_COMPUTE                    | yes                        | LOW                               |
| 23  | HybridFusionNode              | similarity       | PURE_COMPUTE                    | yes                        | LOW                               |
| 24  | PropositionBasedRetrievalNode | similarity       | PURE_COMPUTE                    | yes                        | LOW                               |
| 25  | QueryExpansionNode            | query_processing | PURE_COMPUTE                    | yes                        | MED (S1)                          |
| 26  | QueryDecompositionNode        | query_processing | PURE_COMPUTE                    | yes                        | MED (S1)                          |
| 27  | QueryRewritingNode            | query_processing | PURE_COMPUTE                    | yes                        | MED (S1)                          |
| 28  | QueryIntentClassifierNode     | query_processing | PURE_COMPUTE                    | yes                        | MED (S1 + agent-reasoning flag)   |
| 29  | MultiHopQueryPlannerNode      | query_processing | PURE_COMPUTE                    | yes                        | MED (S1)                          |
| 30  | AdaptiveQueryProcessorNode    | query_processing | PURE_COMPUTE                    | yes                        | MED (S1)                          |
| 31  | CacheOptimizedRAGNode         | optimized        | broken (CacheNode absent)       | construct:yes / run:broken | HIGH (S7)                         |
| 32  | AsyncParallelRAGNode          | optimized        | COMPOSITE                       | construct:yes / run:no     | MED (S5)                          |
| 33  | StreamingRAGNode              | optimized        | PURE_COMPUTE                    | construct:yes / run:yes    | LOW                               |
| 34  | BatchOptimizedRAGNode         | optimized        | PURE_COMPUTE                    | construct:yes / run:yes    | LOW                               |
| 35  | RAGWorkflowRegistry           | registry         | PURE_COMPUTE                    | yes                        | LOW                               |
| 36  | GraphRAGNode                  | graph            | COMPOSITE(LLM+GRAPH+community)  | construct:yes / run:no     | HIGH (S6)                         |
| 37  | GraphBuilderNode              | graph            | PURE_COMPUTE (networkx)         | yes                        | LOW (flag stub extraction)        |
| 38  | GraphQueryNode                | graph            | PURE_COMPUTE (networkx)         | yes                        | LOW                               |
| 39  | AgenticRAGNode                | agentic          | COMPOSITE(LLM multi-cycle)      | construct:yes / run:no     | MED (S5)                          |
| 40  | ToolAugmentedRAGNode          | agentic          | PURE_COMPUTE                    | yes                        | LOW                               |
| 41  | ReasoningRAGNode              | agentic          | COMPOSITE(LLM)                  | construct:yes / run:no     | MED (S5)                          |
| 42  | MultimodalRAGNode             | multimodal       | COMPOSITE(LLM vision)           | construct:yes / run:no     | MED (S1+S4)                       |
| 43  | VisualQuestionAnsweringNode   | multimodal       | PURE_COMPUTE (synthetic)        | yes                        | LOW (flag synthetic VQA)          |
| 44  | ImageTextMatchingNode         | multimodal       | PURE_COMPUTE                    | yes                        | LOW (verify Union NodeParameter)  |
| 45  | RealtimeRAGNode               | realtime         | PURE_COMPUTE                    | construct:yes / run:yes    | MED (S5)                          |
| 46  | RealtimeStreamingRAGNode      | realtime         | PURE_COMPUTE                    | yes                        | MED (verify alias= decorator)     |
| 47  | IncrementalIndexNode          | realtime         | PURE_COMPUTE                    | yes                        | LOW                               |
| 48  | RAGEvaluationNode             | evaluation       | COMPOSITE(LLM judge)            | construct:yes / run:no     | MED (S1)                          |
| 49  | RAGBenchmarkNode              | evaluation       | PURE_COMPUTE (synthetic)        | yes                        | LOW (flag no-op system.run)       |
| 50  | TestDatasetGeneratorNode      | evaluation       | PURE_COMPUTE                    | yes                        | LOW                               |
| 51  | PrivacyPreservingRAGNode      | privacy          | PURE_COMPUTE                    | construct:yes / run:yes    | MED (S5 CredentialManager import) |
| 52  | SecureMultiPartyRAGNode       | privacy          | PURE_COMPUTE (synthetic crypto) | yes                        | LOW (flag fake crypto)            |
| 53  | ComplianceRAGNode             | privacy          | PURE_COMPUTE                    | yes                        | LOW                               |
| 54  | ConversationalRAGNode         | conversational   | COMPOSITE(LLM)                  | construct:yes / run:no     | MED (S1)                          |
| 55  | ConversationMemoryNode        | conversational   | PURE_COMPUTE                    | yes                        | LOW                               |
| 56  | FederatedRAGNode              | federated        | PURE_COMPUTE (synthetic net)    | construct:yes / run:yes    | MED (S5 RESTClient import)        |
| 57  | EdgeRAGNode                   | federated        | PURE_COMPUTE                    | yes                        | LOW                               |
| 58  | CrossSiloRAGNode              | federated        | PURE_COMPUTE                    | yes                        | LOW                               |

## Infra-tier histogram (dominant tier per class; n=58)

| Tier                                                                                                                        | Count  | Notes                                                                                                                                                  |
| --------------------------------------------------------------------------------------------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| PURE_COMPUTE (testable with `[rag]` alone, full run)                                                                        | **31** | incl. 2 RAGConfig + RAGWorkflowRegistry + the synthetic-but-deterministic ones                                                                         |
| PURE_COMPUTE-construction / COMPOSITE-full-run (Shape-W; construction testable with `[rag]`, golden path needs LLM+EMB+VEC) | **22** | the WorkflowNode subclasses + RAGStrategyRouterNode                                                                                                    |
| broken-by-design / broken-stub at runtime (PURE_COMPUTE inputs surface it)                                                  | **5**  | 4 advanced (S2) + CacheOptimizedRAGNode (S7) — these overlap the COMPOSITE bucket; counted here for the breakage roll-up, not double-counted in the 58 |

Net for sharding: **~53 of 58 are behaviorally testable using only the
`[rag]` extra** if "construction + documented-contract assertion" is
accepted as Tier-2 coverage for the 22 Shape-W nodes (construction
exercises the full graph-wiring contract; it is real behavior, not a
mock). Only the Shape-W _golden-path full run_ (≈22 nodes) needs
embedding/vector/LLM infra absent from `[rag]`. NO node needs GRAPH_DB,
true NETWORK, STORAGE(aiosqlite — note: nothing in rag/ actually uses
aiosqlite despite it being in `[rag]`), or true MULTIMODAL — every
"federated/graph/multimodal/privacy-crypto" claim is synthetic Python.

## HIGH-breakage-risk roll-up

**13 HIGH** (provably broken OR unverified-2-month-stale critical
coupling):

- S2 broken-stub class (4): SelfCorrectingRAGNode, RAGFusionNode, HyDENode,
  StepBackRAGNode — `create_hybrid_rag_workflow` returns an empty
  `kaizen.workflow.graph.Workflow` then `.run(documents=,query=,
operation=)` is called on it; provable runtime failure at advanced.py
  :209/:711/:1126/:1464.
- S7 broken-by-missing-dep (1): CacheOptimizedRAGNode — `"CacheNode"`
  string node-type never resolves (CacheNode unimplemented, out-of-scope
  per brief — test pins the broken contract, do NOT build CacheNode).
- S1 direct-LLMAgentNode-ctor unverified (1): RAGStrategyRouterNode
  (`router.py:76` constructs `LLMAgentNode(name=,model=,provider=,
system_prompt=)` directly; advanced.py's 4 also do this but are already
  counted under S2).
- S6 missing-runtime-dep (1): GraphRAGNode — default louvain branch
  `import community` not in `[rag]`.
- String-node-type 2-month-staleness on the 5 strategy/6 workflow nodes
  (6 of these are HIGH because their entire `.execute()` contract depends
  on `EmbeddingGeneratorNode`/`VectorDatabaseNode`/`SemanticChunkerNode`/
  `HybridRetrieverNode` still being registered in kailash — unproven by
  the resurrection import-smoke which only checks representative
  registration): SemanticRAGNode, StatisticalRAGNode, HybridRAGNode,
  HierarchicalRAGNode, SimpleRAGWorkflowNode, plus AdvancedRAGWorkflowNode/
  AdaptiveRAGWorkflowNode/RAGPipelineWorkflowNode share the dependency
  (counted in the 22 Shape-W set; the 4 strategy + SimpleRAG are the 5
  flagged HIGH here because they are the leaf string-node-type assertions
  the others compose).

(Counting convention for the report headline: distinct CLASSES carrying a
HIGH tag in the summary table = 13 — rows 2,3,4,5,6,9,10,13,14,15,16,31,36.
Rows 7,8 are HIGH-by-composition of those; the 13 is the actionable
deep-fix set for the implement phase.)

**MED (14):** S1 query_processing ×6, S5 import-existence
(AgenticRAGNode, ReasoningRAGNode, AsyncParallelRAGNode, RealtimeRAGNode,
PrivacyPreservingRAGNode, FederatedRAGNode), RealtimeStreamingRAGNode
(alias decorator), advanced.RAGConfig (S3), MultimodalRAGNode,
ConversationalRAGNode, RAGEvaluationNode. (Some overlap; ~14 distinct.)

**LOW (~31):** the self-contained pure-`run()` retrieval/index/eval/
privacy/memory nodes + RAGConfig dataclass + RAGWorkflowRegistry.

## Sharding implications (feeds /todos — not the plan itself)

- Natural shard axis = (testability shape) × (module cluster):
  - Shard cluster **PC-leaf** (~24 LOW pure nodes: similarity ×7,
    query_processing ×6, router ×2, conversational memory, federated
    edge/cross-silo, evaluation benchmark/dataset, privacy
    compliance/SMPC, multimodal VQA/match, graph builder/query,
    incremental index, RAGConfig, registry) — all PURE_COMPUTE, `[rag]`
    alone, golden+edge tests, no infra. Highest value-per-cost.
  - Shard cluster **broken-fix** (5: S2 ×4 + S7 ×1) — these REQUIRE
    in-shard SDK fixes (`zero-tolerance.md` Rule 4), not just tests. S2 is
    one root cause (the `advanced.create_hybrid_rag_workflow` stub) → likely
    one fix unblocks 4 nodes. CacheNode is out-of-scope (test the broken
    contract).
  - Shard cluster **Shape-W-construction** (~17 WorkflowNode subclasses) —
    construction + graph-wiring assertion with `[rag]`; golden-path
    full-run is a SEPARATE decision the user must gate (needs
    embedding/vector/LLM infra the `[rag]` extra deliberately excludes —
    surface as a recommendation, do not auto-decide).
  - Shard cluster **import-smoke-expansion** (S5) — extend the existing
    resurrection import-smoke to assert ALL 17 modules import (cheap,
    converts S5 silent breakage to loud).
- Each shard carries its own value-anchor per `value-prioritization.md`
  MUST-2 (the F8 brief's value-anchor: "the RAG capability the user chose
  to preserve is provably correct, not merely importable").
- The 13 HIGH classes are the per-shard zero-tolerance fix obligations:
  any HIGH that is provably broken (the 5 broken-fix) MUST be fixed in the
  shard that covers it, in the same PR, with a version bump if behavior
  changes (brief success criteria + `zero-tolerance.md` Rule 4).
