# Wave 2 — privacy over-claim strip: /redteam round history

Wave-loop G1 gate for Wave 2 (PrivacyPreservingRAGNode + SecureMultiPartyRAGNode +
ComplianceRAGNode flags). BUILD repo: working-tree only, no commits. Receipts = agent
task IDs (verify-resource-existence MUST-4 / wave-loop MUST-5).

## Implementation (kaizen-specialist, task ac45c00f5830f0eae)
Stripped homomorphic-encryption over-claims (zero impl); relabeled the real Laplace noise
as "score perturbation — NOT differential privacy" (dropped fake `actual_epsilon` +
guarantee strings; ctor `privacy_budget`→`score_noise`); kept regex PII redaction as the
honest lead capability; determined SecureMultiPartyRAGNode is pure simulation → stripped
all crypto guarantees + loudly documented as non-functional (flagged for removal, not
deleted, per Rule 6a); ComplianceRAGNode left genuine (initially).

## Round 1
- reviewer (af… a2c66476ac9075b20): **CHANGES-REQUIRED** — (CRIT) orphaned `privacy_budget`
  rename callsite → real test FAILURE at test_issue_f8b9a:79; (HIGH) fake
  `gdpr/ccpa/hipaa_compliant` audit block + `compliance_score: 0.95` magic number.
- security-reviewer (ab4783ab4bf9ffff9): **CHANGES-REQUIRED** — the strip nailed prose but
  left hardcoded fake protection flags in OUTPUT/AUDIT dicts: (CRIT) `privacy_protected:
  True` incl. single-doc truncation; (HIGH) compliance verdicts + `anonymization_strength:
  "high"` + `data_minimization: True`; (MED) `privacy_applied: True`. SMPC warning, PII
  best-effort honesty, ex-DP no-privacy-claim, log hygiene = PASS.

## Fix round (kaizen-specialist resume, task ac45c00f5830f0eae)
All 7 findings + the orphan + the LOW PII-hash-comment fix resolved: every protection/
compliance flag now DERIVED from runtime state (`aggregated` from k≥2; `pii_redaction_
attempted` + disclaimer replacing regulatory verdicts; `anonymization_strength` from
technique count; `data_minimization` from redaction/anonymization-ran / fields_redacted>0;
`compliance_score` from 4 real signals; `privacy_applied` from upstream anonymization).
Tests updated to assert derived behavior (TestDerivedHonestFlags + ComplianceRun).

## Round 2 (verification)
- security-reviewer (a0b2420bdc9131ad6): **CONVERGED** (all 7 derivations correct, no new;
  recommended orchestrator run the green receipt — done).
- reviewer (a615de4a179a44e1f): **CONVERGED** (orphan gone, repo-wide grep clean,
  derivations logically correct incl. compliance_score arithmetic 4/4 vs 1/4, tests
  meaningful, 81 passed).
- Orchestrator green receipt: ruff clean (4 files); **81 passed** (privacy unit+integration
  + f8b9a + f9); hardcoded-verdict sweep CLEAN.

## G1 VERDICT: CONVERGED
Receipts: agent task IDs ac45c00f5830f0eae, a2c66476ac9075b20, ab4783ab4bf9ffff9,
a0b2420bdc9131ad6, a615de4a179a44e1f + orchestrator green-run.

## NOTE: Wave 2 converged the CLAIM-HONESTY scope only.
A separate, pre-existing, VERIFIED defect surfaced (recorded in 08-wave-plan § MAJOR NEW
FINDING): F9 #1117-class — `dp_noise_injector`/`secure_aggregator`/`result_formatter`
codegen define their function but never call it at module scope, so the
PrivacyPreservingRAGNode workflow publishes NO end-to-end result. Distinct bug class,
likely systemic across the rag tree → NEW WAVE, user-gated prioritization.
