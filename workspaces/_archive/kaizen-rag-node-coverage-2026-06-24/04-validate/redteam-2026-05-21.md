# Redteam — kaizen-rag-node-coverage (2026-05-21, L5 baseline Round 1)

**Trust posture:** L5_DELEGATED (Round 1 OPTIONAL per `skills/32-trust-posture/redteam-integration.md`)
**Scope:** Mechanical spec-compliance audit of `specs/kaizen-rag.md` against shipped code (post-`kaizen-v2.24.0` on PyPI).
**Auditor mode:** Probe-driven structural verification — AST walks, `grep -n`, `sed -n` per `rules/probe-driven-verification.md` MUST-3 (offline / structural probe). No regex against semantic claims.
**Companion artifact:** `workspaces/kaizen-rag-node-coverage/.spec-coverage-v2.md` (literal verification commands + actual outputs for every spec assertion).

## Convergence verdict

| Severity | Count |
| -------- | ----- |
| CRITICAL | 0     |
| HIGH     | 4     |
| MEDIUM   | 0     |
| LOW      | 1     |

**Disposition:** baseline NOT clean — 4 HIGH stale-citation findings against `rules/spec-accuracy.md` MUST-1. Surfacing immediately per L5 convergence criteria. Per the instructions: "Any HIGH = surface immediately; do not iterate to round 2 unless user authorizes." Returning control to user.

The F8/F9/R1 work product (the 58 RAG class definitions, the StreamingRAGNode rename, the test coverage, the resurrection-smoke gate) survives the mechanical audit on structural / behavioral substance. The HIGH findings are spec-side drift in `file:line` anchors — documentation accuracy violations, not runtime defects.

---

## Findings

### [HIGH-1] Phantom citation `privacy.py:152` (off-by-8)

- **Spec §:** "Privacy & compliance" — `PrivacyPreservingRAGNode` "A3-triage R4 LEAK disposition" table (spec line ~944)
- **Spec text:** "`privacy.py:152` `{{hash_value}}`" and "`privacy.py:152` `{{pii_type.upper()}}`"
- **Verification command:** `grep -n "{{hash_value}}\|{{pii_type" packages/kailash-kaizen/src/kaizen/nodes/rag/privacy.py`
- **Actual output:** match is on line **160**, NOT 152. Line 152 is unrelated code.
  ```
  160:                replacement = f"[{{pii_type.upper()}}_{{hash_value}}]"
  ```
- **Rule violation:** `rules/spec-accuracy.md` MUST-1 "Every Citation Resolves Against Working Code" — citation depends on a `file:line` that does not contain the cited pattern.
- **Recommended fix:** update spec table to `privacy.py:160`.

### [HIGH-2] Phantom citation `privacy.py:221` (off-by-19)

- **Spec §:** same "A3-triage R4 LEAK disposition" table
- **Spec text:** "`privacy.py:221` `{{pattern}}`" and "`privacy.py:221` `{{replacement}}`"
- **Verification command:** `grep -n "{{pattern}}\|{{replacement}}" packages/kailash-kaizen/src/kaizen/nodes/rag/privacy.py`
- **Actual output:** match is on line **240**, NOT 221.
  ```
  240:            generalizations.append(f"{{pattern}}->{{replacement}}")
  ```
- **Rule violation:** `rules/spec-accuracy.md` MUST-1.
- **Recommended fix:** update spec table to `privacy.py:240`.

### [HIGH-3] Phantom citation `realtime.py:503` (off-by-31)

- **Spec §:** "Realtime & optimized" — "Pyright cleanup" bullet (~spec line 1200)
- **Spec text:** "`realtime.py` `chunk_idx` possibly-unbound (init at `:503`, post-loop reference at `:559`)"
- **Verification command:** `grep -n "chunk_idx = 0" packages/kailash-kaizen/src/kaizen/nodes/rag/realtime.py`
- **Actual output:** `chunk_idx = 0` is at line **534**, not 503.
- **Rule violation:** `rules/spec-accuracy.md` MUST-1.
- **Recommended fix:** update spec to `:534`.

### [HIGH-4] Phantom citation `realtime.py:559` (off-by-37)

- **Spec §:** same "Pyright cleanup" bullet
- **Spec text:** "post-loop reference at `:559`"; "the previously-unbound edge case (`max_chunks=0` / empty result set) now produces `processing_time == 0`"
- **Verification command:** `grep -n "processing_time.*chunk_idx" packages/kailash-kaizen/src/kaizen/nodes/rag/realtime.py`
- **Actual output:** post-loop reference is at line **596**, not 559.
  ```
  596:             "processing_time": chunk_idx * self.chunk_interval / 1000,
  ```
- **Rule violation:** `rules/spec-accuracy.md` MUST-1.
- **Recommended fix:** update spec to `:596`.

---

## LOW

### [LOW-1] `privacy.py:178` is a section-anchor mismatch

- **Spec §:** "Privacy & compliance" preamble describes "the PII codegen template (`privacy.py:107`) and the query-anonymizer codegen template (`privacy.py:178`)"
- **Verification:** `sed -n '178p' privacy.py` returns `"redaction_count": sum(len(items) for items in pii_found.values())` — inside the PII-detector codegen body, NOT the start of the query-anonymizer template.
- The query-anonymizer `code = f"""..."""` block actually begins later in the file. The `privacy.py:107` anchor is a comment line "`# PII detector and redactor`" which serves as a reasonable section anchor for the PII branch; `privacy.py:178` is misleading because it lands inside the PII branch's result dict, not in the anonymizer template.
- **Severity LOW** because the surrounding prose is correct (the templates DO exist and DO carry the escaped braces); only the anchor line is approximate.
- **Recommended fix:** re-derive the anonymizer template anchor by grep-finding the `query_anonymizer_code = f"""` literal and citing that line.

---

## Cleanly-passing checks (re-derived from scratch)

- **58 class definitions** across 16 modules — `python3 -c "import ast,pathlib; print(sum(1 for f in pathlib.Path('packages/kailash-kaizen/src/kaizen/nodes/rag').glob('*.py') if f.name!='__init__.py' for n in ast.walk(ast.parse(f.read_text())) if isinstance(n, ast.ClassDef)))"` returns `58`. PASS.
- **StreamingRAGNode rename collision-free** — `class RealtimeStreamingRAGNode` at `realtime.py:435`; `class StreamingRAGNode` at `optimized.py:495`. Distinct module paths. Both in `rag/__init__.py::__all__`. Resurrection-smoke test `test_streaming_rename_no_collision` asserts both register. PASS.
- **Zero active xfail markers in resurrection smoke test** — `grep -c "@pytest\.mark\.xfail\|marks=.*xfail" tests/regression/test_rag_resurrection_import_smoke.py` returns `0`. The 8 grep hits on `xfail` are all in source comments documenting historical removals. PASS (matches spec line 1215).
- **Every RAG module has ≥1 importing test** — Section 5 of `.spec-coverage-v2.md` table: 16/16 modules have ≥1 unit or integration test importing from `kaizen.nodes.rag.<module>`. PASS.
- **Test-coverage totals (AST re-derived)** — 632 unit + 194 integration + 7 + 10 + (router/registry regression) test functions. AST-based per `rules/testing.md` "Numerical Claims" MUST. PASS.
- **Brief criteria 1–5** all map to spec §s and all map to working code. Section 7 table in `.spec-coverage-v2.md`. PASS.
- **Cross-spec consistency** — `grep -l "kaizen.nodes.rag\|StreamingRAGNode\|RealtimeStreamingRAGNode\|RAGConfig" specs/` returns only `kaizen-rag.md`. No sibling-spec drift surface. PASS.
- **env-models compliance** in router.py — `_DEFAULT_LLM_MODEL = os.environ.get("OPENAI_PROD_MODEL", os.environ.get("DEFAULT_LLM_MODEL"))` at `router.py:22-24`. Resolves through env only; no hardcoded `"gpt-4"` literal in router-default path. PASS per `rules/env-models.md`.
- **Router self-name fix (B10)** — `router.py:114` uses `f"{self.metadata.name}_llm"` (not `self.name`). Matches spec claim. Regression test `tests/regression/test_issue_f8b10_router_registry_defects.py::TestRouterSelfNameBugClosed` exists. PASS.
- **RAGQualityAnalyzerNode `expected_results` kwarg consumption (B10 Rule 3c fix)** — `router.py:523-575` reads `expected_results` and surfaces `quality_analysis["expected_result_count"]` + `expected_recall_ratio`. PASS.
- **Performance monitor sliding window** — `router.py:785-786` truncates `self.performance_history` to last 100 records. PASS.
- **RAGWorkflowRegistry singleton** — `registry.py:547` constructs `rag_registry = RAGWorkflowRegistry()` at module scope. PASS.

---

## Notes on environment / probe-driven discipline

- All assertions re-derived from scratch via AST + `grep -n` + `sed -n` (structural probes per `rules/probe-driven-verification.md` MUST-3). No regex against semantic claims.
- `pytest --collect-only` was not runnable in this audit shell because the editable-install state of `kailash` is stale (`ModuleNotFoundError: No module named 'kailash.utils'`); per the audit-mode protocol the fallback is AST-based test enumeration, which is the canonical probe for "test exists" structural assertions (a test function declaration at the AST level is a structural property, not a semantic one). The PyPI install verified end-to-end in the prior session (per `.session-notes`) is the runtime witness for the test-collection claim; the AST enumeration is the audit-time witness.
- No test was actually executed in this round (Round 1 audit scope is spec compliance, not test re-runs).

## Recommendation

The 4 HIGH findings are stale `file:line` anchors in the spec — drift from refactors that landed in the B7/B9a/B9b/B9c/B10 shards. Fix is mechanical: re-derive each anchor with `grep -n` and update the spec. Estimated impact: 5–10 minute spec edit, one PR, no code change required.

The F8/F9/R1 work survives the mechanical audit on every structural axis (class count, module coverage, test importing, collision-free rename, xfail closure, env-models compliance, kwarg-consumption fix). The substance of "provably correct, not merely importable" — the F8 brief's value-anchor — is delivered.

Per L5 baseline criteria ("Any HIGH = surface immediately; do not iterate to round 2 unless user authorizes"), I am surfacing the 4 HIGH findings + 1 LOW and stopping. User dispositions:

- **(a) Land a spec-only fix PR** correcting the 4 + 1 phantom anchors. Closes the rule violation; the F8/F9/R1 release stands as-is.
- **(b) Treat as documentation drift** for the next codify cycle and continue with other work.
- **(c) Iterate to Round 2** for deeper coverage (e.g. behavioral execution of selected RAG nodes against a clean venv, cross-spec consistency on the larger kaizen domain, security review of the PII / federated paths).

Recommend **(a)** — the rule cost of phantom citations is ongoing drift (every reader who looks up `privacy.py:152` lands on the wrong line and loses trust in the spec), the fix is one PR with mechanical commands, and the work fits one shard per `rules/autonomous-execution.md` capacity budget. Cons: spends a release-cycle-adjacent PR slot on docs-only work; the user may have higher-value queue items per `rules/value-prioritization.md` MUST-1 (no user-anchored source other than the brief's "provably correct" criterion was checked for current value).

Want me to draft the spec-only fix PR? (yes/no)
