# Session Notes — 2026-07-15

## Where we are

Clean post-release state. The kaizen/EATP cluster (#1713 + #1710 + #1736) is CONVERGED,
released, and verified — kailash **2.52.0** + kailash-kaizen **2.33.0** on PyPI. main clean,
tree clean, 0 open PRs, no active todos. /sweep 2026-07-15 clean across all 9 checks.

## Read first

1. `04-validate/sweep-2026-07-15.md` — end-of-cycle sweep (backlog dispositions)
2. `journal/0011-cross-repo-grant-rust-mirror-issues-1710-1713.md` — the rs#1829/rs#1830 grant
3. GH #1755 (RAG fast-follow) + PRs #1749/#1750/#1751/#1752 (the fixes) + #1753/#1754 (releases)
4. `CLAUDE.md` — project directives

## In-flight state

- None — everything merged, released, verified. Working tree clean.

## Executed this session

- Published **kailash 2.52.0** (tag `v2.52.0`) + **kailash-kaizen 2.33.0** (tag `kaizen-v2.33.0`)
  to PyPI — publish-pypi success, GitHub Releases, clean-venv install+import verified (py3.12).
- Filed cross-SDK mirrors on the Rust SDK BUILD repo: **rs#1829** (#1710 mint gate), **rs#1830**
  (#1713 DoS bounds), both `cross-sdk`-labelled (user-authorized, journal/0011).

## Outstanding ledger (forest)

| ID    | Item                                          | Value-anchor (MUST-1 source)                                  | Status                          |
| ----- | --------------------------------------------- | ------------------------------------------------------------- | ------------------------------- |
| F1755 | RAG `_parse_verification_response` hardening  | holistic /redteam LOW notes over #1736 — live path (GH #1755) | queued — kaizen 2.33.1 fast-follow |
| F1720 | Retire legacy→four-axis LLM redundancy        | user's #1717 follow-on (GH #1720)                             | BLOCKED on co-owner scope       |
| F1721 | Py preset-registry drift vs Rust fixture      | GH #1721 (cross-SDK contract)                                 | BLOCKED on co-owner scope       |
| F1712 | MCP spec-compliance parity gaps (2025-11-25)  | GH #1712 (user-filed)                                         | queued — actionable             |
| F1711 | SOC 2 evidence-collection primitives          | GH #1711 (user-filed)                                         | queued — actionable (larger)    |
| F1694 | Gate-1 onboarding-suite stranded fixes        | GH #1694 (user-filed)                                         | queued — actionable             |
| FVERT | Vertex-Claude/Gemini LIVE validation          | the #1717 headline path; only wire-proven                     | BLOCKED on GCP creds (not .env) |

Closed this session: `#1713` → PR #1749 + kailash 2.52.0; `#1710` → PR #1750 + 2.52.0;
`#1736` → PR #1751 + kaizen 2.33.0; #1710 depth-test regression → PR #1752.
Dispositioned: `#1727` py side already landed (openai_chat max_completion_tokens); only rs sibling remains.

## Traps

- **PyPI index lag**: post-publish `uv pip install` may 404 the new version briefly; PyPI JSON API
  reflects sooner. Verify `--refresh`, retry.
- **cwd shadowing**: `/tmp` has stray `enum.py`/`re`-shadowing files → `re` circular-import on import.
  Run import checks from a CLEAN empty dir.
- **Shared review worktree** let one reviewer's neutralization probe mutate another's read → a false
  "guard un-wired" CRITICAL. Verify contested claims vs the committed blob (`git show <sha>:<path>`).
- main has NO required status checks (`[]`) → owner `--admin` merges; PACT job flakes on a 5-min
  action-timeout (re-run); local PACT needs `KAIZEN_DEFAULT_MODEL` set (CI provisions it).
- venv shebangs STALE — always `.venv/bin/python -m <tool>`.
