# /sweep — repo-wide outstanding-work audit — 2026-07-08 (session 9)

Repo: `terrene-foundation/kailash-py` (BUILD). Run after shipping dataflow 2.14.3 (#1573).
Scope: Sweeps 1–8 project-local; Sweep 9 = N/A (BUILD repo, not the loom orchestration root).

## Findings

### [MED] [Sweep 6] F21 (#1614) present in workspace ledger, absent from root ledger

- **Location:** `workspaces/mops-onboarding/.session-notes` forest ledger → root forest-ledger.
- **Evidence:** `validate-forest-ledger.mjs --aggregate` →
  `[AGG] open workspace-ledger ID "F21" … absent from root ledger — workspace→root no-vanish (#669)`.
- **Disposition:** FIX AT /wrapup — roll F21 into the root forest-ledger reconcile (same step the session-8 sweep did: commit `8d67e5e26`). Value-anchor: #1614 is the query-surface sibling of this session's #1573 (`__tablename__` resolver family); MED; warm-context next pick.
- **Why it matters:** the workspace→root no-vanish gate exists so a new forest workstream isn't stranded in one workspace's notes; F21 is new this session and just needs the reconcile.

### [LOW] [Sweep 9] Ecosystem gate mis-detects kailash-py as orchestration-root

- **Location:** `.claude/bin/lib/loom-links.mjs::resolveRole()` for this clone.
- **Evidence:** the Sweep-9 gate (`!isConfigured() || role==="build" || role==="use-consumer"` → N/A) fell through to "orchestration root" because `resolveRole()` did NOT return `build` for this clone (role undeclared in the local loom-links config).
- **Disposition:** DEFER-WITH-REASON — did NOT run the cross-ecosystem roll-up. Per `repo-scope-discipline.md` (§ Exceptions: "loom is the SOLE carve-out holder; a downstream/BUILD repo is never an orchestration root") kailash-py MUST NOT cross-repo-read. Sweep 9 is a structural N/A here regardless of the gate output. Config note: the local loom-links role for this BUILD clone should be declared `build` so the gate returns N/A cleanly (or this is expected if loom-links is a synced artifact that doesn't classify BUILD clones). Not acted on cross-repo.
- **Why it matters:** acting on the false positive would have read sibling repos in violation of repo scope; recording the sentinel keeps the misfire visible without acting on it.

### [INFO] [Sweep 3] 15 open issues — categorized (none closeable, none stale-for-abandon)

Governance / EATP clusters (BLOCKED on user scoping) + dataflow/kaizen queue. All genuinely actionable or queued; none carry a `deferred` label needing Rule-1b verification; none are age-based closure candidates.

| #     | Title (short)                                              | Bucket                    | Disposition                                        |
| ----- | ---------------------------------------------------------- | ------------------------- | -------------------------------------------------- |
| #1510 | SAFR agnostic governance-hardening (cross-SDK BH1-BH5)     | SAFR/governance (F14)     | BLOCKED on user scoping                            |
| #1514 | disposition by intrinsic action risk-factors               | SAFR/governance (F14)     | BLOCKED on user scoping                            |
| #1515 | timeout + fail-safe default on HITL holds                  | SAFR/governance (F14)     | BLOCKED on user scoping                            |
| #1516 | runtime rate-limit + confidence-threshold routing          | SAFR/governance (F14)     | BLOCKED on user scoping                            |
| #1517 | pluggable identity resolver + outbound-effect seam         | SAFR/governance (F14)     | BLOCKED on user scoping                            |
| #1590 | RFC 8785 JCS encoder + subject_hash Audit Anchor (EATP v3) | EATP v3 keystone          | queued (needs value-rank)                          |
| #1591 | citable WEFT event schema + conformance vectors            | EATP v3                   | queued (needs value-rank)                          |
| #1592 | EATP v3 elements (delegation/attestation/revocation)       | EATP v3                   | queued (needs value-rank)                          |
| #1526 | multi_tenant id-alone PK (two tenants, natural key)        | dataflow design (F13)     | queued                                             |
| #1532 | consolidate delegate connectors → contrib pkgs             | dataflow (F13)            | queued                                             |
| #1601 | cross-sdk: soft_delete/versioned parity in Rust SDK        | cross-SDK                 | queued (needs Rust repo)                           |
| #1606 | cache keys omit DB-instance identity (cross-DB bleed)      | dataflow (F19, cross-SDK) | queued (cross-SDK lockstep)                        |
| #1607 | audit enterprise-doc `__dataflow__` key fiction            | dataflow docs (F20)       | queued                                             |
| #1609 | DeepSeek as first-class LlmProvider (kaizen)               | kaizen feat               | queued (actionable, standalone)                    |
| #1614 | query_builder ignores `__tablename__` (query surface)      | dataflow (F21)            | queued — **filed this session**, warmest next pick |

**New since last ledger (session 8):** EATP v3 cluster (#1590/#1591/#1592) and #1609 (DeepSeek) and #1601 (cross-SDK parity) were not in the session-8 forest table — surface for value-ranking at next planning.

### [INFO] Clean sweeps

- **Sweep 1** (active todos): none.
- **Sweep 2** (pending journal): none.
- **Sweep 4** (open PRs / unmerged branches): none — the two session-9 PRs (#1613 fix, #1615 journal) merged & branches deleted.
- **Sweep 5** (redteam-vs-specs): structural N/A — no `workspaces/*/specs/` tree (`tools/sweep-redteam.py` present but the per-workspace-specs precondition is unmet). `<!-- sweep-redteam:v1:N/A reason=orchestration-mode no_specs=true no_tool=false -->`
- **Sweep 7** (process hygiene): clean tree (only untracked `.session-notes`); 0 behind / 0 ahead of origin/main. Stub-grep = 135 across src+packages/src — this is the known **F-STUBS baseline** (leave-as-baseline per user call 2026-06-26; do NOT re-queue).
- **Sweep 8** (release readiness): release-clean — no shippable-code diff on `src/` since `v2.45.6` nor on `packages/kailash-dataflow/src/` since `dataflow-v2.14.3` (just released). No sibling package main>pypi drift.

## Cross-cutting observations

- The repo is in a genuinely clean post-release state — the #1573 cycle closed fully (merged + published + verified), no orphan branches/PRs/todos.
- The open-issue forest has grown a new EATP v3 cluster (#1590-1592) + governance cluster (#1510/#1514-1517) that are strategy-level and BLOCKED on user scoping — they should not be auto-picked.

## Recommended next-session items (human owns the pick)

1. **#1614 — query_builder `__tablename__`** (MED, dataflow). Warmest context; same resolver family as this session's #1573; focused implement→redteam→release cycle (~1-line resolver swap + a query regression test + query-surface audit).
2. **#1526 — multi_tenant id-alone PK** (MED, dataflow design). Larger design change; user-approved SDK queue (F13).
3. **#1609 — DeepSeek LlmProvider** (kaizen). Standalone, actionable, no cross-repo dependency.
4. **#1606 — cache-key DB-identity** (dataflow, cross-SDK). Needs Rust-SDK keyspace lockstep — surface to user for cross-SDK scoping first.
5. **SAFR (#1510/#1514-1517) + EATP v3 (#1590-1592)** — BLOCKED on user scoping; do not start without a user turn.

<!-- sweep-redteam:v1:N/A reason=orchestration-mode no_specs=true no_tool=false -->
<!-- sweep-ecosystem:v1:N/A reason=not-orchestration-root (BUILD repo; loom is sole carve-out) -->
