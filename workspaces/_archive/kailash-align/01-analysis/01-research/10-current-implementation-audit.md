# Current Implementation Audit: kailash-align\n\n## Executive Summary\n\nkailash-align is **strictly hardcoded for SFT + DPO only**. The architecture, configuration, and training pipeline explicitly support only these two methods. Adding support for GRPO, KTO, IPO, ORPO, SimPO, Online DPO, and RLOO requires significant refactoring across all major components.\n\n## 1. Current Methods Implemented\n\n### Fully Supported\n- **SFT (Supervised Fine-Tuning)**: Complete implementation with TRL SFTTrainer\n- **DPO (Direct Preference Optimization)**: Complete implementation with TRL DPOTrainer\n- **SFT+DPO Pipeline**: Sequential execution (train SFT, then DPO on merged weights)\n\n### Test Coverage\n- Config validation tests (SFT/DPO specific)\n- Preference dataset validation (DPO columns: `prompt`, `chosen`, `rejected`)\n- Pipeline dispatch tests for method routing\n- Registry tests (no method-specific logic)\n\n## 2. Hardcoded Method Locations\n\n### A. `pipeline.py` — Core Trainer Dispatch (Lines 80-96)\n```python\nif self._config.method == \"sft\":\n    return await self._run_sft(dataset, adapter_name)\nelif self._config.method == \"dpo\":\n    if preference_dataset is None:\n        raise TrainingError(\"DPO requires preference_dataset\")\n    return await self._run_dpo(preference_dataset, adapter_name)\nelif self._config.method == \"sft_then_dpo\":\n    # ... chain SFT -> DPO\nelse:\n    raise TrainingError(f\"Unknown training method: {self._config.method!r}\")\n```\n\n**Impact**: Hard if-else on method name. To add a new method:\n1. Add new elif branch\n2. Create new `_run_<method>()` private method\n3. Hard-import the TRL trainer (e.g., `from trl import GRPOTrainer`)\n\n### B. `config.py` — Method Validation (Lines 285-288, 251-254)\n```python\n# AlignmentConfig.__post_init__\nif self.method not in (\"sft\", \"dpo\", \"sft_then_dpo\"):\n    raise ValueError(f\"method must be 'sft', 'dpo', or 'sft_then_dpo', got {self.method!r}\")\n\n# AdapterSignature.__post_init__\nif self.training_method not in (\"sft\", \"dpo\", \"sft_then_dpo\"):\n    raise ValueError(\n        f\"training_method must be 'sft', 'dpo', or 'sft_then_dpo', \"\n        f\"got {self.training_method!r}\"\n    )\n```\n\n**Impact**: \n- Method names hardcoded in string literals\n- Validation rejects any method not in the tuple\n- No parametric method registry\n\n### C. SFT Trainer Implementation (Lines 98-205)\n- **Lines 120**: Hard imports: `from trl import SFTTrainer`\n- **Lines 147-148**: TRL SFTConfig creation, SFTTrainer initialization (SFT-specific)\n- **Lines 182**: AdapterSignature registration: `training_method=\"sft\"`\n- Metrics collection hardcoded for SFT (line 189-193):\n  ```python\n  training_metrics={\n      \"train_loss\": train_result.training_loss,\n      \"train_runtime\": train_result.metrics.get(\"train_runtime\"),\n      \"train_samples_per_second\": train_result.metrics.get(\"train_samples_per_second\"),\n  }\n  ```\n\n**Issue**: \n- Metrics keys are SFT-specific\n- Online methods (GRPO, Online DPO, RLOO) would report different metrics (e.g., `rewards/chosen`, `rewards/rejected`)\n- KTO, IPO, ORPO have their own metric names\n\n### D. DPO Trainer Implementation (Lines 207-328)\n- **Line 228**: Hard import: `from trl import DPOTrainer`\n- **Lines 234**: Preference dataset validation hardcoded for DPO columns:\n  ```python\n  required_columns = {\"prompt\", \"chosen\", \"rejected\"}\n  ```\n- **Lines 267-268**: TRL DPOConfig creation, DPOTrainer initialization\n- **Lines 296, 303**: Training method tracking (dpo vs. sft_then_dpo)\n- **Lines 309-317**: DPO-specific metrics:\n  ```python\n  training_metrics={\n      \"train_loss\": train_result.training_loss,\n      \"dpo_rewards_chosen\": train_result.metrics.get(\"rewards/chosen\"),\n      \"dpo_rewards_rejected\": train_result.metrics.get(\"rewards/rejected\"),\n      \"dpo_rewards_margin\": train_result.metrics.get(\"rewards/margins\"),\n  }\n  ```\n\n**Issues**:\n- Preference dataset validation is DPO-specific (ORPO needs `margin` column; SimPO has different format)\n- Metrics are hardcoded to DPO naming convention\n- Chaining logic (`base_adapter_path`) is SFT→DPO specific\n- No support for online methods requiring rollout/trajectory data\n\n### E. `evaluator.py` — No Method-Specific Logic\n**Good news**: Evaluation is method-agnostic. Uses lm-eval-harness which works with any fine-tuned model.\n\n### F. `serving.py` — No Method-Specific Logic\n**Good news**: Serving (GGUF export, Ollama, vLLM) is method-agnostic. Works with any merged adapter.\n\n### G. `bridge.py` — No Method-Specific Logic\n**Good news**: Kaizen bridge is method-agnostic. Works with any deployed model.\n\n### H. `registry.py` — Generic, But Validates Method Names (Lines 251-254)\nAdapterVersion and AdapterRegistry use `training_method` field, which validates against hardcoded set:\n```python\nif self.training_method not in (\"sft\", \"dpo\", \"sft_then_dpo\"):\n    raise ValueError(...)\n```\n\n**Impact**: Registry will reject adapters trained with new methods unless enum updated.\n\n## 3. Missing Infrastructure for New Methods\n\n### A. Method-Specific Dataset Formats\n**Current State**: \n- SFT expects: `{\"text\": \"...\"}`\n- DPO expects: `{\"prompt\": \"...\", \"chosen\": \"...\", \"rejected\": \"...\"}`\n\n**Missing**:\n- GRPO/Online methods: Need trajectory data (`[state, action, reward]`)\n- RLOO: Needs rollout buffer + value model\n- ORPO: Needs `margin` column in addition to chosen/rejected\n- SimPO: Different preference format (implicit margins from generation length)\n- KTO: Needs implicit/explicit preference labels (not just binary chosen/rejected)\n- IPO: Needs implicit preference information\n\n### B. Method-Specific Config Classes\n**Current State**: \n- `SFTConfig`: inherits from TRL SFTConfig\n- `DPOConfig`: inherits from TRL DPOConfig\n\n**Missing**:\n- `GRPOConfig`, `RTOLConfig`, `KTOConfig`, `IPOConfig`, `ORPOConfig`, `SimPOConfig`\n- Each has unique hyperparameters:\n  - GRPO: `temperature`, `reward_model`, `num_rollouts`, `sft_coeff`\n  - RLOO: `num_return_steps`, `value_model_path`\n  - KTO: `max_target_r`, `min_target_r` (preference strength)\n  - IPO: `beta` (different meaning than DPO)\n  - SimPO: `beta`, implicit `margin_loss`\n  - ORPO: `beta`, `margin_factor`\n\n### C. Method-Agnostic Trainer Factory\n**Current State**: Monolithic if-elif-else in `pipeline.train()`\n\n**Missing**:\n- Trainer registry/factory pattern\n- Method-to-TrainerClass mapping\n- Method-to-ConfigClass mapping\n- Method-to-DataValidatorFunction mapping\n\n### D. Method-Agnostic Metrics Collectors\n**Current State**: Hardcoded metric keys per method\n\n**Missing**:\n- Generic metrics collection: `trainer.metrics`\n- Method-specific metric extractors\n- Flexible metrics schema in registry\n\n### E. Reference Model Handling\n**Current State**: \n- DPO trainer uses TRL's implicit reference model handling (TRL >=0.25)\n- Hardcoded in `DPOTrainer` constructor\n\n**Missing**:\n- GRPO: Needs explicit reference model + reward model\n- RLOO: Needs value model\n- Online methods: May need explicit reference policy loading\n- Generic reference model loader\n\n### F. Data Validation Framework\n**Current State**: \n- Single `_validate_preference_dataset()` method for DPO\n- Hardcoded column validation\n\n**Missing**:\n- Method-to-validator mapping\n- Generic `validate_dataset_for_method()` dispatch\n- Per-method column/field validators\n\n## 4. What Needs to Change\n\n### Priority 1: Infrastructure (Required for any new method)\n\n#### 4.1.1 Method Registry\nCreate `kailash_align/method_registry.py`:\n```python\n@dataclass\nclass MethodConfig:\n    \"\"\"Metadata for a training method.\"\"\"\n    name: str\n    trainer_class: type  # TRL trainer\n    config_class: type   # Config dataclass\n    dataset_validator: Callable\n    dataset_required_columns: set[str]\n    metrics_extractor: Callable\n    requires_preference_data: bool\n    requires_reference_model: bool\n    supports_chaining: bool\n\nMETHOD_REGISTRY = {\n    \"sft\": MethodConfig(...),\n    \"dpo\": MethodConfig(...),\n    \"grpo\": MethodConfig(...),\n    # ... etc\n}\n```\n\n#### 4.1.2 Refactor `pipeline.train()` Dispatch\nReplace if-elif-else with:\n```python\nasync def train(self, dataset, adapter_name, preference_dataset=None, ...):\n    method_config = METHOD_REGISTRY[self._config.method]\n    if method_config.requires_preference_data and preference_dataset is None:\n        raise TrainingError(f\"{self._config.method} requires preference_dataset\")\n    \n    return await self._run_generic_training(\n        trainer_class=method_config.trainer_class,\n        config_class=method_config.config_class,\n        dataset=dataset,\n        preference_dataset=preference_dataset,\n        adapter_name=adapter_name,\n    )\n```\n\n#### 4.1.3 Generic Trainer Wrapper\nCreate `_run_generic_training()` that:\n- Loads model (with QLoRA if configured)\n- Applies LoRA via PEFT\n- Instantiates trainer from registry\n- Trains and saves adapter\n- Registers with flexible metrics collection\n\n### Priority 2: Config Extensibility\n\n#### 4.2.1 Generalized Stage Config\nCreate base class `AlignmentStageConfig`:\n```python\n@dataclass(frozen=True)\nclass AlignmentStageConfig:\n    \"\"\"Base class for method-specific configs.\"\"\"\n    num_train_epochs: int\n    per_device_train_batch_size: int\n    learning_rate: float\n    # ... common fields\n    \n    def to_trl_config(self, output_dir: str) -> Any:\n        \"\"\"Convert to TRL config. Override in subclasses.\"\"\"\n        raise NotImplementedError\n\n# SFTConfig, DPOConfig inherit from this\n# New classes: GRPOConfig, KTOConfig, IPOConfig, ORPOConfig, SimPOConfig\n```\n\n#### 4.2.2 AlignmentConfig Refactor\n```python\n@dataclass\nclass AlignmentConfig:\n    method: str\n    base_model_id: str\n    lora: LoRAConfig\n    method_config: AlignmentStageConfig  # Generic, not sft/dpo-specific\n    # Remove: sft, dpo fields\n```\n\n### Priority 3: Dataset Validation\n\n#### 4.3.1 Generic Dataset Validator Interface\n```python\ndef validate_dataset_for_method(\n    method: str, dataset: Any, stage: str = \"training\"\n) -> None:\n    \"\"\"Route to method-specific validator.\"\"\"\n    validator = METHOD_REGISTRY[method].dataset_validator\n    validator(dataset)\n```\n\n#### 4.3.2 Validator Implementations\n```python\n# For DPO, ORPO (and other preference methods)\ndef validate_preference_dataset(dataset, required_columns):\n    # Current logic, generalized\n\n# For GRPO, RLOO (trajectory-based)\ndef validate_trajectory_dataset(dataset):\n    # Validate: [state, action, reward]\n\n# For SimPO, KTO (implicit preference)\ndef validate_implicit_preference_dataset(dataset):\n    # Validate format-specific columns\n```\n\n### Priority 4: Metrics Collection\n\n#### 4.4.1 Generic Metrics Extractor\n```python\ndef extract_training_metrics(\n    trainer_result, method: str\n) -> dict[str, float]:\n    \"\"\"Extract method-appropriate metrics from trainer result.\"\"\"\n    extractor = METHOD_REGISTRY[method].metrics_extractor\n    return extractor(trainer_result)\n```\n\n#### 4.4.2 Per-Method Extractors\n```python\ndef extract_sft_metrics(trainer_result) -> dict:\n    return {\n        \"train_loss\": trainer_result.training_loss,\n        \"train_runtime\": trainer_result.metrics.get(\"train_runtime\"),\n    }\n\ndef extract_dpo_metrics(trainer_result) -> dict:\n    return {\n        \"train_loss\": trainer_result.training_loss,\n        \"rewards_chosen\": trainer_result.metrics.get(\"rewards/chosen\"),\n        \"rewards_rejected\": trainer_result.metrics.get(\"rewards/rejected\"),\n    }\n\ndef extract_grpo_metrics(trainer_result) -> dict:\n    return {\n        \"train_loss\": trainer_result.training_loss,\n        \"rewards_mean\": trainer_result.metrics.get(\"rewards/mean\"),\n        \"sft_loss\": trainer_result.metrics.get(\"sft_loss\"),\n    }\n```\n\n### Priority 5: Registry Updates\n\n#### 4.5.1 AdapterSignature Validation\n```python\n@dataclass(frozen=True)\nclass AdapterSignature:\n    # ... existing fields\n    training_method: str\n    \n    def __post_init__(self):\n        if self.training_method not in METHOD_REGISTRY:\n            raise ValueError(\n                f\"training_method '{self.training_method}' not in METHOD_REGISTRY\"\n            )\n```\n\n### Priority 6: Reference Model + Online Methods\n\n#### 4.6.1 Reference Model Handling\n```python\nclass AlignmentPipeline:\n    async def _run_generic_training(\n        self,\n        trainer_class,\n        config_class,\n        dataset,\n        preference_dataset=None,\n        reference_model=None,\n        reward_model=None,\n    ):\n        # Handle optional reference/reward models for online methods\n```\n\n#### 4.6.2 Trajectory Data Support\n```python\nclass AlignmentPipeline:\n    async def train_online(\n        self,\n        base_model_path: str,\n        trajectory_dataset: Any,\n        reward_model: Any,\n        adapter_name: str,\n    ):\n        \"\"\"For GRPO, RLOO, Online DPO.\"\"\"\n```\n\n## 5. Test Coverage Analysis\n\n### Current Test Coverage\n- `test_config.py`: Validates SFT/DPO config validation (hardcoded methods)\n- `test_pipeline.py`: Tests SFT/DPO dispatch, dataset validation (DPO-specific)\n- `test_registry.py`: Generic, works for any method (good!)\n- `test_evaluator.py`: Generic, works for any method (good!)\n- `test_bridge.py`: Generic (good!)\n- `test_merge.py`: Generic (good!)\n\n### Missing Tests\n- Method registry implementation and routing\n- New method configs (GRPO, KTO, IPO, ORPO, SimPO)\n- Dataset validators for non-preference methods\n- Metrics extraction for each method\n- Online method workflows (trajectory data)\n- Reference model + reward model handling\n\n## 6. Dependency Analysis\n\n### TRL Trainers Required\n| Method  | TRL Class         | Required Version | Status    |\n|---------|-------------------|------------------|-----------|\n| SFT     | SFTTrainer        | >=0.25           | Imported  |\n| DPO     | DPOTrainer        | >=0.25           | Imported  |\n| GRPO    | GRPOTrainer       | >=0.13.0         | Needed    |\n| KTO     | KTOTrainer        | >=0.14.0         | Needed    |\n| IPO     | IPOTrainer        | >=0.14.0         | Needed    |\n| ORPO    | ORPOTrainer       | >=0.10.0         | Needed    |\n| SimPO   | SimPOTrainer      | >=0.12.0         | Needed    |\n| Online DPO | OnlineDPOTrainer | >=0.14.0 (beta) | Needed    |\n| RLOO    | RLOOTrainer       | >=0.13.0         | Needed    |\n\n### Current Dependency: `trl>=0.25,<1.0` \n**Impact**: \n- Covers SFT, DPO at current version\n- May need version bump or flexible pinning for new methods\n- Check compatibility before adding trainers\n\n## 7. Refactoring Effort Estimate\n\n| Task | Effort | Risk |\n|------|--------|------|\n| Method registry + factory | 2 days | Low |\n| Generic trainer wrapper | 2 days | Medium |\n| Method-specific configs (5 new) | 3 days | Low |\n| Dataset validators (4 new) | 2 days | Medium |\n| Metrics extractors (6 new) | 1 day | Low |\n| Reference/reward model handling | 2 days | Medium |\n| Tests (unit + integration) | 3 days | Medium |\n| Documentation + examples | 2 days | Low |\n| **Total** | **17 days** | — |\n\n## 8. Key Design Decisions\n\n### Recommendation 1: Registry-Driven Architecture\nUse a method registry instead of if-elif-else. This allows:\n- Extensibility without modifying pipeline.py\n- User-defined methods (custom trainer classes)\n- Dynamic method discovery\n\n### Recommendation 2: Lazy Trainer Import\nImport trainers only when needed:\n```python\ndef _import_trainer(method: str):\n    method_config = METHOD_REGISTRY[method]\n    return importlib.import_module('trl').method_config.trainer_class\n```\n\n### Recommendation 3: Separate Online Method Pipeline\nOnline methods (GRPO, RLOO, Online DPO) may need a separate `train_online()` flow because they require:\n- Trajectory rollouts\n- Reward model forward passes\n- Reference model management\n- Different data format\n\n### Recommendation 4: Backward Compatibility\nKeep existing `_run_sft()` and `_run_dpo()` for backward compatibility, but refactor to use generic wrapper.\n\n## 9. Gap Summary Table\n\n| Area | Current | Needed | Effort |\n|------|---------|--------|--------|\n| **Trainer Dispatch** | if-elif-else | Registry factory | 2 days |\n| **Config Classes** | 2 (SFT, DPO) | 7 (add 5 new) | 3 days |\n| **Dataset Validation** | 1 validator (DPO-specific) | 4 validators | 2 days |\n| **Metrics Collection** | Hardcoded per method | Extensible extractors | 1 day |\n| **Method Enums** | 3 options | Dynamic registry | 0.5 days |\n| **Tests** | 80% SFT/DPO focused | Method-agnostic | 3 days |\n| **Online Methods** | None | Trajectory pipeline | 2 days |\n| **Reference Models** | Implicit (DPO) | Explicit handling | 2 days |\n\n---\n\nThis audit shows that while the framework is well-structured for SFT and DPO, it requires systematic refactoring to become method-agnostic. The good news: evaluation, serving, and registry components are already generic, so only the training pipeline needs changes."}],"stop_reason":"end_turn","stop_sequence":null,"stop_details":null,"usage":{"input_tokens":6,"cache_creation_input_tokens":680,"cache_read_input_tokens":102554,"output_tokens":5150,"server_tool_use":{"web_search_requests":0,"web_fetch_requests":0},"service_tier":"standard","cache_creation":{"ephemeral_1h_input_tokens":0,"ephemeral_5m_input_tokens":680},"inference_geo":"","iterations":[],"speed":"standard"}},"requestId":"req_011CZcWTs5K9WBCkVuLo1p5z","type":"assistant","uuid":"62fe6b2e-5d89-42a1-9fac-3a092607318f","timestamp":"2026-04-01T05:43:04.155Z","userType":"external","entrypoint":"cli","cwd":"/Users/esperie/repos/loom/kailash-py","sessionId":"2c4f37fc-f84c-4241-8853-9ad158b41175","version":"2.1.88","gitBranch":"main","slug":"stateful-inventing-nova"}