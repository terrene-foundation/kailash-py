# Session Notes — 2026-07-14

## Where we are

Clean post-release state. **#1741 (per-connection credential callback / token-based
DB auth) is COMPLETE and RELEASED end-to-end** — kailash 2.51.0 + kailash-dataflow
2.18.0 on PyPI, verified installable, issue closed. main clean, tree clean, no
open PRs, no active todos.

## Read first

1. `workspaces/issue-1717-vertex-claude/journal/` — session decisions/discoveries (if any created)
2. GH #1741 (CLOSED) + PRs #1745 (code) / #1746 (release) — the token-auth completion
3. `packages/kailash-dataflow/src/dataflow/core/credential_provider.py` — the shared
   helper + contextvar side-channel (the architectural core of #1741)
4. `CLAUDE.md` — project directives

## In-flight state

- None — everything committed, merged, released, and verified.

## Executed this session (external / not in this repo's git log)

- Published **kailash 2.51.0** (tag `v2.51.0`) + **kailash-dataflow 2.18.0**
  (tag `dataflow-v2.18.0`) to PyPI — OIDC publish, clean-venv install verified,
  GitHub Releases created.

## Outstanding ledger (forest)

| ID    | Item                                          | Value-anchor (MUST-1 source)                                   | Status                          |
| ----- | --------------------------------------------- | -------------------------------------------------------------- | ------------------------------- |
| F1720 | Retire legacy→four-axis LLM redundancy        | User's #1717 follow-on ("resolve the redundancy") — GH #1720   | BLOCKED on co-owner scope       |
| FVERT | Vertex-Claude/Gemini LIVE validation          | The #1717 headline path; only wire-proven, never live          | BLOCKED on GCP creds (not .env) |

Closed this session: `F1741` (CRUD-path credential extension) → PRs #1745/#1746,
released kailash 2.51.0 + kailash-dataflow 2.18.0.
Dispositioned: README/Sphinx token-auth guide — user approved leaving as-is
(docstrings + CHANGELOG document it; no docs-only release).

## Traps

- **Machine disk-full** hit at session start (Data volume 100%); cleared regenerable
  Rust `sccache`. If bash output writes fail → check `df -h /System/Volumes/Data`.
- `test_bootstrap_realizer.py::test_resolve_llm_model_returns_none_when_env_unset`
  fails LOCALLY only (unrelated) — resolver's `load_dotenv()` re-reads this operator's
  `.env` (defines `OPENAI_PROD_MODEL`). CI-skipped; main green. Not a blocker.
- Duplicate `pull_request` CI runs race (`cancel-in-progress`) — a job can show
  `fail`/canceled on the superseded run while the LIVE run passes; verify the live run.
- `gh --repo terrene-foundation/kailash-py` trips the repo-scope lexical hook though
  it's the CWD repo (false positive); omit `--repo` on same-repo gh.
