# Session Notes — 2026-04-19 (Round 10)

## Where we are

Round-9 bundle release is live on PyPI (7 packages: kailash 2.8.8,
kailash-dataflow 2.0.11, kailash-ml 0.11.0, kailash-align 0.3.2,
kailash-pact 0.8.2, kailash-trust 0.1.1, kaizen-agents 0.9.3). Issue
triage + housekeeping on top. Cross-SDK parity for kailash-rs#424
(UTF-8 corruption at parameter binding) confirmed via Tier 2
integration test; issue #525 closed with test reference.

## Read first

1. `deploy/deployments/2026-04-19-bundle-round-9.md` — full release record
2. `packages/kailash-dataflow/tests/integration/database/test_issue_525_cross_sdk_utf8_parity.py` — cross-SDK parity regression test
3. `workspaces/kailash-ml-gpu-stack/04-validate/02-revised-stack.md` — ML Phase 1 deferred items 3, 4, 5, 7, 8 (the only remaining scope work)

## In-flight state

- No open issues on terrene-foundation/kailash-py.
- No open issues or PRs on terrene-foundation/kailash-coc-claude-py.
- 6 pending journal candidates in `workspaces/cross-sdk-audit/journal/.pending/` — auto-generated by SessionEnd hook from merged PRs #517, #518, #519, #522, #523; committed to repo for next /journal processing.
- 16 stale todos in `workspaces/gh-issues-apr14/todos/active/` archived to `completed/` (all referenced issues #443, #446, #440, #445 CLOSED on 2026-04-14).

## Traps

- `kailash-dataflow` `DataFlow.express.bulk_create` returns a dict (`{"processed","inserted","batch_size","operation","success"}`), NOT a `List[Dict[str, Any]]` as the docstring claims. Pyright will complain; runtime is correct. Docstring needs fixing separately.
- `execute_raw(self, sql: str) -> Any` accepts SQL only — no `params` argument. The kailash-rs#424 bug class (parameter-buffer-reuse at the binding layer) cannot exist at this Python API. The new `test_issue_525_execute_raw_has_no_params_arg` locks this in as a structural invariant.
- PyPI Trusted Publisher was registered for kailash-trust this session; future `trust-v*` tags will publish via OIDC without the local `~/.pypirc` token.
- Deferred ML Phase 1 follow-up (items 3/4/5/7/8 of the GPU-first punch list) remains out-of-scope per user instruction this session.
