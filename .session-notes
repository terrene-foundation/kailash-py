# Session Notes — 2026-04-19 (Round 7)

## Where we are

Round 7 closed 6 of 8 outstanding issues in one session. Five PRs
merged (#502-#506). Kailash-ml now has first-class sklearn-compatible
composites (`Pipeline` / `FeatureUnion` / `ColumnTransformer` +
`register_estimator`); Nexus has a typed `HttpClient` + `ServiceClient`
surface (112 new tests, SSRF-aware by default); DataFlow got 5 of 9
MED defense-in-depth findings fixed. Also delivered: a validated
architecture for GPU-first kailash-ml (see workspaces/kailash-ml-gpu-stack/).

## Read first

1. `gh pr list --state merged --repo terrene-foundation/kailash-py --limit 8` —
   Round-6 shipped #502 (embed), #503 (PG quoting). Round-7 shipped
   #504 (defense-in-depth), #505 (Nexus HttpClient+ServiceClient),
   #506 (ml Pipeline+register_estimator).
2. `gh issue list --state open --repo terrene-foundation/kailash-py` —
   remaining open: #499 (reopened, 4/9 MED findings deferred), #465
   (OpenAPI-generated S2S client, depends on delivered #464 pattern).
3. `workspaces/kailash-ml-gpu-stack/04-validate/00-convergence.md` —
   validated architecture for GPU-first kailash-ml. Ships the redteam
   findings + revised stack + 8-item implementation punch list.
4. `workspaces/kailash-ml-gpu-stack/journal/0001-0003-*.md` — three
   journal-worthy findings from the redteam (cuML eviction, DeviceReport
   missing, XGBoost OOM silent crash).

## In-flight state

- `workspaces/OUTSTANDING.md` is STALE (round-5 snapshot). The real
  current list: #499 (4 deferred findings), #465 (S2S OpenAPI
  codegen). Every other cross-SDK issue from round-5 is closed.
- Pre-existing proposal `.claude/.proposals/latest.yaml` has 7 entries
  status `pending_review` — user was running /sync at loom/ when
  Round 6 closed; state unchanged this session.

## Traps

- **Pre-commit auto-stash bug** still active — every commit MUST use
  `git -c core.hooksPath=/dev/null commit ...` per `rules/git.md`.
  Hit it again on 4/5 commits this session.
- **Specialist-agent-dumps-in-main-tree trap** bit HARD this session.
  Three agents launched with `isolation: worktree`: only Nexus actually
  used its worktree. DataFlow and ML agents both edited in main's
  working tree. Both terminated mid-implementation leaving partial
  state on main. Recovery: committed DataFlow's partial as #504,
  took over and finished ML myself as #506. Next session: verify
  worktree status IMMEDIATELY after spawning, not on completion.
- **CodeQL `__all__`-not-defined check** caught kailash-ml lazy-load
  pattern. Scanner-surface-symmetry rule forbids "other entries on
  main also do this" — must eager-import new entries. Existing
  entries grandfathered (baseline).
- **ML agent budget exhaustion mid-message.** Reported "Now let me
  write the implementation" with zero actual implementation files
  written. Same pattern as round-6 kaizen agent. Always verify files
  after agent completion claim.

## Next priorities

1. **Remaining #499 findings (4 of 9)** — SQLite PRAGMA in 3 files,
   force_drop gate on 5 migration DROP paths, spy-based schema_manager
   test, split test_phase_5_11 per-manager. All low-risk; batch into
   one follow-up PR.
2. **kailash-ml GPU first-class rollout** — `workspaces/kailash-ml-gpu-stack/`
   has the full implementation plan. 3 sessions of work: evict cuML
   extra, add DeviceReport, sklearn Array-API auto-wrapping, OOM
   fallback helper, RL device routing audit, `km.device()` helpers,
   Tier-2 regression tests per backend. Open GH issue first linking
   to workspace.
3. **#465 S2S OpenAPI codegen** — depends on the delivered #464
   pattern. User designated it MEDIUM; revisit after GPU rollout.

## Session totals

- 5 PRs merged: #502 #503 #504 #505 #506
- 112 new Nexus tests + 14 new ML tests + regression tests for
  defense-in-depth findings
- 6 issues closed: #462 #480 #464 #473 #479 #488
- 1 issue reopened with remaining-work note: #499
- 1 validated architecture delivered (GPU-first kailash-ml)
- 3 journal entries (workspaces/kailash-ml-gpu-stack/journal/0001-0003)
