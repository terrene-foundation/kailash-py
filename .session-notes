# Session Notes — 2026-04-18 (Round 2)

## Where we are

Completed the entire `issues-492-497/` workspace queue. 5 GH issues
closed with delivered-code references. 3 commits pushed to
`origin/main` (7a4fd364 → 27c77cf4). Cross-SDK ticket #405 filed on
kailash-rs.

## This session closed items

- **#492 (P0 SQLi)** — `BulkUpsertNode._build_upsert_query` rewritten
  to return `(sql, params)` with placeholder binding. 18 SQLi
  regression tests + 6 existing unit tests + 18 existing integration
  tests all green. Identifier validation routed through
  `_validate_identifier` for `table_name`, `columns`, `conflict_on`,
  `version_field`. Dialect allowlist enforced. Bulk batch errors now
  log WARN with continue semantics; raise only if every batch failed.
  Commit 7a4fd364.
- **#493 (sanitizer contract)** — All 3 drifted tests pass against the
  pinned contract. New type-mismatch guard in `validate_inputs` (both
  create/update + bulk_* paths). Rule pinned at
  `rules/security.md` § Sanitizer Contract. Commit 2dbb9107.
- **#495 (ML register_estimator audit)** — NO-GAP. kailash-py's
  string-import + prefix-allowlist pattern dissolves the trait-object
  problem. Audit at `04-validate/495-ml-register-estimator-audit.md`.
- **#496 (PG placeholder audit)** — Placeholder bug NOT present.
  Audit surfaced 2 HIGH identifier-validation gaps fixed in same
  commit (sync_ddl_executor SQLite PRAGMA + engine ALTER TABLE
  paths). 13-test regression at `tests/regression/
  test_issue_496_identifier_safety.py`.
- **#497 (Nexus webhook HMAC audit)** — Gap exists in generic
  `Nexus.register(workflow)` path, but `WebhookTransport` ships as
  the structural answer for signed webhooks. Architectural decision
  recorded.
- **Cross-SDK** — kailash-rs#405 filed for bulk_upsert.rs parity check.

## Spec updates

- `specs/security-data.md` § 11.3 — bulk_upsert parameterization
  pattern + sanitizer contract pin
- `specs/dataflow-express.md` — bulk_upsert API now documents
  `(sql, params)` internal contract + WARN-on-batch-error semantics

## Read first — NEXT WORKSPACE

No active workspace. The `issues-492-497/` queue is fully cleared.
Next session can either:

1. Pick a new GH issue from the open queue
2. Run `/wrapup` on the workspace and archive
3. Address out-of-scope loose ends below

## Out-of-scope loose ends

- **Loom-side commit** for `event-payload-classification.md` rule
  file + manifest entry (placed in loom in 2026-04-18 round 1, commit
  still deferred). Then `/sync py` to flow into BUILD repo.
- **Pool path dead code** in `BulkUpsertNode._execute_query` — the
  pool branch silently falls back because
  `DataFlowConnectionManager.execute()` doesn't implement
  `operation="execute"`. Reviewer flagged H2 as orphan-pattern. Fix:
  either implement the pool operation OR delete the dead branch.
  Defer to a separate PR — out of scope for this session's #492 fix
  per `rules/autonomous-execution.md` Per-Session Capacity Budget
  (load-bearing already at the 5-invariant ceiling).
- **`LocalRuntime.execute()` deprecation** — 8 tests in
  `test_connection_sql_injection_protection.py` still use the
  no-context-manager pattern. Pre-existing, not introduced this
  session, scope = test-style only. File a follow-up enhancement.

## Traps (carried forward)

- `pytest --timeout-method=signal` hangs on asyncio selector loops;
  use `--timeout-method=thread`.
- Pre-commit `python-use-type-annotations` hook flags any `# type X`
  comment as an annotation. Use "type mismatch" not "type confusion"
  in code comments.
- Pre-commit auto-stash + pyright noise — ignore `reportAssignmentType`
  on `from kailash.nodes.base import ...` (pre-existing).
- `BulkUpsertNode` adds `created_at`/`updated_at` automatically when
  `auto_timestamps=True` (default). Test tables must include these
  columns OR the node must be configured with `auto_timestamps=False`.
- SQLite `UpdateNode`/`UpsertNode` return metadata only (not the
  row). Read-back via `self.read()` is the dialect-portable pattern
  (#490 fix).
