# Session Notes — 2026-06-20

## Where we are

Canonical-encoder cross-SDK conformance on `fix/audit-chain-canonical-conformance`
(HEAD pushed to origin). PR #1411 DRAFT, BLOCKED. This session: full `default=str`
family sweep + redteam-converged + cross-SDK `kailash-rs#1451` filed. Main
unchanged; working tree clean.

## Read first

1. `workspaces/cross-sdk-audit/journal/0011` — family sweep + dispositions (broadened lockstep)
2. `workspaces/cross-sdk-audit/journal/0012` — rs#1451 filing receipt (cross-repo-authorized)
3. `workspaces/cross-sdk-audit/04-validate/sweep-2026-06-20.md` — repo-wide sweep + ranked next items
4. `specs/trust-canonical-encoders.md` — encoder-family map (conformant / blocked / local sites)
5. `tests/regression/test_canonical_encoder_family_conformance.py` — the byte pins (re-pin only in rs lockstep)

## In-flight state

- Branch pushed; PR #1411 reflects the converged state but stays DRAFT until rs#1448 + rs#1451 land.
- Nothing uncommitted.

## Outstanding ledger (forest)

| ID | Item | Value-anchor (MUST-1) | Status |
| -- | ---- | --------------------- | ------ |
| F-AUDIT-XSDK | cross-SDK canonical lockstep (audit-chain + witness-family + envelope HMAC) | user "coordinate lockstep" + kailash-rs#449/#1448/#1451 | BLOCKED — rs#1448+#1451; PR #1411 draft |
| F-STUB-1406 | package-wide stub-marker audit vs Production/Stable | #1406 [gap] / zero-tolerance R2 | queued — top next-session item |
| F-XSDK-TRUST-R2 | rs parity for 2.42.0 trust defect classes | EATP D6 cross-SDK | SURFACED — human-gated |
| F-XSDK-PACT | rs parity for #1393/#1394 | EATP D6 cross-SDK | SURFACED — human-gated |
| F7 | rs KSP.compartments-unenforced parity | EATP D6 cross-SDK | BLOCKED — human-gated |
| F4 | rs Nexus JWT middleware revocation gap | EATP trust-plane revocation | BLOCKED — human-gated |
| F-VAULT-S34 | §3.4 KEK re-establishment hierarchy gap (#630) | project_trustplane_day1 | DEFERRED |
| F-ATOMIC | cross-store atomic cascade invalidation | trust-plane revocation correctness | DEFERRED |
| F-XSDK-VAULT-630 | rs vault N12-CL-02a + recommit/retire | EATP D6 cross-SDK | FILED rs#1442 — awaiting rs |
| F-XSDK | rs LlmClient lifecycle parity | EATP D6 cross-SDK | FILED rs#1420 — awaiting rs |
| F10 | loom Gate-1 Rule-10 proximity + evidence-first sync into py | loom artifact-flow Gate-1 | queued — loom-side |

Closed this session: `F-SELDISC` → ae0118b64 + rs#1451 + journal/0011 (pinned, folds into F-AUDIT-XSDK); `F-ENVELOPE-817` → ae0118b64 (byte-neutral shipped); `F-ENVELOPE-1039` → ae0118b64 + rs#1451 (pinned); `F-DFAUDIT` → journal/0011 (NO_CHANGE, documented in spec).

## Traps

- PR #1411 BLOCKED on rs#1448 AND rs#1451 — never merge/release py-only (shared byte contracts).
- Witness-family + envelope `to_canonical_json` are byte-CHANGING — the pins in `test_canonical_encoder_family_conformance.py` break loudly if you "unify" py-only; re-pin ONLY in lockstep with rs.
- `envelope_hash` uses SPACED default separators (not compact); pact/audit legacy `_compute_hash_*` keep default=str by design.
- Workflow parallel fan-out throttles errored agents to null — use the `ran`-evidence gate (caught it twice this session).
- Cross-repo to kailash-rs needs ALL 5 repo-scope conditions + receipt-BEFORE-acting (journal/0011→0012 are the templates).

## Unreleased packages

None on `main` (latest tag `v2.43.0`; `pyproject` 2.43.0 matches). The canonical-conformance work is blocked-in-PR (#1411), not releasable until the rs lockstep.
