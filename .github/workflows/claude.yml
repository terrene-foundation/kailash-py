name: Claude Code Maintenance

on:
  issue_comment:
    types: [created]
  issues:
    types: [opened, assigned, labeled]
  pull_request_review_comment:
    types: [created]
  pull_request_review:
    types: [submitted]

jobs:
  claude:
    if: |
      (github.event_name == 'issue_comment' && contains(github.event.comment.body, '@claude')) ||
      (github.event_name == 'issues' && github.event.action == 'assigned' && github.event.assignee.login == 'claude') ||
      (github.event_name == 'issues' && github.event.action == 'labeled' && github.event.label.name == 'auto-fix') ||
      (github.event_name == 'pull_request_review_comment') ||
      (github.event_name == 'pull_request_review' && contains(github.event.review.body, '@claude'))
    runs-on: self-hosted
    timeout-minutes: 120
    permissions:
      contents: write
      pull-requests: write
      issues: write
      id-token: write
      actions: read
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - uses: anthropics/claude-code-action@v1
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          assignee_trigger: "claude"
          label_trigger: "auto-fix"
          settings: ".claude/settings.json"
          prompt: |
            You are the automated maintenance agent for the Kailash Python SDK.
            You MUST follow the full COC (Cognitive Orchestration for Codegen) lifecycle.
            CLAUDE.md and all .claude/rules/ are your absolute directives.

            ## Phase 1: Analyze
            - Read the issue body carefully. Identify the affected package(s).
            - Use the appropriate specialist agent (dataflow-specialist, nexus-specialist, etc.)
            - If the issue is unclear, comment on the issue asking for clarification and STOP.

            ## Phase 2: Reproduce
            - Write a regression test in `tests/regression/` that reproduces the exact bug.
            - Name it `test_issue_<NUMBER>_<description>`.
            - Run it — it MUST fail. If it passes, the bug is not reproduced; investigate further.

            ## Phase 3: Fix
            - Implement the fix following all .claude/rules/ (zero-tolerance, no-stubs, patterns).
            - Use the framework-first approach — check existing patterns before writing new code.
            - Update __init__.py __version__ if pyproject.toml version changes.
            - The regression test MUST now pass.

            ## Phase 4: Validate
            - `black --check .` — must pass
            - `ruff check` — must pass
            - `pytest` — all tests pass (not just the new one)

            ## Phase 5: Commit & PR
            - Commit with conventional format: `fix(scope): description`
            - Create a PR referencing the issue: `Fixes #N`
            - If the fix is high-confidence (all tests pass, clear reproduction), label PR `auto-fix`
            - If uncertain or issue is complex, label PR `needs-review`

      - name: Notify Discord
        if: always()
        run: |
          if [ -n "${{ secrets.DISCORD_WEBHOOK_URL }}" ]; then
            STATUS="${{ job.status }}"
            ISSUE="${{ github.event.issue.number || github.event.pull_request.number }}"
            TITLE="${{ github.event.issue.title || github.event.pull_request.title }}"
            REPO="${{ github.repository }}"

            if [ "$STATUS" = "success" ]; then
              COLOR=3066993
              MSG="Claude completed work on #${ISSUE}: ${TITLE}"
            else
              COLOR=15158332
              MSG="Claude failed on #${ISSUE}: ${TITLE}"
            fi

            curl -s -H "Content-Type: application/json" \
              -d "{\"embeds\":[{\"title\":\"${MSG}\",\"url\":\"https://github.com/${REPO}/issues/${ISSUE}\",\"color\":${COLOR}}]}" \
              "${{ secrets.DISCORD_WEBHOOK_URL }}" || true
          fi
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
