# /sweep findings — 2026-04-30

Repo-wide outstanding-work audit at the close of the the v2.13.0 cluster (#712/#713/#714) release. Goal of the audit: surface every category of outstanding work so the human can decide scope before declaring "kailash-py complete."

## Summary

| Severity | Count |
| -------- | ----- |
| CRIT     | 0     |
| HIGH     | 2     |
| MED      | 5     |
| LOW      | 4     |
| INFO     | 2     |

**Headline:** No CRIT/blocker findings. Repo is in clean structural shape — zero active todos, zero open PRs, zero PyPI version drift, zero stale worktrees, codify proposal `distributed`. Outstanding work is **forward-leaning, not remedial**: 4 actionable open issues, 2 unfiled cross-SDK companions, plus housekeeping debt (deferred-codify list cleanup, journal pending entries, two ready-to-archive workspaces).

## Findings (ordered by severity)

### [HIGH] [Sweep 11] WebhookSigner #687 cross-SDK companion not filed at kailash-rs

**Location:** PR #717 (merged 2026-04-29, commit `a835e5c4`)
**Disposition:** FILE-ISSUE
**Evidence:** Commit body explicitly says `Cross-SDK: kailash-rs follow-up issue (file separately)`. `gh issue list --repo esperie/kailash-rs --search "WebhookSigner"` returns `[]`.
**Why this matters:** `rules/cross-sdk-inspection.md` MUST Rule 1: every fix at one SDK MUST have been inspected for the other SDK's applicability. Pluggable transport-signer protocol is a clear cross-cutting surface; without the companion filed, the next kailash-rs session re-discovers the design from scratch.
**Action taken:** None — visible-to-others action; needs user authorization.

### [HIGH] [Sweep 11] kailash-rs#707 (the downstream consumer #714 DDL parity) awaiting implementation

**Location:** [kailash-rs#707](https://github.com/esperie/kailash-rs/issues/707) — OPEN, filed 2026-04-30
**Disposition:** DEFER-WITH-REASON (cross-repo workstream — not implementable from kailash-py)
**Evidence:** Issue body has full acceptance criteria + Python implementation reference. State: OPEN, no linked PR.
**Why this matters:** kailash-rs likely has the same `auto_migrate=True` DDL connection-thrash failure mode against pgbouncer / Azure PostgreSQL `max_connections`. Customer-facing risk if a Rust-SDK consumer hits the same incident.
**Action taken:** None — implementation belongs to the kailash-rs repo. Surface in next kailash-rs session.

### [MED] [Sweep 9] Deferred-codify list contains stale items (3+ already resolved)

**Location:** `.claude/learning/learning-codified.json::deferred_to_next_cycle`
**Disposition:** FIX-NOW (cleanup commit) — surfaced for batch handling
**Evidence:**

- "MED-1: `__version__` imported but absent from `__all__`" → verified TRUE: `__version__ in __all__: True` in `packages/kailash-ml/src/kailash_ml/__init__.py`. **Already resolved.**
- "Duplicate ModelNotFoundError in kailash_ml/engines/model_registry.py" → grep shows the local class is gone; line 23 imports from `kailash_ml.errors`; comment at line 56-58 documents the de-duplication. **Already resolved.**
- "Rule file overflow — zero-tolerance.md is now 228 lines and testing.md is 237 lines" → verified: `wc -l` reports `189 zero-tolerance.md` and `200 testing.md`. **No longer overflowing.**
- "HIGH-1: register symbol redefinition shadowing in `kailash_ml/__init__.py`:57+246" → grep finds no `def register` shadowing pattern; needs deeper inspection but likely resolved.

**Why this matters:** Stale deferred-codify items pollute the next codify cycle — agent re-evaluates resolved-but-listed items, wasting budget and risking re-fixing fixed bugs. Per `rules/journal.md` decision tree, resolved items must be removed.
**Action taken:** None — recommend a single cleanup commit pruning the resolved entries with delivered-code references.

### [MED] [Sweep 10] Two workspaces ready for archive (ML-1.5x followup + issues-712-714)

**Location:** `workspaces/kailash-ml-1.5.x-followup/`, `workspaces/issues-712-714/`
**Disposition:** FILE-WORKSTREAM (closure)
**Evidence:**

- `workspaces/kailash-ml-1.5.x-followup/todos/` has only `completed/` (no `active/`). All issues #699/#700/#701 closed via PR #708/#709.
- `workspaces/issues-712-714/todos/` has only `completed/`. Cluster fully shipped (PRs #722-#730).
- Both have `.session-notes` from 2026-04-30. Both have `.pending` journal entries (Sweep 2).

**Why this matters:** Closed workspaces with pending journals + stale local-only state accumulate. Per `rules/journal.md`, the closure ritual is: promote-or-discard pendings → move `.session-notes` to `_archive/`. Unclosed workspaces inflate `/ws` dashboards and confuse the next session about what's in flight.
**Action taken:** None — surfaced for batch handling.

### [MED] [Sweep 3] 4 actionable open kailash-py issues; 3 external-blocked

**Location:** `gh issue list --repo terrene-foundation/kailash-py --state open`
**Disposition:** SURFACE FOR SCOPING
**Evidence:** 7 open total:

| #   | Title                                                                            | Status     | Note                                                                         |
| --- | -------------------------------------------------------------------------------- | ---------- | ---------------------------------------------------------------------------- |
| 731 | TraceEvent.to_dict() timestamp microsecond padding (cross-SDK)                   | ACTIONABLE | Filed 2026-04-30, fresh                                                      |
| 711 | dataflow: db.transactions_sync.begin() sync surface                              | ACTIONABLE | Single-shard candidate                                                       |
| 673 | nexus: Origin/Host allowlist on register_websocket (cross-SDK + security)        | ACTIONABLE | Security-relevant                                                            |
| 643 | ml: FeatureStore canonical surface migration (breaking-change deprecation cycle) | ACTIONABLE | Multi-session — needs deprecation cycle plan per `zero-tolerance.md` Rule 6a |
| 693 | spec: promote dataflow-ml-integration.md from 1.0.0 (draft)                      | BLOCKED    | Hard block on M2 surface landing                                             |
| 630 | Trust Vault SLIP-0039 binding                                                    | BLOCKED    | Hard block on mint ISS-37                                                    |
| 596 | TieredAuditDispatcher                                                            | BLOCKED    | Hard block on mint ISS-06                                                    |

**Why this matters:** This is the actionable inventory for "kailash-py is complete" — once the 4 actionable land, only externally-blocked work remains.
**Action taken:** None — recommend a focused workstream for #731 + #711 + #673 + #643.

### [MED] [Sweep 11] MountInfo `__all__` fix (#720) cross-SDK applicability uninspected

**Location:** PR #720 (merged 2026-04-29) — `fix(nexus): add MountInfo to __all__ per orphan-detection.md Rule 6`
**Disposition:** SURFACE FOR INSPECTION
**Evidence:** `gh issue list --repo esperie/kailash-rs --search "MountInfo"` returns `[]`.
**Why this matters:** `orphan-detection.md` Rule 6 is a global rule (Python `__all__` ↔ Rust `pub use` re-exports). The pattern (eagerly-imported public symbol absent from public-API list) reproduces in Rust verbatim. Cheap inspection: `grep -rn "pub use.*MountInfo\|MountInfo" kailash-rs/`.
**Action taken:** None — fold into the next kailash-rs sync as an inspection task.

### [MED] [Sweep 9] Pre-commit hook upward-`.venv` resolution still unresolved

**Location:** Carry-forward from 2026-04-27 codify cycle
**Disposition:** DEFER-WITH-REASON (low blast radius; documented workaround in `rules/git.md` § Pre-Commit Hook Workarounds)
**Evidence:** Last several worktree-isolated commits used `git -c core.hooksPath=/dev/null commit ...`.
**Why this matters:** Recurring across sessions; workaround is documented and discoverable via `git log --grep`. Each session that hits it pays a small re-discovery tax. A focused fix (probably ~30 min in pre-commit config) eliminates the tax.
**Action taken:** None — single-shard candidate for next infra-touching session.

### [LOW] [Sweep 2] 3 pending journal entries — discard candidates

**Location:**

- `workspaces/issues-712-714/journal/.pending/1777515098049-0-RISK.md` (commit `273128e5`, S7 specs update)
- `workspaces/kailash-ml-1.5.x-followup/journal/.pending/1777472524358-0-RISK.md` (commit `a835e5c4`, WebhookSigner #687)
- `workspaces/kailash-ml-1.5.x-followup/journal/.pending/1777472524358-1-DECISION.md` (commit `cbf297de`, TransactionScope.execute_raw #707)

**Disposition:** DISCARD (commit bodies are rich + grep-able; no novel institutional knowledge to add)
**Evidence:** All three pending entries are auto-generated commit-body summaries. The S7 entry is meta-narrative on a specs update already covered by journal entries 0001-0004 in the same workspace. The WebhookSigner / TransactionScope entries are merge-commit narratives whose context lives in the PR descriptions. Per `rules/journal.md` decision tree, no value-add beyond `git log --grep`.
**Action taken:** None — `rm` on each is reversible local-only state; surfaced for batch handling.

### [LOW] [Sweep 9] Pyright diagnostics on `dataflow/core/engine.py` (verification deferred)

**Location:** `packages/kailash-dataflow/src/dataflow/core/engine.py`
**Disposition:** DEFER-WITH-REASON (pyright not installed in current `.venv`)
**Evidence:** Cannot verify the deferred-codify item (~10 errors: `TenantContextSwitch undefined`, `max_overflow unknown`, `_engine unknown`, `asyncio possibly-unbound`). Inline grep shows imports + usage look syntactically fine.
**Why this matters:** May be already-fixed; may still be live. Either way, single-shard load — open `dataflow-engine-pyright-cleanup` workspace per the deferred-codify recommendation.
**Action taken:** None — needs pyright install to verify before scoping.

### [LOW] [Sweep 5/0] CI hygiene debt: `release/v*` PR-gate skip clause not adopted

**Location:** `.github/workflows/*` (kailash-py workflows)
**Disposition:** DEFER-WITH-REASON (documented in carryover session-notes; not blocking)
**Evidence:** Latest release-prep PR #729 (release/v2.13.0-cluster) fired the full PR-gate matrix. The `git.md` § "Release-Prep PRs MUST Use `release/v*` Branch Convention" + `ci-runners.md` skip clause exists at loom; not yet emitted to kailash-py.
**Why this matters:** ~10 min of avoidable CI per release-prep PR. With cluster releases averaging 1-2/week, the cost is 1-2 GitHub-billable runner-hours per quarter. Real but not urgent.
**Action taken:** None — single-shard CI workflow refactor; adopt next time the workflow files are open for another reason.

### [LOW] [Sweep 0] `.claude/rules/git.md` overdue extraction (~295 lines)

**Location:** `.claude/rules/git.md`
**Disposition:** DEFER-WITH-REASON (carryover trap from prior session-notes)
**Evidence:** `wc -l` confirms 295 lines vs `rule-authoring.md` § 6 200-line guidance.
**Why this matters:** Files >200 lines crowd context when the rule loads as `baseline` scope. Extraction to `guides/rule-extracts/git.md` with the load-bearing MUST clauses kept inline is the documented pattern (mirroring `agents.md`, `worktree-isolation.md`, `zero-tolerance.md`).
**Action taken:** None — single-shard meta-cleanup; out of any current workstream.

### [INFO] [Sweep 6] Spec-vs-code drift sweep NOT performed

**Location:** `specs/*.md` (73 spec files)
**Disposition:** SCOPE-FOR-DEDICATED-WORKSTREAM
**Evidence:** 73 spec files exceeds `/sweep` budget for AST/grep verification per spec. Sweep 6 was scoped down to "verify recently-touched specs" but even that requires a full `/redteam`-style mechanical sweep.
**Why this matters:** Spec authority depends on the implementation honouring the spec's MUST clauses; drift accumulates silently between sweeps. The last full audit was the kailash-ml-audit (2026-04-23) which scoped to ML; non-ML domains haven't had an end-to-end sweep this quarter.
**Action taken:** None — recommend a `portfolio-spec-audit-v2` workspace (the v1 is 2d old per session-start hook; could be revived).

### [INFO] [Sweep 10] 14 of 17 workspaces have session-notes >7d (system-start hook flag)

**Location:** `workspaces/*/.session-notes`
**Disposition:** INFORMATIONAL — none crossed 30d archive threshold
**Evidence:** Oldest session-notes is 2026-04-01 (29d) at `workspaces/kailash/.session-notes`. None >30d. The session-start hook flagged 10 of these as STALE (likely uses 14d threshold).
**Why this matters:** Stale workspace flagging is informational; the actionable subset is "ready for archive" (Sweep 10's primary finding above).

## Cross-cutting observations

1. **Forward-leaning state, not remedial.** Zero CRIT findings, zero blocker debt. Every HIGH/MED is "next-cycle work," not "fix-the-ship-before-it-sinks." This is the structurally healthiest state kailash-py has been in across the recent sweeps (compare to the 2026-04-12 bug-sweep-april workspace which surfaced 7 SQLi / type-confusion issues).

2. **Cross-SDK obligation pattern.** 3 of the 11 findings (HIGH WebhookSigner, HIGH kailash-rs#707, MED MountInfo) are kailash-rs-side work. Kailash-py is structurally complete; the remaining work is parity. Whoever takes the next kailash-rs session inherits a 3-issue queue.

3. **Two clusters fully shipped, neither archived.** Both the downstream consumer (#712-714) and ML 1.5x followup (#699-701) workspaces are at completion-state but unclosed. Closure ritual takes ~5 min per workspace; recommend batching with the deferred-codify cleanup.

4. **Self-resolving deferred-codify items.** 3 of 8 deferred items have already been silently resolved by other shards (MED-1 **version**, ModelNotFoundError dedup, rule-file-overflow under threshold). The deferred-codify list is the source of truth for "what's outstanding"; if it doesn't reflect reality, downstream automation is wrong. Cleanup matters.

## Recommended next session scope (ranked by impact)

1. **Cross-SDK parity workstream (HIGH × 2 + MED × 1)** — file `kailash-rs#WebhookSigner-companion`, file `kailash-rs#MountInfo-orphan-port`, surface kailash-rs#707 to whoever picks up the next Rust session. Pure issue-filing + handoff; ~15 min.

2. **Actionable kailash-py issue cluster (#731 + #711 + #673)** — 3 single-shard issues, mixed framework surface (kailash core diagnostics + dataflow + nexus). Estimated 1 session if sharded properly. #643 (FeatureStore breaking-change) needs its own deprecation-cycle planning session (per `zero-tolerance.md` Rule 6a) and should not be bundled.

3. **Repo housekeeping batch (MED × 2 + LOW × 1)** — close ML-1.5x followup + issues-712-714 workspaces, prune deferred-codify list of 3+ resolved items, discard 3 pending journal entries. Single commit, ~20 min.

4. **`portfolio-spec-audit-v2` workspace (INFO)** — full spec-vs-code drift sweep across 73 specs. This is the dedicated workstream Sweep 6 wants. Multi-session per the kailash-ml-audit precedent. Schedule when there's appetite for thorough spec hygiene.

5. **Infra debt batch (LOW × 3)** — `release/v*` CI skip adoption, `git.md` extraction, pre-commit hook `.venv` fix. Each is single-shard; bundle if any of those files are open for another reason.

## Audit hygiene

This sweep performed Sweep 1, 2, 3, 4, 5, 7, 8, 9, 10, 11. **Sweep 6 (spec-vs-code) was NOT performed** — see INFO finding above. The 11-of-11 contract was not met; flagging explicitly.
