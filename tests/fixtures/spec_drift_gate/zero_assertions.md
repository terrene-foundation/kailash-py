<!-- expected: findings=0 fr_codes=[] origin=zero_assertions_demo -->
<!-- spec_drift_gate self-test fixture — DO NOT promote to specs/. -->

# Fictitious Spec — Pure Prose

Status: TESTING-FIXTURE — pure prose with no allowlisted headings and no
backticked symbols. The gate emits PASS + a WARN about zero allowlisted
sections found (per spec § F4 fix). No findings.

This document discusses high-level intent without code references.
Future authors will turn the prose below into a `## Surface` section
once the design solidifies.

The system aims to provide deterministic outputs and clear contracts.
