# /sweep — kailash-py — 2026-07-02 (full-surface, thorough)

Repo-wide outstanding-work audit. Scope: kailash-py only (current repo). Branch `main` @ `7155b46e2`, in sync with `origin/main` (0/0).

> Supersedes `SWEEP-2026-06-26.md` (was @ `9238dd4fb`, verdict CLEAN). Since that run, **4 new GH issues** were filed (2026-06-27 → 07-01) — the only material change to the surface. Everything else re-verified clean against current `main`.

## Verdict

**Near-clean checkpoint with 4 genuinely-actionable open issues.** 0 open PRs, 0 orphan/unmerged remote branches, 0 active todos, 0 pending journal candidates (one discarded inline this run), 1 worktree (main, clean), working tree clean except the deliberately-untracked `.session-notes` + sweep files. Every package's in-tree version equals its latest release tag with **0 shippable commits after tag — nothing genuinely unreleased.** The 4 open issues are all trust/PACT feature or security work filed in the last 5 days; none are deferred, none are closeable. The `# TODO` stub baseline is unchanged and remains user-designated leave-as-baseline (F-STUBS).

## Findings

| #   | Sweep              | Severity | Finding                                                                                                                      | Disposition                                           |
| --- | ------------------ | -------- | ---------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------- |
| 1   | 1 Todos            | —        | No active todos in any of the 40 workspaces                                                                                  | CLEAN                                                 |
| 2   | 2 Pending journals | LOW      | 1 dormant `.pending` auto-capture (`1782467412188-0-DECISION.md`, mops-onboarding) for shipped+documented commit `0903538e1` | FIXED (discarded inline; gitignored, reconstructable) |
| 3   | 3 Issues           | HIGH     | 4 open issues — all actionable, none deferred/closeable (see detail)                                                         | SURFACE-FOR-PRIORITIZATION (user call on next scope)  |
| 4   | 4 PRs/branches     | —        | 0 open PRs; 0 remote branches unmerged to `origin/main`                                                                      | CLEAN                                                 |
| 5   | 5 Redteam          | N/A      | No `workspaces/*/specs/` (spec_count=0); `tools/sweep-redteam.py` present but no in-scope per-workspace specs                | N/A (structural — sentinel below)                     |
| 6   | 6 Worktrees        | —        | 1 worktree (`main` @ `7155b46e2`, clean); 0 stale `.pending` >14d; 0 stale `.session-notes` >30d                             | CLEAN                                                 |
| 7   | 7 Process          | —        | Working tree clean except `.session-notes` (M, expected) + `SWEEP-*.md` (??, expected pattern); 0/0 vs origin                | CLEAN                                                 |
| 8   | 7 Stub markers     | LOW      | `# TODO` / `NotImplementedError` baseline in `src/` + `packages/*/src` — unchanged; 0 introduced                             | DEFER-WITH-REASON (F-STUBS user baseline, 2026-06-26) |
| 9   | 8 Release          | —        | All 8 packages: in-tree version == latest tag; 0 shippable commits after each package's tag                                  | NO RELEASE WARRANTED                                  |

<!-- sweep-redteam:v1:N/A reason=orchestration-mode no_specs=true no_tool=false -->

## Detail

### #3 — Open issues (HIGH — 4 actionable, filed since last sweep)

None carry a `deferred` label; none have delivered code (not closeable per `rules/git.md` § Issue Closure). All are trust-plane / PACT governance work. Ranked by risk:

| Issue     | Age | Class                    | Summary                                                                                                                                                                                           |
| --------- | --- | ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **#1480** | 5d  | **Security** (cross-sdk) | `SqliteOrgStore._deserialize_org` rebuilds `CompiledOrg` from JSON **without re-validation** — an unvalidated authorization root. Highest risk: a tampered store row becomes trusted authz state. |
| **#1483** | 1d  | Feature (cross-sdk)      | eatp `HoldQueue` approval/rejection signature should **bind the reviewed disclosure** (cross-SDK of kailash-rs#1562) — signature-scope integrity.                                                 |
| **#1482** | 3d  | Feature (trust)          | Per-recipient disclosure-trace tokens bound to audit records (leak attribution).                                                                                                                  |
| **#1481** | 3d  | Feature (trust)          | First-class `ConsentAttestation` (affirmative human-acceptance record).                                                                                                                           |

**Why this matters:** #1480 is a governance-security finding on the authorization root — a deserialization path that reconstructs a trusted `CompiledOrg` without re-running validation. That is the class of bug where a corrupted/tampered persisted row silently becomes authoritative. #1483 is cross-SDK-paired with an already-filed rs issue, so it wants parity coordination. #1481/#1482 are additive trust primitives (net-new records), lower urgency than the two integrity items.

### #2 — Dormant pending journal candidate (FIXED)

`workspaces/mops-onboarding/journal/.pending/1782467412188-0-DECISION.md` was a SessionEnd-hook auto-capture for commit `0903538e1` (`test(dataflow): close leaked resources in infra-free regression suite`). That work already shipped and is fully documented in its own commit body (345 passed, 0 warnings). Per `rules/journal.md` (already-codified → discard with note) it was discarded inline. The file is gitignored, so no commit is required; it is reconstructable from the commit. Note: the prior (2026-06-26) sweep also reported discarding this candidate — the SessionEnd hook regenerated it because `0903538e1` still matches its journal-worthy pattern; regeneration on a future session end is expected hook behavior, not a defect.

### #8 — Stub-marker baseline (LOW, DEFER-WITH-REASON)

The `# TODO` / `NotImplementedError` matches in `src/` + `packages/*/src` are the F-STUBS baseline the user designated leave-as-baseline on 2026-06-26 (auto-memory `project_fstubs_baseline`). The raw grep over-counts: most `NotImplementedError` are legitimate ABC/override contracts or codegen templates emitting scaffolding for _users_, most `XXX` are hex/substring false positives, and the genuine `# TODO` comments (~29) are the unchanged pre-existing set. 0 introduced this run. Not re-queued per user call.

## Cross-cutting observations

- The repo is in a genuinely quiet state: no in-flight PRs/branches/todos, no release backlog, all package versions tag-consistent. The entire live surface is the 4-issue trust/PACT queue.
- Two of the four issues (#1480, #1483) are `cross-sdk`-labeled — they need coordination with kailash-rs (rs#1562 is the pair for #1483). Cross-repo work is out of this session's scope per `rules/repo-scope-discipline.md`; flagging for the user's orchestration-level call only.
- Sweep report files continue to accumulate untracked at root (now `SWEEP-2026-06-26.md` + `SWEEP-2026-07-02.md`) per the established main-protected pattern. Not committed (no direct push to main; deliberately excluded from PRs).

## Recommended next-session items (ranked)

1. **#1480 — PACT authz-root re-validation** (Security). Fix `SqliteOrgStore._deserialize_org` to re-validate `CompiledOrg` on rehydration. Highest risk; single-workspace scope; likely fits one shard.
2. **#1483 — HoldQueue disclosure binding** (cross-sdk). Coordinate parity with rs#1562; bind the reviewed disclosure into the approval/rejection signature.
3. **#1481 + #1482 — trust primitives** (ConsentAttestation + disclosure-trace tokens). Additive, can be a paired shard; lower urgency than the integrity items.

The report is the deliverable. Scope selection for the next session is a human call.
