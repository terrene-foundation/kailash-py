# Basic Lifecycle Example

See `examples/quickstart.py` in the package for the full runnable example.

The quickstart demonstrates all four EATP operations:

1. **ESTABLISH** — Create trust for an agent
2. **VERIFY** — Check trust before action
3. **DELEGATE** — Transfer trust to sub-agent
4. **AUDIT** — Record agent action

```python
--8<-- "examples/quickstart.py"
```
