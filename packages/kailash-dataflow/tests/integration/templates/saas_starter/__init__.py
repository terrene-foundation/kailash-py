"""Integration tests for SaaS Starter template — Tier-2.

Sibling of tests/integration/templates/api_gateway_starter/; both rely on the
SQLite-file-backed DataFlow fixture pattern (no PostgreSQL infrastructure)
so the suite is collectable with a clean ``[dev]``-only install per the
3-tier contract in specs/testing-tiers.md.
"""
